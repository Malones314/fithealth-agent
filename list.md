# FitHealthAgent 需求清单

> 更新日期：2026-08-17  
> 审查范围：main.py、fithealth_agent/、templates/、tests/、Docker 与 Git 配置  
> 使用范围：个人单用户、本地健康管理 Agent；不以公网或多用户部署为目标。

## 1. 当前能力基线

项目已经具备可用的个人健康管理 MVP：

- [x] FastAPI 单页应用（42 条路由）及用户档案完整度引导。
- [x] 每日手动记录、训练记录、训练计划、跨会话记忆与本周本地汇总。
- [x] Garmin FIT/ZIP/CSV 导入；心率、睡眠、压力、血氧、HRV、活动趋势查询。
- [x] 力量训练组编辑、跨休息组合并、心率重新计算、保存确认令牌与重复保存防护。
- [x] 训练计划上传、优化、保存、备忘与偏好记忆。
- [x] 餐盘照片热量估算，支持补充文字说明，图片不在本地持久化。
- [x] 数据管理、单项/批量删除、全量删除、备份导出和事务式恢复。
- [x] 输入大小限制、ZIP 路径穿越/压缩炸弹防护、Markdown DOMPurify 清洗。
- [x] 当前测试基线：python -m pytest -q 为 44 项通过；源码可编译。

## 2. P0：立即处理

### SEC-001 轮换历史泄露的密钥并清理 Git 历史

**问题**：.env 曾在多个历史提交中被 Git 跟踪，且包含非占位配置；仓库配置了 GitHub remote。即使当前 .env 已被忽略，历史中的 API、数据库与第三方服务凭据仍应视为泄露。

**需求**：

- 立即轮换曾写入 .env 的所有 API Key、数据库密码、访问令牌和服务凭据。
- 使用 git filter-repo 或 BFG 清理 Git 历史中的 .env；强制推送前先备份仓库。
- 启用 GitHub Secret Scanning / Push Protection，并在 CI 中执行 secret scan。
- 保留 .env.example，但仅含空值或明显占位值。

**验收标准**：旧凭据全部失效；历史与当前工作树的扫描均不包含真实秘密；新 clone 不会得到 .env。

### SEC-002 收紧默认网络暴露与数据接口访问

**问题**：服务在 main.py 默认监听 0.0.0.0，Docker Compose 映射所有网卡；健康数据、备份和删除接口没有认证。

**需求**：

- 默认监听 127.0.0.1；Docker 使用 127.0.0.1:7860:7860。
- 为所有非 GET 数据接口配置本地访问口令或会话认证，并校验 Host / Origin。
- 默认禁用 CORS；如需局域网访问，必须显式开启并使用独立口令。
- 对备份恢复、全量删除等危险操作增加限速与二次确认。

**验收标准**：同网段其他设备不能访问默认服务；无有效口令不能读取、导出、写入或删除任何数据。

### SEC-003 防止密钥和健康数据进入 Docker 镜像

**问题**：Dockerfile 使用 COPY . .，但仓库没有 .dockerignore；.gitignore 不影响 Docker 构建上下文。

**需求**：

- 新增 .dockerignore，排除 .env、.git、data、memory、tool-output、测试缓存和本地 IDE 文件。
- 运行时仅通过环境变量或 Docker secret 注入配置；健康数据目录挂载为本地卷。
- 在 CI 中增加镜像文件/秘密扫描。

**验收标准**：构建后的镜像不含 .env、Git 历史、健康数据库、原始 Garmin 文件或本地会话数据。

### DATA-001 修复备份恢复后的任意文件删除风险

**问题**：健康导入记录将 raw_path 存在 SQLite 中；恢复备份目前仅验证 SQLite 完整性。恶意或错误备份可写入任意绝对路径，之后删除导入或全量删除时 health_store.py 会对该路径执行删除。

**需求**：

- 不再持久化绝对路径；只保存健康原始文件的受控相对标识。
- 删除前统一解析并验证目标路径位于 data/health-imports/ 内，拒绝目录外路径、链接逃逸和缺失路径。
- 备份校验同时检查数据库 schema、raw_path 约束与外部文件清单。
- 新增恶意 SQLite 备份、路径穿越、符号链接/重解析点的回归测试。

**验收标准**：任何备份内容均无法让删除接口触及健康原始文件目录以外的文件。

### DATA-002 实现可迁移、完整且一致的备份

**问题**：备份仅包含 JSON 和 health.db，不包含 data/health-imports/ 的原始 Garmin 文件；数据库又引用绝对路径，换机器或换目录恢复会产生失效引用。

**需求**：

- 升级备份格式到 v2：manifest 记录 schema 版本、哈希、导出时间和原始文件清单。
- 将原始健康文件安全打包，或在“轻量备份”中明确排除并清理相应引用。
- 恢复时先完成完整性校验，再在事务目录中重建相对路径，最后原子切换。
- 提供“完整备份/仅汇总数据备份”选项，并在界面说明差异。

**验收标准**：完整备份在另一目录恢复后，可查看、删除和追溯所有导入项；篡改任一文件会被拒绝恢复。

### PRIV-001 明示外部模型数据流并允许关闭

**问题**：项目不是纯离线应用：Agent 上下文会发送档案、记忆和对话；餐盘照片会发送到外部视觉模型；退出摘要和训练计划鉴定也可能调用外部 LLM。

**需求**：

- 首次启动显示数据外发说明，逐项列出：主模型、轻量模型、视觉模型、YouTube 和发送的数据类型。
- 支持关闭联网模型功能；关闭后仍可使用本地健康查询、导入、编辑、删除和备份。
- 发送前最小化数据：不必发送出生日期原文时改发年龄；按功能选择性注入档案与记忆。
- 不记录完整模型请求/响应；诊断日志默认脱敏。

**验收标准**：用户能查看和调整外发开关；关闭后不会产生任何 LLM/视觉 API 请求。

### SAFE-001 建立健康建议安全规则

**需求**：

- 在 Agent 系统提示、后端策略和前端提示中统一加入风险分级。
- 对胸痛、晕厥、呼吸困难、急性损伤、极端异常心率等高风险描述，停止训练建议并建议及时就医。
- 禁止诊断、处方、药物调整，以及未经确认的极端热量限制和过量训练建议。
- 所有基于 Garmin 数据的结论必须说明日期、样本范围、缺失数据与“非医疗诊断”。

**验收标准**：高风险测试语料不生成训练处方；低风险建议带有必要的不确定性说明。

## 3. P1：稳定性、Agent 可控性与测试

### DATA-003 统一并发写入与多文件操作

**问题**：多个 JSON Store 采用“读完整文件 → 修改 → 原子替换”，但没有跨请求或跨进程锁；并发写入可能互相覆盖。

**需求**：

- 对 JSON Store 引入进程内锁和跨进程文件锁，或逐步统一迁移到 SQLite。
- 备份恢复、全量删除与健康导入使用全局维护锁，避免与写入并行。
- 写入失败保留可诊断的错误和可恢复副本。

**验收标准**：并发新增、编辑、删除压力测试无记录丢失、JSON 损坏或部分恢复状态。

### AGENT-001 训练计划改为结构化 Artifact

**需求**：

- 定义训练计划 JSON Schema：日期、训练主题、热身、动作、组次、负重、RPE、休息、拉伸、限制、警告和数据来源。
- Agent 输出结构化 artifact；前端基于 schema 渲染和编辑，而非依赖文本/正则猜测。
- 将 Markdown 仅作为导出展示格式；保存前执行 schema 与业务规则验证。

**验收标准**：典型训练请求都可生成、编辑、保存并重新加载结构化计划；无效数据被明确拒绝。

### AGENT-002 收紧 Agent 工具写入权限

**问题**：tools.py 的 save_daily_record 仅验证对象类型，未复用手动记录的字段和日期校验，Agent 的写入安全主要依赖 Prompt。

**需求**：

- 复用/抽取统一的记录 schema 校验，限制日期、分类、字段、数值范围、嵌套层级和单条大小。
- Agent 的高影响写入采用“生成草稿 → 前端确认 → 后端保存”状态机。
- 所有工具调用记录动作、输入摘要、结果、来源和失败原因；日志不含敏感原文。

**验收标准**：模型无法借助工具写入未知字段、超范围数值或无效日期；未确认的高影响记录不会落盘。

### AGENT-003 增加回答来源、置信度与记忆解释

**需求**：

- API 返回 provenance：本地健康数据、档案、记忆、模型估算或外部视频搜索。
- 返回分析日期、样本数量、缺失数据、置信度和使用的记忆 ID。
- 前端支持查看并删除/确认具体记忆，说明“为何本次回答使用它”。

**验收标准**：涉及历史数据的回答可追溯到具体来源；估算不会被渲染为确定事实。

### SEC-004 固定前端供应链并配置 CSP

**问题**：index.html 从公共 CDN 动态加载未固定版本、无完整性校验的字体、marked 和 DOMPurify；第三方脚本可在同源页面读取全部健康数据。

**需求**：

- 将 marked 与 DOMPurify 固定版本并以本地静态文件提供，或使用 SRI 且严格固定版本。
- 配置 Content Security Policy，限制 script-src、connect-src、img-src 和 frame-src。
- 移除不必要的第三方字体请求，提供本地系统字体回退。

**验收标准**：断网时核心页面仍可安全加载；CSP 报告模式无意外违规后转为强制模式。

### TEST-001 建立端到端测试与持续集成

**需求**：

- 增加 FastAPI 端点测试：认证、上传、恢复、删除、错误响应和限额。
- 增加恶意备份、并发读写、模型超时、上传魔数、Agent 工具契约和前端关键交互测试。
- 引入 CI：pytest、覆盖率门槛、格式/静态检查、依赖审计、secret scan、Docker 构建检查。
- 建立脱敏 Garmin FIT/ZIP/CSV 回归语料库，覆盖多运动类型与损坏文件。

**验收标准**：每次 PR 自动通过质量门；核心后端覆盖率目标不低于 80%。

### OPS-001 依赖与运行环境可复现

**问题**：requirements.txt 只有宽松范围，且当前 Python 环境 pip check 报告若干与项目无关的全局依赖冲突；仓库没有 CI、锁文件或检查配置。

**需求**：

- 使用独立虚拟环境，生成可复现的锁文件或约束文件。
- 明确支持的 Python 版本、启动命令、环境变量和 Docker 构建方式。
- 将健康数据目录、日志目录与临时目录配置化，并在启动时自检可写性。

**验收标准**：全新环境按 README 可完成安装、测试、启动和 Docker 构建；pip check 在项目虚拟环境中通过。

## 4. P2：产品完善与长期能力

### AGENT-004 Garmin 多运动与导入质量体系

- 为运动类型建立解析器注册机制，未知字段保留原始元数据和兼容性提示。
- 展示导入覆盖日期、样本量、重复项和部分失败原因。
- 提供导入预览、选择性保存、映射修正和回归样本收集入口。

**验收标准**：新增设备/运动类型可通过独立解析器扩展，不影响既有导入。

### AGENT-005 记忆检索、冲突与生命周期

- 在现有确认制记忆基础上增加语义检索、负向偏好、来源链接、冲突检测和过期提醒。
- 关键记忆要求用户确认；对“计划决策”“限制”提供更长 TTL 和撤销记录。
- 检索结果必须标为历史信息，当前用户消息优先。

**验收标准**：能准确检索偏好和限制；过期或冲突记忆不影响当前建议。

### FOOD-001 餐食估算人工校正与入账

- 分析结果支持修改食材、份量、热量及三大营养素。
- 用户确认后才写入每日记录，并保留“模型估算/人工修正”的来源。
- 支持低置信度提示和无法识别时的手动录入。

**验收标准**：餐食记录可回看、编辑、删除并正确进入周汇总。

### PLAN-001 训练计划业务规则校验

- 基于结构化计划检查肌群恢复间隔、训练量突增、重复动作、疼痛限制、连续高强度日和目标冲突。
- 输出可解释的警告，而非自动修改用户确认的计划。

**验收标准**：异常训练计划产生明确警告，并附带可执行替代建议。

### DOC-001 更新使用与安全文档

**问题**：README.md 仍提及 Gradio，与实际 FastAPI 单页应用不一致。

**需求**：

- 重写 README：安装、启动、测试、Docker、本地安全边界、数据目录、备份差异、模型外发和故障恢复。
- 增加密钥轮换、历史清理、数据导出/删除、恢复失败和离线模式 Runbook。
- 维护功能矩阵，区分“已实现”“实验性”“计划中”。

**验收标准**：新用户无需阅读源码即可完成本地运行并理解数据去向。

## 5. 建议实施顺序

1. SEC-001：轮换密钥并清理历史。
2. SEC-002、SEC-003：收紧本地服务和 Docker 构建边界。
3. DATA-001、DATA-002：修复备份安全和可迁移性。
4. PRIV-001、SAFE-001：明确数据外发和健康建议安全策略。
5. TEST-001、OPS-001：建立回归保护和可复现环境。
6. DATA-003、AGENT-001 至 AGENT-003：提高数据与 Agent 的可控性。
7. P2 产品增强：多设备兼容、记忆检索、餐食校正和计划校验。
