# FitHealthAgent 下一阶段开发计划及实现

> 版本：2026-09-09  
> 基线：当前工作树  
> 目标周期：4 个迭代，约 3～5 周  
> 总原则：先收口安全与可靠性，再扩展 Agent 能力；任何涉及健康建议、数据写入和外部模型调用的功能必须有确定性后端闸门。

## 1. 文档目的

本计划基于当前代码、README、2026-08-23 收尾审查和现有测试整理。

2026-08-23 清单中的 89 条历史问题已经在 2026-08-27 收尾审查中标记为：已完成 86 条、用户决定不做 2 条、取消 1 条。因此本计划不会把这些旧条目重新列为当前缺陷，除非有新的可复现证据。

本阶段聚焦以下目标：

1. 将刚加入的 LLM 连通性测试从“可用原型”提升为安全、可观测、可测试的诊断能力。
2. 收紧单用户本地应用的网络信任边界，避免误部署到局域网或公网后暴露健康数据与高成本模型调用。
3. 统一外部模型的超时、重试、错误分类和运行状态。
4. 降低单文件前端和非结构化 Agent 输出带来的维护与解析风险。
5. 建立持续集成、发布检查和回归基线。

## 2. 当前功能基线

当前系统已经具备：

- HelloAgents ReAct 主对话 Agent。
- 可配置的最大步数、温度、最大输出、超时和 SDK 重试次数。
- Garmin FIT、ZIP 和睡眠 CSV 导入。
- 训练组修改、合并、删除、撤销、恢复和确认保存。
- 全天健康、睡眠、心率、HRV、压力、血氧和活动数据查询。
- 训练计划生成、上传、校验、修正、保存、编辑和下载。
- 餐盘图片识别与营养记录。
- 肌群恢复、酸痛反馈、Garmin 恢复时间和训练负荷整合。
- 本地健康风险筛查与生成后安全校验。
- 用户档案、可确认记忆、遗忘和回滚。
- 数据备份、恢复点、全量重置和文件引用审计。
- Agent trace、模型调用 trace、成本统计和保留策略。
- 导航条 LLM 连通性测试原型。

当前规模约为 69 条 HTTP 路由、87 个测试模块，前端仍集中在一个约 4700 行的 index.html 中。

## 3. 当前可证实的风险

### 3.1 P0：LLM 连通性端点可被重复触发

当前 POST /settings/llm-connectivity 没有服务端频率限制、并发限制或短期结果缓存。按钮禁用只能阻止当前页面重复点击，无法阻止多个标签页、脚本或其他本机进程并发调用。

影响：

- 重复产生模型费用。
- 大量慢请求可能占满 FastAPI 线程池。
- 主聊天和计划生成请求可能被诊断请求拖慢。

### 3.2 P0：探针复用主模型的长超时和重试

当前本地配置为：

- LLM_TIMEOUT=90
- LLM_MAX_RETRIES=3

最坏等待时间约为 90 × 4 = 360 秒，另加重试退避。连通性测试的目标应该是快速判断“当前是否可用”，不应该继承主对话的长等待策略。

### 3.3 P0：应用没有身份认证

README 已明确当前是单用户本地工具，没有账号、登录和权限隔离。Docker Compose 默认绑定 127.0.0.1，风险可控；但 main.py 直接运行时监听 0.0.0.0。

一旦部署者把端口暴露到局域网、公网或无认证反向代理，攻击者可以：

- 触发付费模型调用。
- 读取健康与训练数据。
- 修改、删除或恢复用户数据。
- 下载包含敏感健康信息的备份。

这属于当前部署模型的明确限制。下一阶段必须让“非本机监听”变成显式选择，而不是入口默认值。

### 3.4 P1：状态变更端点缺少统一的来源校验

项目没有统一的 Origin、Host 或 CSRF 防护。即使浏览器禁止跨域读取响应，第三方网页仍可能向无请求体的 POST 端点发起请求。LLM 连通性测试尤其容易成为跨站触发的付费操作。

### 3.5 P1：连通性端点错误处理仍可能泄露内部信息

请求阶段已经使用固定错误文案，但客户端初始化阶段仍有 broad except 并返回 str(exc)。未来框架异常如果包含内部地址、请求参数或供应商信息，可能被原样返回前端。

### 3.6 P1：新模型调用没有纳入统一可观测性

observability/http.py 的 TRACED_ROUTES 尚未包含 /settings/llm-connectivity。当前无法统一回答：

- 探针调用了哪个模型类别。
- 是否跳过、超时或供应商失败。
- 实际耗时和重试次数。
- 连续失败是否达到告警阈值。

### 3.7 P1：连通性功能缺少正式回归测试

目前仅做了手工和模拟验证，尚无专用测试文件覆盖：

- 外部模型开关关闭。
- API Key 缺失。
- 非法超时或重试配置。
- 成功响应。
- 超时、HTTP 错误和网络错误。
- 敏感异常文本不出现在响应中。
- 并发、频率限制和缓存。
- 前端按钮加载、成功、失败和恢复状态。

### 3.8 P1：第三方前端依赖缺少供应链约束

首页直接从 Google Fonts 和 jsDelivr 加载 marked、DOMPurify，没有本地固定版本、Subresource Integrity 或 Content Security Policy。

影响：

- 外部 CDN 可观察用户访问。
- CDN 或依赖被篡改时，可在展示健康数据的页面执行脚本。
- 离线环境下 Markdown 渲染和净化能力可能退化。

### 3.9 P2：外部模型配置仍然分散

主 Agent 已使用集中配置，但意图路由、训练计划鉴定、肌群映射、健康风险辅助判断和视觉识别仍有各自硬编码的超时、温度和最大输出值。

这不是立即漏洞，但会造成：

- 不同调用点的超时语义不一致。
- 全局诊断无法准确展示配置。
- 调整供应商时需要修改多个模块。
- 重试策略可能在不同 SDK 中悄悄漂移。

### 3.10 P2：单文件前端的变更风险持续增大

templates/index.html 同时承载 HTML、样式、状态管理、网络请求和业务交互。新增一个导航按钮需要跨多个远距离位置修改，容易出现：

- DOM 引用遗漏。
- disabled 状态泄漏。
- 错误响应未恢复按钮。
- 移动端布局回归。
- 文本替换造成无关 diff。

## 4. 下一阶段总体顺序

| 阶段 | 主题 | 优先级 | 预计 |
| --- | --- | --- | --- |
| S0 | 收口 LLM 连通性测试 | P0 | 1～2 天 |
| S1 | 网络信任边界与防滥用 | P0 | 2～3 天 |
| S2 | 外部模型运行时统一 | P1 | 3～4 天 |
| S3 | 前端模块化与结构化计划 | P1 | 4～7 天 |
| S4 | CI、供应链与发布治理 | P1/P2 | 2～4 天 |

每个阶段独立提交、独立验收。S0 和 S1 未通过前，不增加新的外部模型入口。

# 5. S0：收口 LLM 连通性测试

## 5.1 目标

把导航条测试按钮做成低成本、快速失败、不会泄露敏感配置的诊断功能。

## 5.2 后端实现

新增独立配置，不复用主聊天等待参数：

| 变量 | 默认值 | 范围 |
| --- | ---: | ---: |
| FITHEALTH_LLM_PROBE_TIMEOUT | 10 秒 | 2～30 |
| FITHEALTH_LLM_PROBE_RETRIES | 0 | 0～1 |
| FITHEALTH_LLM_PROBE_CACHE_TTL | 15 秒 | 0～300 |
| FITHEALTH_LLM_PROBE_COOLDOWN | 5 秒 | 1～60 |

建议文件：

- fithealth_agent/settings.py：增加 LlmProbeSettings 和严格解析。
- fithealth_agent/services/llm_probe.py：封装探针执行、缓存、并发控制和错误归类。
- fithealth_agent/routes/settings.py：只处理 HTTP 契约，不直接构造 SDK 客户端。
- fithealth_agent/observability/http.py：登记探针路由。
- fithealth_agent/observability/model_trace.py：记录 probe 类型，不记录请求正文和响应正文。

探针请求固定为最小内容，最大输出限制为 8～16 token。成功只验证供应商返回了非空响应，不依赖必须逐字返回 OK，避免不同模型的系统策略造成假失败。

## 5.3 API 契约

POST /settings/llm-connectivity

成功，HTTP 200：

~~~json
{
  "ok": true,
  "status": "reachable",
  "model": "configured-model",
  "latency_ms": 842,
  "cached": false,
  "checked_at": "2026-09-09T10:00:00+08:00"
}
~~~

失败分类：

| HTTP | reason | 场景 |
| ---: | --- | --- |
| 400 | invalid_configuration | 模型名、地址、超时或重试配置非法 |
| 401 | credentials_rejected | 供应商明确拒绝凭据 |
| 403 | external_models_disabled | 用户关闭外部模型 |
| 429 | probe_rate_limited | 探针冷却期内重复请求 |
| 502 | provider_error | 供应商 5xx 或响应格式错误 |
| 503 | network_unreachable | DNS、连接或 TLS 失败 |
| 504 | probe_timeout | 探针超过专用短超时 |

响应禁止包含：

- API Key。
- 完整请求 URL 或 query。
- 用户档案、聊天、训练或健康数据。
- 供应商原始响应正文。
- traceback 和异常 message 原文。

## 5.4 并发与缓存

进程内维护：

- 同一时刻只允许一个探针请求。
- 后续并发请求共享同一个执行结果，或返回 429。
- 成功与失败都可缓存 15 秒，避免多标签页重复计费。
- 缓存键只使用模型名、endpoint host、timeout 和 retry，不使用 API Key。
- 配置变化后缓存键自然失效。

多进程部署时先接受“每进程一份限制”，并在文档标明；真正多用户部署再迁移到 Redis 或反向代理限流。

## 5.5 前端实现

导航按钮使用三个稳定状态：

- idle：LLM 测试。
- loading：旋转图标，禁用点击，aria-busy=true。
- result：通过现有通知区域展示成功、缓存命中、禁用或失败。

要求：

- 使用 AbortController 设置前端 15 秒等待上限。
- 页面请求超时后恢复按钮，但说明服务端请求可能仍在收尾。
- 不把模型回复正文显示到页面。
- 移动端保持固定高度和不收缩，导航条可横向滚动。
- 处理非 JSON 响应和空响应。

## 5.6 测试

新增 tests/test_llm_connectivity.py：

- 外部模型关闭时绝不构造客户端。
- 缺少 Key 返回 400。
- 超时映射为 504。
- 401、429、5xx、DNS 和 TLS 错误分类正确。
- 异常中放入伪造密钥和私有 URL，响应和日志摘要均不包含它们。
- 重复请求命中缓存。
- 并发只产生一次供应商调用。
- 专用 timeout/retry 不受主 Agent 配置影响。
- 路由纳入 trace 边界且不记录探针正文。

新增前端结构测试：

- 按钮存在且 type=button。
- 有 loading、success、error 三条处理路径。
- finally 中恢复按钮。
- 手机断点包含该按钮。

## 5.7 验收

- 单次失败在 12 秒内返回，默认不重试。
- 连续点击不会产生并行付费调用。
- 所有错误响应通过敏感信息扫描。
- 外部模型关闭时网络调用数为 0。
- 专项测试和全量测试通过。

# 6. S1：网络信任边界与防滥用

## 6.1 默认监听策略

修改 main.py：

- 默认 host 从 0.0.0.0 改为 127.0.0.1。
- 新增 FITHEALTH_HOST 和 FITHEALTH_PORT。
- 当 host 不是 loopback 且没有配置认证时，启动失败并输出明确说明。
- Docker Compose 继续只映射 127.0.0.1:9999。

## 6.2 可选单用户认证

实现适合本地服务的单令牌认证：

- FITHEALTH_ACCESS_TOKEN 未设置：仅允许 loopback。
- 设置后：所有非静态业务端点要求 Authorization: Bearer。
- Token 只从环境变量读取，不写入数据目录、trace 或响应。
- 使用 secrets.compare_digest 比较。
- /health/storage-status 可配置为只返回最小存活状态。

不在此阶段引入账号系统、密码重置或多用户权限模型。

## 6.3 Origin、Host 和 CSRF

新增 SecurityBoundaryMiddleware：

- 校验 Host 是否属于显式允许列表。
- 浏览器状态变更请求校验 Origin。
- 拒绝 null Origin，除非明确允许本地 CLI。
- JSON API 要求正确 Content-Type。
- 对备份导入、删除、重置、模型探针等敏感操作增加 CSRF token 或严格 SameSite Cookie 方案。

## 6.4 通用速率限制

至少覆盖：

- /chat。
- /analyze_food。
- /upload_plan 中的模型鉴定。
- /logout 中的摘要模型。
- /settings/llm-connectivity。

先实现进程内 token bucket，按客户端地址和路由分类；README 明确多进程时必须由反向代理补全全局限流。

## 6.5 验收

- 默认启动不再监听所有网卡。
- 非本机监听且无认证时拒绝启动。
- 跨站 POST 无法触发模型调用或数据写入。
- 未认证请求无法读取健康数据。
- 限流响应统一为 429，并带 Retry-After。

# 7. S2：统一外部模型运行时

## 7.1 统一配置模型

建立 ExternalModelRuntimeSettings：

- main：主 ReAct。
- lite：分类、路由、摘要、肌群映射。
- vision：餐盘图片。
- probe：连通性测试。

每类包含：

- model。
- base_url。
- timeout。
- max_retries。
- temperature。
- max_tokens。
- enabled。
- endpoint_host 的脱敏展示值。

## 7.2 统一调用器

新增 fithealth_agent/model_runtime.py：

- 集中创建 OpenAI 兼容同步客户端。
- 统一 timeout 和 retry。
- 统一异常分类。
- 统一 observability。
- 统一敏感信息清洗。
- 支持依赖注入，测试不需要 patch 模块级环境变量。

逐步替换以下模块中的手写请求：

- chat_intent_router.py。
- information_router.py。
- plan_classifier.py。
- plan_goal_validator.py。
- muscle_map.py。
- health_safety.py。
- food_analysis.py。
- agent.py。

每替换一个模块就独立提交和回归，不做一次性大爆炸迁移。

## 7.3 熔断和降级

按 endpoint host + model 建立轻量熔断器：

- 连续 3 次可重试基础设施失败后打开 30 秒。
- 401、403 和配置错误不自动重试。
- 429 尊重 Retry-After。
- 熔断打开时快速失败，不继续占线程。
- 本地确定性健康安全闸门永远不依赖熔断器或外部模型。

## 7.4 状态页面

在数据管理中增加只读“模型运行状态”：

- main、lite、vision、probe 是否配置。
- 最近一次成功时间。
- 最近一次失败类别。
- 熔断状态。
- timeout 和 retry。
- 不显示 API Key 或完整内部 URL。

## 7.5 验收

- 所有外部模型调用进入统一 trace。
- 不再存在散落的 requests.post 模型调用。
- 同类错误在所有端点映射一致。
- 外部模型关闭后，本地导入、编辑、查询、安全筛查仍正常。

# 8. S3：前端模块化与结构化计划

> 方案归属说明（2026-09-10）：本节是早期方向稿，其中“保持无构建工具”已被专项计划 `frontend-modularization-plan.md` 覆盖。实际实施以专项计划冻结的 Vite + TypeScript、`frontend/dist/`、Playwright 和分领域迁移方案为准；本节的结构化训练计划属于独立业务演进，不混入机械前端迁移提交。

## 8.1 前端拆分

保持无构建工具也可运行的前提下，将 index.html 拆为 ES Modules：

- static/app.js：启动与共享状态。
- static/api.js：fetch、超时、错误标准化。
- static/notifications.js：通知展示。
- static/llm-status.js：连通性测试和状态。
- static/chat.js：聊天。
- static/data-management.js：数据管理。
- static/workout-editor.js：训练编辑。
- static/styles.css：样式。

第一步只做机械迁移，要求 UI 和接口行为完全不变；后续功能才在模块中开发。

## 8.2 结构化训练计划

减少依赖 Markdown 正则判断计划是否完整：

- 后端定义 TrainingPlanDraft 数据模型。
- Agent 生成 JSON schema 约束的计划主体。
- Markdown 仅作为展示和下载格式。
- 校验器读取结构字段，不从自然语言猜组数、RPE 和动作。
- 前端按结构渲染并支持局部编辑。
- 保存前在后端重新校验目标、健康限制、恢复状态和容量上限。

迁移策略：

- 旧 Markdown 计划继续可读。
- 新计划同时保存 schema_version 和 rendered_markdown。
- 无法迁移的旧计划不自动改写。
- 提供一次只读预检报告，再决定是否批量迁移。

## 8.3 验收

- index.html 降至 500 行以内。
- API 请求都有统一超时和错误展示。
- 计划校验不再依赖 Markdown 排版。
- 旧计划仍可查看、下载和删除。
- 移动端 390 px、桌面 1440 px 无导航溢出或遮挡。

# 9. S4：CI、供应链与发布治理

## 9.1 CI

> 分工说明（2026-09-10）：`frontend-modularization-plan.md` 负责建立基础 workflow，并接入 Python 回归、前端 lint/typecheck/test/build、Playwright 和 Docker smoke；本节继续负责 Python 版本矩阵、依赖审计、发布制品、发布门禁与部署治理。两者必须落在同一套 workflow 中，不重复建设。

新增 GitHub Actions 或等价流水线：

1. Python 3.11、3.12 矩阵。
2. 从 requirements.lock 安装。
3. python -m compileall。
4. 全量 pytest。
5. agent 和 route 快照校验。
6. ruff 或等价静态检查。
7. pip-audit 依赖漏洞扫描。
8. Docker 镜像构建与健康检查。
9. 禁止测试访问仓库真实 data 目录。

## 9.2 前端依赖

优先将 marked、DOMPurify 和字体资源固定到本地静态文件：

- 固定版本和 SHA256。
- 不在运行时依赖 CDN。
- 增加 Content-Security-Policy。
- 禁止 inline script 后，再逐步移除 unsafe-inline。
- 对 innerHTML 使用点进行清单化，动态数据必须走 textContent 或 DOMPurify。

## 9.3 发布门禁

每次发布必须产出：

- 依赖锁文件差异。
- 数据迁移说明。
- API 契约变化。
- 备份恢复演练结果。
- 全量测试结果。
- 已知限制。
- 回滚步骤。

## 9.4 验收

- 干净环境可以仅凭锁文件构建。
- CDN 断网不影响核心页面。
- CSP 下页面无违规资源加载。
- CI 不读取或修改真实健康数据。
- Docker 健康检查通过后才标记发布成功。

# 10. 实施提交顺序

建议按以下粒度提交：

1. test: freeze llm connectivity contract
2. refactor: extract llm probe service
3. fix: add probe timeout cache and concurrency guard
4. feat: add navbar probe states
5. security: enforce local binding and host policy
6. security: add token auth origin checks and rate limits
7. refactor: centralize model runtime
8. refactor: split frontend modules
9. feat: add structured training plan schema
10. ci: add test build and dependency gates

每个提交必须满足：

- 可以独立回滚。
- 不混入数据文件、trace、临时锁文件和格式化噪声。
- 新行为先有失败测试，再实现。
- 变更 README 和 .env.example。
- 不把 .env 或真实模型凭据加入 Git。

# 11. 测试策略

## 11.1 单元测试

- 配置解析边界。
- 错误分类。
- 熔断状态机。
- 缓存和限流。
- 结构化计划校验。
- 敏感信息清洗。

## 11.2 端点测试

- 正常、非法配置、超时、供应商错误、维护期、外部模型关闭。
- 认证、Origin、Host、Content-Type 和 CSRF。
- 429、Retry-After 和缓存命中。
- 响应 schema 固定。

## 11.3 集成测试

使用本地假 OpenAI 兼容服务模拟：

- 延迟响应。
- 401、403、429、500、502、503。
- 非 JSON。
- 中途断流。
- 返回空 choices。
- 首次失败后成功。
- 请求取消。

测试不得访问真实供应商，不消耗真实额度。

## 11.4 前端验证

- DOM 结构测试。
- Playwright 桌面和移动端截图。
- 键盘操作、焦点和 aria-live。
- 快速连点、多标签页和网络断开。
- 页面刷新后状态一致性。

## 11.5 数据安全回归

继续保持：

- 测试强制使用独立临时数据目录。
- 真实 data 目录只读。
- 备份恢复前后校验 manifest。
- 重置和恢复失败可回滚。
- trace 不含凭据和原始健康数据。

# 12. 指标与完成定义

下一阶段完成时应达到：

| 指标 | 目标 |
| --- | --- |
| LLM 探针默认失败耗时 | 小于 12 秒 |
| 探针并行供应商调用 | 每进程最多 1 个 |
| 探针重复点击额外调用 | TTL 内为 0 |
| 敏感异常文本泄漏 | 0 |
| 未认证远程数据访问 | 0 |
| 外部模型调用统一 trace 覆盖 | 100% |
| 全量自动化测试 | 全绿 |
| 真实 data 被测试修改 | 0 |
| index.html 行数 | 小于 500 |
| CDN 运行时依赖 | 0 |
| 高风险健康闸门外部依赖 | 0 |

# 13. 明确不在下一阶段做

- 多用户账号、角色和组织权限。
- 云端健康数据同步。
- 医疗诊断或治疗建议。
- 自动修改药物或康复处方。
- 重新开启用户已决定不做的 Garmin 精确动作名解码。
- 在没有新复现证据时重做 2026-08-27 已关闭的问题。
- 为追求覆盖率而访问真实模型、YouTube 或真实健康数据。

# 14. 第一批可直接执行的任务

第一批建议立即执行，预计 1～2 天：

- 新增 LlmProbeSettings，默认 10 秒、0 重试。
- 抽出 llm_probe.py。
- 加单飞并发保护、15 秒缓存和 5 秒冷却。
- 定义稳定错误码，删除 str(exc) 响应。
- 将探针接入 model trace。
- 补 tests/test_llm_connectivity.py。
- 整理导航按钮脚本为独立函数，补 AbortController。
- 验证 390 px 和 1440 px 布局。
- 更新 README 和 .env.example。
- 跑全量测试并记录基线。

完成第一批后，再进入网络认证和通用限流；不要直接开始结构化计划重构。
