# AGENT-08 修改方案：肌群恢复追踪与优先级裁决

> 状态：**阶段 1–4 已实现（映射、推导、进入系统清单、反馈、优先级裁决与计划校验）**。
> 目标读者：你自己。文中每个"实测"都是我在真实 `data/daily_records.json` 与
> `2026-08-*.zip` 上跑过的，不是推测。

---

## 0. 一句话结论

AGENT-08 列了六个校验缺口，其中**肌群恢复间隔**是唯一需要新建数据链路的一条，
其余五条都是在现有 `validate_generated_training_plan` 里补规则。你描述的流程
（保存训练 → 推导肌群 → 进入系统时询问 Garmin 剩余恢复时间、列出正在恢复的肌群并询问感受 → 反馈进入
优先级裁决）本质上是给系统加了一条**新的事实来源**，它比周计划优先、比安全限制
和当前明确指令低。

建议拆成 5 个部件、4 个阶段落地。**不新增数据库表，不新增独立存储文件**——肌群
负荷全部从已有的 `daily_records.json` 派生；只有"用户报告的酸痛"需要落盘。

---

## 1. AGENT-08 现状核实

清单原文曾指出计划鉴定模块名不副实、真正的校验在 `main.py`。阶段 4 完成后已将
鉴定模块重命名为 `plan_classifier.py`，避免和安全校验混淆。

| 清单说的 | 实际情况 |
|---|---|
| `plan_classifier.py` 是计划鉴定器 | 是。只判断"上传的 md 是不是训练计划" |
| 真正校验在 `main.py` | 是，`validate_generated_training_plan()`，约 95 行 |
| 全部来源于已确认记忆事实 | 是。函数签名 `(answer, memories, expected_subject)`，**没有任何 store 访问权** |
| 缺肌群恢复间隔 | 是。没有任何地方读过"上次练了哪些肌群" |
| 缺训练量突增 | 是。没有历史容量基线 |
| 缺同一计划内重复动作 | 初始核查时是；✅ 阶段 4 已补充规范化动作名去重校验 |
| 疼痛/伤病限制只进 prompt 没进校验 | 初始核查时是；✅ 阶段 4 已将生成路径的 `active_safety_constraints` 和保存路径的已确认健康限制传入校验器 |
| 整篇子串匹配会误判 | 是。`if exercise.casefold() in answer.casefold()` —— 计划里写"本次**避免**深蹲"会被判违规 |

**本方案只做「肌群恢复」这一条 + 它必然连带的优先级改动。** 其余五条见 §9。

---

## 2. 三个决定设计的实测发现

这三条是我原本想当然、实测后必须改设计的地方。

### 2.1 `category_raw` 已经被改名覆盖，FIT 枚举在已保存记录里拿不到

我原本打算优先用 FIT 的动作枚举（`bench_press`、`curl` 这种英文 id）做映射，
因为它是设备给的、稳定、不受用户写法影响。**实测发现这条路目前是断的**：

```
2026-08-17  category='哑铃平板卧推'   category_raw='哑铃平板卧推'
2026-08-18  category='右哑铃单臂划船' category_raw='右哑铃单臂划船'
2026-08-21  category='臀桥/臀推（负重）' category_raw='臀桥/臀推（负重）'
```

原因在 `workout_store.update_set()` / `update_sets()`：

```python
target.category = category
target.category_raw = category   # ← 用户改名时把英文枚举一起冲掉了
```

注释写着"用户自定义，raw 跟随"。后果是**全部 4 条力量训练记录都没有枚举可用**。

**结论**：
- 映射器**必须**能处理中文自由文本，这是硬约束，不是优化项。
- 本方案**不修 `category_raw` 覆盖，也不新增 `category_source`**。肌群判断以用户上传并
  最终保存的动作名为准：先走本地确定性规则；规则未命中时，可以调用
  `LLM_LITE_MODE_ID` 查询动作主要使用的肌群。外部模型关闭、调用失败或结果不可信时
  返回空，不猜测，也不阻塞训练记录保存。

### 2.2 中文关键词映射在真实数据上是 16/16，但**踩到一个碰撞**

我按肌群写了 14 条关键词规则，拿真实库里全部 16 个动作名跑了一遍：

```
哑铃平板卧推    → 胸大肌(胸部)、核心(核心)   ← 后者是误判
哑铃坐姿肩上推举 → 三角肌前束(肩部)
高脚杯深蹲      → 股四头肌(腿部)
臀桥/臀推（负重）→ 臀大肌(腿部)
哑铃罗马尼亚硬拉 → 竖脊肌(背部)、股二头肌(腿部)
哑铃凳胸部支撑划船→ 背阔肌(背部)、斜方肌/菱形肌(背部)
...
覆盖 16/16
```

**「平板卧推」被核心规则的关键词「平板」命中了**（本意是匹配"平板支撑"）。
这不是个别失误，而是这类映射的典型失效模式：短关键词是别的动作名的子串。

**结论**：关键词表必须遵守两条规则，且要有测试钉住：
1. **关键词取完整动作名，不取词根**：用「平板支撑」不用「平板」；用「推胸」不用「胸部」
   （否则"胸部支撑划船"会被判成胸部训练）。
2. **加否决词**：`core` 规则应带 `exclude=["卧推", "推举"]`。

我另外注意到一个**近失**：`哑铃凳胸部支撑划船` 里有"胸部"两字，只是因为我的胸部
规则用的是「推胸」才没误判。这说明否决机制不是可选项。

### 2.3 有氧记录没有动作名，只有 sport，查询用户是否在备忘中写了他坐了什么动作

```
2026-08-15  category='跳绳'      sport='跳绳'      36 段  segment_type='lap'
2026-08-20  category='有氧运动'  sport='有氧运动'  1 段
2026-08-21  category='有氧运动'  sport='有氧运动'  1 段
```

lap 型分段的 `category` 是 `None`，5 条有氧/跳绳记录一个动作名都没有。

**结论**：需要**第二层 sport 级映射**（跳绳 → 小腿+股四头肌，跑步 → 股四头肌+
股二头肌+小腿，骑行 → 股四头肌），并且这类负荷**权重要低**——36 段跳绳不该
和 36 组深蹲一样把腿判成"需要 72 小时恢复"。见 §B.1 的容量口径。

---

## 3. 设计：五个部件

```
                 ┌──────────────────────────────────────────┐
   保存训练 ────→│ A. 动作/运动 → 肌群映射（本地纯函数优先）   │
                 └──────────────────┬───────────────────────┘
   未知动作 ─────→ LLM_LITE_MODE_ID 可选兜底（受外部模型开关控制）
                                    │
   daily_records.json ─────────────→│ B. 肌群负荷与恢复推导（纯函数）
   当前会话 Garmin 小时 ───────────→│
   （唯一负荷来源，不另存）          │    输出 MuscleRecoverySnapshot
                                    ├──────────┬──────────────┐
                                    ↓          ↓              ↓
                          进入系统时的清单   PlanContext    计划校验
                          （/session/intro） （优先级裁决）  （AGENT-08）
                                    │
   用户回答"手臂有点酸" ──→ C. 酸痛反馈采集（唯一需要落盘的部分）
                                    │
                                    └→ D. 优先级插入 ──→ E. 校验规则
```

### A. 动作 → 肌群映射（新文件 `fithealth_agent/muscle_map.py`，约 150 行）

**本地映射核心保持纯数据 + 纯函数，不碰任何 store。** 这样它能被直接测试，也能被
校验器安全调用。另在纯函数之外提供可选的轻量模型兜底：只有本地规则未命中时，才用
`.env` 中的 `LLM_LITE_MODE_ID` 查询动作主要使用的肌群。

```python
@dataclass(frozen=True)
class MuscleRule:
    muscle_id: str        # "biceps"
    zh: str               # "肱二头肌"
    region: str           # "手臂"      ← 用户对话里说的粒度
    keywords: tuple[str, ...]
    exclude: tuple[str, ...] = ()   # §2.2 的否决词，缺它必然误判
    role: str = "primary"           # primary / secondary

MUSCLE_RULES: tuple[MuscleRule, ...] = (...)   # 14~18 条
SPORT_RULES: dict[str, tuple[str, ...]] = {    # §2.3 的有氧兜底
    "跳绳": ("calf", "quad"),
    "跑步": ("quad", "hamstring", "calf"),
    "骑行": ("quad",),
    "有氧运动": (),        # 说不出练了哪儿就不猜，宁可漏报
}

def muscles_for_exercise(name: str) -> list[MuscleHit]: ...
def muscles_for_sport(sport: str) -> list[MuscleHit]: ...
def resolve_muscles_for_exercise(
    name: str,
    *,
    allow_external_models: bool,
) -> list[MuscleHit]: ...  # 本地规则优先，轻量模型只兜底未知动作
```

设计要点，每条都对应一个已知的失效模式：

1. **`region` 固定为六个大区，`muscle_id` 是内部粒度。** 六个允许值是
   `胸部/背部/腿部/肩部/手臂/核心`，不再把手臂拆成前臂/上臂。用户按大区思考，
   训练量仍按具体肌群计算（肱二头和肱三头是两回事，同一天练它们不冲突）。
2. **一个动作可命中多个肌群，且分主次。** 罗马尼亚硬拉主要是股二头 + 竖脊肌；
   卧推主要是胸，三头是次要。`role` 让次要肌群按折扣计入容量（建议 0.4），
   否则"卧推 13 组"会让三头也显示成需要长时间恢复。
3. **本地核心命中不了就返回空，不猜。** 外层解析器可以再尝试轻量模型；若模型不可用
   或仍无法确定，也返回空。`有氧运动` 这种 sport 说不出练了哪里，返回空比随便归到
   腿部好——漏报只是少提醒一次，误报会让计划无理由避开某个部位。
4. **不做医学声明。** 这张表是训练常识的编码，不是解剖学真值；文档和界面都要
   说明它是"按动作名推断"，用户可以在对话里纠正。
5. **轻量模型只能补漏，不能改写已命中的本地结果。** 返回值必须经过结构化校验：
   肌群只能取 `MUSCLE_RULES` 已知的 `muscle_id`，主次角色只能取 `primary/secondary`。
   调用受 `external_models_enabled` 控制；关闭外部模型、超时、空结果或非法结果都按
   未知动作处理。同一次快照内按规范化动作名做内存去重，避免重复查询；不建立跨会话
   映射缓存。发送给外部模型的内容只包含动作名与允许的肌群枚举，不发送整份训练记录。
   测试中 mock 模型，不依赖真实外部服务。

### B. 肌群负荷与恢复推导（新文件 `fithealth_agent/muscle_recovery.py`，约 200 行）

**关键决策：不新建存储，全部从 `daily_records.json` 现场派生。**

理由（这条我认为不该妥协）：
- 记录被删除、被编辑、被合并时，派生值**自动跟着变**。若另存一份负荷表，就必须
  在 `delete_record` / `update_record` / `merge_sets` 四处同步——这正是 DATA-16
  之前那类"两份数据总有一份先腐坏"的坑。
- 成本可忽略：当前 9 条记录；就算一年后 500 条，一次全扫也是毫秒级。真慢了再加
  缓存，缓存失效条件是 `daily_records.json` 的 mtime。

```python
@dataclass(frozen=True)
class MuscleLoad:
    muscle_id: str
    zh: str
    region: str
    last_trained_at: datetime      # 北京时间
    weekday_zh: str                # "周三"  ← 你要的措辞
    exercises: tuple[str, ...]     # ("哑铃弯举", "锤式弯举")
    effective_sets: float          # 主项 1.0 + 次要 0.4 加权
    recovery_hours: float          # 由 §B.2 的表算出
    recovered_at: datetime
    hours_remaining: float         # <= 0 表示已恢复

def build_recovery_snapshot(
    records: list[dict],
    *,
    now: datetime,
    soreness: list[SorenessReport] | None = None,
    garmin_recovery_hours: float = 0.0,
    lookback_days: int = 10,
) -> MuscleRecoverySnapshot: ...
```

#### B.1 容量口径

`effective_sets = Σ(主项组数 × 1.0) + Σ(次要组数 × 0.4)`

有氧 lap 型记录**不按段数算**（36 段跳绳不是 36 组深蹲），改为按时长折算：
`effective_sets = 总时长分钟 / 10`，并且上限封在 4 —— 有氧对肌群的破坏与力量训练
不同量级，让它触发 72 小时恢复是错的。

#### B.2 恢复窗口表与 Garmin 加成（**这是估算，不是测量，必须在界面上说明**）

| 肌群 | 基础小时 | 依据 |
|---|---|---|
| 股四头肌 / 臀大肌 / 背阔肌 / 胸大肌 / 竖脊肌 | 48 | 大肌群 |
| 股二头肌 / 斜方肌·菱形肌 / 三角肌各束 | 36 | 中等 |
| 肱二头肌 / 肱三头肌 / 小腿 / 核心 | 24 | 小肌群，恢复快 |

第一版固定使用这组 `48/36/24` 小时，不做用户档案配置，也不接受按肌群自定义窗口。

容量调整：`recovery_hours = base × (1 + max(0, effective_sets - 6) / 12)`，封顶
`base × 1.5`。也就是 6 组以内按基础值，超出部分线性拉长，最多拉到 1.5 倍。

进入系统时询问「Garmin 建议的恢复时间还有几小时？(请输入`0–96.0`)」。输入
`0–96.0`：

- 输入 `0`：不加成，继续使用上述固定窗口与容量调整结果。
- 输入 `0.1–96.0`：先算每个最近训练肌群的基础剩余小时。只有
  `baseline_hours_remaining > 0` 时才增加 Garmin 数值；基础上已经恢复的肌群保持
  `hours_remaining = 0`。Garmin 是对仍在恢复肌群的当前会话补充估计，不能把已经
  完成恢复的肌群重新激活。最终令 `recovered_at = now + hours_remaining`。
- 其他值非数字：不静默截断，提示用户重新输入；在有效输入前按 `0` 处理。

这个数值只属于当前会话，不写入用户档案或长期存储。每次重新进入系统都再次询问，
避免把已经过时的 Garmin「剩余小时」沿用到下一次会话。它只加到最近训练过、已进入
快照的肌群，不为从未训练的肌群凭空创建恢复状态。

竖脊肌单独说明：它被硬拉/罗马尼亚硬拉/深蹲反复征用，实测 08-21 一次训练里
`哑铃罗马尼亚硬拉` 同时命中竖脊肌和股二头肌。它恢复慢且影响面广，所以给 48 而不是 36。

#### B.3 用户报告的酸痛如何改写快照

普通酸痛只能延长训练推导出的窗口；明确报告已恢复可以把剩余时间归零：

| 用户回答 | 语义 | 对快照的影响 |
|---|---|---|
| 正常 / 恢复了 / 没感觉 | `recovered` | `hours_remaining` 直接归零（用户身体感受优先于表格估算） |
| 有点酸 / 还有点紧 | `sore` | `recovery_hours × 1.5`，并标 `needs_reduction` |
| 疼 / 刺痛 / 使不上劲 | `painful` | **升级为安全信号**，交给 `health_safety` 走既有的急性损伤路径 |
| 没回答 | `unknown` | 按表格估算，不额外调整 |

`sore` 只产生「该肌群需要减量」的定性约束，不规定 60% 等固定比例，也不做确定性的
组数上限校验。模型结合当天计划自由裁量减量方式，并明确告知用户；用户训练时保留
根据主观感受继续调整或停止的决定权。

计算顺序固定为：训练动作与容量得到固定窗口 → `sore` 将该训练窗口乘以 `1.5` →
计算相对 `now` 的基础剩余小时 → 仅当基础剩余大于 0 时增加当前会话 Garmin 小时；
基础上已经恢复的肌群保持 0。`recovered` 是用户对估算的明确覆盖，最终直接归零，
不再叠加 Garmin 小时。

**为什么"正常"能归零而"酸痛"不能缩短**：这是刻意的不对称。用户说恢复了就是恢复了
（他比表格更清楚自己的身体）；而表格说恢复了、用户说还酸，则以用户为准。两个方向
都是"用户覆盖表格"，只是结果不同。

**`painful` 必须走安全路径，不能只当成"多减一点量"**：现有 `health_safety.py` 已经
有急性损伤分档（关节肿胀、无法负重、剧烈疼痛 → 停止该部位训练）。肌群酸痛问答是
一个**高频**入口，如果它自己判断"疼就减量"，就绕过了那套已经写好的分级。这里只做
一件事：把 `painful` 转成一次 `screen_health_risk` 输入，让既有逻辑裁决。

### C. 酸痛反馈的采集与存储

#### C.1 存储方案：独立的短时状态存储，即时写入、显式可见

新增 `fithealth_agent/soreness_store.py` + `data/muscle_soreness.json`：

```python
@dataclass(frozen=True)
class SorenessReport:
    region: str            # "手臂"（用户说话的粒度）
    muscle_ids: tuple[str, ...]   # 展开后的具体肌群
    level: str             # recovered / sore / painful
    reported_at: datetime
    expires_at: datetime   # reported_at + 72h
    evidence: str          # 用户原话片段，便于回溯
```

即时写入、**不需要确认**。理由是三条不对称：

1. **它只会让训练变轻，不会变重。** `sore` 的效果是减量或避开；误记一次的代价是
   某天手臂少练两组。反过来，用户说"手臂酸"而系统不记，代价是照原计划练伤。
   两侧代价不对称，所以默认应该偏向"记下来"。
2. **它自己会过期。** 72 小时 TTL，不像 `injury_or_constraint` 那样长期塑形计划，
   所以不需要确认关卡来防止长期污染。
3. **它不进长期上下文。** 只进 PlanContext 的当次裁决，不写 `info_store`，
   不参与 `format_cross_session_memories`。

但**必须满足三个可见性条件**，否则就是"静默写入"，与项目一贯的取向冲突：
- 写入后立刻在对话里回执一句（"已记录：手臂酸痛，未来 3 天会相应减量"）。
- 数据管理面板新增「肌群酸痛记录」区块，可逐条查看/修改/删除。
- `/session/intro` 每次都列出当前生效的酸痛记录，不让它悄悄影响计划。

落盘复用 `atomic_json.atomic_write_json`（DATA-06 那套 fsync + 原子替换）与
`JsonFileLock`，不要再写一份裸 `write_text`。

#### C.2 怎么解析用户的回答

**确定性优先，模型兜底。**

```python
def parse_soreness_reply(message: str, asked_regions: list[str]) -> list[SorenessReport]
```

- 先用正则在消息里找 `部位 + 程度` 组合（部位取自 `asked_regions`，即刚才问过的
  那些，不做开放域抽取）。
- "都正常"/"都还行"/"没问题" → 对全部 `asked_regions` 记 `recovered`。
- 只说了程度没说部位（用户答"有点酸"）→ 若只问了一个部位就归给它；问了多个则
  **不猜**，回一句让用户指明。
- 完全解析不出 → 什么都不写，正常走对话；不要让模型去写这份状态。

**为什么不交给 LLM 抽取**：这份数据会直接改写训练计划的优先级裁决。外部模型关闭时
（`external_models_enabled=False`）这条路必须照常可用——这正是 BUG-02 的教训。

### D. 优先级插入（改 `main.py`）

你的要求是"肌群恢复状态应该在比周计划高的优先级"。现有阶梯（`format_plan_context`）：

```
1. 安全/伤病限制
2. 用户当前明确指令（只有明确改练/休息/跳过才覆盖当天周计划）
3. 已确认周计划
4. 健身目标
5. 长期训练偏好
6. 历史摘要
```

改为**七级**，在 2 与 3 之间插入：

```
1. 安全/伤病限制
2. 用户当前明确指令
3. 【新】肌群恢复状态（未恢复 / 用户报告酸痛）   ← 你要的位置
4. 已确认周计划
5. 健身目标
6. 长期训练偏好
7. 历史摘要
```

`resolve_plan_context()` 新增三个键：

```python
"muscle_recovery": {
    "recovering": [ {region, zh, weekday_zh, exercises, hours_remaining}, ... ],
    "reduce": ["手臂"],        # sore，减量
    "avoid":  [],              # painful 升级后由安全路径填
    "ready":  ["腿部", "胸部"], # 可正常安排，供模型选替代科目
    "explicitly_requested": [], # 用户明确要求训练的区域，供裁决与校验识别例外
    "garmin_recovery_hours": 0.0,
},
"suppressed_rules": [...],     # 周计划被恢复状态压制时在这里留痕（已有键，复用）
```

**第 2 与第 3 级的冲突要写清规则**，否则模型会乱。建议：

- 用户明确说"今天就要练手臂"且手臂报了 `sore` → **仍按用户指令练**，但必须
  (a) 在回复里说明冲突，(b) 由模型结合计划自由裁量并下调该肌群容量，不设固定比例。
  理由：`sore` 是延迟性酸痛，不是伤；
  用户有权决定，系统的职责是告知而不是拒绝。
- 若报的是 `painful` → 已经升级成第 1 级安全信号，此时**拒绝生成该部位计划**，
  走既有 409 `constraint_conflict` 路径。这与"忽略我的伤，照常练"被拦截是同一套机制。

**周计划被压制时必须留痕。** `suppressed_rules` 已经有这个语义（DATA/BUG-03 时期
建的），复用它：`{"rule": "周计划：手臂", "reason": "肱二头肌预计还需 14 小时恢复",
"scope": "today"}`。这样模型的回复里能说出"今天本该练手臂，但…"，而不是无声换科目。

### E. 计划校验（AGENT-08 本体，改 `main.py`）

清单说"函数没有 store 访问权"。**不要给它 store**——那会让这个纯函数变得无法测试
（现在它能被直接调用，测试里 15 条断言都是纯输入输出）。改为**多传一个已解析好的
快照**：

```python
def validate_generated_training_plan(
    answer: str,
    memories: list[dict] | None,
    expected_subject: str = "",
    recovery: MuscleRecoverySnapshot | None = None,   # ← 新增，默认 None 保持兼容
) -> list[str]:
```

新增一条规则：

1. **未恢复肌群被安排了主项训练** → `violations.append("腿部股四头肌预计还需 26 小时恢复，
   但计划包含深蹲（主项 4 组）")`。判定方式：把计划正文按 §A 的映射器扫一遍，
   命中的肌群与 `recovery.recovering` 求交集。若某区域同时属于 `reduce` 与
   `explicitly_requested`，说明用户已明确选择带着普通酸痛训练；该区域不走硬拒绝，
   只保留提示与模型自由减量。`painful` 不适用这个例外，仍由安全路径拒绝。

不新增「酸痛肌群必须降到上次训练的某个比例」规则。自由裁量没有可靠的确定性阈值，
校验器只检查可客观判断的未恢复主项冲突，不伪装成能验证模型是否“减得足够”。

**注意一个陷阱**：这里必须复用 `_exercise_set_counts()`（已经处理了"总组数：13 组"
这类汇总行的排除，见它的注释），不要另写一套组数提取——BUG-06 就是两处各写一套
正则造成的。

**同时修掉整篇子串匹配**（清单第三点，与本条同一个函数，顺手改成本很低）：
现在 `if exercise in answer` 会让"本次**避免**深蹲"被判违规。改为**按行判定 + 否决词**：
一行里若出现 避免/不做/已排除/替换为/不建议 就跳过该行。

---

## 4. 进入系统时的清单长什么样

`/session/intro` 现有的「今日健康复查」区块就是模板（`build_session_intro()` 里的
`temporary_health` 分支）。首次加载时，前端先显示数值输入：

```text
Garmin 建议的恢复时间还有几小时？(请输入`0–96.0`)？ [ 0.0 ]
```

控件使用 `min=0`、`max=96.0`、`step=0.1`，业务校验只接受`0–96.0`。
提交后再请求 `/session/intro?garmin_recovery_hours=N`；前端把有效值保存在当前页面会话
状态中，并随本次会话的 `/chat` 请求传递。服务端不把它写入用户档案、记忆或独立文件。
页面刷新或新会话再次询问，不能静默复用旧值。

随后照现有健康复查的形状增加一节：

```markdown
### 肌群恢复状态

- Garmin 恢复时间加成：12 小时（仅当前会话）
- **手臂 · 肱二头肌**：周三训练过（哑铃弯举、锤式弯举，6 组）、周二训练过（哑铃交叉锤式弯举，12组），预计还需 19 小时
- **手臂 · 肱三头肌**：周一训练过（哑铃颈后臂屈伸、哑铃俯身臂屈伸，6 组），已恢复
- **腿部 · 股四头肌**：周五训练过（高脚杯深蹲，4 组），预计还需 26 小时

这些部位现在感觉如何？可以直接说"手臂有点酸""腿都正常"，我会按你的反馈调整今天的安排。
```

措辞对齐你举的例子（"手臂的肱二头肌：周三训练过（哑铃弯举）"）。两处细节：

- **同时列出"已恢复"的项**，不只列未恢复的。用户需要知道系统认为哪些能练——
  这也是模型选替代科目的依据。
- **只列最近 7 天有训练的肌群**，不列从没练过的。否则第一次进系统会刷出 18 行
  "从未训练"，没有信息量。

`/session/intro` 的响应体加 `muscle_recovery_checkin` 数组，与已有的
`temporary_health_checkin` 并列，并回显校验后的 `garmin_recovery_hours`，前端可据此做
交互（不做也能用，文本里已经有了）。

---

## 5. 分阶段实施

每阶段都能独立合并、独立验收，不留半成品。

### 阶段 1：映射与推导（只读，无行为改变）

> 实现状态：✅ 已完成。新增 `muscle_map.py`、`muscle_recovery.py` 与
> `tests/test_muscle_recovery.py`；本阶段不修改 `daily_records.json` 的结构，
> 也不改变 `category_raw` 的现有写入行为。

| 改动 | 文件 |
|---|---|
| 新增 `MUSCLE_RULES` / `SPORT_RULES` / `muscles_for_exercise` / `muscles_for_sport` | `fithealth_agent/muscle_map.py`（新） |
| 新增未知动作的 `LLM_LITE_MODE_ID` 可选兜底与结构化结果校验 | `fithealth_agent/muscle_map.py`（新）及模型调用边界 |
| 在外部模型数据披露中增加「未知动作肌群查询」，明确只发送动作名 | `main.py` |
| 新增 `build_recovery_snapshot` 与恢复窗口表 | `fithealth_agent/muscle_recovery.py`（新） |

**验收**：
- 真实库 16 个动作名 16/16 命中，且「平板卧推」**不再**命中核心。
- 08-17 那条记录推导出 胸大肌 13 组 / 肱三头肌 6 组（次要，折扣后 5.2）/ 核心 0 组。
- 跳绳 36 段折算后 `effective_sets ≤ 4`。
- 未知动作在允许外部模型时只调用 `LLM_LITE_MODE_ID`，结果限制为已知肌群与合法主次角色；
  关闭外部模型、调用失败或非法结果时返回空；同一快照内相同动作只查询一次；外部模型
  披露中能看到该用途。`category_raw` 的现有写入行为保持不变。

已执行：阶段 1 专项测试及现有 FIT/训练记录/数据删除回归测试共 40 项通过。

### 阶段 2：进入系统时的清单（只读，用户可见）

> 实现状态：✅ 已完成。`/session/intro` 现在接收当前请求的 Garmin 恢复小时，
> 返回 `muscle_recovery_checkin`，欢迎页提供输入控件并刷新只读清单。本阶段没有把
> Garmin 值或恢复快照写入存储，也没有接入 `/chat` 计划生成。

| 改动 | 文件 |
|---|---|
| 初始界面增加 Garmin 剩余恢复小时输入与业务校验 | `templates/index.html` |
| `build_session_intro()` 增加「肌群恢复状态」节 | `main.py` |
| `/session/intro` 接收 Garmin 小时并响应 `muscle_recovery_checkin` | `main.py` |

**验收**：用当前真实数据进系统，能看到 08-21 的腿部三个肌群、08-19 的肩部两束、
08-18 的背部与肱二头肌，且措辞与 §4 一致。输入 `0` 时按固定窗口显示；输入 `12.5`
时每个已进入快照的肌群剩余小时统一增加 `12.5`；非法输入提示重填。此阶段
**不影响任何计划生成**。

已执行：session intro、阶段 1 专项及相关回归测试通过；Garmin 输入 `0`、`12.5`、
非法值和刷新后不复用旧值均已覆盖。

### 阶段 3：反馈采集与优先级裁决（开始影响行为）

> 实现状态：✅ 已完成。酸痛反馈采用 72 小时短期存储并即时回执，当前会话 Garmin
> 小时随 `/chat` 请求进入恢复快照；PlanContext 已升级为七级优先级，普通酸痛由模型
> 自由裁量减量，疼痛升级为不可覆盖的安全信号。数据管理支持逐条查看、修改和删除，
> `muscle_soreness.json` 已纳入备份、恢复与“删除全部数据”。

| 改动 | 文件 |
|---|---|
| `SorenessStore` + `data/muscle_soreness.json` | `fithealth_agent/soreness_store.py`（新） |
| `parse_soreness_reply()` 确定性解析 | `fithealth_agent/muscle_recovery.py` |
| `/chat` 里在意图路由**之前**尝试解析酸痛回答 | `main.py` |
| `/chat` 接收当前会话的 `garmin_recovery_hours` 并传入恢复快照 | `main.py`、`templates/index.html` |
| `resolve_plan_context()` 加 `muscle_recovery` 键 | `main.py` |
| `format_plan_context()` 阶梯改七级 | `main.py` |
| `prompts.py` 第 11 条与新阶梯对齐 | `fithealth_agent/prompts.py` |
| 数据管理面板加「肌群酸痛记录」 | `templates/index.html` |

**验收**（这几条是本方案的核心行为，必须逐条实测）：
- 回答"手臂有点酸" → 落盘 + 对话回执 + 当次 PlanContext 出现 `reduce: ["手臂"]`。
- 回答"都正常" → 全部 `asked_regions` 归零，计划不再减量。
- 回答"手臂疼" → 走 `screen_health_risk`，按急性损伤档处理，不是简单减量。
- 周三练过手臂 + 周计划周四也是手臂 + 报了酸痛 → 生成的计划改练别的科目，
  且 `suppressed_rules` 里能看到"周计划：手臂 被肌群恢复状态压制"。
- 用户明确说"今天就要练手臂" + 报了 `sore` → 仍按指令练，但回复说明冲突且减量。
- `sore` 的减量方式由模型结合当天计划自由裁量，不要求固定比例；回复必须把调整说明
  清楚，并保留用户训练时继续调整或停止的决定权。
- 本次会话 Garmin 输入 `8` → 计划裁决使用加成后的剩余小时；新会话不会沿用该值。
- **关闭外部模型时**酸痛解析照常工作（确定性正则，不依赖 LLM）。

已执行：阶段 3 解析、存储、聊天端点、PlanContext、数据管理 API、备份与重置测试通过；
同时覆盖多区域澄清、同句不同区域状态、恢复表达覆盖历史疼痛和疼痛不可强制训练。

### 阶段 4：计划校验（AGENT-08 结案）

> 实现状态：✅ 已完成。恢复快照以可选纯数据参数接入计划校验；计划生成与保存路径
> 均传入当前会话快照。恢复冲突只拦截仍在恢复且没有普通酸痛减量信号的主项动作，
> `sore` 保留模型自由减量，`painful` 继续由既有健康安全路径阻断。动作和频道的
> 禁止项改为逐行识别，并支持“避免/不做/不建议/替换”等否决词，避免整篇子串误判。
> 同一动作按去除 Markdown 装饰和列表编号后的名称去重；已确认的伤病限制由计划生成与
> 保存路径显式传入校验器，限制直接提到动作时按动作拦截，未点名动作的区域性限制才按
> 动作肌群映射拦截，无法可靠映射的健康事实不臆测为全部动作禁做。

| 改动 | 文件 |
|---|---|
| `validate_generated_training_plan` 增 `recovery` 参数与一条恢复冲突规则 | `main.py` |
| 顺手修整篇子串匹配（`avoid_exercise` / `avoid_channel` 两处） | `main.py` |
| 同一计划内重复动作校验 | `main.py`、`tests/test_generated_plan_safety_rules.py` |
| 已确认疼痛/伤病限制进入生成及保存前校验 | `main.py`、`tests/test_generated_plan_safety_rules.py` |

**验收**：
- 手工构造一份"股四头肌还需 26 小时却安排了深蹲 4 组"的计划正文 → 被拒，
  且 `plan_validation.violations` 里说明具体肌群与剩余小时。
- 手臂为 `sore` 且用户明确要求练手臂 → 不因缺少固定减量比例而硬拒绝；普通未覆盖的
  恢复冲突仍拒绝，`painful` 仍走安全路径。
- 计划里写"本次避免深蹲" → **不再**被判违规（这是当前的误判）。
- 同一计划里“`1. 深蹲：4组`”和“`**深蹲**：3组`” → 被拒；杠铃深蹲与高脚杯深蹲不误判为重复。
- “膝盖疼痛，避免深蹲”会拒绝深蹲；“腰部不适”会拒绝罗马尼亚硬拉等可确定的相关动作；
  “高血压”这类无法可靠映射到具体动作的事实不会被臆测为动作禁令。
- 现有 15 条校验测试全部不变（`recovery=None` 时行为与今天逐字一致）。

已执行：阶段相关回归测试 49 项通过；`main.py` 编译检查和差异格式检查通过；
手工验证“股四头肌还需 26 小时 + 深蹲 4 组”会被拒绝，且违规信息包含肌群、
剩余小时、动作和组数；“本次避免深蹲”不会触发禁止动作违规。

---

## 6. 测试计划

新增 `tests/test_muscle_recovery.py`，按现有惯例分四类：

1. **映射表**：16 个真实动作名逐一断言；「平板卧推 ≠ 核心」单独一条；
   否决词生效；六大区枚举固定；未知动作本地返回空；轻量模型兜底使用 mock，覆盖
   合法结果、非法肌群、关闭外部模型、超时、空结果、单次快照去重和最小化外发内容。
2. **推导**：容量加权（主项 1.0 + 次要 0.4）；有氧折算与封顶；恢复窗口的容量调整
   与 1.5 倍封顶；Garmin 输入 `0`、`96.0` 的边界和统一加成；
   负数、超过 `96.0` 与非数字被拒；新会话不复用旧值；`lookback_days` 边界；
   **记录被删除后快照自动跟着变**（这是选
   "现场派生"而非另存的核心理由，必须有测试守住）。
3. **反馈**：四种回答语义；"有点酸"在多部位提问下不猜；`painful` 走安全路径；
   TTL 到期后不再影响裁决；`recovered` 能归零而 `sore` 不能缩短。
4. **裁决与校验**：七级阶梯顺序；周计划被压制时 `suppressed_rules` 有痕；
   明确指令 > 普通酸痛；模型减量不设固定比例；`painful` 不可覆盖；校验器
   `recovery=None` 时与今天逐字一致；同一规范化动作重复出现时拒绝；显式动作限制与
   可确定的伤病区域限制会进入生成和保存前校验，否定动作行不误判，无法映射的健康事实不猜测。

外加 `SourceInvariantTest`（与本仓库其他测试一致的做法）：
- 阶梯段落在 `build_agent_input` 的结果里**只出现一次**（BUG-03 的教训）。
- `prompts.py` 的条目数与阶梯级数一致。
- 组数提取只有 `_exercise_set_counts` 一处实现（BUG-06 的教训）。
- `soreness_store` 的写路径含 `fsync`（DATA-06 的教训）。

**并且按本仓库惯例逐项还原成旧实现，确认每条断言真的会红**——不然就是在陈述
当前行为。

---

## 7. 已拍板的设计决策

1. **恢复窗口固定**：第一版写死 `48/36/24` 小时，不做用户档案配置。初始界面询问
   Garmin 建议的剩余恢复小时；`0` 不加成，`0.1–96.0` 只增加到当前会话中基础上
   仍未恢复肌群的剩余小时。已恢复肌群保持 0；该值不落盘，新会话重新询问。
2. **酸痛减量由模型自由裁量**：后端只给出 `needs_reduction` 定性信号，不规定 60%
   等固定比例，也不做伪精确的组数校验。模型说明调整，用户训练时根据主观感受最终决定。
3. **`region` 固定六大区**：只使用 `胸部/背部/腿部/肩部/手臂/核心`，不拆前臂/上臂；
   具体肌群仍由 `muscle_id` 表达。
4. **以保存后的动作名判断肌群**：本方案不修 `category_raw`，也不新增枚举保留字段。
   本地规则优先；未知动作可在外部模型允许时用 `LLM_LITE_MODE_ID` 查询，失败则留空不猜。

---

## 8. 已知会失效的边界（先说清楚，不要事后当 bug）

- **动作名是用户自由文本，映射必然有漏。** 用户把动作改名成"周三那个动作"就完全
    匹配不上。本地规则未命中时可用轻量模型查询，但外部模型关闭、调用失败或语义仍不明
    时依然会漏报。此时该肌群不出现在清单里（少提醒一次），不会误报；系统列出未识别
    动作，让用户决策。
- **同一天多次训练**：`build_recovery_snapshot` 取该肌群**最近一次**训练时间，
  容量取当天累加。这对"早上练腿晚上再练腿"是对的，对"跨零点训练"会按两天算。
- **手表漏记的训练**（没导入 FIT 就等于没练过）不会进入恢复追踪。这一点要在
  界面上说明，否则用户会以为系统知道他昨天在健身房干了什么。
- **恢复窗口是估算**。界面与计划文本都必须写明"固定窗口按动作与组数估算，Garmin
  小时是用户输入的当前会话加成，不是系统直接读取设备所得"。Garmin 值每次进入系统
  都要重填，不能把旧的剩余小时继续使用。
- **不做医学判断**。`painful` 只走既有的 `health_safety` 分级（建议评估/就医），
  不给康复处方——这条已经是系统提示里的最高优先级规则。

---

## 9. 本方案明确不做的部分（AGENT-08 的其余缺口）

| 缺口 | 建议 |
|---|---|
| 训练量突增 | **单开一条**。需要历史容量基线（周总组数滑动均值），与肌群恢复共用 §B 的推导层，但判定逻辑独立 |
| 同一计划内重复动作 | ✅ 已完成。按去除 Markdown 装饰与列表编号后的动作名去重，不把不同动作变体合并 |
| 疼痛/伤病限制进校验 | ✅ 已完成。生成路径传入 `active_safety_constraints`，保存路径传入已确认健康限制；直接动作限制优先，区域限制仅在可确定映射时生效 |
| 连续高强度日 | 依赖训练量基线，随"训练量突增"一起做 |
| 目标冲突 | 语义判定，确定性校验做不了；建议不做，或只做"计划科目与主目标明显矛盾"的窄规则 |
| `plan_validator.py` 名不副实 | ✅ 已重命名为 `plan_classifier.py`，避免下一个人再找错地方 |
