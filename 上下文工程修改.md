当前结构化记忆已经解决了“正则误判”和“确认后才生效”的核心问题，但仍有以下重要缺口。

## P0：影响记忆正确性的缺陷

1. **模型返回的事实没有和用户原文逐项核对**

后端目前主要校验 namespace、key、value 类型和长度，但没有确认：

- 用户是否真的明确说过这个事实；
- 模型是否擅自补充了用户未说的信息；
- 模型是否把助手自己的建议当成了用户偏好。

例如用户说：

> 我今天练胸感觉还可以

模型却返回：

```json
{
  "namespace": "plan",
  "key": "preference",
  "value": "以后胸部训练应保持当前强度"
}
```

目前结构校验可能接受它。应增加“事实证据”字段，并让后端验证事实能在用户消息中找到依据，或至少标记为低置信度、禁止直接确认。

2. **记忆确认是整条确认，不能逐条确认**

一轮对话可能同时产生：

- 胸部低强度偏好；
- 久坐状态；
- 骨盆前倾；
- 周二练背；
- 一个错误的动作偏好。

现在只能整条点击“确认使用”。用户无法只确认其中两条、拒绝其中一条。

建议改为：

- 每个事实单独显示；
- 每个事实单独确认、拒绝、编辑；
- 只有确认的事实进入长期上下文。

3. **长期记忆和短期反馈使用相同的 3 天 TTL**

当前所有记忆都默认 3 天后过期，包括：

- 健身目标相关偏好；
- 骨盆前倾；
- 久坐人群；
- YouTube 频道排除；
- 每周训练安排。

这会导致用户确认周计划后，几天后它自动消失。

建议分层：

- 永久或长期：目标、伤病、体态、器械、周计划；
- 中期：训练偏好、动作偏好、频道偏好；
- 短期：本次训练疲劳、酸痛、恢复情况；
- 临时：当前会话决策。

4. **事实没有正式的作用范围**

例如：

```text
胸部训练低强度
```

目前可能保存为普通 `plan.preference` 文本，但没有结构化字段说明：

- 作用部位：胸部；
- 作用类型：强度；
- 适用范围：所有胸部训练；
- 强度值：RPE 6-7；
- 是否永久；
- 是否仅本周有效。

建议将事实拆成：

```json
{
  "namespace": "training",
  "key": "intensity_limit",
  "scope": {
    "subject": "胸部训练"
  },
  "value": {
    "max_rpe": 7,
    "volume": "medium_low",
    "failure": false
  }
}
```

5. **大多数记忆还只是通过提示词生效，没有后端硬约束**

当前后端真正确定性使用的主要是：

- YouTube 频道排除；
- 每周训练安排。

但以下事实主要依赖模型自己遵守：

- 避免某个动作；
- 最大 RPE；
- 骨盆前倾；
- 久坐；
- 低强度偏好；
- 训练量限制。

模型如果忽略提示词，后端没有拦截或校验计划。

建议增加计划生成后的结构化检查：

- 计划是否包含被禁止动作；
- RPE 是否超过上限；
- 是否缺少体态相关热身；
- 是否违反周计划科目；
- 是否使用了被排除频道。

## P1：冲突和更新逻辑不足

6. **旧的待确认记忆可能覆盖新的已确认记忆**

场景：

1. 用户第一次说周二练胸；
2. 生成待确认记忆，但用户没有确认；
3. 用户后来确认周二练背；
4. 旧待确认记忆再被确认。

当前确认时没有检查版本和时间关系，旧事实可能重新生效。

应增加：

- 事实版本号；
- 生成时的基准版本；
- 确认时冲突检测；
- 如果已有更新事实，要求用户重新确认。

7. **冲突消解只有“按时间覆盖”，没有优先级系统**

真实场景可能同时存在：

- 健身目标：减脂；
- 周计划：周二背部；
- 临时偏好：今天不想练背；
- 伤病限制：今天腰部不适；
- 训练偏好：胸部低强度。

当前没有统一的优先级模型。

建议固定优先级：

```text
安全/伤病限制
> 用户当前明确指令
> 已确认周计划
> 健身目标
> 长期训练偏好
> 历史摘要
```

并让后端输出冲突结果，而不是完全交给 LLM 自己解释。

8. **“清除”语义不够完整**

当前可以用 `status: cleared` 清除单个事实，但缺少：

- 用户明确说“忘记我有骨盆前倾”后的确定性入口；
- 清除整类事实；
- 清除某个事实的历史版本；
- 清除后是否保留审计记录；
- 清除后是否仍被摘要文本影响。

建议增加独立工具：

```text
forget_memory
```

并要求指定：

- namespace；
- key；
- value；
- 作用范围。

9. **事实缺少时间有效范围**

“我最近膝盖疼”和“我长期有骨盆前倾”不应拥有相同生命周期。

建议支持：

```json
{
  "valid_from": "2026-08-19",
  "valid_until": "2026-09-02",
  "duration_type": "temporary"
}
```

对于伤痛、疲劳、恢复期尤其重要。

10. **周计划结构还不够完整**

当前周计划是：

```json
{
  "mon": "胸部训练",
  "tue": "背部训练"
}
```

但无法表达：

- 休息日；
- 有氧日；
- 上午/晚上；
- 每周循环；
- 每两周变化；
- 临时替换；
- 周计划起止日期；
- 当前是否启用。

建议增加：

```json
{
  "timezone": "Asia/Shanghai",
  "effective_from": "2026-08-18",
  "effective_until": null,
  "days": {
    "mon": {"subject": "胸部训练", "status": "active"},
    "tue": {"subject": "背部训练", "status": "active"},
    "wed": {"subject": null, "status": "rest"}
  }
}
```

## P1：上下文注入问题

11. **记忆检索仍然按重要度和时间，不按当前任务相关性检索**

当前最多注入前 5 条记忆。可能出现：

- 当前要生成背部计划；
- 前 5 条全是胸部偏好和视频频道；
- 周二安排被排除在外；
- 模型因此忘记当天科目。

建议按任务相关性检索：

- 生成训练计划：优先周计划、伤病、动作限制、强度限制；
- 搜索视频：优先频道偏好、动作限制；
- 查询个人信息：返回全部已确认事实；
- 训练复盘：优先近期训练反馈。

12. **结构化事实和摘要仍然混合注入**

当前模型同时接收：

- 结构化事实；
- 自然语言历史摘要；
- 近期会话记录。

摘要可能和结构化事实冲突，模型可能误采信旧摘要。

建议分层：

```text
硬约束：后端已确认事实
软偏好：已确认偏好
近期反馈：短期记忆
历史摘要：只作背景
```

并在模型输入中明确禁止摘要覆盖硬约束。

13. **历史会话截断会丢失重要事实**

退出时只使用当前浏览器内的独立会话历史，而且有数量和字符上限。

如果用户在很早之前提出周计划，后面进行了很多对话，退出时周计划可能已经不在历史窗口内，导致无法保存。

建议：

- 用户创建周计划时立即生成“待确认记忆候选”；
- 不要等退出时才提取长期记忆；
- 退出只保存本次新增的短期反馈。

14. **多标签页之间没有共享会话记忆状态**

两个页面可能分别产生：

- 页面 A：用户确认周计划；
- 页面 B：仍使用旧周计划；
- 页面 B 退出后又保存旧记忆。

建议增加：

- memory revision；
- 页面加载时获取最新版本；
- 确认和删除接口使用版本校验；
- 过期页面提交时返回冲突。

## P1：生命周期和可审计性不足

15. **没有记忆来源证据**

当前保存了 summary 和 facts，但没有保存：

- 来源对话 ID；
- 来源消息 ID；
- 原始用户句子；
- 模型调用时间；
- 使用的模型；
- 用户确认时间；
- 用户确认前后的版本。

建议保存：

```json
{
  "source": {
    "conversation_id": "...",
    "message_ids": ["..."],
    "evidence": "以后练胸的强度低一些"
  },
  "confirmed_at": "...",
  "confirmed_by": "user"
}
```

这样用户才能知道这条记忆为什么出现。

16. **无法查看“某条记忆影响了哪些计划”**

用户现在只能看到“这条记忆已确认”，但不知道：

- 哪次计划使用了它；
- 哪个动作因为它被替换；
- 哪个 YouTube 结果因为它被过滤；
- 哪次计划违反了它。

建议增加 memory usage log：

```text
记忆：胸部低强度
使用于：2026-08-18 今日训练计划
影响：卧推 RPE 从 8 调整为 6-7
```

17. **没有记忆变更历史**

如果用户多次修改：

```text
周二练胸
周二练背
周二休息
```

当前只保留最终状态，用户无法查看变化过程，也无法恢复旧版本。

建议增加：

- revision；
- changed_at；
- changed_reason；
- previous_value；
- rollback。

18. **确认操作缺少幂等和并发保护**

两个标签页同时点击确认可能导致：

- 重复写入；
- 旧页面确认已被删除的记忆；
- 确认结果覆盖最新状态。

建议确认接口使用：

- `revision`；
- `If-Match`/ETag；
- 幂等请求 ID。

## P2：语义质量问题

19. **中文同义词没有规范化**

例如这些可能被保存成不同事实：

- 盆骨前倾；
- 骨盆前倾；
- 骨盆前倾问题；
- 久坐；
- 长期久坐；
- 久坐人群。

目前主要依赖字符串去重，容易产生重复记忆。

建议增加规范化词典和 canonical value：

```text
盆骨前倾 -> 骨盆前倾
久坐人群 -> 长期久坐
```

20. **负面表达仍需要更严格处理**

以下表达不能都当成正向偏好：

- 我不喜欢这个动作；
- 我不想今天练这个动作；
- 这个动作让我疼；
- 这个频道的视频不适合我；
- 我不喜欢这个频道；
- 这次训练没有效果。

应区分：

- 长期负偏好；
- 当前临时拒绝；
- 疼痛/安全限制；
- 对训练效果的反馈；
- 对频道的负偏好。

21. **没有区分事实、偏好、建议和状态**

例如：

```text
我想减脂
我正在减脂
我建议减脂
减脂期应该这样训练
```

它们的语义不同，但目前可能被模型归入相同 `plan.preference` 或 `training_feedback`。

22. **没有健康事实的严重程度和可信度**

“有一点腰酸”和“医生诊断腰椎问题”不应拥有相同优先级。

建议增加：

- `severity`；
- `confidence`；
- `source_type`；
- `medical_confirmation`；
- `needs_professional_review`。

## P2：用户体验和测试不足

23. **待确认记忆没有明确区分“候选”和“已生效”**

虽然界面已有确认状态，但聊天回复仍可能说“我会记住”。应统一显示：

- 本次会话有效；
- 已生成待确认候选；
- 已确认，跨会话生效；
- 已过期；
- 已被清除；
- 被新事实覆盖。

24. **用户无法主动编辑结构化记忆**

目前主要是确认和删除，没有编辑功能。用户需要删除后重新对话，成本较高。

建议提供：

- 编辑事实；
- 修改有效期；
- 修改适用范围；
- 修改优先级；
- 修改备注。

25. **退出时才保存长期记忆，时机太晚**

长期事实应该在用户明确表达后立即显示候选，例如：

> 检测到你提出了一个长期偏好：周二安排背部训练。是否加入跨会话记忆？

退出再处理会造成：

- 用户忘记确认；
- 浏览器刷新丢失；
- 会话历史被截断；
- 用户误以为已经保存；
- 模型在当前会话中说“记住了”，但实际没有候选。

26. **没有针对记忆流程的完整端到端测试**

目前主要是函数级测试，仍需增加真实流程测试：

- 用户表达周计划；
- 模型调用 `save_memory`；
- 前端显示待确认；
- 用户确认；
- 刷新页面；
- 输入具体日期；
- 后端解析星期；
- 生成指定科目计划；
- 保存卡显示；
- 检查计划内容没有违反限制。

27. **缺少对模型异常输出的可观察性**

如果模型没有调用工具、调用参数不合法或返回多个工具调用，用户通常只看到“未保存”，但不知道：

- 是模型没调用；
- 参数非法；
- 事实被过滤；
- 外部模型关闭；
- 记忆冲突；
- 数据写入失败。

建议返回用户可理解的状态码，并在日志中保留内部诊断信息。

## 最建议优先实现的五项

1. **事实证据校验**：防止模型凭空创造记忆。
2. **长期记忆与短期反馈分离 TTL**。
3. **逐事实确认、拒绝和编辑**。
4. **周计划、伤病、强度限制的后端确定性校验**。
5. **记忆版本、来源和冲突审计**。

当前最关键的架构方向是：让记忆从“退出时生成的一条摘要”升级为“有来源、有范围、有生命周期、可确认、可冲突消解、可被后端规则执行的事实对象”。