# FitHealthAgent 代码审查与需求清单

> 审查日期：2026-08-19
> 审查范围：main.py(2345行)、fithealth_agent/(20个模块)、templates/、tests/(30个文件)、data/ 实际数据、git 历史
> **使用定位：个人本地单用户，不上线、不外泄。** 因此本清单**不含**认证/CORS/监听地址/CSP/供应链/密钥轮换/多用户隔离类条目（list.md 中的 SEC-001~004 在此定位下可降为「有空再说」）。
> 本次测试基线：补齐 `hello_agents` 桩后 `pytest` = **118 通过 / 5 失败**。5 条中 3 条是沙箱桩对象缺属性导致的假失败，**2 条是真实失败**（见 BUG-13）。
>
> **进度更新 2026-08-20**：DATA-01、DATA-02、DATA-03、**DATA-04**、**DATA-05**、**DATA-08**、AGENT-01、BUG-01、BUG-02、**BUG-03**、**BUG-04**、**BUG-05**、BUG-06、**BUG-07**、**BUG-08**、**BUG-12**、BUG-13、AGENT-02（校验部分）已修复并验证（共 18 条）。新增测试文件 `test_data_fixes_20260819.py`、`test_health_safety_gate.py`、`test_local_fallback_and_set_counts.py`、`test_agent_write_validation.py`、`test_plan_draft_integrity.py`、`test_info_store_corruption_guard.py`、`test_quarantined_workout_recovery.py`、`test_fit_parser_sport_routing.py`、`test_memory_fact_addressing.py`、`test_plan_date_context.py`、`test_multi_zip_activity_import.py`、`test_workout_discard_button.py`。当前基线 **369 通过 / 0 失败 / 1 跳过**。
>
> **第一批「止血」已全部完成**：AGENT-01、DATA-01、DATA-02、DATA-05、DATA-08。
> **第二批「正确性」已全部完成**：BUG-01+safety_bypass 正则、BUG-02、BUG-03、BUG-04、DATA-03、DATA-04、BUG-05、BUG-06、BUG-07、BUG-08、BUG-12、BUG-13。**第 3 节「写了但从未接线」的死代码已清零。**
> 
> **进度更新 2026-08-21**：第三批「数据安全底座」**已全部完成**——**DATA-06**（fsync 全仓库落地）、**DATA-07**（profile 锁）、**DATA-09**（journal 退 DELETE + 临时存储只读）、**DATA-11**（备份补全内容 + 每成员 sha256，格式升 v2）、**DATA-12**（恢复期间 maintenance 开关 + JSON 文件锁 + HealthStore 独占/reopen），另加 **DATA-13**（合并已保存训练组的数值聚合与心率三档口径）与 **DATA-14**（`/data/reset` 先落恢复点 + 逐项容错 + 返回明细）。新增 `fithealth_agent/atomic_json.py`（所有 JSON 写路径的唯一持久化入口）与 `fithealth_agent/maintenance.py`（全局维护开关），新增测试 `tests/test_durable_writes_and_profile_locking.py`（19 条）、`tests/test_journal_backup_and_restore_safety.py`（41 条）与 `tests/test_reset_transactionality.py`（32 条），重写 `tests/test_saved_training_record_editing.py`（14 条）。第三批剩余的 **ARCH-04**(锁泄漏) 已完成。
>
> 另完成第三节的 **BUG-09**（每日打卡按日期 upsert）、**BUG-10**（缺 session 的 FIT 不再静默丢光整场训练；**清单原描述的"抛 AttributeError"前提是错的**，详见该条）、**BUG-11**（合并携带编辑区草稿，按钮不再永久变灰），新增 `tests/test_checkin_fit_and_editor_fixes.py`（35 条）。当前基线 **524 通过 / 0 失败**，并已在 7 种随机执行顺序下复跑通过——过程中修掉了 `test_reset_transactionality.py` 自己留下的一处顺序依赖（它把 main 首次 import 绑到了用例自己的临时目录上，该目录随后被删除，会让另一个文件里一条无关的中间件测试变红）。
>
> 再完成 **BUG-14**（view_* 只在纯导航时短路，复合请求挂起 artifact 继续走 Agent）、**BUG-15**（**清单前提经实测不成立，其建议修法会造成 8 小时偏移**；改为把"naive 即 UTC"的契约写死 + 字段改名不再静默丢光压力数据）、**BUG-16**（按天趋势与日汇总共用同一份去重 SQL），新增 `tests/test_view_intent_stress_tz_and_trend_dedup.py`（31 条）。当前基线 **566 通过 / 0 失败**，同样在 7 种随机顺序下复跑通过。
>
> **进度更新 2026-08-22**：新增 **DATA-15**（扩展 wellness 解析范围：静息代谢率 / 静息心率 / 强度分钟 / 设备时区，另修 FIT 睡眠时长把清醒算进去的口径错误并补夜间心率）与 **DATA-16**（身体电量与 METRICS 文件的逆向线索，列为待办）。新增 `scripts/backfill_health_fields.py` 并已在真实库回灌，`tests/test_extended_health_fields.py`（35 条）。
>
> 随后**删除后端的 Mifflin-St Jeor 基础代谢估算**（手表实测的静息代谢率已入库，再留一个公式估算只会让两个数打架，且档案缺一项就退化成"待补充信息"），并在健康趋势中**加入「总热量（静息 + 活动）」**：日视图是日内累计曲线（静息按日速率均摊、活动取截至该小时的累计值，末点等于日汇总的 `total_calories`），周/月读日汇总那一列。`tests/test_calorie_trend_and_bmr_removal.py`（23 条）。当前基线 **624 通过 / 0 失败**，6 种随机顺序下复跑全绿。
>
> **进度更新 2026-08-23**：AGENT-08 的肌群恢复链路阶段 1–4 已完成。训练记录现在可派生肌群负荷和恢复窗口；当前会话 Garmin 加成与用户酸痛反馈会进入计划裁决；计划生成和保存前会拒绝仍在恢复中的主项训练。同步修复了计划中“避免某动作/不建议某频道”被整篇子串误判的问题。阶段相关回归测试 30 项通过。

---

## 0. 一句话结论

项目已经是一个完成度很高的个人健康管理 MVP，力量训练组编辑、心率重算、结构化记忆确认这几处设计（脏活留 Python、决策交 LLM）是站得住的。但当前存在四类真实问题，按危害排序：

1. **数值口径错误正在持续污染数据**——速度被放大 3.6 倍、自重动作被吞成"休息"，且这些错值已随训练记录落盘，Agent 会把它们当事实读。
2. **一批"写了但从未接线"的死代码**——安全约束拦截、本地兜底路由、优先级阶梯全部定义完毕却零调用，看起来实现了，实际完全没生效。这是最危险的一类，因为它让你误以为有兜底。
3. **记忆系统的文档承诺与代码实现存在明显差距**——14 项声称中 4 项未实现、4 项部分实现，且有 2 条你自己的测试正在失败。
4. **Agent 仍缺少部分健身场景核心分析工具**——PR 追踪、训练容量和趋势对比尚未实现；肌群恢复间隔已实现为进入系统、计划裁决和计划校验共用的派生链路。

---

## 1. 文档声称 vs 代码实际（核对表）

对 `上下文工程修改.md` 提出、`开发日志.md` 声称已在 08-18/08-19 修复的 14 项逐条核实：

| # | 声称 | 结论 | 证据 | 差距 |
|---|---|---|---|---|
| 1 | TTL 四层分离 | ✅ 已实现 | info_store.py:62-108,131-150 | `permanent` 档不可达，模型传 `duration_type="permanent"` 无效 |
| 2 | 逐事实确认/拒绝/编辑 | ✅ 已实现 | info_store.py:520-581 | — |
| 3 | 记忆卡片内联编辑 | ✅ 已实现 | index.html:1483-1515 | 只能改 value/evidence，改不了有效期与作用范围 |
| 4 | save_memory 受限事实数组 | ✅ 已实现 | info_store.py:193-268 | schema 允许 500 字但后端截断到 120，长值永远校验不过 |
| 5 | 事实证据校验 | ⚠️ 部分 | information_router.py:167-206 | 标点常量写错，拉丁文本可绕过（AGENT-04） |
| 6 | 冲突需用户确认 | ✅ 已实现 | info_store.py:388-399,528-536 | 重复确认同一事实会抛异常，不幂等 |
| 7 | 计划生成后硬校验 | ⚠️ 部分 | main.py:154-243 | 只校验"已确认记忆"里的项；体态热身无规则；正则误伤（BUG-06） |
| 8 | 混合优先级策略 | ❌ 未生效 | main.py:585-604 定义后零调用 | 优先级阶梯从未进入模型上下文（BUG-03） |
| 9 | 周计划休息日/有氧/起止 | ❌ 未实现 | info_store.py:172-190 | 实测 `{"wed":null}` 与带 `effective_from` 的对象均被整条丢弃 |
| 10 | 记忆来源证据 | ⚠️ 部分 | info_store.py:255 | 无 conversation_id / message_id / 模型名 / confirmed_at |
| 11 | 变更历史与回滚 | ❌ 未实现 | edit_fact:557-581 原地覆盖 | 无 previous_value / rollback |
| 12 | 确认的幂等与并发保护 | ⚠️ 部分 | info_store.py:528-536 | 有版本检测，无 If-Match/请求ID；reject/edit/delete 完全无版本校验 |
| 13 | 按任务相关性检索 | ❌ 未实现 | info_store.py:492-509 | 仍只按 (importance, created_at) 取前 5 |
| 14 | forget_memory 工具 | ❌ 未实现 | 全仓无此工具 | 只能靠模型自发写 status=cleared |

---

## 2. P0 —— 正在产生错误数据 / 会丢数据

### DATA-01 速度被放大 3.6 倍（已实测确认）

> ✅ **已修复 2026-08-20** —— `fit_parser.py:349-361` 新增 `_KMH_TO_MPS` 常量与 `_speed_mps()` 辅助函数，在**读取处**就把 fitparse 交来的 km/h 还原成 m/s；4 个读取点已全部切换：lap 均速/最大（`fit_parser.py:486-487`）、session 均速/最大（`fit_parser.py:735-739`）。展示层的 `× 3.6` 保持不动（`main.py:1200`、`index.html:2419`），链路自此自洽：10.8 km/h 进来 → 存 3.0 m/s → 显示 10.8 km/h（修复前显示 38.9）。`total_distance_m` 未受影响，仍是正确的米。
>
> **无需数据迁移**：已核查 `daily_records.json` 中 82 个速度字段当前全为 0（尚未保存过骑行/跑步记录），不存在历史脏数据。
>
> 回归测试见 `tests/test_data_fixes_20260819.py::SpeedUnitContractTest`（4 条）。其中 `test_fitparse_still_converts_speed_to_kmh` 是**契约锁**——它直接断言 fitparse 的 `StandardUnitsDataProcessor` 仍把 `*_speed` 转成 km/h 且不动 `total_distance`。一旦 fitparse 升级后改了这个行为，这条会先失败，而不是让速度反向错 3.6 倍且悄无声息。

`fit_parser.py:655` 用了 `fitparse.StandardUnitsDataProcessor()`，该处理器**已经把所有 `*_speed` 字段从 m/s 转成 km/h**。实测：

```
avg_speed  3.0 m/s -> 10.8 km/h      total_distance 5000 m -> 5000 m（未转换，是对的）
```

但代码把它存进名为 `avg_speed_mps` / `max_speed_mps` 的字段（fit_parser.py:458-459、707-712），下游又全部按 m/s 再乘 3.6：`main.py:1200`、`main.py:1216`、`main.py:1273`、`index.html:2419`。

**后果**：骑行/跑步的均速与最大速度在对话文本和 UI 卡片里都被放大 3.6 倍（10.8 km/h 显示成 38.9 km/h），且错值随 `to_dict()` 落进训练记录，Agent 当事实读取并据此给建议。

**修法**：解析处除以 3.6 还原成 m/s，或（更干净）字段改名 `avg_speed_kmh`，删掉三处 `* 3.6`。注意 `total_distance` 未被转换，`total_distance_m` 是正确的米，不要一起改。**建议同时加一个断言型单测锁死这个契约**，因为 fitparse 升级可能改行为。

### DATA-02 raw_path 存绝对路径，删除全部落空、原始文件永远删不掉（已实测确认）

> ✅ **已修复 2026-08-20** —— 三处改动：(1) `health_importer.py` 改为只存 `raw_path.name`；(2) `health_store.py` 新增 `resolve_raw_file()`（只取 basename + 强制 `parent == raw_dir` 归属校验）与 `_delete_raw_file()`，`delete_import` / `clear` 全部改走它，越界只记日志不删；(3) 迁移脚本 `scripts/migrate_raw_path.py` 已在真实库执行，18 条记录全部规范化，可解析文件数从 **0/18 提升到 13/18**（另 5 条的原始文件确已丢失，导入数据本身不受影响）。数据库已自动备份为 `data/health.db.pre-rawpath-*`。磁盘上另有 3 个孤儿文件，脚本已列出，可手动删除。

`health_importer.py:498` 写入 `str(raw_path.resolve())`。实测当前 `data/health.db`：

```
health_imports 有 raw_path 的行数: 16
其中指向 data/health-imports 之外的: 16      <- 全部是 /opt/project/data/health-imports/...
按存库路径实际存在的文件数: 0 / 16          <- 删除操作 100% 落空
磁盘上实际的原始文件数: 14                   <- 已产生 14 个删不掉的孤儿
```

这是 Docker 容器路径（`/opt/project`）被写进库后，在 Windows 本机运行时全部失效。后果有二：其一，删除导入时 `health_store.py:1033/1043` 的 `unlink` 全部落空，原始 zip 无限累积；其二，一旦路径在某个环境下**恰好可解析**，`delete_import` / `clear()` 会对数据目录之外的任意路径执行删除（备份跨机恢复时尤其危险）。

**修法**：只存相对 `raw_dir` 的文件名；删除前强制 `resolved.is_relative_to(self.raw_dir.resolve())` 校验，不通过只记日志不删。**另需写一次性迁移脚本**，把现有 16 条按 basename 重新关联到磁盘上的 14 个文件，并清理 2 条孤儿记录。

### DATA-03 自重/计时动作被误判成"组间休息"，且界面无法修正（已实测确认）

> ✅ **已修复 2026-08-20** —— `fit_parser.py` 的兜底加了 `set_type != "active"` 前置条件：显式 `rest` 和缺失 set_type 的老文件维持原行为，`active` 的自重/计时组不再被吞。修复后平板支撑等会正常进入 `set_active`、可编辑、计入组数。前端"这不是休息"翻转按钮**尚未做**（属可选的人工出口，见下）。

`fit_parser.py:266-267` 的兜底**不看 set_type**：

```python
if weight_kg == 0.0 and reps == 0:
    return "组间休息", "rest_fallback"
```

实测：`平板支撑 active set -> ('组间休息','rest_fallback')`；`俯卧撑(未计次) active -> 同样被吞`。

后果是连锁的且**无恢复路径**：`workout_store.py:717` 过滤掉它导致组数/次数少算 → `workout_store.py:400,449` 拒绝编辑 → `index.html:2486` 只渲染一条休息横条不生成编辑器 → 用户即使想手改也过不了 `workout_store.py:513-525` 的校验。只能重新导入。

**修法**：兜底加 set_type 前置条件（`if set_type != "active" and weight==0 and reps==0`）；并给前端加一个"这不是休息"的翻转按钮作为人工出口。

### DATA-04 `sport=training` 且无 set 消息时，lap 与心率被整段丢弃（已实测确认）

> ✅ **已修复 2026-08-20** —— 四处改动：
> - **(1) `StrengthTrainingParser.parse()` 降级**：`if not segments: return LapBasedParser().parse(...)`。降级条件用"解析结果为空"而不是"没有 set 消息"，因为 set 消息存在但全都缺 `start_time` 时结果同样是空。这是最关键的一道保险——无论路由怎么判错，lap 与心率都不会再凭空消失。
> - **(2) sub_sport 参与路由**：`_pick_parser(sport_raw, sub_sport_raw)`，`SportParser` 新增 `SUB_SPORT_KEYS`。裁决规则：**sub_sport 只在 sport 缺失或只是大类（`training`/`generic`）时才有裁决权**——否则 `sport=cycling` 配上 `sub_sport=hiit` 会被间歇解析器抢走。`training` + `cardio_training/yoga/pilates/breathing/flexibility_training` → LapBased；`training` + `hiit/interval_training/circuit_training/tabata` → Interval；`training` + `strength_training` 或**无 sub_sport**（老文件，无从判断）→ 仍走 set 解析，保持原行为。
> - **(3) `IntervalSportParser.can_handle()` 不再是死代码**：路由统一走 `can_handle`，删掉了 `_pick_parser` 末尾手写的那份重复整数判断。基类 `can_handle` 只看 sport；顺带修了 `isinstance(True, int)` 会把布尔值当自定义运动的问题。注册表顺序改为 Strength → Interval → LapBased → **Fallback 必须最后**（它 `can_handle` 恒真）。
> - **(4) `_sport_to_zh` 接受 sub_sport**：`sport=training` 单看只能得到"训练"这种没有信息量的名字，sub_sport 能说清是有氧/HIIT/瑜伽时就用它。文件自带的 `sport.name` 仍然优先。
>
> **实测复现（用被误隔离那份文件的真实 session + 1802 条心率）**：`sport_raw=training / sub_sport=cardio_training` 路由从 `StrengthTrainingParser`（segments=**0**，lap 与心率全丢）变成 `LapBasedParser` → **segments=1**，`avg_hr=94 / max_hr=147 / 232 kcal / 3853.5s`，`lap_trigger=session`。
> **无回归**：`data/health-imports/` 里三份真实力量训练 FIT（628992811 / 629691036 / 629296504，均为 `training + strength_training`，38 / 12 / 31 个 set 消息）解析结果逐一比对，segment 数与类型完全不变。
>
> 顺带修了 `main.py` 的渲染分支：原来 `is_strength = sport_raw in ("strength_training","training")`，降级出来的 lap 分段会被当成"动作组"渲染成一串重量 0、次数 0 的假数据。现在以**实际分段类型**为准（`has_sets`），只有在完全无分段时才回落到 sport_raw 判断以保留原有的提示文案。前端 `index.html` 早已按 `segment_type` 分支，无需改动。
>
> 回归测试 `tests/test_fit_parser_sport_routing.py`（19 条 + 19 个子用例），补上了 ARCH-06 指出的 `fit_parser.py` 零覆盖缺口的一部分：路由表逐项、降级三种情形（无 set / set 不可用 / 无 set 也无 lap 时退到 session 整段）、正常力量解析不受影响、命名规则、以及源码断言钉住 `_pick_parser` 确实调用 `can_handle`、`parse_fit_file` 确实传了 sub_sport。
>
> **已知遗留（未改，属口径判断）**：`_save_confirmed_workout` 的 `cat_key = "training" if "strength" in sport or "训练" in sport else sport`，会把"有氧训练"也归到 `training` 分类。这是**既有行为**（改之前这类文件叫"训练"，同样命中），改动会切分历史记录，留待需要按运动类型统计时一并处理。

`fit_parser.py:386` 把 `"training"` 路由给 `StrengthTrainingParser`，但 Garmin 的 `sport=training(10)` 是**大类**（HIIT、瑜伽、普拉提、有氧训练都用它），只靠 sub_sport 区分。而 `StrengthTrainingParser.parse()` 只读 `raw_messages["set"]`，**无任何 fallback**。

实测一段 20 分钟、3km、心率 140 的 training 活动：`StrengthTrainingParser 产出 segments=[]`（lap 与心率全丢），走 `LapBasedParser` 则正常得到 avg_hr=140。用户只看到"未找到任何训练组数据"。

**修法**：`StrengthTrainingParser.parse()` 开头加 `if not raw_messages.get("set"): return LapBasedParser().parse(...)`；并让 sub_sport 参与路由。顺带：`IntervalSportParser.can_handle()`（fit_parser.py:508-515）是死代码，`_pick_parser` 从未调用它。

### DATA-05 待确认训练被误判"损坏"并单向隔离，合法数据已实际丢失

> ✅ **已修复 2026-08-20** —— 四处改动，另附一次性回灌：
> - **(a) 隔离改 copy-then-unlink**：`_quarantine_corrupt_persisted` 先 `shutil.copy2` 出副本、副本落盘成功后才删原件；**隔离失败时不再 `unlink`**（原实现在这里直接删掉唯一一份数据，正是本条的成因），代价只是每次启动多一条告警。
> - **(b) 先降级为"部分可用"**：新增 `_restore_activity(payload, lenient=)`。严格恢复失败时自动重试宽松模式——逐条跳过读不出来的分段/心率点，只要 session 能读出来就照常恢复，并把 `restore_issue` 置为 `PARTIAL_PENDING_WORKOUT`。**只有连 session 都解析不出来才隔离。**
> - **(c) 隔离文件可列出、可重放**：`list_quarantined()` 对每份文件做只读试恢复，返回运动类型/起始时间/分段数/心率条数；`replay_quarantined(name)` 载回当前待确认状态并生成新的确认凭据。对应 `GET /workout_state/quarantined` 与 `POST /workout_state/quarantined/restore`，前端在没有待确认训练时会主动提示可恢复的隔离文件。重放**不删除**隔离文件，且只接受纯文件名 + 强制目录归属校验（同 DATA-02 思路），也拒绝覆盖已有的待确认训练。
> - **(d) 1Hz 心率流不再被丢弃**：新增 `fithealth_agent/hr_stream_store.py`。确认保存时把心率流按记录 id 旁挂成独立文件，训练记录里只留一份体积恒定的摘要（条数/最小/最大/均值/覆盖窗口）。
>
> **心率流为什么既不进 `daily_records.json` 也不进 `health.db`**（这是审查时没预料到的冲突，实测后定的口径）：
> - 进 `daily_records.json` → `query_daily_records` 会把整条 record 交给 ReAct 观察，一段 64 分钟训练就是 1802 个点，直接撑爆上下文；
> - 进 `health.db` 的 `heart_rate_samples` → 那张表是全天 1 分钟级监测数据，日汇总与趋势按"每个采样点等权"算均值。实测 2026-08-15 已有 **1212 个分钟级时间点且完整覆盖这段训练窗口**，再灌 1802 条 1Hz 进去，训练那一小时会被加权约 30 倍，当天日均心率从 78.7 直接失真。
>
> **一次性回灌已执行**：`scripts/backfill_quarantined_hr.py`（默认预演，`--apply` 才写入并自动备份）。实测结果——那条训练记录**其实并没丢**：用户后来重新导入过同一份 FIT（`628252995_ACTIVITY.fit`，sha256 相同），记录 `16e93f38` 一直在库里；真正丢的只是确认时被丢弃的心率流。所以脚本走的是"只补挂心率流、绝不覆盖记录正文"的路径：**1802 条心率已挂到 `16e93f38`**（均 94.3 / 峰 147 / 覆盖 10:52:53–11:57:06 UTC），revision 1→2，其余字段逐字未动。重复执行是 no-op。备份在 `data/daily_records.json.pre-hrbackfill-*`，隔离文件按设计保留在原处未删。
> 另一个隔离文件 `corrupt-20260814T105853` 确实没有可救内容（无 session 起始时间、0 条心率），脚本会跳过。
> 顺带：`_persist_meta` 改成 `flush + os.fsync + replace` 且 tmp 名带 pid+uuid——心率流可达 1MB，半截文件正是走进隔离路径的主要原因；删除训练记录与 `/data/reset` 会同步清理旁挂的心率流，不留孤儿文件。
>
> 回归测试 `tests/test_quarantined_workout_recovery.py`（27 条 + 9 个子用例）：空 sets 的有氧照常恢复、单个坏分段只降级不隔离、隔离副本逐字节一致、隔离失败保留原文件、列表/重放/路径穿越拒绝、确认后记录里**只有摘要没有原始采样点**。含 `SourceInvariantTest` 钉住隔离不再用 `replace`、失败分支不含 `unlink`、以及接口确实接进了 main.py。
>
> ⚠️ **本条第一版引入过一个 UX 缺陷，已修（2026-08-20 晚）**：启动时用阻塞式 `window.confirm` 逐个询问是否恢复隔离文件，而提示只看磁盘上有没有 `pending_workout.corrupt-*.json`，**没有任何"已处理"状态**——点取消不留痕迹，点载入之后 `replay_quarantined` 又刻意不删文件（重放本该非破坏性），于是同一个弹窗永远拦在进入路径上。修法四条：(1) 启动改成**非阻塞** `addNotify`，并跳过既无分段也无心率的空壳文件；(2) 新增旁挂标记 `<文件名>.dismissed`，`list_quarantined()` 默认跳过、`include_dismissed=True` 才返回全部，**数据一个字节都不动**；(3) `replay_quarantined()` 成功后自动标记——处理过就不该再问；(4) 新增 `POST /workout_state/quarantined/dismiss` 与 `/delete`，数据管理面板加「被隔离的未确认训练」区块（载入编辑 / 不再提醒 / 永久删除）。**教训：任何"启动时提示用户处理遗留数据"的设计，必须同时给出持久化的已处理状态和删除出口。**
>
> **未做**：`sets` 为空的根因是 DATA-04（`sport=training` 被路由给力量训练解析器，lap 在解析阶段就丢了），仍待修；修好后重新导入这份 FIT 才能拿回分段。

`data/` 下的两个 `pending_workout.corrupt-*.json` **根本没有损坏**——我直接解析确认：

| 文件 | JSON 合法性 | 内容 |
|---|---|---|
| corrupt-20260814T105853 | 完全合法 | version:2, sets:[], hr_records:[] |
| corrupt-20260816T085939 | 完全合法 | version:2, sets:[], **1802 条心率**, 完整 session（有氧 2026-08-15 18:52） |

根因在 git 历史：commit `48847f6` 引入、`1182bd8` 才删除的 `if not segments_data: raise ValueError("训练分段为空")`——**空 sets 对有氧/跳绳/无 lap 的 FIT 是完全正常的**，却被当成损坏；`699fbe5` 又给这条路径加了隔离动作，于是合法数据被 `_PERSIST_PATH.replace()` 单向搬走，且 `workout_store.py:235-267` 只回一句提示文案，**没有列表接口、没有重放接口**。

**修法**：(a) 隔离前 `copy` 而非 `replace`；(b) 恢复失败先降级为"部分可用"；(c) 加隔离文件列表与一键重放；(d) **给那 1802 条心率写个一次性回灌脚本，值得救**。

### DATA-06 全仓库零 `fsync`，断电可能得到 0 字节文件

> ✅ **已修复 2026-08-21** —— 抽出**唯一**的持久化写入点 `fithealth_agent/atomic_json.py`：
> - `atomic_write_json(path, data)` = 写临时文件 → `flush` → `os.fsync(fileno)` → `os.replace`，之后再 best-effort `fsync` 一次父目录（让 replace 这次目录项变更本身也落盘；Windows 无法 fsync 目录，那里直接跳过，失败也不让一次已经 fsync 过内容的写入报错）。
> - 临时文件名统一为 `<name>.<pid>.<uuid4>.tmp`。固定的 `<name>.tmp` 才是 DATA-07 产出拼接垃圾的直接原因，所以这两条一并收在同一个工具里。
> - 接入的写路径：`storage.py`（`DailyRecordStore._write_all` **以及首次建库那次 bootstrap 写入**——它原先是裸 `write_text("[]")`、`UserProfileStore._write`）、`plan_store.py`（`_write_all`）、`external_model_settings.py`（`_write`）。
> - **额外补上一处审查时漏掉的**：`external_model_settings.py`。它存的是"要不要把数据发给外部模型"的隐私开关，而 `_read` 在读失败时静默回落到 `DEFAULT_SETTINGS`（也就是 `external_models_enabled: True`）——断电后读到 0 字节文件，会把用户显式关掉的开关又打开。
> - 早先已在 `info_store.py` / `workout_store.py` / `hr_stream_store.py` 三处就地落地（见 DATA-05、DATA-08），本次没有回头重构它们：那三处各自带着降级判断与专用 payload 组装，动它们的收益不如风险。改为用源码断言钉住"这三个模块的写路径必须含 fsync"。
>
> 回归测试 `tests/test_durable_writes_and_profile_locking.py`。取向是钉不变量而不是数调用次数——断电没法在单测里复现，所以验证的是：fsync **发生在 replace 之前**（用一个记录式 fsync 在被调用时读回目标文件，此刻必须还是旧内容）、临时名互不相同且带 pid、写入中途失败时原文件逐字节不变且不留 `.tmp` 垃圾、以及五个 store 的写路径确实都走到了 fsync。

`Path.write_text` 只写到 OS 页缓存，`replace()` 只保证目录项替换原子、不保证内容落盘。Windows 上突然断电，`daily_records.json` 可能变成 0 字节或半截。

已 grep 确认 `fithealth_agent/` 与 `main.py` 内**没有任何 `os.fsync`**（storage.py:11-17、info_store.py:368-374、plan_store.py:33-36、workout_store.py:157-162）。

**修法**：写 tmp 后 `f.flush(); os.fsync(f.fileno())` 再 `os.replace`。

### DATA-07 `UserProfileStore` 完全无锁，并发写档案会产生拼接垃圾

> ✅ **已修复 2026-08-21** —— 四处改动：
> - **tmp 名带 pid+uuid4**：走 DATA-06 的 `atomic_write_json`，两个并发写入不再共用同一个临时文件。
> - **所有公开方法套 `RLock` + `JsonFileLock`**：`get_profile` / `update_profile` / `reset` 全部加锁，`__init__` 里的首次建档也加锁并**双重检查**（否则两个实例同时初始化时，后来者会把前者刚写好的档案覆盖回默认值）。读路径也加——档案是个整体，读到一半被替换掉没有意义。
> - **read-modify-write 整段收在锁内**：新增私有的 `_normalized_profile()`（无锁版 `get_profile`），`update_profile` 在同一把锁里完成"读 → 改 → 写"。**私有方法 `_read` / `_write` / `_normalized_profile` 刻意保持无锁**：`JsonFileLock` 每次进入都新开一个文件句柄，**不可重入**，在私有方法里再套一层会直接死锁。这条有源码断言钉住。
> - **合并逻辑从 main.py 挪进锁内**：`update_profile(updates, *, merge=...)` 新增可选的合并回调。`/profile/confirm-update` 原先是 `get_profile()` → `merge_profile_updates_with_existing()` → `update_profile()`，**这三步之间的窗口才是真正丢更新的地方**——两个并发确认读到同一份旧档案，后写的那个把前一个追加的器械直接抹掉。现在改成 `update_profile(updates, merge=merge_profile_updates_with_existing)`，合并用的是锁内刚读到的档案。
>
> 回归测试同 DATA-06 的文件。除了"并发写入永不产出拼接垃圾"，关键一条是**丢更新**：12 个线程跨 3 个 store 实例各追加一件器械，最终必须全部在册。已用一份还原成旧实现（无锁 + 固定 tmp 名）的子类实测对照：**15 轮累计丢失 165 条追加**，证明这条断言不是空转。另有一条专测公开方法互相调用不会死锁（`is_complete()` 无参时内部会走 `get_profile()`）。

`storage.py:224-240` 的 `_write` **既没有 `RLock` 也没有 `JsonFileLock`**，而临时文件名是固定的 `user_profile.json.tmp`（storage.py:12）。两个并发 PATCH 会同时 `open("w")` 同一个 tmp → 交错写入 → replace 出去的是拼接垃圾。`update_profile` 本身也是典型的 read-modify-write 丢失更新。

**修法**：tmp 名加 `pid+uuid4`；所有写路径套 `JsonFileLock`。

### DATA-08 `InfoStore._read_all` 吞掉一切异常返回 `[]`，下一次写入即永久清空

> ✅ **已修复 2026-08-20** —— 核心思路：**读失败必须显式暴露，绝不伪装成"空记忆库"**。
> - 新增 `MemoryStoreDegradedError` 与只读降级状态。`_read_all` 现在区分四种读失败并逐一进入降级：OSError、**0 字节文件**（`__init__` 至少写 `[]`，所以空文件只可能来自被中断的写入）、JSON 解析失败、顶层不是数组。
> - 降级时 `_enter_readonly_degraded()` 用 **copy 而非 move** 隔离一份 `.corrupt-<ts>` 副本——原文件留在原处供用户直接修，这正是 DATA-05「隔离即等于丢失」的反面做法。
> - 降级期间 `_write_all` **一律抛异常**，连显式 `clear()` 也不放行（此时用户看到的"0 条记忆"是假象，清空意图不可信）。唯一例外是 `cleanup_expired`：它在降级时 removed=0 因而不写盘，**这一点是刻意的**，否则 `/chat` 会因为记忆库坏掉而整条链路挂掉。
> - **逐条隔离不连坐**：单条记录规范化失败只跳过该条，其余记忆照常可用；坏记录存进 `_unreadable_entries`，并在**每次写入时原样带回**，避免"一条坏记录导致其余记忆被裁掉后永久丢失"。
> - `created_at` 缺失/非法不再让所有记忆接口 500：`_parse_utc` 兜底（先试 `created_at`，再试 `id`，最后回填当前时间并告警）。
> - 顺带把 `_write_all` 改成 `flush + os.fsync + replace`（DATA-06 在本 store 落地），tmp 名加 `pid+uuid4`（当时 DATA-07 里 profile 还是固定 tmp 名，已于 2026-08-21 一并修掉）。
> - `revalidate()` 让用户修好文件后**无需重启**即可解除降级；`GET /health/storage-status` 会自动尝试一次并返回 `memory_store` 明细；新增全局 `MemoryStoreDegradedError` 处理器统一回 **503 + 隔离副本路径 + 恢复指引**，而不是把写入失败伪装成 500。
>
> 回归测试 `tests/test_info_store_corruption_guard.py`（17 条），三组不变量：隔离副本存在且原文件字节不变、降级期间各写入路径全部被拒、坏记录不连坐且写入后仍在盘上。含 `SourceInvariantTest` 源码断言（无裸 `except` 返空、写路径有 fsync、写入前检查降级标志），防止退化。
> 另有两条反向用例钉住"拒绝写空"这条保护**不会**挡掉正常的删除最后一条与健康 `clear()`。

`info_store.py:315-322` 的 `except Exception: data = []`。文件一旦不可解析 → 读到空 → 用户随手触发一次记忆保存 → `_write_all([新条目])` **把整个记忆库永久覆盖**。无隔离、无告警、无备份，与 workout_store 至少会隔离的做法自相矛盾。这是全项目最隐蔽的静默销毁路径。

附带：`info_store.py:357,362` 的 `datetime.fromisoformat` 在 try 之外，**一条缺 `created_at` 的记忆会让所有记忆接口 500**。

**修法**：读失败先 copy 成 `.corrupt-<ts>` 再进入只读降级；拒绝"文件非空却读到 0 条"这类反常写入；逐条 try 跳过坏记录。

### DATA-09 SQLite journal 静默降级到 MEMORY，崩溃即数据库结构损坏

> ✅ **已修复 2026-08-21** —— 三处改动：
> - **WAL 失败退 DELETE，绝不用 MEMORY**：新增 `_apply_journal_mode()`，按 `SAFE_JOURNAL_MODES = ("WAL", "DELETE")` 依次尝试。关键是**靠读回返回值判断成功**而不是靠 try/except——`PRAGMA journal_mode = X` 被拒绝时**不一定抛异常**，它会返回当前实际生效的模式，原实现只 catch `OperationalError`，这种静默失败根本抓不到。两种安全模式都设不上时读回真实模式并 `logger.error`，但**绝不主动切到 MEMORY/OFF**。
> - **临时目录回落改为只读**：`_use_temporary_storage()` 现在会设 `_degraded_reason`，`save_import` / `delete_import` / `clear` 三个写入口开头 `_require_writable()`，抛新增的 `HealthStoreDegradedError`；main.py 加全局处理器统一回 **503 + 库路径 + 恢复指引**。读接口照常可用（降级的目的是别让整个应用挂掉）。原实现允许写，于是 Windows 清理 %TEMP% 时新导入的数据直接蒸发，而用户全程看到的是"导入成功"。
> - **降级状态可见、可自愈**：新增 `HealthStore.storage_status()`（available / writable / degraded_reason / database_path / using_temporary_storage / journal_mode / journal_mode_safe）与 `revalidate()`。`GET /health/storage-status` 现在返回 `health_store` 明细并自动试一次 revalidate——用户修好权限后**无需重启**。journal 模式不安全时即使数据当下没坏也会带一条显式警告（`available` 仍为 true，但消息说清崩溃会损坏库）。
>
> 回归测试见 `tests/test_journal_backup_and_restore_safety.py`。WAL 被拒绝这条分支要网络盘才能真实触发，所以用一个只回答 `PRAGMA journal_mode` 的假连接直接构造：钉住"请求顺序是 WAL→DELETE、从不请求 MEMORY"，以及"拒绝是从返回值看出来的"。另有源码断言禁止 `journal_mode = MEMORY` 再出现在代码行里，以及 AST 断言三个写方法都调了 `_require_writable()`。

`health_store.py:58-63` 在 WAL 失败时 fallback 到 `journal_mode = MEMORY`，异常被 `pass` 吞掉。该模式下回滚日志只在内存，进程崩溃/断电会留下**结构性损坏的 health.db**，用户毫不知情。

**修法**：WAL 失败退回 `DELETE`（有崩溃恢复能力），**绝不用 MEMORY**；降级状态暴露到 `/health/storage-status`。同理 `health_store.py:26-47` 在磁盘错误时切到系统临时目录后**仍允许写入**，Windows 清理临时文件时新数据直接蒸发——应改为只读模式。

### DATA-10 Docker 没挂载 data 卷，容器重建 = 全量丢失
> ✅ 已修复（2026-08-21）
> - 根因：持久化仅"顺带"生效，依附于为热重载而挂的 `./:/app` 源码挂载；
  按正常方式跑镜像（不挂源码）时，所有数据写在容器可写层，容器重建即全量丢失。
  另：`Dockerfile` 里 `FROM fit_health_agent:dev` 以自身产物为基础镜像，全新克隆无法构建。
> - 修复：
>   - `docker-compose.yml` 声明独立数据卷 `./data:/app/data`，不再与源码挂载耦合。
>   - `Dockerfile` 改用官方 python 基础镜像（`ARG BASE_IMAGE=python:3.12-bookworm`，可用 `--build-arg` 覆盖；下限是 3.11，因为 StrEnum，见 ARCH-05），设 `FITHEALTH_DATA_DIR=/app/data`。 
>   - 新增 `.dockerignore`，将 `data/`、`.env`、`.git/` 排除出镜像层。 
>   - 新增 `docker-compose.dev.yml` 承载热重载，默认配置保持干净。 
> - 遗留：构建仍需通过 `vendor/`、私有索引（`PIP_EXTRA_INDEX_URL`）或预装 `BASE_IMAGE`
> - 提供 `hello-agents==1.0.0`；沙箱内无法真正 `docker build`，需在有 Docker 的机器上确认。



### DATA-11 备份不含原始导入文件，恢复后引用全部悬空

> ✅ **已修复 2026-08-21** —— 备份格式升到 **v2**（v1 老备份仍可恢复，否则用户手上那些直接作废）：
> - **补全内容**：`health-imports/`（唯一可重新解析的数据源）与 `hr_streams/` 整目录打进 zip，外加 `pending_workout.json`、`external_model_settings.json`。**`hr_streams/` 是审查时还不存在的新缺口**——DATA-05（08-20）才把 1Hz 心率流旁挂出去，`daily_records.json` 里只留摘要 + `file` 指针，不备份它就是又一批悬空引用。可选 JSON 只在**存在时**打包："没有待确认训练"本身是有意义的状态，不能拿空对象顶替。
> - **每成员校验和**：manifest 新增 `members: {成员名: {sha256, bytes}}`，校验时逐个比对，并双向核对（zip 里有而 manifest 没登记 → 拒；manifest 登记了而 zip 里没有 → 拒）。这才是"结构合法但内容被替换"能被抓住的地方。
> - **备份文件是外部输入，按外部输入处理**：成员名只接受白名单顶层文件与 `<备份目录>/<单层文件名>`，`..`／绝对路径／反斜杠／多层嵌套一概拒绝；新增解压后总大小上限 300 MiB（现在会展开整个目录，没有这道闸门一个高压缩比 zip 就能吃光内存）。
> - **恢复语义**：目录整体替换而非合并——库是整份换掉的，留着不在库里的 raw 文件只会变成永久孤儿。v2 备份里没有的可选 JSON 会被删除（回到那份备份的状态）；v1 备份区分不了"没有"和"没记"，所以不动。目录换法是"先把现有目录整体挪进事务目录、再把暂存目录 rename 进来"，两步都是同一文件系统内的 rename（目录不能像文件那样直接 `os.replace` 覆盖）。
> - **顺带修掉一个由本条引出的新 bug**：`/data/backup/import` 原先在恢复后调 `workout_store.clear_current()`——在备份不含 `pending_workout.json` 的年代那是对的，现在会把刚恢复回来的待确认训练直接删掉。改为新增的 `workout_store.reload_from_disk()`，按盘重载。
>
> **关于 `raw_path` 改写：不需要，且刻意没加。** 库里存的已经是裸文件名（DATA-02），`HealthStore.resolve_raw_file()` 也只信任文件名部分，所以老备份里那些绝对路径同样能被重新对上。多一处改写逻辑只是多一处会腐坏的代码。这一点由 `test_restored_database_raw_path_still_resolves_to_a_real_file` 钉住。

`backup_service.py:17-23` 的 `JSON_FILES + HEALTH_DB_NAME` 漏掉了 `data/health-imports/**`（**唯一可重新解析的数据源**）、`pending_workout.json`、`external_model_settings.json`。恢复后 DB 里每条 import 的 raw_path 都指向不存在的文件，且永远无法从备份重建。

同时 `backup_service.py:34-40` 的 manifest 只有 format/version/created_at/files，**无每文件校验和**，"结构合法但内容被替换"检测不到。

**修法**：把 `health-imports/` 整目录打进 zip，恢复时按文件名落回并同步改写 DB 为相对名；manifest 增加每成员 sha256 + 字节数。

### DATA-12 恢复过程不隔离并发，且直接删 WAL 边车文件

> ✅ **已修复 2026-08-21** —— 三重隔离，加锁顺序固定为 **gate → JSON 文件锁 → 数据库**：
> - **全局 maintenance 开关**：新增 `fithealth_agent/maintenance.py`。`MAINTENANCE.exclusive(reason)` 先竖开关（main.py 的 HTTP 中间件让新请求拿到 **503**），**再等在飞请求排空**——顺序反过来的话在飞计数永远归不了零。诊断接口（`/`、`/health/storage-status`）维护期间仍然放行，否则用户被 503 挡住之后连"系统在干什么"都看不到。发起恢复的那条请求自己不计入在飞计数，否则排空会等它自己、直接死等。
> - **为什么是"排空 + 超时"而不是"直接去抢锁"**：恢复要同时持有若干 `JsonFileLock` 和 HealthStore 的访问锁。一个已经拿着 `daily_records.json` 锁、接下来还要查 `health.db` 的在飞请求，会与恢复形成经典的循环等待。排空把这种情况变成一次**干净的失败**（`MaintenanceBusyError` → 409，数据一个字节都没动），而不是一次挂死。
> - **JSON 换文件时持锁**：`restore()` 按固定顺序拿下四个 JSON 的 `JsonFileLock`（导出也走同一顺序，所以两者之间不可能反向等待）。这条正是本缺陷的核心：不持锁时并发 `add_record` 拿着恢复前读到的列表在恢复后写回，**两边都"成功"，恢复结果被静默覆盖**。
> - **换库前独占 HealthStore**：`HealthStore` 所有连接改从一把 `RLock` 下取（`_connection()` 持锁期间外部无法换库），新增 `exclusive_access()` 与 `reopen()`。边车文件 `-wal`/`-shm` 只在独占期内删除，`reopen()` 也在独占期内调用——换库与重新校验 schema 之间不能有别人插进来。
>
> 回归测试见 `tests/test_journal_backup_and_restore_safety.py`。关键两条：(1) 恢复期间竞争线程去拿 `daily_records.json` 的锁必须被**挡住** ≥ 0.2s；(2) 排在恢复之后的 `add_record` 必须基于**恢复后**的内容做 read-modify-write（备份 1 条 → 盘上 3 条 → 恢复 + 并发追加 → 最终必须是 2 条且只剩恢复的那条 + 新追加的那条）。第二条已用一份还原成旧行为（恢复期间不持锁）的子类实测对照：**8 轮 8 次恢复结果被破坏**。另有 `exclusive_access`/`reopen` 的调用时序断言（必须是 enter → reopen → exit）、排空超时后开关必须落下（否则整个服务永久 503）、以及中间件真的挡住了请求（防止"写了开关但没人读"变成第二个 BUG-01）。

`backup_service.py:117,144-152` 的 `restore()` 用 `os.replace` 换 JSON **但不持有 `JsonFileLock`**：并发的 `add_record` 拿着旧内存列表在恢复后写回，直接覆盖恢复结果。health.db 更危险——任何在飞请求持有连接时删除 `health.db-wal`/`-shm` 会让那条连接把旧 WAL 写回新库；Windows 上覆盖被打开的 health.db 还会直接 `PermissionError`。

**修法**：恢复期间置全局 maintenance 开关拒绝其他请求；换 DB 前先关闭/失效 HealthStore（加 `reopen()`）。

### DATA-13 合并已保存训练组会不可逆丢弃卡路里/距离/心率

> ✅ **已修复 2026-08-21** —— 按字段语义分类聚合，心率改为**三档**：
> - **心率不再被丢弃**。优先级从高到低：(1) 有旁挂的 1Hz 原始流（DATA-05 起按记录 id 存）→ 在新时间窗内**精确重算**，用的是 `fit_parser.compute_hr_in_window`，与待确认训练的 `workout_store.merge_sets` 完全同一套口径；(2) 没有流但各段有 `avg_hr` → 按各段**时长**加权还原均值、峰值取各段最大值（比算术平均可靠，组时长差异很大）；(3) 只有两者都没有时才是 None。原实现无条件 `"avg_hr": None`，把段里**本来就存着**的心率白白丢掉。
> - **清单原文说"历史记录未保留原始心率流"，这一点已经过期**：DATA-05（08-20）把 1Hz 流旁挂出去之后，08-20 起保存的记录都能精确重算，只有更早的老记录才需要走加权近似。接口现在把 `hr_stream_store.load(record_id)` 一并传进合并函数——不接线的话等于 DATA-05 存下来的流白存了，这条有源码断言钉住。
> - **字段按语义分三类**：可加性（`distance_m`、`calories`）求和；峰值（`max_speed_mps`）取最大；均值（`avg_cadence`、`avg_power_w`）按时长加权。`avg_speed_mps` 优先用"总距离 ÷ 合并后总时长"（那是它的定义），拿不到距离时才退回加权。
> - **不给原本没有这些字段的力量组凭空造 `distance_m: 0`**。当前格式下 `set_active` 的 `to_dict()` 根本不输出有氧字段（`fit_parser.py:78-105`），凭空补出来的 0 会一路进到 ReAct 观察里，而且看起来像"真的测到了 0"。所以只聚合**至少有一段存在**的字段。
> - **刻意保留 `{**targets[0]}` 的展开**，只是把每个需要聚合的字段显式覆盖掉。白名单式重建会把将来新增的字段静默丢掉，展开 + 覆盖只会保留不认识的字段。
> - 返回的 notice 改为只说心率口径——前端本来就会自己拼"已合并第 X、Y 组"，两句叠着说很啰嗦。
>
> 回归测试 `tests/test_saved_training_record_editing.py`（14 条）。原有的 `test_merges_saved_sets_without_claiming_recomputed_heart_rate` **本身在断言这个缺陷**（要求 `avg_hr is None`、notice 含"无法重新计算"），已替换为断言三档口径：加权那档钉死 `(120×30 + 125×40)/70 → 123`（算术平均会给 122，所以这条真的能区分加权与否）、有流那档钉死 `mean(100,140,150,160,170) = 144 / peak 170`、流里没有落在新窗口内的采样点时**不能因此把心率清空**、只有一组有心率时用那一组而不是整体作废。另有可加/峰值/加权三类字段各自的断言与"不凭空造字段"的反向断言。已实测端到端：同一份记录，无流 → 123/138，挂上 5 条 1Hz 流 → 144/170。
>
> **未做**：没有加"撤销合并"。清单原文提到"原始数据无副本，不可回滚"，但修法列的是聚合口径。修好之后合并对聚合字段已经是信息保全的（和不合并算出来的值一致），再加撤销需要新的端点 + 前端入口 + 存一份合并前的段副本（后者会让记录体积翻倍并进入 LLM 上下文，与 DATA-05 定下的"记录里只留体积恒定的摘要"口径冲突）。真要做应单开一条。

`main.py:1724-1736` 的 `merged = {**targets[0], ...}` 只重算 duration/reps/weight/category，**其余数值字段（calories、distance_m、avg_cadence）全部沿用第一段的值而不求和**；`main.py:1734-1735` 又把 avg_hr/max_hr 硬写成 `None`。原始数据无副本，不可回滚。

**修法**：可加性字段显式求和；心率虽无原始流，但可用各段 avg_hr 按时长加权还原、max_hr 取各段最大值，比直接丢弃精确得多。

### DATA-14 `/data/reset` 非事务，中途失败留下半删状态

> ✅ **已修复 2026-08-21** —— 清单给了两条修法（落快照 / 逐项 try+返回明细），**两条都做了**，因为哪一条单独都不够：快照给的是退路，逐项容错给的是"把删除做完并且说清楚"。
> - **这不是理论风险，是一条可达路径**。`info_store.clear()` 在记忆库只读降级时抛 `MemoryStoreDegradedError`，`health_store.clear()` 在数据库只读降级时抛 `HealthStoreDegradedError`，**两者都是 `RuntimeError` 子类**，原来那句 `except (OSError, sqlite3.Error)` 根本抓不到，异常直接逃到全局处理器变成 503。用户看到的是一句"记忆库只读降级"，完全不知道训练记录和训练计划已经被永久删掉了。已实测对照旧实现：`(1, 0) → (0, 0)`，响应体里**没有任何 `_removed` 计数**、没有明细、没有恢复点。
> - **删除前先落恢复点，写不出来就一个字节都不删**。`/data/reset` 是全仓库唯一不可逆的批量删除，没有退路的删除不该执行。快照走 `export_bytes()`，落在 `data/recovery-points/pre-reset-<14 位时间戳>.zip`，写法与 JSON 落盘同构（临时文件 → `flush` → `os.fsync` → `replace`），否则"恢复点已生成"可能只是页缓存里的一句空话，正好在断电时和数据一起消失。写失败**抛出**而不是返回失败标记，调用方因此必须中止（返回 500，数据一个字节没动）。
> - **恢复点刻意就是一份普通备份 zip**，所以"怎么还原"需要的新代码是零：下载后走已有的 `/data/backup/import` 即可，`validate()` 原样接受它。这个前提有测试钉住——它一旦不成立，恢复点就变成一份谁也用不了的文件。`recovery-points/` 刻意**不进** `BACKUP_DIRECTORIES`，否则第 N 份快照会套进第 N+1 份里。
> - **命名必须严格单调**。时间戳只到秒，半失败后用户立刻重试会撞进同一秒，而后一份快照拍的是**已经被清空**的数据，覆盖等于把唯一一份真数据的恢复点弄丢。修法不是"找个空位"：prune 删掉的是名字最小的那份，腾出的空位在**过去**，填进去会让新快照拿到比现存更旧的名字，紧接着的 prune 又把它当成最老的删掉——实测踩到过，刚写好的快照当场消失。现在取 `max(now, 现存最大 + 1s)`，定宽 + 严格递增，字典序永远等于创建顺序；同时给 prune 传 `keep=` 显式排除刚写的那份（"写成功就一定有一份可用恢复点"是整条修法的前提，交给命名顺序隐式保证太脆）。
> - **保留 3 份**。一个恢复点等于一份完整数据（含 `health-imports/`），无上限攒下去会吃掉磁盘。同时这也是隐私问题——用户点了"删除全部数据"，盘上却留着一份全量副本，所以响应里显式报告恢复点、前端加「重置前恢复点」区块可下载/永久删除，明说而不是偷偷留着。
> - **逐项 try/except，`except Exception` 是刻意的宽**。8 个互相独立的破坏性操作，"一个失败就把后面全部跳过"正是这条需求描述的缺陷；窄 except 又抓不到上面那两个 `RuntimeError`。
> - **步骤顺序按"先清引用方，再清被引用方"**：`daily_records.json` 按 id 引用 `hr_streams/`，`health.db` 的 import 行按文件名引用 `health-imports/`。按这个顺序中途失败留下的是没人引用的孤儿文件（占点磁盘，读不到也不报错）；反过来会留下悬空引用——记录还在、指向的流没了，那是静默的数据损坏。
> - **补了第 8 步 `plan_draft_cache`**（原实现只有 6 步 + profile）。它是进程内缓存，但存着完整的训练计划正文（BUG-05）；"删除全部数据"之后它还在内存里，下一句"保存刚才的计划"就能把已删内容写回磁盘。
> - **持维护开关**（DATA-12 的 `MAINTENANCE.exclusive`）：新请求 503、在飞请求排空，快照与删除之间没有别人插进来写数据，否则刚写入的记录会被快照漏掉却仍被删掉。`/data/reset` 必须进 `MAINTENANCE_UNTRACKED_PATHS`，否则它持开关后排空会等自己、直接死等到超时。
> - **半删状态返回 200 + `partial: true`，不是 5xx**。客户端必须能读到究竟删了哪些；5xx 会让前端只显示一句"操作失败"，正是原实现的毛病。响应里既有 `steps` 逐项明细（含每项 `removed` 与 `error`），也把计数平铺成顶层字段保持与旧响应兼容。
> - **刻意没做自动回滚**。用户明确要求删除全部数据，失败后自动还原是把他的意图撤销掉，他只会再点一次撞进同一个失败；重置本身是幂等的，还原与否应当由用户看着明细自己决定。
>
> 回归测试 `tests/test_reset_transactionality.py`（32 条），当前基线 **489 通过 / 0 失败**。已用一份还原成旧实现（六个 store 顺序清空 + 单个外层窄 `except`）的对照实测：第 3 步抛错时旧实现异常逃逸→503、记录与计划已永久删除、响应无任何计数、无恢复点；新实现 200 + `partial` + 8 项明细 + 恢复点，且恢复点里装的是**失败前**的真数据。另有恢复点 zip 能被现成 `validate/restore` 吃下去、fsync 必须发生在 replace 之前（fsync 时目标文件还不该存在）、写失败后既不留半截目标也不留 `.tmp`、同一秒连写 4 次名字互不相同且单调、刚写好的快照永远不被 prune、外部传入的名字（`../health.db`、`pre-reset-1.zip`、`%2e%2e/x` 等）一律拒绝、以及前端确实接线（`loadDataOverview` 调 `renderRecoveryPoints`、200-with-error 时**不再提前 return**，否则界面会停在删除前的旧视图上）。

`main.py:2319-2341` 六个 store 顺序清空，`except` 只在最外层。若第 3 个抛错，前 2 个已永久删除且响应不含任何已删计数。

**修法**：删除前先 `backup_service.export_bytes()` 落一份 pre-reset 快照；或逐项 try/except 并返回明细。

### DATA-15 wellness 包里有四类数据一条都没入库

> ✅ **已修复 2026-08-22** —— 起因是清点了一份真实的 `2026-08-21.zip`（16 个 FIT：11 WELLNESS + 1 SLEEP_DATA + 1 HRV_STATUS + 4 METRICS）。逐字段核对后发现四类数据一直躺在包里没被读出来。
> - **静息代谢率**（`monitoring_info.resting_metabolic_rate` = 2230 kcal/天）。`daily_health_summary` 早就有 `resting_calories` / `total_calories` 两列，却 **16 天全是空**——读取方去 `monitoring` 里找 `resting_calories`/`bmr_calories`，而这个数其实在 `monitoring_info` 里，**而那条消息整个没被处理**。实测该值随体重重算（08-07~08-13 是 2257，08-14 起 2230）。总热量按"静息 + 活动"推导（Garmin 自己的定义），且**只在两项都有时才给**——把缺失当 0 会产出一个看着像真值的错数。
> - **静息心率**（Garmin 私有消息 `unknown_211`，**逆向推断**）。证据两条：`unknown_1` 在 2026-08-14 取到 56，与那天睡眠 CSV 的「静息心率 56 bpm」**精确一致**；跨 15 天它稳定落在 55–63，而当日睡眠时段最低心率是 52–58（静息心率是一段均值而非最小值，略高是对的）。`unknown_0` 15 天里几乎恒为 57–59，更像 7 日基线，所以存成 `resting_heart_rate_baseline`，**名字上不声称它一定是什么**，界面上也标注了"来自手表私有字段，非官方接口"。带 25–120 的范围闸门：逆向出来的字段猜错了也不该把垃圾写进库。
> - **强度分钟**（`monitoring.moderate_activity_time` / `vigorous_activity_time`）。两个坑：(1) **是区间增量而不是累计值**——实测同一天出现 60s/180s/60s/240s，累计量不可能回落，所以必须 SUM，取 MAX 会把 5 分钟报成 3 分钟；(2) 它们**不与任何活动字段同现**（steps/active_calories 全空），所以挂不到 `daily_activity_observations` 上，那张表只在有活动字段时才建行——这就是它们以前一条都没进去的原因。汇总按 Garmin/WHO 口径：中等 + 高强度×2。
> - **设备 UTC 偏移**（`monitoring_info` / `local_time` / `start` 里 UTC 与本地墙钟之差）。实测 15 份包全是 +480，存下来只为可见可核对，不改变现有的本地日期计算。**这个 `local_timestamp` 是真正的本地墙钟**（与 BUG-15 里 stress 那个同名字段相反），所以计算偏移时刻意不走 `_utc_datetime`——走了会算出 +16h。
> - 新增两张表 `device_daily_metrics`（日级指标，每列各取"该列非空的最新一条"——RMR 来自 `monitoring_info`、静息心率来自 `unknown_211`，时间戳不同，一行里通常只有一列有值，不能整行取最新）与 `intensity_observations`（去重口径与 BUG-16 一致）。
> - **每天开头往往还留着前一天的静息心率**（实测 08-21 00:07 是 61，当天真实值是之后稳定的 55），按 `local_date` 取当天最后一条正好避开它。
> - 顺带修掉一个由本条引出的缺陷：`delete_import` 收集"受影响日期"时只列了 5 张表，漏掉新增的两张。跨零点那天常常**只**有一条静息心率，漏掉的话日汇总行删不掉，界面上会留下一个没有来源的幽灵日期。
> - **另修一处口径错误**：`get_sleep` 的 FIT 兜底把**清醒时间也算进 `duration_min`**。睡眠 CSV 的「睡眠时长」是深+浅+REM（6时48分 = 101+222+85，清醒 43 分钟不计），而 FIT 兜底用同一个键名给出全部阶段之和，界面上完全看不出差别。真实数据里 2026-08-21 因此多报 156 分钟（537 vs 381），`memory/traces/` 里那次对话给用户报的 459 分钟实际只有 319 分钟。躺床总时长另出 `time_in_bed_min`，不丢信息。`weekly_summary` 读的是同一个键，一并修正。
> - **补了夜间心率**：睡眠分数与质量是 Garmin 云端算的、FIT 里没有，但夜间平均/最低心率手上已经有全部原料（分钟级心率 + 睡眠阶段时间线）。对照 2026-08-14 睡眠 CSV 的「夜间平均心率 60 bpm」，这样算得 59.9。
> - 回灌脚本 `scripts/backfill_health_fields.py`：重解 `data/health-imports/` 下已存的原始包，**动库前先落恢复点**（复用 DATA-14 那套），缺省只预演。`--extra-source` 可指向别处的原始包目录补齐 raw 文件已丢失的记录，但**按同名取来的文件必须逐字节核对 sha256**——实测真的拦下了一份同名但内容不同的 `2026-08-15.zip`。已在真实库执行：15 天补上 RMR/总热量/时区，16 天补上静息心率，9 天补上强度分钟；BUG-16 的"趋势与日汇总同口径"不变量复查 78 组 0 不一致。
> - 回归测试 `tests/test_extended_health_fields.py`（35 条）。10 处逐一还原成旧实现实测，各被 1–3 条测试抓住。当前基线 **601 通过 / 0 失败**，9 种随机执行顺序下复跑全绿。

### DATA-16 身体电量与 METRICS 文件仍未解码（待办）

> **2026-08-24 更正**：本条与 `开发日志.md` 里「睡眠分数与质量是 Garmin 云端算的、FIT 里没有」那句判断**是错的**。睡眠分数就在包里，17 份日包 + 5 份 CSV 真值全部对得上，已实现入库（见 `需求清单-20260823.md` 的 `DATA-36`）：`METRICS.unknown_330.unknown_2` / `unknown_384.unknown_2`、`SLEEP_DATA.unknown_521.unknown_1` / `unknown_346.unknown_6` 四处同值。同一批消息里 `unknown_384.unknown_24` 是清醒分钟、`unknown_384.unknown_9/11` 是躺床起止。真正云端算的只有「质量」那个中文分档（可由分数分档推出）。**身体电量按用户 2026-08-24 的决定不再做**；Garmin 恢复时间由用户手输。下面这段线索里关于 METRICS「只产出一行时区偏移」的部分已不再成立。

清点 `2026-08-21.zip` 时确认这两块**有数据但解不出来**，刻意没有猜着存——存错的身体电量比没有更糟。已知线索记在这里，以后单独做：

- **身体电量**疑似在 `unknown_233`：每天 1442 条（与压力同频，恰好一天分钟数），字段 `unknown_2` 是 4 字节私有结构。逐字节看：`byte[3]` 取值集中在 0x20/0x30/0x38/0x40/0x48/0x50（都是 8 的倍数），`byte[3] & 0x7f` 有 86% 落在 0–100 但明显不是直读；按 LE/BE u16 组合也都不成立。1442 条里 416 个不同取值。**是位域，需要真正的逆向**。
- **4 个 METRICS 文件**（VO2max / 训练状态 / 体能年龄通常在这里）目前只产出一行时区偏移。内容全在私有消息里，已观察到的规律：
  - `unknown_339` 四个字段跨全部文件恒为 `1602 / 3525 / 8230 / 19067` → 像是配置或阈值，不是日数据。
  - `unknown_378.unknown_0` 一天内递增 `52 → 73 → 80`，`unknown_3` 同步递增 `165 → 222 → 235`，`unknown_4` 恒为 137。
  - `unknown_232.unknown_11` 与 `unknown_378.unknown_5` 同步给出 `12 → 17 → 17`，与当天强度分钟量级接近，但对不上确切值。
  - `unknown_410.unknown_0/1` 恒为 480 → 就是时区偏移（分钟），已确认。
  - `unknown_384` 单条里有 20 多个字段（`unknown_0=52`、`unknown_1=100`、`unknown_3=1315`、`unknown_15=unknown_16=82`…），其中 1315 与当天距离 1315.21 m 接近，但只是一份文件的一次巧合，不足以定论。
- 其余明确但低价值、经确认**不存**的字段：`cum_ascent`/`cum_descent`（FR255 无气压计，实测全 0）、`cycles`（步频累计，已被 steps 覆盖）、`hrv_status_summary.baseline_balanced_low/high`（40960/128 = 320 ms 不是合理的 HRV 基线，标度可疑）、`event.data16`/`ts_15`、设备型号与固件版本。
- 另一个已知现象（非缺陷，但会让趋势图出毛刺）：一份「2026-08-21」的包会写进**三天**汇总——跨零点溢出让 08-20 多 11 条 HRV、08-22 只落 1 条心率。这种"只有个别样本的日子"目前照常参与日/周趋势。

---

## 3. P0 —— 死代码：写了但从未接线（最危险的一类）

以下均已 grep 确认「定义 1 次、调用 0 次」。它们让你误以为有兜底，实际完全没生效。

### BUG-01 安全约束冲突拦截是死代码，危险请求会被放行

> ✅ **已修复 2026-08-20** —— 两处必须同改，缺一不可：
> **(1) 接通拦截**：`resolve_plan_context` 现在真正构造并返回 `blocking_reasons`（含命中的具体安全限制原文），`/chat` 的 409 分支据此生效，响应里会列出当前生效的限制而不再只是一句空话。
> **(2) 收紧误报正则**：新增模块级 `_SAFETY_BYPASS_PATTERN`，删掉了裸的「不管 / 忽略 / 无视」，改为必须与「伤 / 伤病 / 限制 / 约束 / 禁忌 / 医嘱」搭配出现（中间允许最多 3 个汉字，以覆盖「忽略我的**腰**伤」），另加「跳过限制 / 照常训练 / 硬练」等短语。
> **(3) 消除双状态机**：`workflow_state` 不再由 main.py 手写字符串，统一改用 `plan_workflow.state_for_context()` 推导——原先 `plan_workflow` 导入后零调用、main.py 另写一套裸字符串，正是这个 bug 藏了这么久的原因。
>
> 实测 7/7：「忽略我的腰伤，照常给我练腿」→ 拦截（`constraint_conflict`）；「不管怎样都行」「今天不管练胸还是练背」→ 放行。另外，**没有已确认安全事实时不拦截**，避免把没设过限制的人挡在外面。
>
> 回归测试 `tests/test_health_safety_gate.py::SafetyBypassPatternTest` / `ResolvePlanContextBlockingTest`，其中专门有一组用例钉住「不管」类语气词不得触发拦截。

`main.py:950` 判断 `resolved_plan_context.get("blocking_reasons")`，但 `resolve_plan_context`（main.py:623-635）**从不返回这个 key**——全仓库只有 `plan_workflow.py:19` 和它的测试用到它。真正的信号存在 `workflow_state == "constraint_conflict"` 里。

**后果**：用户说"忽略我的腰伤，照常给我练腿"时，后端把 `constraint_conflict` 原样塞进 200 响应体，请求照常送进 Agent 并返回计划。整个"安全约束优先"设计的兜底目前**完全没生效**。

**修法**：让 `resolve_plan_context` 实际填充 `blocking_reasons` 列表，并接上已导入的 `state_for_context`。

⚠️ **必须与下面这条一起修**：`main.py:622` 的 `safety_bypass` 正则含「不管」，而中文里「不管怎样」「不管练什么都行」绝大多数是语气词。修好 BUG-01 后这条会立刻变成"正常提问被 409 拒绝"。应改为 `忽略(?:我的)?(?:伤|限制)|无视.{0,4}(?:伤|限制)|跳过限制|照常训练`。

### BUG-02 关闭外部模型后本地查询全废，而本地兜底函数是死代码

> ✅ **已修复 2026-08-20** —— 三个函数全部接线，且本地分支都排在 503 之前：
> - `is_training_record_query` + `_requested_record_date` → 接进 `view_training_records` 分支（与模型意图取 `or`），离线也能查记录、并能识别消息里的日期。
> - 新增 `is_profile_query` → 接进 `view_profile` 分支。刻意写得窄，"我的器械有哪些，顺便安排今天的胸推"这类复合请求不会被短路吞掉（避免踩到 BUG-14）。
> - `is_training_related` → 用于给 503 生成有针对性的提示，明确列出「查看训练记录」「查看个人信息」「查看本周数据」三条可直接照抄的本地指令，而不是原来空泛的一句"仍可使用本地功能"。
>
> 修复过程中测试又抓到两个真实缺陷：(1) `is_training_record_query` 原来的 `.{0,8}` 间隔太窄，「查询 2026-08-18 的训练记录」去空格后光日期就占 11 字符，**所有带日期的问法全都漏掉**——而带日期恰恰是离线查记录的主要场景，已放宽到 16；(2) `_requested_record_date` 依赖 `date.fromisoformat` 解析「2026-8-9」，但这只有 Python 3.11+ 才支持，已改为显式补零，行为不再随版本变化。
>
> 回归测试 `tests/test_local_fallback_and_set_counts.py::LocalFallbackRoutingTest` / `LocalFallbackWiringTest`，后者用源码断言钉住四个函数确实被调用、且本地分支排在 503 之前——防止再次退化成死代码。

`chat_intent_router.py:183` 在 `allow_external_models=False` 时直接返回空 ChatIntent，所有 intent 位全 False。于是"查看训练记录"一路掉到 `main.py:933` 返回 503——**而 `main.py:2149` 的欢迎语明明写着"查看训练记录：输入'查看训练记录'"**，`list.md:94` 的验收标准也写着"关闭后仍可使用本地查询"。

更关键的是，本地确定性兜底 `is_training_related`(main.py:264)、`is_training_record_query`(main.py:274)、`_requested_record_date`(main.py:1608) **在 main.py 里一次都没被调用**，只有测试通过源码提取在测它们。功能做了一半就断线了。同样的问题在网络抖动时也会出现。

**修法**：把这三个已有函数接成 `route_chat_intent` 之前的一级本地路由。

### BUG-03 优先级阶梯从未送进模型

> ✅ **已修复 2026-08-20** —— 关键取舍：**不是把死代码另外拼上去，而是合并进已经接线的函数并删掉它**。
>
> 修之前实际有**三处**并存的优先级表述：(1) `build_agent_input` 里手写的 `【训练设计优先级】` 三条；(2) `format_plan_context` 里压缩成一行的 `安全限制 > 当前明确指令 > 周计划 > ...`；(3) `training_priority_context` 的完整六级阶梯，**定义 1 次、调用 0 次**。直接把 (3) 拼上去只会变成三段互相重叠的优先级说明，还会把安全事实/周计划/训练偏好第二次塞进上下文（这些 `format_cross_session_memories` 的【已确认约束与偏好】里本来就有）。
>
> 所以做法是：把 (3) 的**独有内容**并入 (2)，把 (1) 整段也并入 (2)，然后**删除 `training_priority_context`**。全局只剩一段 `【后端 PlanContext（已裁决优先级，必须遵守）】`。并入的独有内容有四项：
> - 显式编号的六级阶梯（比原来那句一行版清楚）；
> - **「只有明确改练、休息、跳过或不执行才覆盖当天周计划，普通抱怨或提问不算」**——整个阶梯里最关键的一句，以前只存在于死代码中，于是"今天有点累"可能被当成覆盖周计划的指令；
> - 「冲突时按上述顺序决策，并说明采用的优先层级」；
> - 检测到可能覆盖时的那句「由模型结合完整原文确认语义，不得因此修改长期档案」（`resolve_plan_context` 新增 `override_detected`）。
>
> `prompts.py` 补了第 11 条，让系统提示与每次请求的 PlanContext 对齐，并单独点明"普通抱怨不算覆盖，应先关心状态、按需下调强度，而不是擅自取消当天科目"。
>
> 上下文体积基本不变（合并抵消了新增的阶梯行）：实测一条带伤病+周计划的请求 758 字符。
>
> 回归测试改在 `tests/test_context_memory.py`：原来那条排序测试是断言 `training_priority_context` 的返回值——**测的正是那段模型永远看不到的文本**，所以它一直是绿的。现在改成断言 `format_plan_context` 的输出，并新增两条：`test_priority_ladder_actually_reaches_the_agent_input`（阶梯与那句语义澄清必须出现在 `build_agent_input` 的结果里，且"今天有点累"不触发覆盖提示）、`test_priority_section_appears_exactly_once`（`优先级由高到低` 只出现 1 次，且不再有 `【训练设计优先级】`/`【后端统一优先级`——BUG-04 的教训：同一件事两处实现，必有一处失效）。

`main.py:585-604` 的 `training_priority_context` 定义了完整六级优先级（含关键规则「普通抱怨或提问不算覆盖当天周计划」），**零调用**。`build_agent_input` 只拼了 `format_plan_context`，后者不含这条语义澄清。结果模型可能把"今天有点累"误判为覆盖周计划。

**修法**：在 `build_agent_input` 拼上，或删掉——现在这样是最坏的状态。

### BUG-04 `plan_workflow` 整模块导入未用，状态字符串在 main.py 里硬编码重复

> ✅ **已修复（2026-08-20 随 BUG-01 与 BUG-03 一并收尾）** —— `PlanWorkflowState`、`state_after_generation`、`state_for_context` 三个导入现在都有实际调用点（`main.py` 的 736 / 939 / 1153 / 1161 / 1238 行），main.py 里手写的裸状态字符串已全部清除；随 BUG-03 删掉 `training_priority_context` 后，这一族"写了但没接线"的死代码已清零。

`main.py:52` 导入 `PlanWorkflowState / state_after_generation / state_for_context` 后零调用，而 `main.py:624,1014` 用裸字符串手写了同一套状态机。`plan_workflow.py` 有独立测试在跑，给人"这套状态机在用"的假象——**这正是 BUG-01 逃过审查的原因**。

---

## 4. P1 —— 功能错误

### BUG-05 「保存刚才的训练计划」会把被截断的计划存盘

> ✅ **已修复（分两次，2026-08-20 收尾）** —— 主干是新增 `fithealth_agent/plan_draft_cache.py`：生成计划、且通过后端校验之后，把**完整正文**存进进程内短期缓存（20 条上限 / 6 小时 TTL / 带锁），`artifact` 带回 `draft_id`，前端保存时回传，服务端按 id 取原文。`looks_like_complete_training_plan` 开头先用 `contains_truncation_marker` 排除残片（一份被截断的计划长度往往仍有 4000+ 字符，原来的三个条件全都会满足）。回捞聊天历史只保留为进程重启后的兜底。**刷新页面或新开标签页后仍能保存**——正文在服务端。
>
> 第二轮补的三个缺口（第一版留下的）：
> - **`/plans` 的校验顺序反了**：截断拦截排在取草稿之前，于是一个带合法 `draft_id` 的请求会因为前端那份恰好带标记而先被 400 掉，服务端的完整原文根本没机会生效。现在先取草稿，只有回落到前端正文时才拦截。
> - **`latest()` 无条件取最近一份**：一个会话里生成过多份计划时（先练背、又改练腿），用户说"保存那份腿的计划"会静默存错。新增 `PlanDraftCache.find(subject=, suggested_date=)`，按"科目+日期 → 任一 → 最近一份"逐级回落，`/chat` 保存分支改用它并把 `chat_intent.saved_plan_subject/date` 传进去。
> - **`local_plan_save` 的 artifact 不带 `draft_id`**：这条路径退回"靠前端传值"，只是碰巧内容完整才没出错。现在也带上。
>
> 顺带把那段顺序敏感的裁决抽成纯函数 `resolve_plan_save_content(payload, cache, is_truncated=)`。原因：它内联在路由里时，沙箱装不了 fastapi，测试只能对 main.py 做字符串断言——**改个变量名测试就绿着失效**。截断判断由调用方注入而不是在缓存模块里 import context_budget，这样按文件路径单独加载也不需要包上下文（对照 ARCH-02）。
>
> 回归测试 `tests/test_plan_draft_integrity.py`（现 31 条）：新增 `SaveContentResolutionTest` 真正调用裁决函数（含"带合法 draft_id 且前端正文是残片"这条原顺序错误）、`DraftLookupTest` 覆盖 `find()` 的四级回落。剩下的源码断言只用于沙箱里没法调用的接线点（route / 前端）。

`main.py:826,870` 的 `most_recent_complete_training_plan(history)` 中，history 已被 `context_budget._normalize_history` 两级截断（单条 4000 字符 + 总量 12000 字符，并追加"[历史消息已截断]"标记）。一份正常计划轻松超过 4000 字符，截断后 `looks_like_complete_training_plan` 仍返回 True（只要 ≥300 字符），于是**带截断标记的残缺计划原样落盘**，且 plan_store 按 content_hash 去重，重存也不会修正。

附带：`HISTORY_CONTEXT_MAX_ITEMS = 8`，生成后再聊 8 轮该计划就不在 history 里；且 `sessionConversationHistory` 是前端内存变量，**刷新或新开标签页即归零**，"保存刚才的计划"直接 400。

**修法**：不要从聊天历史回捞正文。生成时把全文存进服务端短期 pending 缓存（key=计划id），保存时按 id 取原文。

### BUG-06 计划总组数校验把汇总行也算进去，合规计划被误拦（已实测确认）

> ✅ **已修复 2026-08-20** —— 新增 `_exercise_set_counts()` 逐行提取「动作名：N 组」，并跳过标签里含「总组数/总计/合计/共计/累计/总量/总共/小计」的汇总行。单动作上限与总组数**两处校验共用同一个提取器**，避免两边各写一套正则而行为不一致（这正是原来的状态）。
>
> 实测同一份计划：总组数 14 → **7**（正确），单动作最大 7 → **4**（不再误报超过上限 5）。同时验证了真正超限的计划仍会被拦下——修误报不能把真超限也放过去。
>
> **已知局限（有意保留，已写成测试记录现状）**：表格写法 `| 高位下拉 | 4组 |` 与无冒号的 `4 组 × 8 次` 目前统计不到。这不影响本次修复（少算只会让上限偏松，不会误拦），彻底解决要等 AGENT-08 的结构化计划 artifact。另外，热身组目前仍计入总组数，是否应排除属于口径判断，未改动。

`main.py:204` 的正则 `[：:]\s*(\d+)\s*组` 会命中"总组数：7组"这类汇总行。实测两个动作（4组+3组）+ 一行"总组数：7组"：

```
匹配到 ['4','3','7']  求和 = 14   (正确答案应为 7)
```

若你确认过 `max_total_sets=10`，一份完全合规的计划会被判违规，artifact 被置 None（main.py:1008），你被要求"修正"一个不存在的问题且完全无法保存。`main.py:191` 的单动作正则同理。

**修法**：先剔除汇总行（`^\s*(?:总|合计|共)[^\n]*组`），或只统计带动作名前缀的行。顺带：该正则只认"动作：4组"一种写法，"4 组 × 8 次"或"3×10"完全不匹配。

### BUG-07 消息里任意 ISO 日期都会被当成"计划日期"施加硬约束

> ✅ **已修复 2026-08-20** —— 两条各自独立的毛病一起修：
> - **语境门控**：新增 `requested_plan_date(message)`，只有语境确实是"为某一天出计划"时才返回日期。用**白名单意图词 + 方向敏感的否决词**，而不是纯黑名单：没有意图词就不生效，代价只是某些说法下日期约束不施加（退回原有行为，**不会产生错数据**）。否决词分前后两套，因为方向本身带信息——「上次 / 查询 / 回顾」出现在日期**之前**说明在查历史，「拉伤 / 体检 / 发烧」出现在日期**之后**说明这个日期是往事。
> - **只看日期所在的那一个小句**（按 `，。；！？、` 切分）。这一点是必须的：`我在 2026-08-01 拉伤了腰，2026-08-19 能练什么` 里，如果用固定字符窗口，第二个日期会被前半句的「拉伤」连坐否决。现在取的是**第一个语境通过的日期**，而不是第一个日期。
> - **两处正则统一**：新增 `extract_iso_dates(text)`，宽松匹配 `\d{4}-\d{1,2}-\d{1,2}` + 显式补零 + `date.fromisoformat` 校验 + 2000–2100 范围。计划侧与查记录侧共用它，`2026-8-9` 不再只被其中一边识别。**查记录侧刻意不做语境门控**——那条路径只在"查看训练记录"意图已经命中之后才走到，用户意图本身就是查历史，再要求意图词只会把「查询 2026-08-18 的训练记录」这种最常见的问法挡掉。
>
> 实测原例：`我在 2026-08-01 拉伤了腰，今天能练什么？` → `requested_plan_date` 返回 None，`daily_schedule_constraint` 返回空串（原来会注入"2026-08-01 是周六，当天科目必须为全身训练"）。
>
> 回归测试 `tests/test_plan_date_context.py`（20 条 + 15 个子用例）：解析一致性（含非补零、非法日期、越界年份、数字粘连）、六种应被否决的语境、六种真实计划请求、裸日期免意图词、多日期时取第一个通过的、周计划仍需已确认科目、显式覆盖仍优先。顺带把 `test_local_fallback_and_set_counts.py` 与 `test_training_record_queries.py` 的手写常量清单换成自动收集——它们正因为这个手写清单而断了一次。

`main.py:670-686` 抓消息里**第一个** `20\d{2}-\d{2}-\d{2}`，不区分语境。用户说"我在 2026-08-01 拉伤了腰，今天能练什么？"→ 匹配到 08-01（周六）→ 注入硬约束"当天科目必须为 X"，并可能生成 `suggested_date=2026-08-01` 的计划存到错误日期。

另：`main.py:674` 用严格补零 `20\d{2}-\d{2}-\d{2}`，`main.py:1609` 用宽松 `\d{4}-\d{1,2}-\d{1,2}`。实测"2026-8-9"只被后者识别——查记录正常，生成计划时周计划约束却静默不生效。

**修法**：要求日期前后有意图词；统一两处正则并用 `date.fromisoformat` 兜底校验。

### BUG-08 一次上传多个 ZIP 时，第 2 个之后的活动被静默丢弃

> ✅ **已修复 2026-08-20** —— 后端把收集与裁决分成两步：循环里只往 `pending_activities` / `activities_index` 收集（不再在循环内 set_current），循环结束后由 `select_autoload_activity()` 判断——**只有全局恰好一个活动时才自动载入编辑区**，多于一个一律交给前端选择器。
> - 响应新增 `activities: [{zip, name}]`。这是前端能跨 ZIP 逐个处理的前提：`/upload_health/activity` 需要**重新上传对应的那个 ZIP** 才能取出 FIT，所以每个活动必须记住来源。扁平的 `activity_files` 保留，避免旧前端拿不到任何东西。
> - 前端 `openActivityPicker(files, activities)` 把每条活动映射回它自己的 `File` 对象（原实现只记住单个 `activityPickerZip`，跨包时必然发错文件）。多 ZIP 时列表项标出来源 `包名 › FIT 名`，否则同名 FIT 分不清。"保存完一个自动回到选择器处理剩下的"这条流程按 `zip + name` 组合键过滤——只按名字过滤会在两个包含同名 FIT 时一次删掉两条。
> - 原来多 ZIP 时只弹一句"请每次上传一个 ZIP"，等于放弃处理，已删除。
> - 顺带：单个活动 FIT 解析失败不再让整次健康数据导入失败（导入本身已经成功了），改为记进 `errors` 并把状态降为 `partial`。
>
> 实测：用 `data/health-imports/` 里两个各含一个活动的真实 ZIP 走一遍收集逻辑 → `pending=2` → 不自动载入，两条都进 `activities`（修复前只载入 `628992811_ACTIVITY.fit`，`629691036_ACTIVITY.fit` 被静默丢弃）。
>
> 回归测试 `tests/test_multi_zip_activity_import.py`（14 条）：`select_autoload_activity` 真调用（单活动 / 单包两活动 / **两包各一活动**＝本条原缺陷 / 空），后端接线（收集必须早于裁决、旧条件已删、响应字段、活动解析失败不连坐），前端接线（按 item.zip 解析 File、发的是对应那个 ZIP、组合键过滤、多包标来源、旧的"放弃"提示已删）。
> **沙箱装不了 fastapi**，所以端点本身仍是源码断言；前端逻辑用 `node --check` 验过语法。

`main.py:1440` 的条件是 `if len(activities) == 1 and workout is None`，`workout` 是整个循环的单一变量。前端 `index.html:2209-2218` 在 `data.workout` 非空时走第一分支，**ZIP#2 的训练既不进选择器也无任何提示**，用户会以为两个都导入了。

**修法**：后端把所有活动收集进 `activity_files`，多于 1 个时不自动 set_current，前端统一用选择器逐个处理。

### BUG-09 每日打卡只能新增不能编辑，同一天会产生重复记录

> ✅ **已修复 2026-08-21** —— 按日期 upsert，同一天再保存是**更新**而不是再加一条。实测对照旧实现：同日保存两次得到 2 条并列记录，权重 `[71, 70.5]`，且无从修改。
> - **去重必须在 store 的锁内做，不能在端点里先查后写**。新增 `DailyRecordStore.upsert_dated_record(date, category, record)`，查重与写入同处 `_write_lock + JsonFileLock`；端点先查再写的话，两个并发请求会同时判定"当日无记录"、各自追加一条，重复照旧（DATA-07 在 profile 上踩过同一个坑）。已实测：8 个并发请求后仍然恰好 1 条。
> - **记录体按字段合并，不整体替换**。早上记体重、晚上记热量是正常用法，整体替换会把早上那条抹掉。代价是没法把某个字段改回"空"，相比静默丢数据这是更可接受的一侧。
> - **磁盘上已有的重复记录会被折叠而不是删除**：旧→新依次合并（新值覆盖旧值），保留最早那条的 id 与 created_at，折叠条数在响应里原样返回并显示给用户。实测 3 条重复折叠成 1 条后，`weight_kg`/`note`/`energy_level`/新写入的 `fatigue_level` 一个都没丢。
> - **死端点 `PATCH /data/checkins/{record_id}` 是删掉而不是接线的**。它按 record_id 定位并接受任意 date，能把一条记录改到另一个已有打卡的日期上，正好又造出这次要消灭的重复——留着它等于给刚修好的缺陷开个后门。
> - 前端配套：打开表单时载入当天已有记录（`GET /data/checkins/{day}`，"这天还没打卡"返回 `checkin: null` 而不是 404，因为那是正常状态不是错误），并提示"保存将更新这一条"。**改日期会重新载入**——否则表单里留着的是上一天的值，一按保存就写进了另一个日期。餐盘照片那条路径改为 `await openCheckin()`：预填现在是异步的，不等它结束就写字段，估算出的卡路里会被预填覆盖掉。

`main.py` 原实现只调 `add_record`，每次生成新 uuid 追加。同一天点两次保存 → 两条 daily_checkin 并列显示。而 `PATCH /data/checkins/{record_id}` 是**完全不可达的死端点**。

**修法**：`save_daily_checkin` 先查同日记录，存在则走 update；或前端在已有当日记录时改发 PATCH。

### BUG-10 FIT 缺 session 记录时崩溃，且报错信息误导

> ✅ **已修复 2026-08-21** —— **但这条的前提描述是错的，真正的缺陷比它写的更糟。**
> - **不会抛 AttributeError**。`parse_fit_file` 取的是 `raw_msgs.get("session", [{}])[0]`——缺 session 消息时得到的是一个字段全为默认值的 `SessionSummary`（`sport="未知运动"`、`sport_raw="unknown"`），**`session` 恒非 None**。清单里那句 `sess.sport if sess else "未知运动"` 确实是死代码，但它不是"作者知道会 None"的证据，反而正是它让人以为"缺 session 会崩"，把注意力从真缺陷上引开了。这句已删。
> - **真正发生的是静默丢光整场训练**：缺 session → `sport_raw="unknown"` → 路由拿不到依据 → `FallbackParser` → 它一律走 lap 解析 → 没有 lap → `_from_session()` 因 `start_time is None` **返回 `[]`**。`set` 消息从头到尾没被看一眼。实测同一份含 10 组卧推的 FIT：有 session 时解析出 10 组，去掉 session 消息后 **0 组**，而用户看到的是一句「[FIT 解析完成]」。中断的活动往往正是"有 record/set、没写出 session"的形状，且原始 FIT 通常已不在磁盘上——等发现时数据拿不回来了。
> - **修法是按消息证据路由**：`FallbackParser.parse` 先看有没有 `set` 消息（set 的存在本身就是"这是力量训练"的确凿证据），有就交给 `StrengthTrainingParser`，没有才退到 lap。走到 Fallback 就意味着 sport 没能给出裁决，那就该问文件里到底有什么，而不是假定它是有氧。
> - **`_from_session` 补上 record 流兜底**：既无 session 也无 lap 时，用 `hr_records` 首尾时间戳构造时间窗（段上标 `lap_trigger="records"` 以便区分是不是近似出来的），而不是把整条心率流一起丢掉。
> - **一段都没解析出来时不再报告成功**：返回 `status: "empty"` + 明确说明「文件本身可以读取，但里面既没有动作组也没有分段记录，请保留这个 FIT 文件」，并清掉待确认区（否则用户会对着一个空壳点"确认并保存"，而 `confirm` 会因缺开始时间失败）。**刻意不返回 5xx**：文件读得动、解析没报错，把它说成服务器错误同样是误导；前端也单独处理这个状态，不套"上传失败："前缀去让用户重传一个传得好好的文件。
> - **`HealthImportError` 单独 catch**（这条清单说得对）：它带着精确中文原因和自己的 status_code，被 `except Exception` 吞掉之后用户看到的是"请确认文件完整"，于是去检查一个完好无损的文件。实测修复前 500 + 通用文案，修复后 400 + 原文。窄 except 排在兜底之前，有测试钉住这个顺序。
> - 兜底文案也改了：不再断言"文件损坏"——走到那里说明是**未预期**异常，代码 bug 与坏文件同样可能。

`main.py` 原有 `sess.sport if sess else "未知运动"` 的空值保护，但 `sess.sport_raw` 和 `_session_summary_line(sess)` 都无保护。任何只有 record/lap 而无 session 的 FIT（中断的活动）都会抛 AttributeError，被吞成 500「FIT 文件解析失败，请确认文件完整」——**把代码 bug 说成用户文件损坏**。

同样地，`upload_fit` 只有一个 `except Exception` 兜底，会把 `HealthImportError`（带精确中文原因和 status_code）吞成通用 500，而 `/upload_health` 就正确地单独捕获了它。

### BUG-11 前端 `editorBusy` 只置位不复位，合并按钮永久变灰

> ✅ **已修复 2026-08-21** —— 按钮确实永久变灰（置位 2 处，复位 10 处但全都在"保存/合并/切换记录**成功之后**"），但**只按清单删掉 `editorBusy` 是不够的，那等于用丢数据换按钮可用**。
> - **`editorBusy` 当初拦住的是一个真问题**：合并只发 `{action:'merge_sets', indices}`，**不带 updates**。服务端拿它自己那份旧数据合并，前端随后 `refreshWorkoutState(true)` 重渲染——用户没保存的"上斜卧推 80kg"被静默丢弃。实测对照：改成 80kg/12 次后合并，得到的是 `category='卧推' weight=60 reps=20`，即服务端的旧值。
> - **所以修法是让合并把编辑区草稿一起提交**，与已保存记录那条路径（`updates + merge_indices` 一次提交）对齐：`merge_sets(indices, updates, note)` 先应用编辑再聚合。**顺序不能反**——聚合要读 category/weight/repetitions，反了读到的就是旧值，有测试钉住。修复后同一操作得到 `上斜卧推 / 80kg / 24 次`。
> - 编辑无效时**整个合并被拒**，不留"编辑应用了一半、合并没做"的状态；`updates` 为 None 时（Agent 路径、没刷新的老页面）行为与从前完全一致。
> - 这之后 `$merge.disabled` 才改成只看 `count < 2`。**`editorBusy` 本身保留**——它抑制轮询覆盖正在编辑的字段，那个用途是对的，连它一起删掉会让 3 秒一次的轮询把用户正在改的值冲掉。

`index.html` 在任意编辑框输入时置 `editorBusy = true`，而 `$merge.disabled = count < 2 || editorBusy` 只在"保存成功/合并成功/切换记录"时复位。于是高频顺序卡死：**改了某组重量或写了训练感受 → 再勾选两组想合并 → 按钮一直是灰的且无任何提示**，只能先保存或刷新。

**修法**：`editorBusy` 只用于抑制轮询覆盖，不参与合并按钮 disabled 计算。

### BUG-12 记忆事实用下标寻址，列表会在用户眼皮底下位移

> ✅ **已修复 2026-08-20** —— 给每条事实一个稳定的 `fact_id`，接口按 id 定位。
>
> **关键选择：id 用内容摘要而不是随机 uuid。** 随机 id 需要一次数据迁移，而且在迁移落盘之前每次 `_read_all` 都会生成不同的 id——前端拿到的 id 下一秒就失效了。内容派生（`sha256(namespace\x1fkey\x1fvalue_key)[:16]`）**对存量数据天然稳定**：不写盘也能算出同一个 id，零迁移。已在真实 `data/info_store.json` 上验证：4 条记忆 / 10 条事实全部拿到唯一 id，两次读取完全一致。
> - id 只需在**一条记忆条目内**唯一（寻址总是 `entry_id + fact_id` 成对），而 `normalize_memory_facts` 已按 (namespace, key, value) 去重，所以条目内不会重复。跨条目相同值会得到相同 id，无害。
> - `_find_fact` 改为 `(items, entry_id, fact_index=None, fact_id=None)`，**fact_id 优先**，并返回下标供 `reject_fact` pop 用。三个方法都加了 `fact_id` 关键字参数，位置参数形式保持兼容。
> - 路由从 `{fact_index}` 改为 `{fact_ref}`：纯数字按老式下标处理（兼容还没刷新的页面，会打一条 info 日志），否则按 fact_id。不新增并行路由，避免又出现两套机制。
> - 前端改用 `fact.fact_id || String(factIndex)`。
> - `edit_fact` 会改变 id（值变了就是另一条事实，这是预期行为），并新增一条校验：**新值若与同条目里另一条事实撞车则拒绝**——否则两条会共用一个 id 而无法再分别寻址。
>
> 回归测试 `tests/test_memory_fact_addressing.py`（15 条 + 3 个子用例），四组不变量：id 跨读取/跨 `cleanup_expired`/跨他条事实被删都不变；位移后按 id 操作命中的仍是原来那条且不误伤邻居；未知 id 一律 404 而不是落到别的事实上；编辑语义。其中 `test_stale_index_would_have_hit_the_wrong_fact` **刻意把原缺陷的行为写成测试**——拒绝首条后，老页面手里的下标 1 会命中"硬拉"而它以为是"深蹲"。含 `WiringTest` 断言路由、参数传递与前端三处都真的走了 id。

`main.py:1987-2019` 传 `fact_index`，而每次 `/chat` 都会 `cleanup_expired` **物理删除**过期事实（info_store.py:460-477），`reject_fact` 也会 pop 移位。老页面点"确认/拒绝/编辑"会作用到**另一条事实**——比"旧记忆覆盖新记忆"更危险。

**修法**：为每条 fact 生成稳定 `fact_id`，接口按 id 定位。

### BUG-13 纯摘要记忆永远进不了上下文（★ 你的测试正在失败）

> ✅ **已修复 2026-08-20** —— `info_store.get_context_memories` 的收录规则改为三条：
> (1) 有**已确认且仍有效**的结构化事实 → 收录，且只带上这些事实；
> (2) 条目级已确认、且**完全没有事实** → 同样收录（纯自然语言摘要，这是原先漏掉的情况）；
> (3) **有事实但一条都没确认、或全部被拒绝/清除 → 不收录**。
>
> 第 (3) 条是刻意保留的安全边界：不能因为条目级已确认，就把用户明确拒绝掉的事实所对应的摘要原文重新灌回上下文——否则"逐事实拒绝"就形同虚设。
>
> 你原本失败的那 2 条测试已转绿，另补 4 条边界用例（`tests/test_info_store.py`）覆盖：纯摘要未确认不进、事实全被拒绝不进、部分确认只带确认的那条。**全量测试现在 162 通过 / 0 失败。**

`info_store.py:499-504` 的 `get_context_memories` 要求 `confirmed_facts` 非空才收录。后果：**只有自然语言摘要、没有结构化事实的记忆，无论你怎么确认都永远不会被使用**，`format_cross_session_memories` 的"历史摘要"分支在 /chat 中恒为空。

这不是推测——你自己的两条测试正在失败：

```
FAILED tests/test_info_store.py::test_pending_memories_are_not_injected_until_confirmed
        context = self.store.get_context_memories()
    >   self.assertEqual(context[0]["type"], "constraint")
    E   IndexError: list index out of range
FAILED tests/test_info_store.py::test_context_memories_are_ranked_by_importance
```

**修法**：条目级 `user_confirmed=True` 且 facts 为空时也应入选。

### BUG-14 view_* 意图命中后直接短路，用户的真实问题被丢弃

> ✅ **已修复 2026-08-21** —— 实测确认两个例子都是 **Agent 完全未被调用**，"上次卧推推了多少公斤"无人回答。
> - **但清单给的第一种写法（一律"附加 artifact + 继续走 Agent"）不能采纳**：短路正是 BUG-02 建立的**离线本地读路径**，关闭外部模型时「查看训练记录」全靠它，改成一律走 Agent 会让那条路变成 503。所以走的是第二种：**只在"纯导航"时短路**。
> - 新增 `navigation_only_message()`：把动作词（查看/查询/打开/回顾…）、宾语（训练记录/个人档案…）、日期、礼貌语与标点整个剥掉，剩下还有字就说明另有诉求。**按长度降序剥离**，否则"看"会先把"查看"啃掉半截。
> - **误判方向是刻意选的**：把纯导航误判成"有额外诉求"只是多花一次模型往返，用户仍然拿到查看面板；反过来（把复合请求当成纯导航）才是这条 bug 本身。所以遇到没见过的说法一律按"有额外诉求"处理，测试里也钉住了这一点。
> - 训练记录有 artifact，所以复合请求时把它**挂起**（`pending_view_artifact`），继续走 Agent，最后一起返回。个人档案**没有** artifact——"看到的东西"就是那段正文，所以它作为 `pending_reply_prefix` 前缀留给最终回复，正文与短路分支逐字相同，两条路径不会给出两种说法。
> - **计划保存卡优先于挂起的查看面板**：前端一次只渲染一个 artifact，需要用户操作的那张卡更重要，顺序是 `profile_update_artifact or artifact or pending_view_artifact`。
> - 挂起的面板也接到了 onboarding（档案没填完）与本周汇总两条分支上——不接线的话复合请求会拿到面板为 None，等于换了个方式丢东西。
> - **离线 + 复合请求**：仍然短路并给出面板，但明说"你在同一句里还问了别的问题，但外部模型当前已关闭"。静默丢弃正是这条 bug 的本体，离线也不例外。

`main.py` 原实现命中即 return 固定话术，不再走 Agent。具体误判：

- "帮我看看我的训练记录，**上次卧推推了多少公斤**？" → 必然命中 view_training_records → 只回"已打开本地训练记录。选择日期后…"，重量问题无人回答。
- "我的可用器械有哪些，**用这些器械帮我安排今天的胸推**" → 走 view_profile → 只回档案，计划请求被吞。

**修法**：view_* 改为"附加 artifact + 继续走 Agent 回答"，或在消息含额外诉求时不短路。

### BUG-15 压力数据时区语义与同文件其他指标不一致，可能整体偏移 8 小时

> ✅ **已核实并加固 2026-08-21** —— **这条的前提经实测不成立，而清单给出的修法会亲手造出它要防的那个 bug。**
> - 清单自己写了"两种解释不可能同时成立，当前实现等于在赌"，并给了验证方法。已按那个方法验证，取的是**三份真实 wellness ZIP**：stress 的 naive 时间戳当 UTC 解释后，跨度与**同一份文件里** monitoring 心率的 `timestamp`（`utc=True`，语义确定）**逐分钟重合，偏差 0.0h**；当成本地墙钟解释则整体差 **8.0h**。三份文件结论一致。
> - 另一条独立证据：同一批文件里 `monitoring_info` 同时带 `timestamp` 与 `local_timestamp`，两者之差确实是 **+8h**。也就是说 fitfile 的 `utc=False` 在**那个**字段上是对的，只是被错用到了 stress 上——FIT 规范里 stress_level 字段 1 是 `stress_level_time`，基类型 `date_time`（UTC 基准）。
> - 所以**现有的时区解释是对的**，清单建议的 `local_ts.replace(tzinfo=BEIJING)` 会把全部压力数据提前 8 小时。顺带一提，那个写法的第一个分支 `values.get("timestamp")` **永远取不到值**：实测 stress_level 消息里根本没有 `timestamp` 字段（0/60）。
> - **但它是碰巧对的**，这才是要动的地方：`_utc_datetime` 对任何 naive 值一律当 UTC，而代码里原先没有任何地方记下"这里为什么可以"。现在把契约写进 `_utc_datetime` 的 docstring（`date_time` 是 UTC，真正的本地墙钟字段是 `local_date_time`，那种值不能走这个函数），并用测试钉住"naive 04:00 → 北京 12:00"，同时显式断言**不是**前一天 20:00。
> - **不再就地覆盖 `timestamp` 变量**：原实现在 stress 分支里把它换成另一个字段，而它同时是下面 `_timed_value(...)` 与 hrv_status 分支读的那一个。分支目前互斥所以没出事，但那是运气不是设计，已改用独立的 `metric_time`。
> - **字段改名不再静默丢光数据**：原实现直接 `values.get("local_timestamp")`，取不到就是 None，随后 isinstance 检查失败，**1440 条/天的压力样本一条不剩地被跳过且不留痕迹**。现在按「规范名 `stress_level_time` → 通用名 `timestamp` → fitfile 当前的错名 `local_timestamp`」依次尝试（上游自己就认为这个名字是错的），三个都取不到时记一条 warning 而不是沉默。

`health_importer.py` 对所有消息统一取 `values.get("timestamp")`（fitfile 定义为 `utc=True`，返回带时区 UTC），monitoring/respiration/pulse_ox/hrv 走的都是这条、语义正确。但 stress 单独换成 `local_timestamp`，而 fitfile 对该字段定义是 `utc=False` → 返回 **naive 本地墙钟时间**；`_utc_datetime()` 对 naive 值一律当 UTC，再 `astimezone(BEIJING)` +8 小时。

**验证方法**：导入一天 wellness ZIP，看 `/health/trend?metric=stress&period=day` 曲线是否整体右移约 8 小时、凌晨是否出现密集读数。
**修法**：`ts = values.get("timestamp") or (local_ts.replace(tzinfo=BEIJING) if local_ts else None)`——无论哪种解释成立这个写法都对。

### BUG-16 按天趋势不去重，与日汇总口径对不上

> ✅ **已修复 2026-08-21** —— 已在真实 `health.db` 上量到规模：`heart_rate_samples` 16220 行 / 去重 15005（**重复 7.5%**）、`health_metric_samples` 50684 行 / 去重 46971（**重复 7.3%**）；**2026-08-15 整日每个指标的趋势 samples 恰好是日汇总的 2 倍**（心率 2424 vs 1212），68 组比对里 **11 组口径不一致**。
> - **刻意不在写入侧去重**：每行都记着自己的 `source_file_id`，`delete_import` 靠它精确回退某一次导入。表上本来就有 `UNIQUE(source_file_id, timestamp_utc, …)`——它挡的是同一个 FIT 文件里的重复，挡不住跨导入重叠（`source_file_id` 不同）。但**也不能**把它收紧成不含 `source_file_id` 的全局唯一：那样两次导入会共享同一行，删掉任一个都会连带抹掉另一个的数据。有测试钉住这个边界。
> - 所以去重仍放在读取侧，但**把 SQL 抽成 `DEDUPED_HEART_RATE_SQL` / `DEDUPED_METRIC_SQL` 两个模块级常量**，由 `_rebuild_daily_summary` 与 `get_metric_trend` 共用。这样"同一口径"是结构上的而不是注释上的——原先两处各写一份，其中一份漏了去重，正是这条 bug。
> - `MIN(timestamp_local)` 而不是任取一个：重复行理论上可能带不同的本地时间字符串，取 MIN 让小时分桶确定，且与日汇总的 `coverage_start` 用同一个值。
> - metric 过滤刻意放在**外层**：内层必须与日汇总逐字一致，否则又变成两份要各自维护的 SQL。一天的指标行数在 5 千量级，先整日去重再筛指标的代价可以忽略（实测 20 次调用 43–61 ms）。
> - `week` / `month` 读的是 `daily_health_summary`，本来就已去重，未改动。
> - 修复后在真实库上复跑：**68 组全部一致**（修复前 11 组不一致），2026-08-15 原始表 2424 行 → 趋势 samples 1212 = 日汇总 1212。均值口径也一并核对：用趋势的 samples 加权还原出的日均值与日汇总逐日一致（差值 ≤ 0.15）。
> - 值的失真在当前数据里较小（只有 1 个小时桶差 0.1 bpm），因为重叠多是逐字复制；**整小时被完整复制时均值不变，只重复一部分才会把那几分钟的权重翻倍**。测试里专门造了这一档：10 分钟 60 bpm + 10 分钟 180 bpm，只把前 10 分钟再来一遍——不去重得 100.0，去重得 120.0。

`health_store.py` 的日汇总和 `query_heart_rate_window` 都很谨慎地做了 `GROUP BY timestamp_utc` 去重，但 `get_metric_trend(period="day")` **直接扫原始表**按小时聚合。Garmin 官网按天导出经常互相重叠（同一 WELLNESS.fit 出现在相邻两天的 ZIP 里），而整包 sha256 不同不会被去重拦截。结果同一天的小时曲线被重复时段权重翻倍、samples 虚高，**而同一天的日汇总卡片却是对的，两处数字对不上**。

### BUG-17 重复导入同一次训练时，用户的编辑被静默丢弃

> ✅ **已修复 2026-08-22** —— 重新导入同一训练时，后端不再把待确认状态直接清空，用户的动作编辑、合并结果和备注都会保留。
> - **同一 FIT 哈希重放仍保持幂等**：只有源文件哈希一致时才按重放处理，不重复创建记录。
> - **同日期 + 同运动 + 开始时间但来源不同**时，后端返回 `DUPLICATE_TRAINING` 冲突，不再谎报 `saved: True`。待确认训练仍在页面和磁盘上保留，用户可选择保留旧记录或覆盖。
> - **覆盖走并发安全的修改路径**：按旧记录 `revision` 调用 `update_record_if_revision`，避免两个页面同时覆盖彼此的编辑。
> - 前端在冲突后弹出明确的覆盖确认；取消时旧记录保留，当前导入仍可继续编辑或主动丢弃。

原问题是 `workout_store.py` 在命中重复后调用 `clear_current()`，用户编辑被静默丢弃。现在已拆分为“幂等重放”和“需用户决策的重复冲突”两条路径，并补了修改后的心率流更新。

### BUG-18 其他已确认的小缺陷

> ✅ **已修复 2026-08-22** —— 已对 FIT 解析、训练合并、档案 onboarding 和前端轮询路径做回归验证，相关测试 **81 个全部通过**。
> - **多 session / 铁三 FIT**：`parse_fit_file` 不再只取首个 session，按各 session 的起始时间和结束边界分配 lap/set，后续段不再挂到第一个运动类型下混算距离和配速。
> - **lap 缺 `start_time`**：使用 `timestamp - elapsed` 反推起始时间，避免时间窗口被压成 0 秒。
> - **心率统计回补**：`avg_hr` 或 `max_hr` 任一缺失时都从原始心率流计算，前端对部分缺失字段不再展示 `null`字面量。
> - **`merge_sets` 时间窗口**：改为吸收与合并窗口相交的休息段，包括在窗口外开始或结束的跨界休息段。
> - **档案完善分支**：用包含当前 `updated_fields` 的投影档案计算 onboarding 状态，不再对同一份未更改的 dict 重复调用 `is_complete()`。
> - **onboarding 已记录前缀**：返回 `onboarding_reply` 时传入实际 `updated_fields`，用户可看到“已记录：X”。
> - **记忆和轮询稳定性**：限制确认事实注入上限，对 check-in 网络异常增加明确提示，训练状态轮询仅在数据变化时重建 DOM，不再持续覆盖用户的滚动位置。

原始清单中所列的小缺陷分别对应 `fit_parser.py`、`workout_store.py`、`main.py` 和 `templates/index.html` 的已修复路径；主要回归包括解析分段时间窗、心率回补、重复训练覆盖和前端错误提示。

---

## 5. P1 —— Agent 能力

### AGENT-01 高风险症状在 prompt 和后端都没有任何拦截

> ✅ **已修复 2026-08-20** —— 新增 `fithealth_agent/health_safety.py`，一个不依赖任何外部模型的确定性风险筛查器，三档分流：
>
> | 档位 | 覆盖 | 后端行为 |
> |---|---|---|
> | `emergency` | 胸痛胸闷、晕厥/眼前发黑、呼吸困难、疑似卒中、咯血呕血、剧烈头痛 | **直接短路，不调用模型**，返回停止运动 + 拨打 120 + 就医评估的确定性回复 |
> | `urgent` | 疑似骨折脱臼、撕裂感、关节肿胀、无法负重、剧烈疼痛 | 正常对话，但**确定性地拒绝产出可保存的训练计划**（artifact 置 None 并附说明） |
> | `caution` | 头晕、发热、心悸/静息心率异常、恶心呕吐、血糖血压异常 | 不阻断，向模型注入高优先级硬提示，要求先回应状况并下调强度 |
>
> **筛查位置在意图路由之前**，理由有三：急症不该等一次外部模型往返；关闭联网模型或网络故障时同样要生效；这条判断不依赖已确认记忆——用户在**当前这条消息**里新出现的症状本来就不在记忆里（这正是原设计的漏洞）。只筛查 `source == "chat"` 的消息，上传的计划正文里出现症状词不算主诉。
>
> 风险提示注入在 agent 输入的**最前面**，优先级高于主目标、周计划与任何偏好。`prompts.py` 同步补上了此前完全缺失的「健康安全规则」章节（三档分级 + 不做诊断/不推荐药物/不建议极端热量限制 + 「忽略我的伤」不得据此放弃限制）。
>
> **误报控制**：实现了否定/既往/假设语境识别——「胸口不疼了」「我没有胸痛」「以前有过晕厥现在好了」「如果训练时胸痛会不会危险」均放行。否定标记只在命中词**之前**的窗口里找，因为症状词本身可能含「不」（如「喘不上气」）。实测 16/16 符合预期。
>
> 回归测试 `tests/test_health_safety_gate.py`（18 条 + 43 个子用例），含 `ChatWiringTest` 专门断言拦截确实接进了 `/chat`、筛查确实排在意图路由之前——避免这次修复又变成第二个 BUG-01。

`prompts.py` 全文 93 行**没有任何风险分级或急症规则**，`main.py:914-967` 主链路也无关键词硬校验。`resolve_plan_context` 只读"已确认记忆事实"，而记忆要退出会话、经 router 提取、再由用户确认才生效——**当前消息里的新症状根本不在其中**。

所以"我练到一半胸口疼、有点晕，明天该练什么"会直通 Agent 生成训练处方。`list.md:100-109`(SAFE-001) 与 `开发日志.md` 2026-08-17 第 3 条声称已建立安全规则，**代码中未见实现**。

**修法**：在 `main.py:914` 之前加确定性风险词表（胸痛/胸闷/晕厥/黑视/呼吸困难/急性拉伤/静息心率异常），命中即短路返回就医提示并禁止生成计划；同时把风险分级写进 prompts.py。**这是本清单里唯一涉及人身安全的条目，建议优先级最高。**

### AGENT-02 `save_daily_record` 可绕过确认直接落盘，且几乎无校验

> ✅ **已修复 2026-08-20（校验部分）** —— 新增 `daily_checkin.validate_agent_daily_record()`，与手动录入共用同一份 `_NUMBER_FIELDS` 口径，避免同一个指标两套标准。取向是**结构从严、字段从宽**：
> - **分类白名单**（最关键）：只允许 `recovery / nutrition / summary / daily`。用白名单而非黑名单，因为 FIT 导入会产生「力量训练」「跳绳」「有氧运动」等中文分类，黑名单不可能列全。`category=training` 被明确拒绝，错误信息还会告诉模型正确路径是"提示用户点击侧栏的确认并保存训练"。
> - **日期**：强制 `date.fromisoformat`，拒绝「昨天」这类相对说法；拒绝未来日期（容忍 +1 天时区偏差）与 2000 年前。
> - **结构**：嵌套 ≤3 层、字段数 ≤30、序列化 ≤4000 字符、单个字符串 ≤500、列表 ≤50 项、键名 ≤40 字符，并拒绝不支持的值类型。
> - **来源标记**：写入的记录带 `source: "agent_tool"`，可与用户手动录入区分（也为 P2-13 的餐食来源标记打了基础）。
> - **审计日志**：成功与拒绝都记录动作、日期、分类、字段名清单与失败原因，**不记录原文**，避免把健康细节写进日志。
>
> ✅ **营养记录归类漏洞已修复 2026-08-23** —— 实际 trace 中 Agent 正确调用了 `category=nutrition`，但训练列表采用“不是 `daily_checkin` 就是训练”的反向判断，导致 nutrition/recovery/summary 都可能伪装成训练记录。现在训练列表、查看、编辑与删除端点统一要求记录具备真实训练结构；Agent 营养写入则规范化 `total_kcal → calories_kcal`，按日期 upsert 到 `daily_checkin`，保留同日体重、恢复、备注和照片餐次。历史上 `source=agent_tool` 的 nutrition 记录会在读取时定向迁移并与同日打卡合并，其他来源不做静默迁移。复现数据 `280g 碳水 / 172g 蛋白质 / 50g 脂肪 / 2258 kcal` 已迁移验证。
>
> 顺带修了两处相邻问题：`query_daily_records` 的 `limit` 加了 30 的上限（原先无上限，`limit=200` 会把 ReAct 观察撑爆），以及 `prompts.py` 里"没有保存训练的权限"与"优先调用工具保存结构化数据"两条自相矛盾的指令已合并改写。
>
> 回归测试 `tests/test_agent_write_validation.py`（30 条），含 `ToolBehaviourTest` 用桩 store 验证**校验失败时绝不落盘**，以及源码断言确认没留旁路。
>
> **仍未做**：AGENT-02 原本还要求"高影响写入采用生成草稿 → 前端确认 → 后端保存"的状态机。当前修复把危险面收敛到了安全分类内（模型再也写不出训练记录、越界数值和畸形结构），但恢复/营养类记录仍是直写。完整的草稿-确认流要改动 Agent 工具 → main.py → 前端三层，建议与 AGENT-08 的结构化 artifact 一并做。

`tools.py:29-36` 只检查 date 非空、category 是非空字符串、record 是 dict；`storage.py:53-95` 不做任何日期/字段/大小校验。因此 `date="昨天"`、record 嵌套任意深度都能写入。更严重的是 category 写成 `training` 的记录会直接出现在训练记录列表里（main.py:1591 只排除 CHECKIN_CATEGORY），**而 prompts.py:61 明确写着"Agent 没有保存训练的权限"——工具层实际给了权限，只靠 prompt 挡**。

项目里已有完整校验实现 `daily_checkin.py:26-62`（字段白名单+范围），却没被工具复用。

**修法**：工具复用 `validate_daily_checkin` 的 schema，date 强制 `date.fromisoformat`，category 白名单且禁止 training/daily_checkin；高影响写入改"生成草稿→前端确认→后端保存"。

### AGENT-03 硬约束会被 Top-5 截断丢掉

> ✅ **已修复 2026-08-22** —— 已将“硬约束与普通记忆分开预算”接入记忆读取和聊天上下文两层。
> - **实测根因**：1 条 importance=3 的周计划 + 5 条 importance=5 的训练反馈时，原实现先截取 Top-5 再解析事实，周计划因此在进入 `resolve_confirmed_memory_facts` 前就被丢弃。
> - **硬约束无条件保留**：`health.injury_or_constraint`、`health.recovery_status` 和 `plan.weekly_schedule` 不受 `n=5` 条数限制，即使被更多高重要度的普通反馈挤在后面也会全量进入上下文。
> - **普通记忆仍有预算**：普通偏好、训练反馈仍按条数和字符预算进行裁剪，避免为了保住硬约束又让普通记忆无限壮大。
> - `InfoStore.get_context_memories()` 负责硬约材料的全量保留，`main.py` 的 `format_cross_session_memories()` 再按同一分类规则渲染，避免事实在后续格式化阶段被重新截断。

原修法只保证了“重要度高的优先”，却没有保证安全和周计划约束的最低可见性。现在已用测试固定“低 importance 周计划 + 5 条高 importance 反馈”场景，周计划仍然可见，相关回归测试 **55 个全部通过**。

### AGENT-04 证据校验的标点常量写错，拉丁文本可绕过

> ✅ **已修复 2026-08-22** —— 证据核验现在使用真实的空白字符集合，不再把字母 `t` / `r` / `n` 当成可剥离标点；`_evidence_supported("train hard", "I do rain hard workouts")` 已验证返回 `False`。
> - 原先的 `" \\t\\r\\n…"` 是普通字符串，包含四个字面字符 `\`、`t`、`r`、`n`。紧凑化证据时会错误删除英文单词中的这些字母，从而使不相同的证据片段碰撞。
> - 现改为 `" \t\r\n…"`，只剥离真实的空格、制表、回车和换行以及中英文标点；英文频道名、动作名和训练术语仍按原文字符参与比对。
> - **事实改为逐条处理**：无证据事实会从待保存列表中移除，并通过 `rejected_facts` / `fact_validation` 返回拒绝数量和原因；同批次中已有证据的事实与摘要仍可进入确认流程。
> - **整批拒绝边界保留**：若模型返回的事实全部无法在用户原文中核实，仍拒绝保存整条记忆，避免保存只有模型推断、没有用户证据的摘要。
>
> 回归测试 `tests/test_information_router_memory_safety.py` 覆盖拉丁文本伪造证据和“1 条有效 + 1 条无效事实”的混合批次，**12 个测试全部通过**。

`information_router.py:186` 写成 `punctuation = " \\t\\r\\n，。…"`——这是普通字符串，实际把字母 `t`/`r`/`n` 和反斜杠当标点剥离。实测 `_evidence_supported("train hard", "I do rain hard workouts")` 返回 **True**，频道名、英文动作名的证据可被伪造通过。

附带：`information_router.py:251-256` 中**一条事实无证据即整次记忆全丢**，正确事实和摘要一并损失，用户只看到"整次记忆已拒绝"。

**修法**：常量写成 `" \t\r\n"`；改为逐条丢弃并在响应里列出被拒事实。

### AGENT-05 档案列表字段整表覆盖，疑问句能通过校验

> ✅ **已修复 2026-08-22** —— 档案更新现在要求明确陈述，并把器械变更从“整表数组”改为有语义的 `add` / `remove` / `replace` 操作。
> - **疑问句后端硬否决**：`validate_profile_tool_updates()` 接收当前用户原文；消息带 `?` / `？`、`是不是`、`是否`、`有没有`、`哪些`、`什么`、`吗` 等问句信号时，无论模型是否错误调用 `update_profile`，均不生成可确认更新。模型提示词仍保留“问题不得更新档案”，但不再是唯一防线。
> - **器械改动必须带模式**：路由 Function Calling schema 改为 `equipment_change: {mode, items}`。新增器械走 `add`，不可用器械走 `remove`，只有用户明确给出完整清单时才允许 `replace`；旧的裸 `equipment` 数组不再通过后端校验。
> - **合并发生在档案写锁内**：确认提交时才将操作应用到当前档案。`add` 去重追加、`remove` 仅移除指定项、`replace` 才替换完整列表，避免“新买壶铃”把已有器械静默删除。
> - **确认卡展示 diff**：artifact 带 `before` / `after` / `added` / `removed`，前端在保存前明确展示新增、移除与最终器械列表；不再只显示一个看似完整、实际会覆盖原值的数组。
> - **空列表是有效状态**：用户移除最后一件器械后保留空列表，表示仅做徒手训练，不再在下次读取时被悄悄还原成默认哑铃和哑铃凳。
>
> 回归覆盖疑问句拦截、`add` / `remove` / `replace` 三种操作、移除最后一项以及确认卡 diff，相关测试 **39 个全部通过**。

`storage.py:235-240` 是 `profile[key] = value` 直接覆盖，`main.py:357-368` 只校验条数 1-20、每项 1-24 字符——"哪些""什么""不知道"都能通过。唯一防线是 `chat_intent_router.py:156-158` 里逐字写死的那一句"我的可用器械有哪些？"。

仍会误判的输入：

- "我的器械**是不是**只有哑铃和跳绳？" → 疑问句但含明确名词，模型极易判为陈述 → equipment 被覆盖为 2 项，哑铃凳等静默删除。
- "我**新买了**一个壶铃" → 参数描述要求"完整列表"，模型只能给 `["壶铃"]` → 确认卡只显示"可用器械：壶铃"，**不显示将删除哪些**，一点确认原有器械全丢。

好的一面：写入已是确认制（main.py:797-805 出 artifact），不再静默改档案——这一改是对的。
**修法**：后端加疑问句硬否决；equipment 支持 add/remove/replace 三种模式；确认卡展示增删 diff。

### AGENT-06 prompts.py 的工具名错误、自相矛盾、与 max_steps 冲突

> ✅ **已修复 2026-08-22** —— 工具名、写入权限和视频检索策略已对齐到实际运行行为：
> - 将提示词中的 `youtube` 统一为已注册的 `search_youtube_video`，避免模型调用不存在的工具名。
> - 训练记录保存只保留一条规则：`save_daily_record` 仅可记录每日健康数据；训练记录必须通过侧栏“确认并保存训练”。这与后端对 `category=training` 的拒绝校验一致。
> - 训练计划仅为 **1-3 个核心主项**检索视频，热身、拉伸与重复动作不逐项搜索；每个动作最多一次，且 `max_results=1`。因此在 `max_steps=15` 内仍有足够步骤生成完整计划，不会因 10-14 次视频调用截断，也显著降低 YouTube 配额消耗。
> - `YouTubeSearchTool` 为同一工具实例加入按规范化 query 和结果数区分的缓存；同一轮重复检索直接复用结果，并返回独立副本，调用方不会污染缓存。
>
> 回归测试覆盖频道过滤、规范化 query 缓存命中、不同结果数不串缓存，以及提示词与注册工具名、保存权限、检索上限的契约。

- `prompts.py:19,79` 称"youtube 工具（MCP）"，实际注册名是 `search_youtube_video`（youtube_tool.py:46），模型可能吐出 `name="youtube"` 导致工具未找到。
- `prompts.py:61`"Agent 没有保存训练的权限" 与 `:89`"优先调用工具保存结构化数据" 直接冲突（且见 AGENT-02，权限实际存在）。
- `prompts.py:79` 要求为计划中**每一个动作**各调一次视频检索（热身+主项+拉伸通常 10-14 个），而 `agent.py:89 max_steps=15`——**一份完整计划必然触顶**，末尾动作视频缺失或整体截断。同时 YouTube search.list 每次 100 配额、默认 10000/天，一份计划烧 1000+，**7-9 份即耗尽当日配额**。

**修法**：统一工具名；"保存权限"规则只保留一条；视频检索限制为主项动作并做本轮去重缓存。

### AGENT-07 训练组编辑无撤销

> ✅ **已修复 2026-08-22** —— 待确认训练现在保留两份可恢复状态，且 Agent 与侧栏编辑器走同一套后端：
> - 新解析的 FIT 在进入编辑状态时保存不可变的 `parsed_source` 原件；每次有效的改组、合并或删除前保存一份 `last_edit_snapshot`。两份快照与 `pending_workout.json` 一起原子持久化，刷新或重启后仍可用；旧文件则以当前已恢复内容作为兼容基线。
> - 新增 `undo_last_edit`，可撤销最近一次成功编辑（合并携带的草稿与合并本身视作同一次）；新增 `restore_parsed_source`，可放弃全部待确认编辑并还原 FIT 的原始解析结果。两条操作都会更新确认版本与令牌，旧的保存请求不能覆盖已回退的状态。
> - 注册 `delete_set`，模型可直接删除确定多余的力量训练组；提示词同时规定误删或误改走 `undo_last_edit`，需要完全还原时走 `restore_parsed_source`。
> - 侧栏新增“撤销”和“恢复原件”按钮，按后端可用状态启用；执行时明确提示未提交输入也会丢弃，避免浏览器草稿与服务端状态不一致。
>
> 回归测试覆盖编辑后重启仍可撤销、删除后重启仍可恢复解析原件、无编辑时拒绝撤销，以及三个 Agent 工具的调用与注册契约；既有训练编辑、确认和合并测试同时通过。

`fit_tools.py:63,122` → workout_store 立即落盘。merge 的窗口保护做得不错（会拒绝吞掉未选中的活动段），但模型误解"把第 2、3 组合并"后**没有任何回退路径，只能重传 FIT**。另外 `workout_store.py:540` 的 `delete_set` 已实现但**未注册为工具**——prompt 让用户报"多余组别"，模型却只有 merge 一条路。

**修法**：编辑前保留解析原件快照，新增 `undo_last_edit` / `restore_parsed_source`；注册 delete_set。

### AGENT-08 计划校验覆盖不足

> ✅ **肌群恢复校验与否定语义误判已修复 2026-08-23** —— `validate_generated_training_plan()` 现在接收可选的恢复快照；计划生成和保存均传入当前会话快照。若计划把仍在恢复中的肌群安排为主项动作，后端会返回包含肌群、剩余小时、动作和组数的违规说明。普通酸痛仍只要求模型减量，疼痛仍交由健康安全路径处理。禁做动作和排除频道均改为逐行检查，“避免/不做/不建议/替换”等否定行不再被当作实际安排。

> ✅ **模块命名已修复 2026-08-23** —— 原 `plan_validator.py` 已重命名为 `plan_classifier.py`，明确其职责是鉴定上传的 Markdown 是否为训练计划。真正的安全校验仍位于 `main.py`：除禁做动作、组数上限、RPE 上限、必需元素、周计划科目和排除频道等已确认记忆事实外，现已接入由训练记录现场派生的肌群恢复快照。

> ✅ **重复动作与伤病限制校验已修复 2026-08-23** —— 同一计划中的规范化动作名不可重复；已确认健康限制会由计划生成与保存路径显式传入校验器。限制直接点名动作时按该动作拦截，未点名动作的区域性限制仅在可确定映射到目标肌群时拦截，无法可靠映射的健康事实不会被臆测为全部动作禁令。

仍缺失：训练量突增（无历史容量基线）、连续高强度日、目标冲突。

阶段相关回归测试 49 项通过，并已验证重复动作、显式伤病动作限制、区域性伤病限制和无法映射的健康事实边界；“本次避免深蹲”不会触发禁止动作违规。

### AGENT-09 意图路由 schema 与消费端脱节，两套机制并存

> ✅ **已修复 2026-08-23** —— `create_training_plan` schema 已声明 `temporary_health_constraints`、`excluded_subjects` 与 `needs_clarification`，并保留严格的 `additionalProperties: false`。路由器会验证数组字段的类型和长度；`main.py` 将结构化路由结果传给 `resolve_plan_context()`。有路由结果时，`schedule_decision` 是唯一的排期裁决来源：`rest` 直接结束本次训练安排，`override` 使用路由给出的替代科目，临时健康限制进入当次安全约束，排除科目进入模型上下文并可压制周计划。没有路由结果时仍保留原确定性正则作为离线/失败兼容回退。
>
> 回归覆盖 schema 与解析、休息决策、替代科目、临时限制、排除科目，以及“结构化结果不再被旧正则二次覆盖”；相关测试 66 项通过。

初始问题：`chat_intent_router.py` 的 `create_training_plan` 参数没有 temporary_health_constraints / excluded_subjects / needs_clarification，却设了 `additionalProperties: False`；解析代码仍读取这些字段，导致生产中恒为空。`schedule_decision` 虽在 schema 中，`main.py` 当时没有消费它，排期决策由正则重新猜一遍：

- "今天不太想练背了" → 正则命中 override 但无 replacement → `clarification_required=True` → 409 反问，而 FC 侧其实已能判为 rest。
- "这周三我想把练背挪到练腿" → 不含"今天/本次"，正则完全不命中，周计划硬约束照旧生效。

### AGENT-10 其他

> ✅ **周计划结构已扩展 2026-08-23** —— `plan.weekly_schedule` 同时兼容旧格式 `{"mon":"胸部训练"}` 和结构化格式：顶层支持 `enabled`、`effective_from`、`effective_until`，`days` 中可使用 `training`、`aerobic`、`rest` 三种日类型；`null` 也规范化为休息日。运行时只对启用且处于生效区间的训练/有氧日施加科目约束，休息日会进入本次休息流程，用户明确改练仍可覆盖。信息提取提示词与数据管理编辑占位示例已同步更新。相关测试 84 项通过。

初始问题：`info_store.py` 会拒绝 `{"wed":null}`（休息日）和带 `effective_from` 的对象，导致周计划无法表达休息日、有氧日、生效起止日期或启用状态。
> ✅ **负面动作偏好极性已修复 2026-08-23** —— `training.prefer_exercise` 现在只接受正向动作偏好；含“不喜欢/不想/不要/不做/不练/讨厌/避免”等否定表达的值会在信息路由和 InfoStore 规范化两层被拒绝，历史数据解析时也不会重新注入。信息提取提示词要求将这类用户表达写为 `training.avoid_exercise`，并使用纯动作名作为 value。相关测试覆盖错误模型输出与绕过路由的直接写入。

初始问题：`{"key":"prefer_exercise","value":"我不喜欢深蹲"}` 会通过校验并以 `polarity="prefer"` 生效，反向影响训练计划。
> ✅ **体态/肌肉状态同义词已归一 2026-08-23** —— InfoStore 新增确定性 canonical 词典，覆盖骨盆前倾/后倾、下交叉综合征、臀肌无力、髂腰肌紧张、腹部核心薄弱、腘绳肌紧张、竖脊肌紧张和体态矫正的中文与英文别名。健康限制、恢复状态和计划偏好写入、编辑、旧记录读取、确认事实合并均使用同一归一函数；唯一命中的同义词会写成标准名，出现多个不同术语的复合表述保留原文，避免静默丢失条件。信息提取提示词要求多种状态拆成独立事实。相关测试覆盖词典全部别名、历史去重和复合表述边界。

初始问题："骨盆前倾/盆骨前倾/骨盆前倾问题"等同义词会作为独立值并存，仅靠 `casefold()` 精确去重。
> ✅ **长期记忆即时提取已修复 2026-08-23** —— `/chat` 会对包含“以后/长期/每周/不喜欢/我有”等明确持久性线索的用户消息调用既有信息路由，并立即落为未确认事实候选；不依赖退出、页面刷新或前端历史长度。候选复用现有确认、拒绝、版本冲突和数据管理流程，确认前不会影响对话上下文或计划。相同活跃事实会去重，短期恢复状态不走这条长期记忆路径；退出仍保留为兼容性补扫。前端在生成候选后给出简短回执。另对“骨折/腿断了/肢体折了”等急性伤病信号走本地确定性路径：不等待外部模型，立即生成一条短期、未确认的恢复状态候选。相关测试覆盖即时创建、去重、普通消息不触发、短期事实过滤和急性伤病离线落盘。

初始问题：长期记忆只在用户点击“退出”时提取，前端会话历史存在长度上限；刷新、关闭标签页或超出上限会丢失周计划和长期偏好候选。
> ✅ **餐食估算已支持完整营养校正 2026-08-23** —— 视觉模型现在返回每个食物项目的份量、热量、蛋白质、碳水和脂肪；每日记录表单展示可编辑的多餐次明细，并把四项营养合计带入表单。每餐带 `food_photo_estimate` 来源、可信度、热量范围与假设，低可信度餐次须由用户明确确认后才能保存；编辑或移除餐次会同步校正表单合计。照片说明只追加到既有备注而不覆盖，同日多张图片以独立餐次保存；周汇总会将图片估算与手动营养记录分别列出。相关测试覆盖宏量营养素归一、结构化入库、低可信度确认、来源汇总与前端流程约束。
> ✅ **右侧训练/营养数据编辑器已统一 2026-08-23** —— 训练模式选择历史训练后直接载入原有动作组列表，可逐组修改动作、重量和次数并保存，不再额外展示文本摘要；营养模式按日期和餐次展示结构化编辑器，手动合计可修改热量与三大营养素，照片餐次可逐项修改食物、份量和宏量营养并保存。营养保存使用记录版本防止并发覆盖，照片餐次校正会同步修正当天营养合计。上传图片只存在于单次分析请求，分析结束即释放文件引用，持久层仅保存用户确认后的结构化营养数据，不保存原图。
- **外部模型开关**：6 个调用点中 5 个已正确阻断（主模型/意图路由/退出记忆/计划鉴定/视觉模型），**YouTube 仅因随主模型一起不执行而间接阻断**——`youtube_search.search_videos` 自身不读设置，一旦有非 /chat 路径注册就会绕过。建议在入口再判一次。

---

## 6. P2 —— 架构与可维护性

### ARCH-01 main.py 2345 行，建议按下表拆分

| 行号区间 | 内容 | 建议模块 |
|---|---|---|
| 120-292 | 训练科目推断、计划完整性与记忆约束校验 | 并入计划安全校验模块或新建 `plan_text.py` |
| 295-448 | BMR 计算、档案字段校验、onboarding 文案 | `profile_service.py` |
| 451-724 | 记忆投影、周计划解析、PlanContext、Agent 输入拼装 | `agent_context.py` |
| 771-1032 | `/chat` 编排 | `chat_service.py` + `routers/chat.py` |
| 1189-1294 | FIT 结果的表格/摘要渲染（纯函数，可单测） | `fit_report.py` |
| 1128-1474 | 各类上传端点 | `routers/uploads.py` |
| 1542-1803 | 训练记录读取、校验、合并、PATCH | `training_record_service.py` |
| 1806-2033 | 数据总览、备份、增删 | `routers/data.py` |

**优先拆 `/chat`**：单函数 262 行，混合了请求解析、意图路由、5 条本地短路、Agent 调用、artifact 构造、计划校验和 3 层异常处理——**BUG-01、BUG-13、"信息已完善"恒假这三个 bug 都直接源于这个函数太长导致的状态跟踪失败**。

### ARCH-02 `__init__.py` 急切导入 LLM 栈，污染整个包

> - 状态：✅ 已修复（2026-08-23）
> - 修复：`fithealth_agent.__init__` 不再在包初始化时导入 `agent.py`，而是保留同名公开包装函数，仅在真正调用 `create_fithealth_agent()` 时加载 Agent 与 `hello_agents` 依赖栈。`from fithealth_agent import create_fithealth_agent` 的现有 API 和参数保持不变。
> - 回归：新增独立子进程测试，在主动禁止任何 `hello_agents` 导入的环境中验证包入口、`storage` 和 `context_budget` 均可正常导入，且 `fithealth_agent.agent` 不会进入 `sys.modules`；另验证公开工厂只在调用时委托真实实现。
> - 清理：已将 backup、context budget、external model settings、food analysis、plan classifier、daily check-in、data deletion、information router、info store、plan store 等测试改回正常包导入，删除 11 处为绕过急切导入而存在的 `spec_from_file_location` 样板。刻意操纵 `sys.modules` 来测试恢复、隔离或桩模块的用例继续保留。

`fithealth_agent/__init__.py:3` 导入 `create_fithealth_agent` → `agent.py:24` 导入 hello_agents。于是**任何** `from fithealth_agent.xxx import` 都会触发整条 LLM 依赖链。这就是为什么 30 个测试文件里塞满了 `importlib.util.spec_from_file_location` 样板代码来绕开——问题是已知的，但用逐文件 workaround 掩盖了，而模块内部的 `from fithealth_agent.json_file_lock import ...` 又会重新触发。

**修法**：改成惰性导入（模块级 `__getattr__` 或函数内导入），顺带能删掉大量测试样板。

### ARCH-03 数据目录是 CWD 相对路径
> - 状态：✅ 已修复（2026-08-21） 
> - 根因：各 store 硬编码 `Path("data")`，相对当前工作目录，容器内不可配置，
  从非项目根启动还会在别处凭空建 data/。 
> - 修复：统一经 `fithealth_agent/settings.py` 的 `data_dir()` / `data_path()` 解析，
  每次调用读取 `FITHEALTH_DATA_DIR`，留空回退到相对 `data/`。
  `scripts/backfill_quarantined_hr.py` 亦已跟随。 
> - 验证：设置 `FITHEALTH_DATA_DIR` 后，七个 store 全部落在配置目录下；
  新增回归测试 `tests/test_data_dir_and_docker.py`；全套 385 passed / 1 skipped / 163 subtests。
### ARCH-04 重复实现与不一致

> - 状态：✅ 已修复（2026-08-21）

- BMR 公式在 `main.py:295-312` 和 `400-407` 逐字重复两遍。
- 记忆标签字典在 `main.py:456-468` 和 `543-555` 各一份，且 `("plan","preference")` 的标签一个是"计划偏好"一个是"训练偏好"。
- PlanContext 在一次请求里被重复计算三次（main.py:949、712、969），每次重跑周计划 JSON 解析。
- `json_file_lock.py:14-16` 用 `open("a+b")`，每次加锁追加 1 字节永不回收——实测 `daily_records.json.lock` 已 102 字节（=102 次加锁）。且 `self.handle` 在加锁前赋值，加锁失败时 `with` 不会调 `__exit__` → **句柄泄漏**，Windows 上 `LK_LOCK` 还会先卡约 10 秒。

### ARCH-05 运行环境未声明

> - 状态：✅ 已修复（2026-08-23）
> - 运行时契约：新增 `pyproject.toml`，以 PEP 621 `requires-python = ">=3.11,<3.13"` 声明支持范围；新增 `.python-version` 固定默认开发解释器为 3.12。包入口在 Python 3.10 及以下主动给出明确错误，不再泄露深层 `StrEnum` ImportError。
> - 可复现安装：新增 `requirements.lock`，固定当前 Python 3.12 已验证的直接与传递依赖版本；`requirements.txt` 保留直接依赖声明，README 说明两者职责与更新流程。
> - 部署与文档：Dockerfile 与 Compose 默认统一为 `python:3.12-bookworm`，README 新增本地虚拟环境、锁文件安装和启动命令。新增 `tests/test_runtime_environment.py` 覆盖版本范围、锁文件覆盖、Python 3.10 报错、Docker/文档一致性。

`plan_workflow.py:4` 用了 `StrEnum`（**需 Python 3.11+**），但 requirements.txt 无 `python_requires`、无锁文件，README 也未说明。在 3.10 上直接 ImportError。

### ARCH-06 测试缺口

当前 118 通过，但覆盖分布很不均：

- **`fit_parser.py` 实质零覆盖**——本清单 4 个 P0 数据问题（DATA-01/03/04 + BUG-18 的 lap 问题）**全部落在无测试区**。仓库里也没有任何 `.fit` 夹具。好消息是这些都是纯函数，直接构造 `raw_messages` dict 就能测（我本次验证正是这么做的）。
- **`health_importer._parse_fit_source` 只测了坏文件路径**——心率区间过滤、stress/spo2/hrv 取值与量纲（含 BUG-15）、睡眠阶段切分、local_date 时区归属**一行都没被执行过**。
- **`/upload_fit`、`/upload_health`、`/workout_state/update` 三个主链路端点无端到端测试**。
- 建议优先补的 4 个：速度单位契约、`_decode_exercise_name` 的 rest 兜底、`_pick_parser` 路由表、`StrengthTrainingParser` 无 set 时的降级。

---

## 7. 建议新增的功能与工具

当前 Agent 注册了 9 个工具（agent.py:67-81）：save_daily_record、query_daily_records、query_daily_health、query_health_range、query_sleep、query_heart_rate_window、update_set、merge_sets、search_youtube_video。

缺口集中在**"历史与趋势"**——训练侧只有 `query_daily_records` 一把全量钥匙，且 `storage.py:88-95` 返回整条 record（含全部 segments）、`tools.py:61-62` 的 limit 无上限，极易撑爆 ReAct 观察。而 `health_store.py:886` 的 `get_metric_trend` 已实现却只给了 HTTP 端点，**Agent 拿不到**。

| 工具名 | 作用 | 价值 |
|---|---|---|
| `query_personal_record` | 某动作历史最大重量/最多次数/估算 1RM 及达成日期 | **PR 追踪是健身应用的核心期待，全仓目前零实现** |
| `query_exercise_history` | 单个动作的历史序列（日期、最大重量、总容量、RPE） | 回答"卧推该加多少重量"的必需数据 |
| `query_training_volume` | 按周/肌群聚合组数与容量，返回环比变化 | 训练量突增校验与周复盘的基础 |
| `query_muscle_recovery` | 各肌群距上次训练的天数 | AGENT-08 的肌群恢复间隔校验依赖它 |
| `query_health_trend` | 包装已有 `get_metric_trend`，返回日/周/月均值序列 | 避免用 31 天原始摘要撑爆上下文 |
| `compare_health_periods` | 对比两段时间的睡眠/HRV/静息心率均值与差值 | "这周比上周恢复得如何"是高频问法，现在只能让模型自己算 |
| `query_training_records` | 按日期区间/动作/运动类型查，只回摘要不回 segments | 替代现在的"全量捞"，省上下文 |
| `forget_memory` | 把某条已确认记忆标记失效（"膝盖已经好了"） | 现在只能去数据管理手删；过期的安全限制会持续误触发 |
| `delete_set` / `split_set` | 删除误识别组、拆分被误合并的组 | delete_set 已实现未注册；修 DATA-03 的人工出口 |
| `undo_last_edit` | 回退最近一次 update/merge | 消除 AGENT-07 的不可逆风险 |
| `propose_daily_record` | 生成待确认草稿而非直写 | 修 AGENT-02 |
| `check_plan_safety` | 输出计划前自查恢复间隔/重复动作/疼痛限制 | 把校验前移，减少反复 409 |
| `export_report` | 生成周/月 Markdown 报告 | weekly_summary 只在命中关键词时返回文本，模型无法主动生成 |

---

## 8. 建议实施顺序

**第一批（本周，止血）**

1. **AGENT-01** 高风险症状硬拦截 —— 唯一涉及人身安全的条目。
2. **DATA-01** 速度 3.6 倍 —— 单点修改，立刻停止污染新数据；顺手写迁移脚本修正历史记录。
3. **DATA-02** raw_path 相对化 + 归属校验 + 迁移脚本 —— 已实测 16/16 失效。
4. **DATA-05** 回灌那 1802 条心率，并把隔离改成 copy。
5. **DATA-08** InfoStore 静默清空防护 —— 一次意外就丢光全部记忆。

**第二批（正确性）**

6. **BUG-01 + safety_bypass 正则**（必须一起改）、**BUG-02**、**BUG-03**、**BUG-04** —— 四处死代码一次性接线，让已写好的能力真正生效。
7. **DATA-03 / DATA-04** 解析降级 —— 配套补 fit_parser 的单测（ARCH-06）。
8. **BUG-13** 纯摘要记忆注入 —— 让你自己的两条测试转绿。
9. **BUG-05 / BUG-06 / BUG-12** —— 计划保存与记忆确认的正确性。

**第三批（数据安全底座）**

10. ~~**DATA-06**(fsync) / **DATA-07**(profile 锁) / **DATA-09**(journal) / **ARCH-04**(锁泄漏)~~ ✅ 2026-08-21 已完成。
11. ~~**DATA-11 / DATA-12** 备份完整性与恢复隔离~~ ✅ 2026-08-21 已完成，~~**DATA-13**（合并聚合口径） / **DATA-14**（`/data/reset` 恢复点 + 逐项容错）~~ ✅ 2026-08-21 已完成。
12. **DATA-10** Docker volume + **ARCH-03** 数据目录配置化（一起做）。

**第四批（能力与架构）**

13. **AGENT-02 / AGENT-03 / AGENT-05** 写入权限与记忆注入。
14. 新增 `query_personal_record` / `query_training_volume` / `query_muscle_recovery` / `forget_memory` 四个工具 —— 收益最高的一批。
15. **ARCH-01** 拆 `/chat`，**ARCH-02** 惰性导入。
16. **AGENT-08 后续**：训练量突增、连续高强度日、目标冲突与结构化计划 artifact（肌群恢复阶段 1–4 及重复动作、伤病限制校验已于 2026-08-23 完成）。

---

## 附：确认无问题的部分（避免后续被误改）

审查中验证过、结论是**正确**的实现：

- `total_distance` / `total_distance_m` 是米，`main.py:1208` 的 `/1000` 正确（`StandardUnitsDataProcessor` 只转换字段名恰为 `distance` 的字段）。
- `hrv_value` 与 `hrv_status_summary` 的 `/128.0` 缩放正确（fitfile 用 FloatField 且不自动缩放）。
- `respiration_rate` 无需额外缩放（`_scale=100.0` 由 fitfile 内部应用）。
- `monitoring.distance` 存成 `distance_m` 正确。
- 心率流与动作组的时间对齐**基准一致**：`record.timestamp` 与 `set.start_time` 在 fitparse 下都是 naive UTC，`_to_utc()` 统一贴 UTC；持久化往返保持 aware。Garmin 1989 基准由 fitparse 内部处理。
- 睡眠 CSV 的阶段合计校验正确排除了 `awake_min`，与 Garmin"睡眠时长=深+浅+REM"口径一致。
- 训练记录的重复导入防护是可靠双保险（`find_training_by_start` + `idempotency_key=fit:sha256`）。
- 健康数据跨 ZIP 重复样本在**日汇总**和**心率窗口查询**两处都做了去重，设计正确——只有按天趋势漏了（BUG-16）。
- `merge_sets` 的窗口保护会拒绝吞掉未选中的活动段，这个设计是对的。
- 档案写入已改为确认制（Function Calling + artifact + `/profile/confirm-update`），不再静默改档案 —— 这一改到位了。
