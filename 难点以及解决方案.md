# FitHealthAgent：真实踩坑复盘、修复结果与规避清单

> 这不是功能列表，而是 FitHealthAgent 从能跑的 MVP 逐步变成可交付产品时，真正遇到过的问题复盘。
>
> 统计口径：主线在 2026-08-29 完成 FastAPI 拆分阶段验收；当前文档同时记录后续对 Agent 可观测性的复盘。文中所说的“已修复”，均有代码或回归测试支撑；尚未完成的内容会明确标注。

## 先说最后交付成了什么

FitHealthAgent 最终形成了一个本地优先的单用户健身健康管理智能体，能够处理：

- AI 对话、训练记录、训练计划、营养记录和肌群恢复；
- Garmin 活动 FIT、全天健康 ZIP/CSV 以及睡眠数据导入；
- 训练组修改、合并、撤销和原始解析结果恢复；
- 记忆确认、隐私开关、本地备份、恢复点和全量重置。

它的核心分工是：**LLM 负责理解、规划和生成，Python 负责事实计算、数据读写、权限和安全边界，前端确认负责不可逆操作。**

主线阶段验收结果如下：

- `main.py` 从约 5402 行收敛到 132 行；
- 73 条 HTTP 路由、69 个 OpenAPI operation 及注册顺序保持不变；
- 记录的阶段基线为 893 条测试通过，848 条原始 baseline outcome 未发生变化；
- 关键数据路径均有并发、损坏、恢复、边界输入和随机执行顺序测试。

这些结果不是一开始就设计出来的，基本都是被真实数据、真实交互或失败测试“逼”出来的。

---

## 1. 把自然语言意图交给正则，第一次就改坏了用户档案

### 当时发生了什么

早期为了省一次模型调用，我用正则表达式区分“修改档案”和“查询档案”。用户输入“我的可用器械有哪些”，本意是查看器械，系统却匹配到了“我的可用器械是……”的修改句式，把器械列表写成了“哪些”。

另一个相关问题是：用户说“帮我看看训练记录，上次卧推推了多少公斤”，系统命中“查看训练记录”后直接短路，只打开了记录面板，后半句的重量问题完全没有回答。

### 根因

这不是某一个正则表达式写得不够细，而是把**有破坏性的语义判断建立在表层字符上**。中文疑问句、陈述句和复合请求经常共享相同关键词，正则既看不懂语境，也不知道用户是否还提出了第二个问题。

### 怎么修

1. 修改档案改为受限 Function Calling，只允许 `update_profile` 输出明确字段。
2. 增加只读的 `view_profile` 工具，给查询请求一个明确出口。
3. 服务端再次检查问号、“是不是”“哪些”“有没有”等疑问信号；即使模型错误调用更新工具，也不生成可确认的修改。
4. 意图路由失败、超时、返回多个工具调用时，统一降级为空动作，绝不根据猜测写本地数据。
5. 查看记录只有在“纯导航”时才短路。复合请求会先挂起查看面板，再继续交给 Agent 回答其余问题；外部模型关闭时也明确告诉用户哪一部分无法回答。

### 修复结果

- 查询不会再改写档案；器械新增、移除和替换都有可见的 diff 确认卡片。
- 复合请求不再丢掉后半段问题。
- 相关疑问句拦截、只读路由和复合请求回归测试全部通过。

### 以后怎么规避

- 正则只做低风险、只读的兜底，不让它直接触发不可逆写入。
- “查询”和“修改”必须是两个不同的工具和两个不同的服务端分支。
- 所有模型判断都要有失败即无动作的默认行为，并用真实口语、否定句和复合句做对抗测试。

---

## 2. 记忆不是摘要：晚保存、乱保存和错误寻址都踩过

### 2.1 只在退出时提取记忆，用户一刷新就丢了

最初的设计是用户点击退出时，才让模型判断本次对话是否值得保存。实际使用中，刷新页面、关闭标签页或历史消息超过长度限制，都会让长期偏好和周计划候选消失。

后来又发现，模型做摘要时可能把自己的建议写成用户事实。例如用户只是问“要不要减少深蹲”，模型却可能总结成“用户不做深蹲”。一旦这类内容进入长期上下文，后面的训练计划都会被错误约束。

### 2.2 用列表下标操作事实，过期清理后会点错对象

记忆管理页面原来传 `fact_index`。每次请求都会清理过期或拒绝的事实，列表一旦发生位移，用户手里旧页面的“第 2 条”就可能实际操作了另一条事实。

### 修复方式

- 对带有“以后、长期、每周、不喜欢、我有”等明确持久性线索的消息，立即生成**未确认候选**，退出流程只保留兼容性补扫。
- 信息路由采用本地敏感信息拦截 + 受限 Function Calling。模型只能从固定的 namespace/key 枚举中选择事实。
- 每条事实必须带 `evidence`，且证据要能在**用户原话**中去标点匹配；无法核验的事实逐条丢弃，全部无效则整次不保存。
- 事实先进入待确认状态，只有用户确认的事实才会影响后续上下文；临时酸痛、疲劳和恢复状态使用短 TTL，不冒充长期限制。
- 给每条事实分配稳定的 `fact_id`，管理接口按 id 操作；编辑值改变时生成新的 id，避免旧页面误伤邻居。
- 记忆按长期约束、中期偏好、短期恢复和当前会话决策分层，读取时按当前意图选择相关内容。

### 结果

- 用户不需要等到退出才能看到记忆候选；刷新或关闭页面不会静默丢掉明确表达的长期事实。
- 未确认、已过期或证据不成立的内容不会进入 Agent 的强约束上下文。
- 单条事实确认、拒绝、编辑和回滚均能稳定命中原对象。

### 以后怎么规避

- 不保存整段聊天作为“记忆”，只保存结构化事实、来源证据、状态和有效期。
- 任何模型抽取结果都要做来源核验，不能把模型复述当作用户事实。
- 前端列表操作永远使用稳定 id，不使用会因排序、清理而变化的下标。

---

## 3. 训练计划生成得很像样，不代表它是安全的

### 当时发生了什么

早期只要模型生成了看起来完整的 Markdown，就可以进入保存流程。实际出现过几类问题：

- 计划安排了用户明确不想做的动作；
- 忽略了当前酸痛或已确认的健康限制；
- 用户要求“有氧 + 核心”，计划只覆盖了其中一个目标；
- 同一个动作重复出现；
- 用户明确说“今天练腿”，但系统又用旧的周计划把它改回原科目。

还有一个隐蔽问题：优先级阶梯曾经写在死代码里，没有真正进入 Agent 输入；路由 schema 也没有声明临时限制、排除科目和澄清状态，导致解析代码虽然读取这些字段，生产中却永远拿不到。

### 修复方式

训练计划现在走明确的状态和校验链：

```text
当前消息与档案
  → PlanContext 裁决优先级
  → 健康风险/恢复状态闸门
  → Agent 生成候选计划
  → 后端确定性校验
  → 必要时自动修正
  → 再次校验
  → 用户确认后保存
```

优先级由后端裁决，顺序是：

1. 紧急健康风险和伤病限制；
2. 用户当前明确的改练、休息、跳过或替代指令；
3. 肌群恢复状态和当前酸痛；
4. 已确认的周计划；
5. 健身目标和长期偏好；
6. 普通历史对话。

健康风险筛查放在意图路由和 Agent 调用之前。生成后的校验器会检查禁做动作、恢复中的肌群、动作重复、组数/RPE 上限、必需元素、训练目标覆盖和排除科目。首次不通过时，系统把具体违规项交给修正 Agent；修正后仍不通过，就不生成可保存 artifact。

### 结果

- “今天有点累”不会无故覆盖周计划，只有明确的改练/休息指令才会覆盖。
- 急性疼痛或高风险症状下，不会生成可保存的训练计划。
- 计划即使通过了模型生成，也必须通过后端校验才能进入保存卡片。
- 计划路由、状态机、恢复冲突和自动修正均有回归测试。

### 以后怎么规避

- 把模型输出当候选结果，不当最终事实。
- 安全约束和业务不变量必须在后端再检查一次，不能只写在 Prompt 中。
- 给每个状态定义明确的输入、输出和终止条件，避免用一堆布尔值隐式表示流程。
- 测试不能只测“正常计划”，还要测遗漏目标、重复动作、禁做动作、否定表达和约束冲突。

---

## 4. Garmin 的 `sport=training` 不是“力量训练”，缺字段也不等于解析失败

### 当时发生了什么

Garmin 不同设备导出的 FIT 结构差异很大。早期看到 `sport=training` 就统一走力量训练解析器，结果有氧、瑜伽和间歇训练的 `lap` 消失了。

更严重的是，某些 FIT 没有 `session` 消息。旧逻辑把它们路由到兜底的 lap 解析；如果文件里只有 `set` 或 `record`，最终得到 0 个分段，但界面仍然显示“FIT 解析完成”。这属于最危险的一类错误：**没有报错，却把用户的数据静默丢掉了。**

### 修复方式

- 用策略模式拆分 `StrengthTrainingParser`、`LapBasedParser`、`IntervalSportParser` 和 `FallbackParser`。
- 只有在 `sport` 是大类或缺失时，才让 `sub_sport`参与裁决；避免用无关的 `sub_sport` 抢走明确的骑行/跑步类型。
- 兜底解析先看文件中是否存在 `set` 消息：有就是力量训练证据，没有才尝试 lap。
- 力量解析为空时降级到 lap；既无 session 也无 lap 时，使用心率 record 的首尾时间戳构造近似窗口，并明确标记来源。
- 真的一个分段都没有时返回 `status=empty`，不再伪装成成功，也不允许用户对空壳点击确认保存。
- 对 `HealthImportError` 单独处理，保留具体错误原因；未预期异常才进入通用兜底。

### 结果

同一份真实有氧 FIT 从 `segments=0` 修复为 `segments=1`，得到平均心率 94、峰值心率 147、232 kcal、64 分钟；三份真实力量训练 FIT 的解析结果保持不变。

### 以后怎么规避

- 解析器不能只依赖一个元数据字段，要同时看消息证据和字段完整性。
- “文件读成功”和“业务解析成功”必须是两个状态。
- 每接入一种设备或运动类型，都要保留真实原文件作为 fixture，并覆盖缺 session、缺 lap、缺 set 和字段缺失情况。
- 任何 `0 条结果` 都要有明确业务含义，不能默认当成成功。

---

## 5. 把 1 Hz 心率流放进主记录，既撑爆上下文又算错日均值

### 当时发生了什么

一条训练可能包含数千个 1 Hz 心率采样点。最初考虑把它们直接放进训练记录：这样 Agent 查询记录时会收到巨大的观察结果；如果再把这些点写进全天健康数据库，训练时段会相对于分钟级全天监测被重复加权，日均心率会失真。

编辑合并时还有一个精度问题：合并 A + 休息 + B 后，平均/最大心率不能简单取 A 或 B 的值，必须按新的时间窗口从原始采样重新计算。

### 修复方式

- 训练记录只保存固定大小的心率摘要（采样数、最小/最大/均值、覆盖窗口）。
- 原始 1 Hz 流按 `record_id` 旁挂到 `data/hr_streams/`，不进入 `daily_records.json`，也不写入全天 `health.db` 的分钟级表。
- 合并动作时先应用编辑草稿，再按合并后的 `min(start)` 到 `max(end)` 确定窗口，并吸收中间休息段；有旁挂流就重新切片计算 avg/max。
- 历史记录没有原始流时，按各段持续时间加权平均，峰值取最大；两类数据都没有时才返回 `None`，绝不编造数值。

### 结果

- 避免训练小时的 1 Hz 数据把全天分钟数据加权约 30 倍。
- 同一记录无原始流时，测试得到加权平均/峰值 `123/138`；挂载原始流后，按新窗口精确重算为 `144/170`。
- Agent 看到的是可控摘要，原始流仍然可以在编辑和审计时恢复。

### 以后怎么规避

- 原始数据、派生摘要和面向模型的展示数据分层存储。
- 任何进入 Prompt 的数组都要有条数和字节预算。
- 涉及时间窗口、均值、峰值和累计量的计算全部交给确定性代码，并用算术反例测试加权口径。

---

## 6. 健康数据最容易错的不是 SQL，而是语义：时区、去重、单位和“不要猜”

### 6.1 差 8 小时的时区判断

压力数据的时间字段是 FIT `date_time`，本质上按 UTC 解释；同一批文件里的某些 `local_timestamp` 却是真正的本地墙钟时间。它们字段名字相似，不能套同一套转换。按“看起来像本地时间”的直觉修改，反而会把数据整体提前 8 小时。

修复时用三份真实 wellness ZIP 与同文件 monitoring 心率逐分钟对齐，确认 naive 时间按 UTC 解释后偏差为 0；同时在 `_utc_datetime` 的契约和测试中固定“04:00 → 北京 12:00”，防止以后凭感觉改回去。

### 6.2 趋势图和日汇总口径不一致

Garmin 相邻导出包可能包含重叠时间段。写入侧必须保留 `source_file_id`，否则删除某个导入会误删另一个导入的数据；去重因此放在读取侧，但日汇总和趋势必须共用同一份 SQL。

修复后，真实库中趋势与日汇总的 68 组对比全部一致；2026-08-15 的原始心率行从 2424 条去重为 1212 条，按趋势 samples 加权还原的日均值与汇总差值不超过 0.15。

### 6.3 睡眠、强度分钟和设备私有字段

- FIT 睡眠时长曾把清醒阶段也算进去，真实数据出现 `537` 分钟而正确值应为 `381` 分钟；现在将睡眠时长定义为深睡 + 浅睡 + REM，卧床时间单独保存。
- `moderate_activity_time`/`vigorous_activity_time` 是区间增量，必须 `SUM`，不能取 `MAX`。
- 静息代谢率和静息心率来自不同消息，按字段分别取当天最后一条非空值，不能整行取“最新”。
- 身体电量等私有字段无法可靠解释时刻意不入库，只归档观察结果。存错一个看似精确的数字，比明确缺失更危险。

### 以后怎么规避

- 在字段旁写清楚单位、时区、累计/增量性质和来源消息。
- 用真实导出文件做逐字段核对，不要只用自己构造的理想数据。
- 对无法证实的私有字段保留“未知”，禁止为了填满界面而猜值。
- 同一指标的写入、汇总和趋势读取共用定义或 SQL，避免“注释说一致、代码不一致”。

---

## 7. 本地 JSON 没有事务，最初的写法经不起并发和断电

### 当时发生了什么

项目早期直接用 `Path.write_text()` 写 JSON，临时文件名固定为 `user_profile.json.tmp`。两个请求同时更新档案时，会互相覆盖临时文件；进程在替换文件前断电时，还可能得到 0 字节或半截 JSON。

另一个坑是读取失败时返回空列表。这样一来，损坏的记忆库会被误认为“没有记忆”，下一次写入就可能用空库覆盖原有数据。档案更新也曾采用“先读、再合并、再写”的三步流程，两个并发确认会互相丢掉新增器械。

### 修复方式

- 统一通过 `atomic_write_json()` 写入：临时文件使用 pid + uuid，写完 `flush`、`fsync` 后再 `os.replace`；父目录 fsync 在 Windows 不支持时安全跳过。
- JSON 公开读写方法使用进程内 `RLock` + 跨进程 `JsonFileLock`；私有方法不重复加不可重入的文件锁。
- 合并逻辑放回 Store 锁内，使用锁内刚读到的版本；每日打卡同样在锁内按日期 upsert，而不是端点先查后写。
- 记忆库或健康数据库损坏时进入显式只读降级，返回 503 和恢复指引，不把失败伪装成空库。
- 健康数据库的 journal 只允许 WAL/DELETE，绝不静默退到 MEMORY/OFF；判断失败时读取 PRAGMA 的实际返回值，而不只捕获异常。

### 结果

- 还原旧实现做并发对照，15 轮累计丢失过 165 条追加；修复后 12 个线程跨 3 个实例追加的数据全部保留。
- 同日 8 个并发打卡请求最终只有一条记录，字段按语义合并而不是互相覆盖。
- 写入中途失败时，原文件逐字节不变，也不留下半截 `.tmp` 文件。

### 以后怎么规避

- 持久化写入只保留一个实现，不允许各 Store 自己拼临时文件逻辑。
- 对每个写路径测试并发、写到一半失败、进程重启和损坏文件，而不是只测“函数返回成功”。
- 任何“读失败返回空集合”的代码都要重点审查；涉及用户数据时默认 fail-closed。
- 版本合并、去重和 upsert 必须在持锁的存储层完成，不能拆到 HTTP 端点里。

---

## 8. “删除全部数据”不是普通 DELETE：先留退路，再执行破坏性操作

### 当时发生了什么

早期的 `/data/reset` 会依次清空多个 Store。只要中间一步抛异常，用户得到一个笼统的 503，却不知道前面哪些数据已经删掉；更糟的是，删除前没有完整恢复点，失败后没有可靠退路。

恢复过程也遇到过并发覆盖：恢复线程不持锁时，另一个请求拿着旧列表写回，两个请求看起来都成功，最终恢复结果被静默覆盖。恢复点用秒级时间戳命名时，短时间重试还可能覆盖刚生成的唯一真快照。

### 修复方式

- 删除前先生成经过 `fsync` 的完整恢复点；快照写不成功就一个字节都不删。
- 进入维护模式：拒绝新请求、等待在飞请求排空，诊断接口和发起维护的请求本身除外。
- 固定锁顺序：维护 gate → JSON 文件锁 → SQLite 访问锁；恢复和导出使用同一顺序。
- 恢复点名称严格递增，并在 prune 时显式保护刚写的那一份；最多保留 3 份。
- 删除操作逐项执行并返回 `steps`、`removed`、`error` 和 `partial`，不因为一项失败就隐藏其余结果。
- 清理进程内 `plan_draft_cache`，防止重置后“保存刚才的计划”把已经删除的正文重新写回磁盘。
- 恢复点本身就是普通备份 ZIP，复用已有校验和导入恢复流程；不做未经用户要求的自动回滚。

### 结果

- 恢复点可被现有备份校验和恢复接口完整读取。
- 同一秒连续创建的恢复点名称互不相同且单调，刚生成的快照不会被清理策略误删。
- 部分失败时用户能看到每一步的真实结果，前端也会刷新到删除后的状态，而不是停留在旧页面。
- 相关阶段记录为 489 条测试通过、0 条失败；后续主线总基线继续增长到 893 条。

### 以后怎么规避

- 所有不可逆操作都遵循“快照成功 → 加独占锁 → 执行 → 返回明细”的顺序。
- 不要把一个跨多个资源的操作伪装成原子事务；要么提供恢复点，要么明确返回部分完成状态。
- 锁顺序、快照命名和清理范围写成契约测试，不能只靠注释。

---

## 9. 上传文件就是不可信输入：ZIP、异常和多文件选择都出过问题

### 当时发生了什么

Garmin 健康 ZIP 来自用户设备，不能假定目录结构和文件名可信。早期只校验扩展名，存在路径穿越、超大解压和 zip bomb 风险。

多 ZIP 导入还曾经只保留一个选择结果：用户丢弃其中一个活动时，同批其他活动也被一起放弃。解析某个活动失败时，通用 `except Exception` 又把具体原因吞掉，用户会去重新上传一个其实没损坏的文件。

### 修复方式

- 限制上传数量、单文件大小、压缩包成员数、单成员大小、解压总量和压缩比。
- 用 `PurePosixPath` 检查成员路径，拒绝绝对路径、`..`、反斜杠和多层越界路径。
- 收集和裁决分两步：只有全局恰好一个活动才自动载入；多个活动返回带 ZIP 来源的选择器数据。
- 每条活动用 `zip + activity_name` 作为前端关联键，避免同名 FIT 互相覆盖。
- 单个活动解析失败记入 `errors`，整体降级为 `partial`，不影响同批其他文件。
- 业务异常（如 `HealthImportError`）放在通用异常之前单独处理，保留具体 status code 和原因。

### 结果

健康批量导入最多支持 5 个文件；多活动可以逐个编辑和保存；单个活动失败时其余结果仍然可用，前端能明确显示失败文件和原因。

### 以后怎么规避

- 所有上传内容按敌意输入处理，扩展名只能作为第一层提示，不能作为安全判断。
- 先做资源上限和路径安全检查，再进入解压、解析或模型分析。
- 异常处理要区分“用户输入错误”“业务部分成功”和“程序缺陷”，不要用一个通用文案覆盖三种情况。

---

## 10. 编辑器里“按钮可用”不等于数据正确，前端草稿必须和后端一起提交

### 当时发生了什么

训练编辑器曾经出现过两个看似相反的问题：

- 用户改了动作、重量或次数后，合并按钮一直是灰的；
- 直接把按钮解禁后，合并接口只提交了索引，没有提交输入框里的未保存草稿，后端按旧数据合并，用户的编辑被静默丢掉。

重复导入同一训练时，旧逻辑还会直接清空待确认状态，把用户已经做好的动作编辑、合并和备注全部抹掉。已保存训练组合并时，平均心率、距离和热量等字段也曾只沿用第一段，造成信息丢失。

### 修复方式

- 合并请求一次提交 `updates + merge_indices`，后端先应用草稿，再进行聚合；任何编辑无效则整个操作拒绝，不留下半完成状态。
- `editorBusy` 保留为“防止轮询覆盖正在编辑的字段”的锁，不再把它错误地当成合并权限开关。
- 重复导入拆成“幂等重放”和“需要用户决策的冲突”两条路径，保留待确认编辑。
- 已保存组合并按字段语义聚合：可加字段求和、峰值字段取最大、均值字段按时长加权；有原始心率流时精确重算。
- 使用 revision、确认令牌和幂等键，避免旧页面覆盖新状态或重复保存。

### 结果

用户把动作改成“上斜卧推 / 80 kg / 12 次”后再合并，最终得到正确的“80 kg / 24 次”；重启后仍可以撤销最近编辑或恢复原始解析结果。相关编辑回归测试在主线阶段全部通过。

### 以后怎么规避

- 任何前端草稿都要明确它的权威来源和提交边界，不能让后端悄悄回读旧状态。
- 先设计数据状态转换，再设计按钮的 disabled 样式。
- 所有覆盖、合并和删除操作都要有 revision/幂等策略，并测试“旧页面 + 新页面”并发操作。

---

## 11. 上下文工程最初写了很多规则，但模型根本没收到

### 当时发生了什么

项目曾经同时存在三份训练优先级说明：一份在 `build_agent_input`，一份在 `format_plan_context`，还有一份完整阶梯放在从未调用的函数里。测试只检查了那段死代码，所以测试是绿的，模型却完全看不到最重要的规则。

另一个坑是把截断后的历史或计划正文继续当成完整内容保存。用户最后看到的是断在半句的计划，之后点击保存又把残片写进本地。

### 修复方式

- 将唯一的优先级阶梯合并到真正接线的 `format_plan_context`，并在 `build_agent_input` 结果中断言只出现一次。
- 上下文按当前任务动态组装：当前时间、档案、确认约束、周计划、恢复状态、历史摘要和当前消息分层注入。
- 设置消息、历史和最终 Agent 输入三层预算；超长历史插入明确截断标记。
- 带截断标记的计划正文禁止保存；完整正文从服务端 `plan_draft_cache` 读取，不从前端历史回捞。
- 健康风险提示放在上下文前部，优先级高于目标、周计划和偏好。

### 结果

- 测试验证“优先级真的进入模型输入”，而不是只存在于某个函数文本里。
- 超长请求得到明确的 413/上下文错误，不再静默截断后继续生成或保存。
- 计划保存使用服务端完整正文，并能按 subject/date 找到用户要保存的那一份，而不是简单取最后一条消息。

### 以后怎么规避

- 测试“最终发送给模型的字符串/消息”，不要只测 Prompt 常量或死代码。
- 把上下文预算、截断标记和可保存条件设计成同一个契约。
- 原始数据、摘要、当前指令和历史参考必须标明来源与优先级，避免旧上下文覆盖新指令。

---

## 12. FastAPI 入口过长，状态错误不是偶然而是结构问题

### 当时发生了什么

早期所有 HTTP 路由、请求解析、Agent 调用、文件上传、计划校验、备份和删除都堆在 `main.py`。函数过长以后，出现了“写了但没接线”“短路后丢掉第二个问题”“异常分支没有清理缓存”等问题，单靠代码阅读很难发现。

### 修复方式

按职责拆成四层：

```text
routes/      FastAPI HTTP 适配器
workflows/   聊天、上传、退出等流程编排
domain/      纯业务规则和校验
runtime/     共享依赖、中间件、上传限制和响应转换
```

Store 统一从 `runtime/deps.py` 访问，避免每个模块重新实例化数据存储；路由快照和 OpenAPI 快照冻结外部契约；测试通过模块映射和依赖消费者检查真正的接线位置。

### 结果

`main.py` 从约 5402 行缩减到 132 行，73 条路由和 69 个 OpenAPI operation 保持不变。拆分后可以分别测试 HTTP 适配、流程状态、纯函数规则和存储并发，定位问题的成本明显下降。

### 以后怎么规避

- 入口文件只做应用装配，不承载业务决策。
- 复杂流程先画状态图，再拆 Workflow；纯规则尽量做成无副作用函数。
- 拆分前先冻结路由快照，拆分后同时比对路径、方法、响应和 operation id。
- 依赖要有一个可替换的边界，测试应替换真实消费者，而不是替换一个已经被复制的旧引用。

---

## 13. 后续才发现：框架默认打开的 Trace 也会制造隐私和资源问题

### 当时发生了什么

一开始以为项目“没有 Agent Trace”，后来检查 HelloAgents 默认配置才发现：`trace_enabled=True`、`session_enabled=True`，Agent 构造时就会创建 `memory/traces`、`memory/sessions` 等文件或目录。框架还会默认注册 Skill、Task、TodoWrite 和 DevLog 工具，模型 schema 无故增加。

现有文件式 Trace 只覆盖 ReAct 主循环，意图路由、健康筛查、记忆提取、计划校验等其他模型调用完全不可见；而 `message_written` 可能包含体重、睡眠、心率和健康限制等敏感内容。更麻烦的是，模型尚未真正运行就因为上下文错误返回时，框架已经打开了文件句柄，异常路径还可能留下未闭合 HTML 或会话转储。

### 已采取的止损措施

- 不再依赖第三方框架默认值，在 Agent 工厂中显式冻结 `Config`。
- 关闭本项目不使用的框架子系统：文件式 trace、session、skills、subagent、TodoWrite 和 DevLog。
- 对工具 schema 和有效配置建立运行时快照测试，防止框架升级后悄悄增加工具或改变截断/压缩策略。
- 将框架产物目录加入忽略清单，并盘点、清理已有历史文件。

### 当前边界

这部分目前完成的是**框架默认副作用的止损和契约固定**，还不能宣称已经有完整的端到端 Trace。后续若要重新启用可观测性，应以一次用户回合为单位建立 `trace_id/turn_id`，覆盖 FastAPI 请求、意图路由、确定性闸门、ReAct 工具调用、计划校验和最终响应，并默认只记录脱敏后的结构、长度和耗时。

### 以后怎么规避

- 第三方框架的默认配置不能当成项目设计，必须显式传入并做版本契约测试。
- 任何会记录 Prompt、健康数据或异常上下文的组件，都要先定义脱敏、保留期限和删除路径。
- 可观测性要覆盖“模型调用”和“确定性决策”两类事件，否则看到的只是半条链路。
- Trace 不能影响主流程；记录失败应降级为告警，而不是让业务请求失败。

---

## 共性教训：以后遇到类似项目，我会先做这几件事

### 1. 先划清模型权限

模型可以理解、规划和生成，但不能直接拥有不可逆写权限。写入必须经过 schema、业务校验、用户确认和幂等/版本控制。

### 2. 先定义事实，再定义 Prompt

明确哪些是用户原话、哪些是结构化数据、哪些是模型推断、哪些是临时状态。没有来源和有效期的“记忆”不应该进入长期上下文。

### 3. 把失败设计成一种正常状态

模型超时、工具失败、解析为空、存储只读、部分导入失败，都要有明确状态和可恢复路径；不能用“返回空”“统一 500”或“看起来成功”掩盖问题。

### 4. 用真实失败复现，再写回归测试

最有效的测试不是只验证新实现返回 200，而是先把代码还原成旧版本，确认测试确实能抓住原 bug，再固定不变量。项目中并发丢更新、时区偏移、重复导入、上下文截断和计划违规都采用了这种方式。

### 5. 不确定的地方宁可缺失，不要猜一个精确数字

健康数据尤其如此。一个明确的“暂无数据”比一个来源不明、看起来很专业的错误值更安全。

### 6. 发布前做数据外发和删除审计

不仅要检查源码和 `.env`，还要检查 trace、session、缓存、恢复点、原始导入文件和 Docker 构建上下文。用户点“删除全部数据”后，任何仍可恢复出健康细节的副本都必须被明确告知并可删除。

## 面试时可以用的一句话总结

> 这个项目最大的难点不是把 Agent 调通，而是把一个概率模型放进真实的数据系统后，逐步补齐权限、上下文、数据语义、并发、恢复和安全边界。每次踩坑后，我都把“模型猜测”收回到可验证的后端规则里，再用真实数据和回归测试把这条边界固定下来。
