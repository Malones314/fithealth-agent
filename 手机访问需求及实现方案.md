# FitHealthAgent 手机访问需求及实现方案

> 编写日期：2026-09-09  
> 适用环境：Windows 电脑运行 FitHealthAgent，手机通过浏览器访问  
> 当前约束：应用是单用户本地工具，没有账号、登录认证和权限隔离

## 1. 目标

让用户在电脑上持续运行 FitHealthAgent，并通过手机浏览器完成以下操作：

- 与 Agent 对话。
- 查看健康趋势和单日健康总览。
- 新增每日记录。
- 查看或编辑训练数据。
- 使用 LLM 连通性测试。
- 在需要时上传手机中的图片或文件。

需要覆盖两个主要场景：

1. 手机和电脑连接同一个 Wi-Fi，只在家中或可信局域网使用。
2. 手机使用移动网络或外部 Wi-Fi，在外面访问家中电脑。

## 2. 当前项目网络现状

当前代码存在两种启动方式，它们的网络行为不同。

### 2.1 直接运行 Python

`main.py` 当前使用：

```python
uvicorn.run(app, host="0.0.0.0", port=9999)
```

这意味着程序会监听电脑的所有网络接口。同一局域网中的手机理论上可以通过电脑的局域网 IP 访问，但 Windows 防火墙仍可能阻止连接。

### 2.2 Docker Compose

`docker-compose.yml` 当前使用：

```yaml
ports:
  - "127.0.0.1:9999:9999"
```

这只允许电脑本机访问。即使手机与电脑处于同一个 Wi-Fi，也无法直接连接容器服务。

### 2.3 安全约束

FitHealthAgent 当前没有登录认证。任何能访问该服务的人都可能：

- 查看训练、健康、睡眠、营养和记忆数据。
- 触发付费 LLM 或视觉模型请求。
- 修改或删除数据。
- 导出备份。
- 发起全量重置等敏感操作。

因此不能把 `9999` 端口未经保护地直接暴露到公网。

## 3. 可选方案对比

| 方案 | 是否免费 | 可否在外网访问 | 技术门槛 | 安全性 | 推荐程度 |
| --- | --- | --- | --- | --- | --- |
| 同一 Wi-Fi 直接访问 | 免费 | 否 | 低，1/5 | 取决于局域网可信度 | 推荐用于最快落地 |
| Tailscale 私有网络 | 个人使用通常有免费方案 | 是 | 低，2/5 | 高，设备身份控制 | 最推荐 |
| ZeroTier 私有网络 | 个人使用通常有免费方案 | 是 | 中低，2.5/5 | 高，设备加入需授权 | 可替代 Tailscale |
| Cloudflare Quick Tunnel | 免费 | 是 | 中，3/5 | URL 是公网入口 | 未加认证前不推荐 |
| Cloudflare Tunnel + Access | Tunnel/Access 有免费层，通常需要自有域名 | 是 | 中高，3.5/5 | 高，可做身份认证 | 适合正式远程访问 |
| ngrok 免费层 | 通常有免费层 | 是 | 中低，2.5/5 | URL 是公网入口 | 未加认证前不推荐 |
| 路由器端口转发 | 工具本身免费 | 是 | 高，4/5 | 很低，需自行处理 TLS 和认证 | 不推荐 |

免费套餐和产品限制可能调整，实际使用前应查看对应服务当时的官方免费层说明。

## 4. 推荐决策

### 4.1 只在家里使用

选择“同一 Wi-Fi 直接访问”。

优点：

- 不需要注册第三方账号。
- 不安装额外软件。
- 延迟最低。
- 五到十分钟可以完成。

限制：

- 离开当前 Wi-Fi 后不能访问。
- 同一网络中的其他设备也可能访问。
- 不应在公司、酒店、商场等不可信 Wi-Fi 使用。

### 4.2 需要在外面访问

选择 Tailscale。

优点：

- 不需要公网 IP。
- 通常不需要配置路由器。
- 能穿过家庭宽带 NAT 和多数 CGNAT 环境。
- 只有加入同一 Tailnet 并获得授权的设备可以访问。
- 可以继续让 FitHealthAgent 仅绑定本机地址。
- 不需要把健康数据服务公开到互联网。

这最符合当前“没有应用内登录认证”的架构。

## 5. 方案 A：同一 Wi-Fi 直接访问

### 5.1 前置条件

- 手机和电脑连接同一个可信 Wi-Fi。
- Wi-Fi 没有开启客户端隔离或 AP Isolation。
- 电脑保持开机，程序保持运行。
- Windows 当前网络配置为“专用网络”，不是“公用网络”。

### 5.2 直接运行 Python

启动命令：

```powershell
python -m uvicorn main:app --host 0.0.0.0 --port 9999
```

查找电脑局域网 IPv4：

```powershell
Get-NetIPAddress -AddressFamily IPv4 |
  Where-Object {
    $_.IPAddress -notlike "127.*" -and
    $_.IPAddress -notlike "169.254.*"
  } |
  Select-Object InterfaceAlias, IPAddress
```

假设电脑 IP 为 `192.168.1.25`，手机浏览器访问：

```text
http://192.168.1.25:9999
```

### 5.3 Windows 防火墙

如果手机无法连接，需要允许 TCP 9999 入站，但只允许专用网络和本地子网。

推荐规则要求：

- 协议：TCP。
- 本地端口：9999。
- 配置文件：Private。
- 远程地址：LocalSubnet。
- 不允许 Public 配置文件。

管理员 PowerShell 示例：

```powershell
New-NetFirewallRule `
  -DisplayName "FitHealthAgent LAN" `
  -Direction Inbound `
  -Protocol TCP `
  -LocalPort 9999 `
  -Action Allow `
  -Profile Private `
  -RemoteAddress LocalSubnet
```

不再使用时删除：

```powershell
Remove-NetFirewallRule -DisplayName "FitHealthAgent LAN"
```

### 5.4 Docker Compose

不要直接修改默认 `docker-compose.yml` 的安全基线。新增本地专用覆盖文件，例如 `docker-compose.lan.yml`：

```yaml
services:
  fithealth:
    ports:
      - "9999:9999"
```

启动：

```powershell
docker compose `
  -f docker-compose.yml `
  -f docker-compose.lan.yml `
  up -d --build
```

注意：Compose 端口列表的覆盖/合并行为必须通过以下命令确认：

```powershell
docker compose `
  -f docker-compose.yml `
  -f docker-compose.lan.yml `
  config
```

最终只能存在预期的 LAN 映射，不能误留重复映射。若 Compose 合并结果不符合预期，应使用单独的 LAN Compose 文件完整声明服务端口，而不是盲目启动。

### 5.5 局域网验收

1. 电脑访问 `http://127.0.0.1:9999/health/storage-status` 成功。
2. 手机访问 `http://电脑局域网IP:9999/health/storage-status` 成功。
3. 手机打开首页，导航、聊天和数据面板没有横向遮挡。
4. 手机能发送一条普通消息。
5. 手机锁屏后重新打开页面，不会造成重复提交。
6. 断开 Wi-Fi 改用移动网络后无法访问，证明服务没有意外暴露到公网。

### 5.6 常见问题

#### 手机一直转圈

检查：

- 电脑 IP 是否变化。
- Windows 防火墙是否只放行了其他网络配置。
- 手机和电脑是否真的在同一子网。
- 路由器是否开启客户端隔离。
- VPN 是否改变了路由。

#### 电脑能打开，手机不能打开

通常是服务仍绑定 `127.0.0.1`，或者 Windows 防火墙拦截。

#### IP 经常变化

可以在路由器 DHCP 设置中为电脑固定地址，或者改用 Tailscale 的稳定设备名。

## 6. 方案 B：Tailscale 私有访问

### 6.1 技术门槛

门槛：低，2/5。

需要：

- 在电脑和手机安装 Tailscale。
- 两台设备登录同一账号或加入同一 Tailnet。
- 在管理页面批准设备。

不需要：

- 公网 IP。
- 路由器端口转发。
- DDNS。
- 自己签发 HTTPS 证书。

### 6.2 推荐拓扑

```text
手机浏览器
    │
    │ Tailscale 加密私有网络
    ▼
电脑上的 Tailscale Serve
    │
    │ http://127.0.0.1:9999
    ▼
FitHealthAgent
```

FitHealthAgent 保持只监听电脑本机或 Docker 的 `127.0.0.1:9999`，由 Tailscale Serve 提供私有访问入口。

### 6.3 实现步骤

1. 在 Windows 安装并登录 Tailscale。
2. 在手机安装并登录 Tailscale。
3. 确认两台设备在 Tailscale 管理页面均在线。
4. 保持 FitHealthAgent 本机地址可访问：

   ```text
   http://127.0.0.1:9999
   ```

5. 根据当前 Tailscale 客户端版本查看 Serve 语法：

   ```powershell
   tailscale serve --help
   ```

6. 将 Tailnet 内的 HTTPS 入口代理到本机服务。常见的新版本命令形式为：

   ```powershell
   tailscale serve --bg http://127.0.0.1:9999
   ```

7. 查看分配的访问地址和状态：

   ```powershell
   tailscale serve status
   ```

8. 手机保持 Tailscale 已连接，通过输出的私有 HTTPS 地址访问。

不同 Tailscale 版本的 CLI 参数可能不同，应以 `tailscale serve --help` 的本机输出为准。

### 6.4 安全配置

- 不使用 Tailscale Funnel。Funnel 会创建公网入口，而本项目当前没有应用内认证。
- Tailnet 中只保留自己的设备。
- 开启设备审批。
- 账号开启多因素认证。
- 丢失手机后立即在 Tailscale 控制台撤销该设备。
- 不在聊天或截图中泄露完整管理密钥。

### 6.5 Tailscale 验收

1. 电脑和手机在 Tailnet 中均显示在线。
2. 手机关闭 Wi-Fi、切换移动网络后仍能通过私有地址访问。
3. 未登录 Tailnet 的设备无法访问。
4. FitHealthAgent 的宿主机端口仍只绑定 `127.0.0.1`。
5. `tailscale serve status` 显示目标为 `127.0.0.1:9999`。
6. 停止 Tailscale Serve 后，远程地址立即不可访问。

### 6.6 停止远程访问

先查看当前版本支持的关闭命令：

```powershell
tailscale serve --help
```

常见版本可以通过重置 Serve 配置关闭：

```powershell
tailscale serve reset
```

## 7. 方案 C：ZeroTier

### 7.1 适用情况

- Tailscale 在当前网络不可用。
- 已经在使用 ZeroTier。
- 希望手动管理虚拟网络和成员授权。

### 7.2 技术门槛

门槛：中低，2.5/5。

大致步骤：

1. 创建 ZeroTier 虚拟网络。
2. 电脑和手机加入同一 Network ID。
3. 在控制台批准两台设备。
4. 让 FitHealthAgent 监听 ZeroTier 虚拟网卡地址，或监听 `0.0.0.0` 后用 Windows 防火墙只允许 ZeroTier 子网。
5. 手机通过电脑的 ZeroTier IP 和端口 9999 访问。

相比 Tailscale Serve，这种方式通常需要更多防火墙和网卡判断，因此不是首选。

## 8. 方案 D：Cloudflare Tunnel

### 8.1 Quick Tunnel

Quick Tunnel 可以快速生成临时公网 URL，技术门槛约 3/5，但该 URL 本质上是公网入口。

当前 FitHealthAgent 没有登录认证，因此只适合非常短暂的调试，并且不应载入真实健康数据。不建议作为日常手机访问方式。

### 8.2 Named Tunnel + Access

更正式的方式是：

```text
手机
  → Cloudflare Access 登录
  → Cloudflare Tunnel
  → 电脑 localhost:9999
```

优点：

- 有 HTTPS。
- 可以要求邮箱或身份提供商登录。
- 不需要打开家庭路由器端口。

成本与门槛：

- Tunnel 和 Access 可能可使用免费层。
- 通常需要一个自己的域名，域名本身不一定免费。
- DNS、Access Policy 和 Tunnel 配置增加了运维复杂度。

只有在明确需要通过普通浏览器分享给未安装 Tailscale 的设备时，才建议采用。

## 9. 方案 E：ngrok

ngrok 可以把 `localhost:9999` 映射为公网 HTTPS 地址，使用简单，但免费层的域名、连接限制、浏览器提示和认证能力可能随产品策略变化。

技术门槛约 2.5/5。

风险与 Cloudflare Quick Tunnel 相同：生成地址后，FitHealthAgent 就可能被公网访问。除非同时配置可靠认证，否则不应连接真实数据目录，也不应作为长期方案。

## 10. 不推荐方案：路由器端口转发

不建议将公网端口直接转发到电脑的 9999 端口。

原因：

- 当前应用无登录认证。
- HTTP 没有 TLS。
- 家庭公网 IP 可能变化。
- 很多宽带处于 CGNAT，端口转发根本不可用。
- 扫描器可能很快发现开放端口。
- Windows 或 Docker 配置错误会直接暴露全部健康数据接口。

要安全使用端口转发，至少还需要反向代理、HTTPS、身份认证、访问日志、速率限制、自动更新和安全备份，技术门槛约 4/5，已经失去“简单”优势。

## 11. 推荐实施路线

### 第一阶段：零代码验证

目标：当天可以在手机使用。

如果只在家使用：

1. 直接通过 Uvicorn 监听 `0.0.0.0:9999`。
2. Windows 防火墙只向 Private + LocalSubnet 放行。
3. 手机用电脑局域网 IP 访问。

如果需要外出使用：

1. 保持服务仅监听 `127.0.0.1:9999`。
2. 电脑和手机安装 Tailscale。
3. 使用 Tailscale Serve 暴露私有 HTTPS 地址。

### 第二阶段：项目配置化

新增环境变量：

```dotenv
FITHEALTH_HOST=127.0.0.1
FITHEALTH_PORT=9999
FITHEALTH_REMOTE_ACCESS=off
```

规则：

- 默认仍为 loopback。
- `FITHEALTH_REMOTE_ACCESS=lan` 时允许监听局域网。
- `FITHEALTH_REMOTE_ACCESS=private-overlay` 时保持 loopback，交给 Tailscale Serve。
- 禁止仅通过修改 host 就静默进入公网暴露模式。

建议修改：

- `fithealth_agent/settings.py`：解析并验证网络配置。
- `main.py`：从集中配置读取 host 和 port。
- `.env.example`：增加示例和警告。
- `README.md`：增加手机访问章节。
- `docker-compose.lan.yml`：提供显式 LAN 覆盖文件。

### 第三阶段：移动端体验

当前导航条在窄屏会横向滚动，但仍需专项验证：

- 390 × 844 手机视口。
- 360 × 800 小屏视口。
- 横屏模式。
- 刘海和安全区域。
- 输入法弹出时的聊天输入框。
- 文件与图片选择器。
- 长训练计划和健康趋势图。

建议增加：

- Web App Manifest。
- `apple-touch-icon`。
- `theme-color`。
- `viewport-fit=cover`。
- 安全区域 CSS：`env(safe-area-inset-*)`。
- PWA 安装能力可以后置，不在首期强制实现。

### 第四阶段：远程访问安全能力

如果未来不用 Tailscale，而要生成普通公网 URL，必须先实现：

- 单用户访问令牌或反向代理身份认证。
- HTTPS。
- Origin 与 Host 校验。
- CSRF 防护。
- `/chat`、餐盘识别、LLM 测试等模型端点限流。
- 登录失败和模型调用审计。
- 敏感接口的短期会话。
- 远程模式下禁用或二次确认“删除全部数据”和备份下载。

## 12. 功能需求

### MOB-01 网络模式可配置

系统应提供 `loopback`、`lan` 和 `private-overlay` 三种明确模式，不允许网络暴露范围只依赖隐式命令参数。

验收：

- 默认只能在本机访问。
- LAN 模式启动日志明确显示局域网访问地址和安全警告。
- 私有覆盖网络模式不会打开 LAN 端口。

### MOB-02 手机访问状态提示

应用启动时应输出：

- 本机 URL。
- LAN URL，仅在 LAN 模式显示。
- 是否启用认证。
- 是否允许远程访问。
- 数据目录位置。

不得输出 API Key、访问令牌或完整外部模型凭据。

### MOB-03 Docker LAN 覆盖配置

默认 Compose 保持 `127.0.0.1`。LAN 访问必须使用单独覆盖文件显式开启。

验收：

- 默认 `docker compose config` 仍只绑定 loopback。
- LAN 覆盖配置能从手机访问。
- 停用覆盖文件并重建容器后，手机无法继续通过 LAN 访问。

### MOB-04 移动端导航可用

导航按钮在 360 px 宽度下不得重叠、裁切或覆盖内容。

验收：

- 所有按钮可点击。
- 横向滚动有明确边界。
- 文本不会压缩成不可读宽度。
- LLM 测试的加载状态不会改变导航高度。

### MOB-05 移动端聊天可用

验收：

- 输入法弹出后仍能看到输入框和发送按钮。
- 长消息可以滚动。
- 网络切换时显示失败，不重复发送。
- 页面恢复前台后不会重复提交上一次 POST。

### MOB-06 远程访问安全门禁

当服务检测到非 loopback 监听时：

- LAN 模式必须显示无认证警告。
- 公网模式必须要求认证，否则拒绝启动。
- 高成本模型端点必须有限流。
- 破坏性操作必须二次确认。

## 13. 非功能需求

### 安全

- 默认不扩大当前网络暴露范围。
- 不把 `.env`、API Key 或 Tailnet 管理密钥传给手机页面。
- 不在公网使用裸 HTTP。
- 不把 Cloudflare Quick Tunnel、ngrok URL 或 Tailscale Funnel 当作默认方案。

### 性能

- 同一局域网首页首屏目标小于 2 秒。
- 手机端普通 UI 操作不触发不必要的 LLM 请求。
- 网络断开后请求应有明确超时。

### 可靠性

- 电脑休眠后手机显示不可连接，而不是无限加载。
- 电脑唤醒后无需重建健康数据库。
- IP 变化不能影响本地数据。
- 远程接入工具停止后，FitHealthAgent 本地访问仍正常。

### 隐私

- Tailscale/ZeroTier 只承担传输通道，不改变数据持久化位置。
- Cloudflare/ngrok 等公网中继可能处理连接元数据，应在启用前提示。
- 手机浏览器缓存中不长期保存备份文件或访问令牌。

## 14. 测试计划

### 14.1 配置测试

- 默认 host 为 `127.0.0.1`。
- 非法端口被拒绝。
- 未知网络模式被拒绝。
- 公网模式无认证时拒绝启动。
- 环境变量每次加载行为符合约定。

### 14.2 Docker 测试

- 默认端口仅绑定 loopback。
- LAN 覆盖文件的最终配置正确。
- 数据卷仍为 `./data:/app/data`。
- 重建容器不丢数据。

### 14.3 手机浏览器测试

- Android Chrome。
- iOS Safari。
- Wi-Fi 到移动网络切换。
- 竖屏和横屏。
- 图片上传、FIT/ZIP 文件上传。
- LLM 测试按钮。
- 健康趋势图和数据管理弹窗。

### 14.4 安全测试

- 局域网外无法访问 LAN 模式。
- Public 防火墙配置文件没有放行 9999。
- 未加入 Tailnet 的设备无法访问 Tailscale 地址。
- 未认证公网请求无法读取数据或触发模型。
- 备份下载和重置接口不会被跨站请求触发。

## 15. 故障排查顺序

手机打不开时按以下顺序检查：

1. 电脑本机能否访问 `http://127.0.0.1:9999`。
2. 程序是否监听正确地址。
3. Docker 是否仍只绑定 `127.0.0.1`。
4. 手机与电脑是否同一 Wi-Fi。
5. 电脑局域网 IP 是否正确。
6. Windows 网络是否为 Private。
7. 防火墙是否只允许 LocalSubnet。
8. 路由器是否开启客户端隔离。
9. VPN 是否抢占路由。
10. Tailscale/ZeroTier 两端是否在线并已授权。

诊断命令：

```powershell
Get-NetTCPConnection -LocalPort 9999 -State Listen
Test-NetConnection 127.0.0.1 -Port 9999
docker compose ps
docker compose logs --tail 100 fithealth
```

## 16. 最终建议

### 最简单方案

同一 Wi-Fi + `0.0.0.0:9999` + Windows 防火墙仅放行 Private/LocalSubnet。

适合：只在家里用，追求最快完成。

### 最平衡方案

Tailscale + Tailscale Serve + FitHealthAgent 继续绑定 `127.0.0.1:9999`。

适合：需要在外面访问，同时不想开发登录系统或购买域名。这是当前项目最推荐的方案。

### 暂时不要做

- 路由器直接映射 9999。
- 无认证 Cloudflare Quick Tunnel。
- 无认证 ngrok 公网 URL。
- Tailscale Funnel。
- 在公共 Wi-Fi 上开放 LAN 访问。

## 17. 第一批可执行任务

若将手机访问正式纳入项目，建议第一批按以下顺序实施：

1. 新增 `FITHEALTH_HOST`、`FITHEALTH_PORT` 和 `FITHEALTH_REMOTE_ACCESS` 配置。
2. 将 `main.py` 默认监听改为 `127.0.0.1`。
3. 新增独立 `docker-compose.lan.yml`，默认 Compose 保持不变。
4. README 增加局域网和 Tailscale 操作章节。
5. 增加网络配置和 Compose 契约测试。
6. 用 Android Chrome 和 iOS Safari 验证 360/390 px 页面。
7. 修复手机输入法、导航溢出和请求重复提交问题。
8. 远程公网方案必须等认证、HTTPS、CSRF 和限流完成后再开放。

