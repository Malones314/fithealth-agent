# FitHealthAgent

## 测试版

### 2026_08_10
开发mvp：
1. 基础架构 - HelloAgent ReAct Agent 框架
2. Gradio 前端 - 聊天界面
3. 数据存储 - JSON 文件存储系统
4. 核心 Tool 功能：
保存每日记录（支持多分类：recovery/nutrition/training/summary）
查询历史记录
修改：
1. agent会先查看是否有用户信息，如果没有则引导用户输入所需信息
2. 将前端index模板从main.py中移到./templates/index.html中

### 2026_08_11
1. 前端增加退出按钮
2. 在用户点了退出按钮后，agent自主判断是否要保存此次对话的部分信息，先用正则表达式、关键字拦截，然后用deepseek-v4-flash做判断，做好informationRouter。对于不需要的信息本地保存三天后删除
3. 上传 Garmin 手表生成的原生 `.fit` 二进制文件，由 Python 后端解析出每组动作的时间、动作类型、重量、次数，**并且需要精准关联出该动作组的实时心率范围（平均/最大心率）**。
4. 通过自然语言指令修正动作名称，或合并连续的动作组。当用户下令合并动作组时，系统必须基于原始心率数据流（Record 序列），重新计算合并后的新时间窗口内的平均心率和最大心率。
5. 目前支持的运动包括：力量训练、跳绳、跑步、骑行

### 2026_08_12
1. 给出健身动作时同步给出YouTube对应链接
2. 取最近5条历史拼接到上下文中，支持多轮对话
3. 更改前端助手回复，将纯文本回复改为渲染排版后的回复
4. 增加下载功能，agent回复内容可以下载为.md文件
5. 增加上传功能，可以上传训练计划（.md文件），agent解析并寻找YouTube视频（如果计划中没有视频链接的话），且优化训练计划。且设置上传上线，fit文件上限为50mB，md文件256kB以内直接上传，1mB以内需要用户确认，超过1mB拒绝上传
6. 前端引入DOMPurify，marked 输出经过 DOMPurify 清洗后才写入 innerHTML
7. 结构化力量训练组编辑器，无需依赖agent
8. ui修改，训练侧栏和对话栏各自可拉宽，上传区始终跟随训练侧栏宽度；训练组区域设置稳定高度，内容只在内部滚动。拖拽宽度会保存到浏览器，下次打开继续沿用。

### 2026_08_13
1. 数据修改逻辑更新，支持批量修改，无需多次点击单次运动修改的确认按钮
2. 增加数据删除功能，首版永久删除，不提供回收站，全部数据删除包含用户档案和临时记忆，首版只增加数据管理弹窗
3. 修复上传训练计划后更新错误体重的bug，更改当前体重更新逻辑，只有明确出现“体重”字段时才解析体重、只读取“体重”所在行，不再扫描整份消息。
4. 训练计划保存优化：只保存优化版；日期允许修改；不同内容允许多份；冲突自动编号；弹窗支持完整 Markdown 预览和下载
5. 修复动作合并bug：合并 A + 休息 + B 时：新动作 C 的开始时间为 A 的 start_time，结束时间为 B 的 end_time，总时长为 B.end_time - A.start_time，A、B 之间的休息段会被删除，休息时间自然计入 C 的总时长，心率根据包含休息时间的完整时间窗口重新计算，次数仍为 A+B，重量仍取最大值。

### 2026_08_14
1. 跨会话记忆参与上下文,回答后记录哪些记忆被使用，支持问题追踪和用户删除
2. 设置上下文预算与输入校验
3. 解析Garmin官网得到的心率、压力、睡眠等数据的zip文件，支持批量导入

### 2026_08_15
1. 给记忆加上 type（偏好/限制/计划决策/训练反馈）、importance、expires_at 和“用户确认”字段，然后按类型和重要度注入
2. 结构化手动记录、本地备份、导入与导出

### 2026_08_16
1. 可通过照片添加代谢信息
2. 进入agent时进行基础资料完整度识别
3. 更新前端ui
4. 支持zip中多个运动文件导入，并逐个编辑保存
5. 修复zip文件导入bug

### 2026_08_17
1. 备份恢复改为事务式恢复
2. 改为照片附件待发送模式,将照片上传后不立即输入到llm，允许在聊天框中继续输入一些辅助性文字（允许为空）
3. 建立健康建议安全规则:
   1. 在 Agent 系统提示、后端策略和前端提示中统一加入风险分级。
   2. 对胸痛、晕厥、呼吸困难、急性损伤、极端异常心率等高风险描述，停止训练建议并建议及时就医。
   3. 禁止诊断、处方、药物调整，以及未经确认的极端热量限制和过量训练建议。
   4. 所有基于 Garmin 数据的结论必须说明日期、样本范围、缺失数据与“非医疗诊断”。
4. 修改聊天中正则表达式的bug
5. 添加频道正负偏好设置：
   1. 用户说“不喜欢某 YouTube 频道”时，退出后会生成待确认的偏好记忆；确认后保存为“避免频道”结构化字段。
   2. 后续视频搜索会在工具层过滤该频道，不取消训练动作；若有其他频道，优先返回其他频道；没有替代结果才回退该频道。
   3. 当前对话里的明确负反馈也会立即生效，不必等退出确认后才换频道；跨会话则以确认后的记忆为准。

### 2026_08_18
1. 处理历史训练记录编辑中加入版本控制，若另一页面已先保存，旧页面的写入会被服务端拒绝并返回 409，不会覆盖新内容。前端会提示保存冲突，并让你选择是否重新加载最新记录；重新加载才会放弃当前未保存编辑。
2. 初始界面不会显示敏感信息
3. 废除档案正则入口，完全改为Function Calling + 校验 + 用户确认流程
4. 实现结构化跨会话记忆
   1. 确认后的事实会按时间合并，较新的 cleared 可解除旧限制；未确认记忆不会影响回复或视频推荐。
   2. 模型上下文按“用户档案 → 已确认约束与偏好 → 历史摘要 → 当前会话”分层，频道过滤只读结构化事实，不再信任摘要或旧元数据。
   3. 数据管理界面显示每条记忆的结构化事实，方便确认前审阅。
   4. save_memory 现在受限返回事实数组，支持频道排除、动作偏好/限制、RPE 上限、伤病限制和计划偏好；无效 key/value 不会入库。
5. 修复保存训练计划bug，只保存生成计划的完整训练内容的正文
   1. 新增 save_existing_training_plan Function Calling：专门处理保存本会话已经生成的计划。
   2. 保存时从独立会话历史倒序寻找最近一份完整训练计划，忽略“内容不重复”的简短确认消息。
   3. 如果会话中找不到完整计划，会拒绝生成保存卡，不会再错误保存最后一条回复。
6. 修复每周计划bug
   1. 新增 plan.weekly_schedule 结构化记忆，受限为 mon–sun 到训练科目的 JSON 映射，模型通过 Function Calling 创建候选，用户确认后才生效。
   2. 用户确认周计划后，输入具体日期生成每日计划时，后端根据星期计算当天科目并写入硬约束；
   3. 周计划会在记忆管理和“个人信息”中显示为“每周训练安排”。

### 2026_08_19
1. 长期记忆与短期反馈分离 TTL。save_memory 只校验事实结构，没有校验事实是否来自用户消息。
2. 修复：记忆确认是整条确认，不能逐条确认。现在改为可以每个事实单独显示；每个事实单独确认、拒绝、编辑；只有确认的事实进入长期上下文。
3. 增加记忆卡片内联编辑功能，可显示修改记忆内容。
4. 修改记忆分层，分为：
   * 永久或长期：目标、伤病、体态、器械、周计划； 
   * 中期：训练偏好、动作偏好、频道偏好； 
   * 短期：本次训练疲劳、酸痛、恢复情况； 
   * 临时：当前会话决策。
5. 添加后端拦截校验功能，增加计划生成后的结构化检查
6. 记忆冲突时要用户确认
7. 设置混合优先级策略
   1. 统一数据模型，区分长期事实、临时状态和当前指令。
   2. 提取当前指令、临时限制、排除科目
   3. 后端 PlanContext 优先级裁决
   4. 工作流状态机判断是否澄清/阻断/生成
   5. LLM 生成计划
   6. 后端结构化校验
   7. 保存或拒绝

### 2026_08_20
1. BugFix
2. 训练记录的心率等信息不算入日均心率等记录中
   * 新增 `hr_stream_store.py`：确认保存训练时把 1Hz 心率流按记录 id 旁挂成独立文件，训练记录里只留摘要（条数/最小/最大/均值/覆盖窗口）。
   * 刻意**不**写入 `health.db` 的 `heart_rate_samples`——那张表是全天分钟级监测数据，日汇总与趋势按采样点等权算均值；实测 2026-08-15 已有 1212 个分钟级时间点覆盖整段训练，再灌 1802 条 1Hz 会把训练那一小时加权约 30 倍，日均心率从 78.7 失真。
   * 也**不**写进 `daily_records.json`——`query_daily_records` 会把整条 record 交给 ReAct 观察，1802 个点直接撑爆上下文。
3. DATA-08 记忆库静默清空防护
   * `InfoStore._read_all` 不再把读失败伪装成空库；改为隔离一份 `.corrupt-<ts>` 副本（copy，原文件留着可直接修）并进入**只读降级**，期间所有写入抛 `MemoryStoreDegradedError`（全局处理器回 503 + 恢复指引）。
   * 单条坏记录只跳过不连坐，且在每次写入时原样带回；`created_at` 缺失不再让所有记忆接口 500。
   * 写路径加 `fsync`，tmp 名带 pid+uuid；`revalidate()` 让修好文件后无需重启即可解除降级。
4. DATA-05 被误隔离的待确认训练可以救回来了
   * 隔离改 copy-then-unlink，且隔离失败时**不再删原文件**（原实现在这里销毁唯一一份数据）。
   * 严格恢复失败先降级为"部分可用"（逐条跳过坏分段/坏心率点），只有连 session 都读不出来才隔离。
   * 新增隔离文件列表与一键重放：`GET /workout_state/quarantined`、`POST /workout_state/quarantined/restore`，前端在没有待确认训练时会主动提示。
   * `scripts/backfill_quarantined_hr.py` 已回灌：那条有氧训练记录其实没丢（后来重新导入过同一份 FIT），丢的只是心率流；1802 条心率已补挂到记录 `16e93f38`（均 94.3 / 峰 147），记录正文未动。
   * 注：`sets` 为空的根因是 DATA-04（`sport=training` 被路由给力量训练解析器，lap 在解析阶段就丢了），仍待修。
5. DATA-04 `sport=training` 是大类，不能一律按 set 解析
   * `StrengthTrainingParser.parse()` 解析结果为空时降级到 lap 解析（连 lap 都没有则退到 session 整段），lap 与心率不会再凭空消失。
   * `sub_sport` 参与路由：`training` + `cardio_training/yoga/pilates` → lap 解析，+ `hiit/interval_training/circuit_training` → 间歇解析，+ `strength_training` 或无 sub_sport → 仍按 set 解析。**sub_sport 只在 sport 缺失或是大类时才有裁决权**，避免 `sport=cycling` 配 `sub_sport=hiit` 被抢走。
   * `IntervalSportParser.can_handle()` 从死代码接回路由；注册表顺序改为 Strength → Interval → LapBased → Fallback（Fallback 恒真必须最后）。
   * `_sport_to_zh` 接受 sub_sport：`training` 单看只能得到"训练"，现在能显示"有氧训练""瑜伽""高强度间歇"；文件自带的 sport.name 仍优先。
   * `main.py` 的 FIT 渲染分支改为看**实际分段类型**而不是 sport_raw，否则降级出来的 lap 会被渲染成一串重量 0、次数 0 的假"动作组"。
   * 实测：那份有氧活动从 segments=0 变成 segments=1（avg_hr 94 / 峰 147 / 232 kcal / 64 分钟）；三份真实力量训练 FIT 的解析结果逐一比对无变化。
6. BUG-03 优先级阶梯从未送进模型
   * 修之前有**三处**并存的优先级表述：`build_agent_input` 里的【训练设计优先级】、`format_plan_context` 里压缩成一行的版本、以及零调用的 `training_priority_context`（完整六级阶梯）。
   * 做法不是把死代码另外拼上去（那会变成三段重叠说明，还会把安全事实/周计划/偏好第二次塞进上下文），而是把独有内容合并进已经接线的 `format_plan_context`，删掉 `training_priority_context`，全局只留一段【后端 PlanContext（已裁决优先级，必须遵守）】。
   * 补回的最关键一句：**只有明确改练、休息、跳过或不执行才覆盖当天周计划，普通抱怨或提问不算**。以前这句只存在于死代码里，"今天有点累"可能被当成覆盖周计划的指令。
   * `prompts.py` 加第 11 条与 PlanContext 对齐。原来那条排序测试测的正是模型永远看不到的文本，已改为断言 `format_plan_context`，并新增"阶梯必须出现在 build_agent_input 结果里"和"优先级段落只出现一次"两条。
   * BUG-04 随之收尾：`PlanWorkflowState`/`state_after_generation`/`state_for_context` 三个导入现在都有调用点，第 3 节「写了但从未接线」的死代码清零。
7. BUG-12 记忆事实改用稳定 id 寻址
   * 原来接口收 `fact_index`，而每次 `/chat` 都会 `cleanup_expired` 物理删除过期事实、拒绝事实也会让后面的整体前移，老页面点"确认/拒绝/编辑"会作用到另一条事实上。
   * 每条事实加 `fact_id`。**用内容摘要而不是随机 uuid**：随机 id 需要数据迁移，而且在落盘之前每次读取都会换一个；内容派生对存量数据天然稳定，零迁移。真实 info_store.json 上 10 条事实全部拿到唯一 id，两次读取一致。
   * 路由 `{fact_index}` → `{fact_ref}`：纯数字仍按下标处理以兼容没刷新的页面，否则按 id。前端改用 `fact.fact_id`。
   * `edit_fact` 会改变 id（值变了就是另一条事实），并拒绝把值改成同条目里已有的另一条事实——否则两条会共用一个 id 而无法分别寻址。
8. BUG-05 收尾（第一版留下三个缺口）
   * `/plans` 的校验顺序反了：截断拦截排在取草稿之前，带合法 draft_id 的请求会因为前端那份是残片而先被 400。改成先取草稿，只有回落到前端正文时才拦截。
   * `latest()` 改 `find(subject=, date=)`：一个会话里生成过多份计划时，"保存那份腿的计划"原来会静默存错最后一份。
   * `local_plan_save` 的 artifact 补上 draft_id，这条路径不再退回靠前端传值。
   * 顺序敏感的裁决抽成纯函数 `resolve_plan_save_content`，这样它能被真正调用着测——原先内联在路由里，沙箱装不了 fastapi，测试只能做字符串断言，改个变量名就绿着失效。
9. BUG-07 消息里的日期不再一律当成计划日期
   * 新增 `requested_plan_date`：白名单意图词 + **方向敏感**的否决词（「上次/查询」在日期前 → 查历史；「拉伤/体检」在日期后 → 往事），且**只看日期所在的那一个小句**，否则「我在 2026-08-01 拉伤了腰，2026-08-19 能练什么」里第二个日期会被前半句连坐否决。取第一个语境通过的日期，而非第一个日期。
   * 新增 `extract_iso_dates` 统一两处正则（宽松匹配 + 显式补零 + fromisoformat 校验 + 年份范围），`2026-8-9` 不再只被查记录侧识别。查记录侧刻意不做语境门控——那条路径本身就意味着用户在查历史。
10. BUG-08 多 ZIP 导入不再丢活动
    * 后端收集与裁决分两步：循环里只收集，循环后由 `select_autoload_activity` 判断，**只有全局恰好一个活动才自动载入**，多于一个交给选择器。
    * 响应新增 `activities: [{zip, name}]`——`/upload_health/activity` 要重新上传对应的 ZIP 才能取出 FIT，所以每个活动必须记住来源。前端把每条活动映射回它自己的 File，多包时标出 `包名 › FIT 名`，"保存完一个自动处理下一个"按 zip+name 组合键过滤（同名 FIT 不会一次删两条）。
    * 单个活动 FIT 解析失败不再让整次健康数据导入失败，改为记进 errors 并降为 partial。
11. 训练编辑区加「取消并丢弃」按钮
    * 位置在"确认并保存训练"右侧。工具栏里本来就有「清空」，但它在面板顶部、离决定点太远，用户看着训练列表决定要不要存时需要就地有个出口。
    * 与「清空」**共用同一份实现** `discardPendingWorkout()`，不新开一条 `action:'clear'` 调用路径（同一件事写两遍必然有一处走偏）。带二次确认、请求期间禁用按钮。
    * 查看已保存训练记录时隐藏——丢弃待确认训练与删除已保存记录是两件事，后者入口在数据管理里。
    * 多活动导入时丢弃其中一个会自动回到选择器处理剩下的，不把同批的其余活动一起放弃（配合上面 BUG-08）。
    * 后端无改动，`/workout_state/update` 的 `action: "clear"` 已存在。
12. 修掉启动时反复弹出的「隔离训练可恢复」弹窗（我自己上午引入的缺陷）
    * 根因：提示只看磁盘上有没有 `pending_workout.corrupt-*.json`，**没有任何"已处理"状态**；而且用的是阻塞式 `window.confirm`。点"取消"不留痕迹，点"载入"之后重放又刻意不删文件（重放本该是非破坏性的），于是同一个弹窗永远拦在进入路径上。
    * 启动改为**非阻塞提示**（addNotify 一行），并跳过既无分段也无心率的空壳文件。
    * 新增旁挂标记 `<文件名>.dismissed` 表示不再提醒：`list_quarantined()` 默认跳过，`include_dismissed=True` 才返回全部；数据一个字节都不动。
    * `replay_quarantined()` 成功后自动标记不再提醒——处理过就不该再问。
    * 新增 `POST /workout_state/quarantined/dismiss` 与 `/delete`，数据管理面板加「被隔离的未确认训练」区块，每条可载入编辑 / 不再提醒 / 永久删除。
    * 已把现存两份隔离文件标记为不再提醒（文件保留）。
13. 修复添加器械bug：Agent 提取的是 {"equipment": []}，确认接口随后直接执行 profile_store.update_profile(updates)，而 UserProfileStore 对列表字段是整体替换。

### 2026_08_21
1. DATA-06：全仓库补 `fsync`，抽出唯一的持久化写入点 `fithealth_agent/atomic_json.py`
    * `Path.write_text` 只写到 OS 页缓存，`os.replace` 只保证**目录项替换**原子——它对"内容有没有落盘"不作任何承诺。Windows 上突然断电，目录项已经指向新文件而数据块还没写出去，`daily_records.json` 就是 0 字节或半截。
    * `atomic_write_json(path, data)` = 写临时文件 → `flush` → `os.fsync(fileno)` → `os.replace`，之后再 best-effort `fsync` 一次父目录（让 replace 这次目录项变更本身也落盘）。Windows 打不开目录句柄，那里直接跳过；这一步失败也不让一次已经 fsync 过内容的写入报错——最坏是崩溃后看到替换前的旧文件，那仍然是一份完整数据。
    * 临时名统一 `<name>.<pid>.<uuid4>.tmp`。固定的 `<name>.tmp` 正是下面 DATA-07 产出拼接垃圾的直接原因，所以两条收在同一个工具里。
    * 接入：`storage.py`（`DailyRecordStore._write_all` + **首次建库那次 bootstrap**，它原先是裸 `write_text("[]")`、`UserProfileStore._write`）、`plan_store.py`、`external_model_settings.py`。
    * **额外补一处审查时漏掉的**：`external_model_settings.py` 存的是"要不要把数据发给外部模型"的隐私开关，而 `_read` 读失败时静默回落到 `DEFAULT_SETTINGS`（`external_models_enabled: True`）——断电读到 0 字节，会把用户显式关掉的开关又打开。
    * `info_store` / `workout_store` / `hr_stream_store` 三处早先已就地落地（DATA-05、DATA-08），本次**没有**回头重构：它们各自带降级判断与专用 payload 组装，动它们的收益不如风险。改用源码断言钉住"这三个模块的写路径必须含 fsync"。
2. DATA-07：`UserProfileStore` 加锁，并把丢更新的窗口从 main.py 收回 store 内
    * 原实现 `_write` 既没 `RLock` 也没 `JsonFileLock`，tmp 名还是固定的 `user_profile.json.tmp`：两个并发 PATCH 同时 `open("w")` 同一个 tmp → 交错写入 → replace 出去的是两份 JSON 拼在一起的垃圾。
    * 公开方法（`get_profile` / `update_profile` / `reset`）全部套 `RLock` + `JsonFileLock`，`__init__` 的首次建档也加锁并**双重检查**（否则两个实例同时初始化时后来者会把前者刚写好的档案覆盖回默认值）。读路径也加——档案是个整体，读到一半被换掉没有意义。
    * **私有方法 `_read` / `_write` / `_normalized_profile` 刻意保持无锁**：`JsonFileLock` 每次进入都新开一个句柄，**不可重入**，在私有方法里再套一层就是死锁。这条有源码断言钉住，别好心加回去。
    * 真正丢更新的地方不在 store 而在 main.py：`/profile/confirm-update` 原先是 `get_profile()` → `merge_profile_updates_with_existing()` → `update_profile()`，三步之间的窗口让两个并发确认读到同一份旧档案，后写的把前一个追加的器械直接抹掉（就是 08-20 第 13 条那个器械 bug 的并发版本）。现在 `update_profile(updates, *, merge=...)` 接受合并回调，合并用的是锁内刚读到的档案。
    * 新增测试 `tests/test_durable_writes_and_profile_locking.py`（19 条）。取向是钉不变量而不是数调用次数：fsync 必须发生在 replace **之前**（记录式 fsync 被调用时读回目标文件，此刻必须还是旧内容）、临时名互不相同且带 pid、写入中途失败时原文件逐字节不变且不留 `.tmp`、12 个线程跨 3 个实例各追加一件器械后必须全部在册。最后一条已用还原成旧实现的子类实测对照：**15 轮累计丢失 165 条追加**，证明断言不是空转。
3. DATA-09：SQLite journal 不再静默降级到 MEMORY，临时目录回落改为只读
    * WAL 失败按 `("WAL", "DELETE")` 依次退，**绝不用 MEMORY/OFF**——那两种模式下回滚日志不落盘，崩溃或断电会留下结构性损坏的 health.db，而用户毫不知情。
    * 关键不在于"换个 fallback"，而在于**怎么判断失败**：`PRAGMA journal_mode = X` 被拒绝时**不一定抛异常**，它会返回当前实际生效的模式。原实现只 catch `OperationalError`，这种静默失败根本抓不到。新的 `_apply_journal_mode()` 靠读回返回值判断。
    * 临时目录回落改为只读：`_use_temporary_storage()` 设 `_degraded_reason`，`save_import`/`delete_import`/`clear` 开头 `_require_writable()` 抛 `HealthStoreDegradedError`，main.py 加全局处理器回 503 + 库路径 + 恢复指引。**读接口照常可用**——降级的目的是别让整个应用挂掉。原实现允许写，Windows 清理 %TEMP% 时那些数据直接蒸发，而用户全程看到"导入成功"。
    * 新增 `HealthStore.storage_status()` 与 `revalidate()`，`GET /health/storage-status` 返回 `health_store` 明细并自动试一次 revalidate——修好权限后无需重启。journal 模式不安全时即使数据当下没坏也带一条显式警告。
4. DATA-11：备份补全内容 + 每成员校验和，格式升到 v2（v1 老备份仍可恢复）
    * 补进 `health-imports/`（唯一可重新解析的数据源）、`hr_streams/`、`pending_workout.json`、`external_model_settings.json`。**`hr_streams/` 是 08-19 审查时还不存在的新缺口**——DATA-05 才把 1Hz 心率流旁挂出去，`daily_records.json` 里只留摘要 + file 指针，不备份它就是又一批悬空引用。
    * 可选 JSON 只在**存在时**打包："没有待确认训练"本身是有意义的状态，不能拿空对象顶替。
    * manifest 加 `members: {名: {sha256, bytes}}` 并双向核对（zip 有而 manifest 没登记 → 拒；manifest 登记而 zip 没有 → 拒）。这才是"结构合法但内容被替换"能被抓住的地方。
    * 备份是外部输入，按外部输入处理：成员名只接受白名单顶层文件与 `<备份目录>/<单层文件名>`，`..`／绝对路径／反斜杠／多层嵌套一律拒绝；加解压后总大小上限 300 MiB（现在会展开整个目录）。
    * **`raw_path` 不需要改写，而且刻意没加**：库里存的已经是裸文件名（DATA-02），`resolve_raw_file()` 也只信任文件名，老备份里的绝对路径同样能对上。多一处改写逻辑只是多一处会腐坏的代码，改用测试钉住这个前提。
    * 顺带修掉一个由本条引出的新 bug：`/data/backup/import` 恢复后原先调 `clear_current()`，在备份不含 pending_workout.json 的年代是对的，现在会把刚恢复回来的待确认训练删掉。改为新增的 `workout_store.reload_from_disk()`。
5. DATA-12：恢复过程三重隔离，加锁顺序固定 gate → JSON 锁 → 数据库
    * 新增 `fithealth_agent/maintenance.py`。`MAINTENANCE.exclusive()` 先竖开关（main.py 的 HTTP 中间件让新请求 503），**再等在飞请求排空**——顺序反过来在飞计数永远归不了零。诊断接口维护期间放行，否则用户被挡住后连"系统在干什么"都看不到；发起恢复的那条请求自己不计入在飞计数，否则排空会等它自己、直接死等。
    * **为什么是"排空 + 超时"而不是直接抢锁**：恢复要同时持有若干 JsonFileLock 和 HealthStore 访问锁，一个已经拿着 daily_records 锁、接下来还要查 health.db 的在飞请求会与恢复形成循环等待。排空把它变成一次干净的失败（409，数据一个字节没动），而不是一次挂死。
    * `restore()` 按固定顺序拿四个 JSON 的 `JsonFileLock`（导出走同一顺序，所以两者不可能反向等待）。不持锁时并发 `add_record` 拿着恢复前的列表在恢复后写回，**两边都"成功"，恢复结果被静默覆盖**——已用还原成旧行为的子类实测：8 轮 8 次被破坏。
    * `HealthStore` 所有连接改从一把 RLock 下取，新增 `exclusive_access()` 与 `reopen()`。`-wal`/`-shm` 只在独占期内删除，`reopen()` 也在独占期内调用——换库与重新校验 schema 之间不能有别人插进来。
    * 新增测试 `tests/test_journal_backup_and_restore_safety.py`（41 条）。当前基线 **446 通过**。
6. 修了两处与被测行为无关的测试假失败，全量转绿（**446 通过 / 0 失败**）
    * `test_data_fixes_20260819.py`：`sqlite3.connect(...).execute(...)` 从不关连接，留着的句柄让 Windows 上的 `TemporaryDirectory` 清理直接 `PermissionError`。
    * `test_data_dir_and_docker.py`：`Dockerfile` 已确认改用 `python:3.12-bookworm`，而测试还在字面断言 `python:3.11-slim`。改为**断言真实约束**——从 `ARG BASE_IMAGE=python:X.Y` 解析版本号并要求 `>= (3, 11)`（StrEnum 的下限，ARCH-05），再单独钉住"必须是可覆盖的官方 python 镜像、不能是 `FROM fit_health_agent:dev`"。换 tag 是运维决策，不该每次都让测试变红；但把版本降到 3.10 或换成非 python 镜像仍然会被拦下（已实测这两种情况都失败）。
7. DATA-13：合并已保存训练组不再丢弃心率与可加性字段
    * 原实现 `merged = {**targets[0], ...}` 只重算 duration/reps/weight/category，其余数值字段沿用**第一段**的值，还把 `avg_hr`/`max_hr` 硬写成 `None`。已保存记录没有副本，所以每次合并都是一次不可逆的信息丢弃。
    * 心率改三档：**有旁挂 1Hz 流 → 在新时间窗内精确重算**（用 `fit_parser.compute_hr_in_window`，与待确认训练的 `merge_sets` 同一套口径）；没有流但各段有 avg_hr → **按各段时长加权**还原、峰值取各段最大；两者都没有才是 None。
    * **清单里"历史记录未保留原始心率流"这句已经过期**：DATA-05（08-20）把流旁挂出去之后，08-20 起保存的记录都能精确重算，只有更早的老记录才走加权近似。所以接口把 `hr_stream_store.load(record_id)` 传进合并函数——不接线等于 DATA-05 存下来的流白存了，这条有源码断言钉住。
    * 字段按语义分三类：可加性（distance_m、calories）求和；峰值（max_speed_mps）取最大；均值（avg_cadence、avg_power_w）按时长加权。`avg_speed_mps` 优先用"总距离 ÷ 合并后总时长"，那是它的定义。
    * **不给原本没有这些字段的力量组凭空造 `distance_m: 0`**：当前格式下 `set_active` 的 `to_dict()` 根本不输出有氧字段，凭空补的 0 会进到 ReAct 观察里，还看着像"真测到了 0"。只聚合至少有一段存在的字段。
    * 刻意保留 `{**targets[0]}` 展开、只显式覆盖需要聚合的字段——白名单式重建会把将来新增的字段静默丢掉。
    * 原有测试 `test_merges_saved_sets_without_claiming_recomputed_heart_rate` **本身在断言这个缺陷**（要求 avg_hr is None、notice 含"无法重新计算"），已替换。加权那档钉死 `(120×30 + 125×40)/70 → 123`（算术平均会给 122，所以这条真能区分加权与否）；有流那档钉死 144/170。实测端到端：同一份记录无流 → 123/138，挂 5 条 1Hz 流 → 144/170。当前基线 **457 通过 / 0 失败**。
    * **未做**：没加"撤销合并"。修好之后合并对聚合字段已经是信息保全的；再加撤销要新端点 + 前端入口 + 存一份合并前的段副本，最后一项会让记录体积翻倍并进入 LLM 上下文，与 DATA-05 定下的"记录里只留体积恒定摘要"冲突。真要做应单开一条。


8. DATA-14：`/data/reset` 删除前先落恢复点，删除本身改为逐项容错并返回明细
    * 清单给了两条修法（落快照 / 逐项 try+返回明细），**两条都做了**——快照给的是退路，逐项容错给的是"把删除做完并且说清楚"，哪一条单独都不够。
    * **这不是理论风险，是一条可达路径**：`info_store.clear()` 在记忆库只读降级时抛 `MemoryStoreDegradedError`，`health_store.clear()` 在数据库只读降级时抛 `HealthStoreDegradedError`，**两者都是 `RuntimeError` 子类**，原来那句 `except (OSError, sqlite3.Error)` 抓不到，异常直接逃到全局处理器变成 503。用户看到"记忆库只读降级"，完全不知道训练记录和训练计划已经被永久删掉。已用还原成旧实现的对照实测：`1 → 0` 条记录，响应体里**没有任何 `_removed` 计数**、没有明细、没有恢复点。
    * **删除前先落恢复点，写不出来就一个字节都不删**。`/data/reset` 是全仓库唯一不可逆的批量删除，没有退路的删除不该执行。快照走 `export_bytes()` 落到 `data/recovery-points/pre-reset-<14 位时间戳>.zip`，写法与 JSON 落盘同构（临时文件 → `flush` → `os.fsync` → `replace`），否则"恢复点已生成"可能只是页缓存里的一句空话，正好在断电时和数据一起消失。写失败**抛出**而不是返回失败标记，调用方因此必须中止（500，数据一个字节没动）。
    * **恢复点刻意就是一份普通备份 zip**，所以"怎么还原"需要的新代码是零：下载后走已有的 `/data/backup/import`，`validate()` 原样接受它。`recovery-points/` 刻意**不进** `BACKUP_DIRECTORIES`，否则第 N 份快照会套进第 N+1 份里。
    * **命名必须严格单调**。时间戳只到秒，半失败后立刻重试会撞进同一秒，而后一份快照拍的是**已经被清空**的数据，覆盖等于把唯一一份真数据的恢复点弄丢。修法不是"找个空位"：prune 删掉的是名字最小的那份，腾出的空位在**过去**，填进去会让新快照拿到比现存更旧的名字，紧接着的 prune 又把它当成最老的删掉——实测踩到过，刚写好的快照当场消失。改成 `max(now, 现存最大 + 1s)`（定宽 + 严格递增，字典序即创建顺序），同时给 prune 传 `keep=` 显式排除刚写的那份。
    * **保留 3 份**：一个恢复点等于一份完整数据（含 `health-imports/`），无上限攒下去会吃掉磁盘。这同时是隐私问题——用户点了"删除全部数据"，盘上却留着全量副本，所以响应里显式报告恢复点，前端加「重置前恢复点」区块可下载/永久删除，明说而不是偷偷留着。
    * **逐项 try/except，`except Exception` 是刻意的宽**：8 个互相独立的破坏性操作，"一个失败就把后面全部跳过"正是这条需求描述的缺陷，而窄 except 又抓不到上面那两个 `RuntimeError`。
    * **步骤顺序按"先清引用方，再清被引用方"**：`daily_records.json` 按 id 引用 `hr_streams/`，`health.db` 的 import 行按文件名引用 `health-imports/`。这个顺序中途失败留下的是没人引用的孤儿文件（占点磁盘，读不到也不报错）；反过来会留下悬空引用——记录还在、指向的流没了，那是静默的数据损坏。
    * **补了第 8 步 `plan_draft_cache`**（原实现只有 6 步 + profile）。它是进程内缓存，但存着完整的训练计划正文（BUG-05）；"删除全部数据"之后它还在内存里，下一句"保存刚才的计划"就能把已删内容写回磁盘。
    * **持维护开关**（DATA-12 的 `MAINTENANCE.exclusive`）：新请求 503、在飞请求排空，快照与删除之间没有别人插进来写数据，否则刚写入的记录会被快照漏掉却仍被删掉。`/data/reset` 必须进 `MAINTENANCE_UNTRACKED_PATHS`，否则它持开关后排空会等自己、直接死等到超时。
    * **半删状态返回 200 + `partial: true`，不是 5xx**。客户端必须能读到究竟删了哪些；5xx 会让前端只显示一句"操作失败"，正是原实现的毛病。响应里既有 `steps` 逐项明细（每项带 `removed` 与 `error`），也把计数平铺成顶层字段保持与旧响应兼容。前端相应地**不能再看到 error 就提前 return**——那会跳过 `refreshWorkoutState` 与 `loadDataOverview`，界面停在删除前的旧视图上，而磁盘上七项已经没了。
    * **刻意没做自动回滚**：用户明确要求删除全部数据，失败后自动还原是把他的意图撤销掉，他只会再点一次撞进同一个失败；重置本身是幂等的，还原与否该由用户看着明细自己决定。
    * 新增测试 `tests/test_reset_transactionality.py`（32 条），当前基线 **489 通过 / 0 失败**。除上面的对照实测，另钉住：恢复点 zip 能被现成 `validate/restore` 吃下去、fsync 必须发生在 replace **之前**（fsync 时目标文件还不该存在）、写失败后既不留半截目标也不留 `.tmp`、同一秒连写 4 次名字互不相同且单调、刚写好的快照永远不被 prune、外部传入的名字（`../health.db`、`pre-reset-1.zip`、`%2e%2e/x` 等）一律拒绝、以及前端确实接线。
9. BUG-09：每日打卡改为按日期 upsert，同一天再保存是更新而不是再加一条
    * 原实现只调 `add_record`，每次生成新 uuid 追加；同日点两次保存就并列两条 daily_checkin，而 `PATCH /data/checkins/{record_id}` 前端零调用、是完全不可达的死端点——"改一下今天的体重"这件事根本做不到。实测对照旧实现：同日两次保存得到 2 条，权重 `[71, 70.5]`。
    * **去重放在 store 的锁内，不在端点里先查后写**。新增 `DailyRecordStore.upsert_dated_record()`，查重与写入同处 `_write_lock + JsonFileLock`。端点先查再写的话，两个并发请求会同时判定"当日无记录"、各自追加一条——DATA-07 在 profile 上踩过同一个坑。实测 8 个并发请求后仍恰好 1 条。
    * **记录体按字段合并而不是整体替换**：早上记体重、晚上记热量是正常用法，整体替换会把早上那条抹掉。代价是没法把某个字段改回"空"，相比静默丢数据这是更可接受的一侧。
    * **磁盘上已有的重复记录折叠而不是删除**：旧→新依次合并（新值覆盖旧值），保留最早那条的 id 与 created_at，折叠条数原样返回并显示给用户（"记录数少了"不该看着像丢数据）。实测 3 条折叠成 1 条，`weight_kg`/`note`/`energy_level`/新写入的 `fatigue_level` 一个都没丢。
    * **死端点是删掉而不是接线的**：它按 record_id 定位却接受任意 date，能把一条记录改到另一个已有打卡的日期上，正好又造出这次要消灭的重复。留着等于给刚修好的缺陷开个后门。
    * 前端：打开表单时载入当天已有记录（`GET /data/checkins/{day}`，"这天还没打卡"返回 `checkin: null` 而不是 404——那是正常状态不是错误），并提示"保存将更新这一条"。**改日期会重新载入**，否则表单里留着的是上一天的值，一按保存就写进了另一个日期。餐盘照片那条路径改 `await openCheckin()`：预填现在是异步的，不等它结束就写字段，估算出的卡路里会被预填覆盖掉。
10. BUG-10：缺 session 的 FIT 不再静默丢光整场训练（**清单的前提是错的**）
    * **不会抛 AttributeError**。`parse_fit_file` 取的是 `raw_msgs.get("session", [{}])[0]`，缺 session 时得到的是字段全为默认值的 `SessionSummary`，**`session` 恒非 None**。清单里那句 `sess.sport if sess else "未知运动"` 确实是死代码，但它不是"作者知道会 None"的证据，反而正是它让人以为"缺 session 会崩"，把注意力从真缺陷上引开。已删。
    * **真正发生的是静默丢光**：缺 session → `sport_raw="unknown"` → 路由无依据 → `FallbackParser` → 一律走 lap → 没有 lap → `_from_session()` 因 `start_time is None` 返回 `[]`。**`set` 消息从头到尾没被看一眼。** 实测同一份含 10 组卧推的 FIT：有 session 解析出 10 组，去掉 session 消息后 **0 组**，而用户看到的是一句「[FIT 解析完成]」。中断的活动往往正是"有 record/set、没写出 session"的形状，且原始 FIT 通常已不在磁盘上——等发现时拿不回来了。
    * **改为按消息证据路由**：`FallbackParser.parse` 先看有没有 `set` 消息（set 的存在本身就是"这是力量训练"的确凿证据），有就交给 `StrengthTrainingParser`，没有才退到 lap。走到 Fallback 就说明 sport 没能给出裁决，那就该问文件里到底有什么，而不是假定它是有氧。
    * **`_from_session` 补 record 流兜底**：既无 session 也无 lap 时用 `hr_records` 首尾时间戳构造时间窗（段上标 `lap_trigger="records"` 以便区分是不是近似出来的），而不是把整条心率流一起丢掉。
    * **一段都没解析出来时不再报告成功**：返回 `status: "empty"` + 明确说明并要求保留该 FIT，同时清掉待确认区（否则用户会对着空壳点"确认并保存"，而 confirm 会因缺开始时间失败）。**刻意不返回 5xx**——文件读得动、解析没报错，说成服务器错误同样是误导；前端单独处理这个状态，不套"上传失败："前缀去让用户重传一个传得好好的文件。
    * **`HealthImportError` 单独 catch**（清单这条说得对）：它带着精确中文原因和自己的 status_code，被 `except Exception` 吞掉后用户看到的是"请确认文件完整"，于是去检查一个完好无损的文件。实测修复前 500 + 通用文案，修复后 400 + 原文。窄 except 必须排在兜底之前，有测试钉住这个顺序。兜底文案也不再断言"文件损坏"——走到那里说明是未预期异常，代码 bug 与坏文件同样可能。
11. BUG-11：合并按钮不再永久变灰，且合并会带上未保存的编辑
    * 按钮确实永久变灰：`editorBusy` 置位 2 处，复位 10 处但全在"保存/合并/切换记录**成功之后**"。改了任一输入框再想合并，按钮就一直是灰的且无任何提示。
    * **但只按清单把 `editorBusy` 从 disabled 里删掉是不够的，那等于用丢数据换按钮可用。** 它当初拦住的是真问题：合并只发 `{action:'merge_sets', indices}`，**不带 updates**，服务端拿自己那份旧数据合并、前端随后重渲染，用户没保存的"上斜卧推 80kg"被静默丢弃。实测对照：改成 80kg/12 次后合并，得到的是 `category='卧推' weight=60 reps=20`。
    * **修法是让合并把编辑区草稿一起提交**，与已保存记录那条路径（`updates + merge_indices` 一次提交）对齐：`merge_sets(indices, updates, note)` 先应用编辑再聚合。**顺序不能反**——聚合要读 category/weight/repetitions，反了读到的就是旧值，有测试钉住。修复后同一操作得到 `上斜卧推 / 80kg / 24 次`。
    * 编辑无效时**整个合并被拒**，不留"编辑应用了一半、合并没做"的状态；`updates` 为 None 时（Agent 路径、没刷新的老页面）行为与从前完全一致。
    * 这之后 `$merge.disabled` 才改成只看 `count < 2`。**`editorBusy` 本身保留**——它抑制轮询覆盖正在编辑的字段，那个用途是对的，连它一起删掉会让 3 秒一次的轮询把用户正在改的值冲掉。
    * 新增 `tests/test_checkin_fit_and_editor_fixes.py`（35 条）覆盖上面三条。每条修法都**逐一还原成旧实现实测过**：6 次还原分别被 5 / 4 / 2 / 4 / 4 / 1 条测试抓住，没有一条断言是空转的。当前基线 **524 通过 / 0 失败**。
    * 顺带修掉 `test_reset_transactionality.py`（昨天 DATA-14 留下的）的一处顺序依赖：它把 main 的首次 import 绑到用例自己的临时目录上，而 tearDownClass 会删掉那个目录，于是 `main.profile_store` 永久指向不存在的路径——某些执行顺序下会让 `test_journal_backup_and_restore_safety` 里一条完全无关的中间件测试变红。改用进程级目录做那次 import。**已在 7 种随机顺序下复跑，全部 524 通过**（随机顺序不是摆设：正是它抓出了这个问题，按字母序跑是绿的）。
12. 修复保存计划判断的逻辑错误

13. BUG-14：view_* 只在"纯导航"时短路，复合请求挂起 artifact 继续走 Agent
    * 实测确认两个例子都是 **Agent 完全未被调用**：「帮我看看我的训练记录，上次卧推推了多少公斤？」只得到"已打开本地训练记录"，重量问题无人回答。
    * **清单给的第一种写法（一律"附加 artifact + 继续走 Agent"）不能采纳**：短路正是 BUG-02 建立的离线本地读路径，关闭外部模型时「查看训练记录」全靠它，一律走 Agent 会让那条路变成 503。所以走第二种。
    * 新增 `navigation_only_message()`：把动作词、宾语、日期、礼貌语与标点整个剥掉，剩下还有字就说明另有诉求。**按长度降序剥离**，否则"看"会先把"查看"啃掉半截。
    * **误判方向是刻意选的**：纯导航被误判成"有额外诉求"只多花一次模型往返，用户仍拿到面板；反过来才是这条 bug 本身。所以没见过的说法一律按"有额外诉求"处理，测试钉住了这一点。
    * 训练记录有 artifact → 挂起（`pending_view_artifact`）后继续走 Agent；个人档案**没有** artifact，"看到的东西"就是那段正文 → 作为 `pending_reply_prefix` 前缀留给最终回复，且与短路分支逐字相同，两条路径不会给出两种说法。
    * **计划保存卡优先于挂起的查看面板**（前端一次只渲染一个 artifact，需要用户操作的那张更重要）。挂起的面板也接到了 onboarding 与本周汇总两条分支上——不接线的话复合请求会拿到面板为 None，等于换了个方式丢东西。
    * 离线 + 复合请求：仍然短路并给出面板，但明说"外部模型当前已关闭，这部分我无法回答"。静默丢弃正是这条 bug 的本体，离线也不例外。

14. BUG-15：压力数据时区——**清单的前提不成立，其建议修法会亲手造出这条 bug**
    * 清单自己写了"两种解释不可能同时成立，当前实现等于在赌"并给了验证方法。按那个方法验证，取三份真实 wellness ZIP：stress 的 naive 时间戳当 UTC 解释后，跨度与**同一份文件里** monitoring 心率的 `timestamp`（`utc=True`，语义确定）**逐分钟重合，偏差 0.0h**；当成本地墙钟则整体差 **8.0h**。三份结论一致。
    * 独立第二证据：同一批文件里 `monitoring_info` 同时带 `timestamp` 与 `local_timestamp`，两者之差确实是 **+8h**。说明 fitfile 的 `utc=False` 在**那个**字段上是对的，只是被错用到了 stress 上——FIT 规范里 stress_level 字段 1 是 `stress_level_time`，基类型 `date_time`（UTC 基准）。
    * 所以现有时区解释是对的，清单建议的 `local_ts.replace(tzinfo=BEIJING)` 会把全部压力数据提前 8 小时。且那个写法的第一分支 `values.get("timestamp")` **永远取不到值**——实测 stress_level 消息里根本没有 `timestamp` 字段（0/60）。
    * **但它是碰巧对的**，这才是要动的地方：`_utc_datetime` 对任何 naive 值一律当 UTC，代码里原先没有任何地方记下"这里为什么可以"。契约已写进它的 docstring（`date_time` 是 UTC；真正的本地墙钟字段是 `local_date_time`，那种值不能走这个函数），并用测试钉住"naive 04:00 → 北京 12:00"，同时显式断言**不是**前一天 20:00。
    * **不再就地覆盖 `timestamp` 变量**：原实现在 stress 分支里把它换成另一个字段，而它同时是 `_timed_value(...)` 与 hrv_status 分支读的那一个。分支目前互斥所以没出事，但那是运气不是设计，已改用独立的 `metric_time`。
    * **字段改名不再静默丢光数据**：原实现取不到就是 None，随后 isinstance 检查失败，**1440 条/天的压力样本一条不剩地被跳过且不留痕迹**。现在按「规范名 → 通用名 → fitfile 当前的错名」依次尝试（上游自己就认为这个名字是错的），三个都取不到时记 warning 而不是沉默。

15. BUG-16：按天趋势与日汇总共用同一份去重 SQL
    * 在真实 `health.db` 上量到规模：`heart_rate_samples` 16220 行 / 去重 15005（**重复 7.5%**）、`health_metric_samples` 50684 行 / 去重 46971（**7.3%**）；**2026-08-15 整日每个指标的趋势 samples 恰好是日汇总的 2 倍**（心率 2424 vs 1212），68 组比对里 **11 组不一致**。
    * **刻意不在写入侧去重**：每行都记着 `source_file_id`，`delete_import` 靠它精确回退某一次导入。表上本来就有 `UNIQUE(source_file_id, timestamp_utc, …)`，它挡的是同一个 FIT 内的重复，挡不住跨导入重叠；但**也不能**收紧成不含 `source_file_id` 的全局唯一——那样两次导入共享一行，删掉任一个都会连带抹掉另一个的数据。有测试钉住这个边界。
    * 去重仍放读取侧，但**抽成 `DEDUPED_HEART_RATE_SQL` / `DEDUPED_METRIC_SQL` 两个模块级常量**，由 `_rebuild_daily_summary` 与 `get_metric_trend` 共用。"同一口径"于是是结构上的而不是注释上的——原先两处各写一份，其中一份漏了去重，正是这条 bug。
    * `MIN(timestamp_local)` 而不是任取一个：重复行理论上可能带不同本地时间字符串，取 MIN 让小时分桶确定，且与日汇总的 `coverage_start` 用同一个值。metric 过滤刻意放**外层**：内层必须与日汇总逐字一致，否则又变成两份要各自维护的 SQL；一天 5 千量级的行数下代价可忽略（20 次调用 43–61 ms）。
    * 修复后在真实库复跑：**68 组全部一致**，2026-08-15 原始表 2424 行 → 趋势 samples 1212 = 日汇总 1212。均值口径一并核对：用趋势 samples 加权还原出的日均值与日汇总逐日一致（差 ≤ 0.15）。`week`/`month` 读 `daily_health_summary`，本来已去重，未动。
    * 值的失真在当前数据里较小（只 1 个小时桶差 0.1 bpm），因为重叠多是逐字复制——**整小时被完整复制时均值不变，只重复一部分才会把那几分钟的权重翻倍**。测试专门造了这一档：10 分钟 60 bpm + 10 分钟 180 bpm，只把前 10 分钟再来一遍，不去重得 100.0、去重得 120.0。
    * 新增 `tests/test_view_intent_stress_tz_and_trend_dedup.py`（31 条）覆盖 BUG-14/15/16。每条修法都**逐一还原成旧实现实测过**，5 次还原各被 4 条测试抓住；其中特意还原出"照清单改"的那一版（`replace(tzinfo=BEIJING)`），同样被 4 条抓住——包括钉解释的那条，正是它拦住了这次差点踩进去的 8 小时偏移。当前基线 **566 通过 / 0 失败**，7 种随机顺序下复跑全绿。

### 2026_08_22
1. DATA-15：wellness 包里四类一条都没入库的数据全部接上
    * 起因是清点了一份真实的 `2026-08-21.zip`（16 个 FIT：11 WELLNESS + 1 SLEEP_DATA + 1 HRV_STATUS + 4 METRICS），逐字段核对"文件里有什么"与"库里存了什么"。
    * **静息代谢率**（`monitoring_info.resting_metabolic_rate` = 2230 kcal/天）。`daily_health_summary` 早就有 `resting_calories` / `total_calories` 两列却 **16 天全空**——读取方去 `monitoring` 里找 `resting_calories`/`bmr_calories`，而这个数其实在 `monitoring_info` 里，**而那条消息整个没被处理**。总热量按"静息 + 活动"推导（Garmin 自己的定义），**只在两项都有时才给**：把缺失当 0 会产出一个看着像真值的错数。
    * **静息心率**（Garmin 私有消息 `unknown_211`，**逆向推断**）。两条证据：`unknown_1` 在 2026-08-14 取到 56，与那天睡眠 CSV 的「静息心率 56 bpm」**精确一致**；跨 15 天稳定在 55–63，而当日睡眠时段最低心率 52–58（静息心率是一段均值不是最小值，略高是对的）。`unknown_0` 几乎恒为 57–59，更像 7 日基线 → 存成 `resting_heart_rate_baseline`，**名字上不声称它一定是什么**，界面也标注"来自手表私有字段，非官方接口"。带 25–120 范围闸门：逆向字段猜错了也不该把垃圾写进库。
    * **强度分钟** 两个坑：(1) `moderate_activity_time`/`vigorous_activity_time` **是区间增量不是累计值**——实测同一天出现 60s/180s/60s/240s，累计量不可能回落，必须 SUM，取 MAX 会把 5 分钟报成 3 分钟；(2) 它们**不与任何活动字段同现**，所以挂不到 `daily_activity_observations` 上（那张表只在有活动字段时建行），这就是它们以前一条都没进去的原因。汇总按 Garmin/WHO 口径：中等 + 高强度×2。
    * **设备 UTC 偏移**（实测 15 份包全是 +480）。这里的 `local_timestamp` 是**真正的本地墙钟**，与 BUG-15 里 stress 那个同名字段正好相反，所以算偏移时刻意不走 `_utc_datetime`——走了会得到 +16h。
    * 新增两张表：`device_daily_metrics`（**每列各取"该列非空的最新一条"**——RMR 来自 `monitoring_info`、静息心率来自 `unknown_211`，时间戳不同，一行里通常只有一列有值，不能整行取最新）与 `intensity_observations`（去重口径与 BUG-16 一致）。
    * **每天开头往往还留着前一天的静息心率**（实测 08-21 00:07 是 61，当天真实值是之后稳定的 55），按 `local_date` 取当天最后一条正好避开它。
    * 顺带修掉一个由本条引出的缺陷：`delete_import` 收集"受影响日期"时只列了 5 张表，漏掉新增的两张。跨零点那天常常**只**有一条静息心率，漏掉就删不掉日汇总行，界面上留下一个没有来源的幽灵日期。这条是测试先红出来的。
2. 修正 FIT 睡眠时长口径，并补上夜间心率
    * `get_sleep` 的 FIT 兜底把**清醒时间也算进 `duration_min`**。睡眠 CSV 的「睡眠时长」是深+浅+REM（6时48分 = 101+222+85，清醒 43 分钟不计），而 FIT 兜底用同一个键名给出全部阶段之和，界面上完全看不出差别。真实数据 2026-08-21 多报 156 分钟（537 vs 381）；`memory/traces/` 里那次对话给用户报的 459 分钟实际只有 319 分钟。`weekly_summary` 读的是同一个键，一并修正。躺床总时长另出 `time_in_bed_min`，不丢信息。
    * 睡眠分数与质量是 Garmin 云端算的、FIT 里没有；但**夜间平均/最低心率手上已经有全部原料**（分钟级心率 + 睡眠阶段时间线）。对照 2026-08-14 睡眠 CSV 的「夜间平均心率 60 bpm」，算得 59.9。
3. 回灌脚本 `scripts/backfill_health_fields.py`，并在真实库执行
    * 重解 `data/health-imports/` 下已存的原始包，**动库前先落恢复点**（复用 DATA-14 那套：普通备份 zip，出问题下载后走「导入备份」即可整体还原），缺省只预演。
    * `--extra-source` 可指向别处的原始包目录，补齐 raw 文件已丢失的记录（08-15/08-16 的 raw 早期没留）。但**按同名取来的文件必须逐字节核对 sha256**——实测真的拦下了一份同名但内容不同的 `2026-08-15.zip`。
    * 已执行：22 份重放成功、1 份校验不符跳过。15 天补上 RMR/总热量/时区，16 天补上静息心率，9 天补上强度分钟。BUG-16 的"趋势与日汇总同口径"不变量复查 **78 组 0 不一致**；睡眠 15 天逐日核对 `duration_min == 深+浅+REM` 且 < 卧床时长。
4. DATA-16：身体电量与 METRICS 文件**刻意不猜着存**，只归档线索
    * 身体电量疑似在 `unknown_233`（每天 1442 条，与压力同频，恰好一天分钟数），字段是 4 字节位域：`byte[3]` 取值集中在 8 的倍数，`byte[3] & 0x7f` 有 86% 落在 0–100 但明显不是直读，LE/BE u16 组合也都不成立。**存错的身体电量比没有更糟**，所以留着不动。
    * 4 个 METRICS 文件目前只产出一行时区偏移。已观察到的规律（`unknown_339` 恒为 1602/3525/8230/19067 像配置、`unknown_378.unknown_0` 日内递增 52→73→80、`unknown_232.unknown_11` 与强度分钟量级接近但对不上）全部记进需求清单 DATA-16，以后单独做。
    * 经确认**不存**的低价值字段：`cum_ascent`/`cum_descent`（FR255 无气压计，实测全 0）、`cycles`（已被 steps 覆盖）、`hrv_status_summary.baseline_balanced_low/high`（40960/128 = 320 ms 不是合理 HRV 基线，标度可疑）、`event.data16`、设备型号与固件。
5. 新增 `tests/test_extended_health_fields.py`（35 条）。10 处逐一还原成旧实现实测，各被 1–3 条测试抓住。当前基线 **601 通过 / 0 失败**，9 种随机执行顺序下复跑全绿——过程中随机顺序又抓出一个问题：本文件按路径加载模块时塞的桩 `fithealth_agent` 包没还回去，导致之后任何 `import main` 在 `from fithealth_agent import create_fithealth_agent` 处炸掉，让另外两个文件的 setUpClass 直接 ERROR。改成保存并原样恢复 `sys.modules` 里的三个条目。
6. 删掉后端的 Mifflin-St Jeor 基础代谢估算
    * 手表自己算的静息代谢率现在会入库，是**实测值**（2257 / 2230 kcal/天）。同时再给一个公式估算只会让两个数打架；而且档案缺一项就退化成"待补充信息"，对模型没有任何帮助。
    * 删了三处：`calculate_bmr_kcal()` 整个函数、`profile_summary` 里那一行、`/health/overview` 注入的 `estimated_bmr_kcal`。前端"静态消耗"那一行的回退分支也去掉了，改为直说"该日期未导入手表静息代谢率"。
7. 健康趋势加入「总热量（静息 + 活动）」
    * **不能塞进原来那五个指标**：它们是采样均值（`*_avg` + `*_samples`），总热量是**当天累计量**。所以单独一族 `DAILY_TOTAL_METRICS`，周/月直接读日汇总的 `total_calories` 列，筛选条件用 `IS NOT NULL` 而不是 `samples > 0`（那一列根本不存在），返回的点也**不编造 samples 字段**。
    * **日视图给的是日内累计曲线**，不是逐小时瞬时值——静息代谢只有 kcal/天 的日速率，硬拆成瞬时值只是在编数字。静息按 `RMR × (h+1)/24` 匀速摊；活动取 `active_calories` 截至该小时的最大值再按类型求和（已在真实数据上逐日核对：每个 activity_type 内单调累计）。**空的小时必须沿用上一次累计值**，否则曲线会掉回去——真实数据一天只有约 100 条活动观测，中间大量小时是空的。
    * **末点必须等于日汇总的 `total_calories`**（实测 2026-08-21：2417.0 = 2417.0），有测试钉住。否则趋势图和总览卡片会给出两个数。
    * **只计 walking/running/cycling**，与 `_rebuild_daily_summary` 同一份白名单。手表还会写 `generic` 聚合行（2026-08-21 它累计到 277，而当天活动消耗是 walking 的 187），算进来就是重复计数；有测试同时钉住两处白名单，防漂移。
    * **部分数据的一天不补满 24 格**：曲线只画到当天真正有数据的最后一小时。今天才过到 10 点就画满一整天，等于凭空替用户消耗了 14 小时静息热量。
    * **没有静息代谢率就返回空**：静息是主项（2417 里的 2230），只画活动却标成"总热量"会低报 90% 以上。这与周/月一致——`_rebuild_daily_summary` 也只在静息与活动都有时才写 `total_calories`。
    * 前端：累计曲线**不取平均**（那会得到"半天的量"这种毫无意义的数），改报"截至 HH:00 累计 N kcal（静息 X + 活动 Y，静息按日速率均摊）"——均摊是个假设，界面上得说出来。
    * 新增 `tests/test_calorie_trend_and_bmr_removal.py`（23 条）。12 处逐一还原实测，各被 1–7 条抓住；其中"恢复 Mifflin 行"那次额外单独复核过，确认是断言本身抓住的、不是 setUpClass 因语法错误连坐。当前基线 **624 通过 / 0 失败**，6 种随机顺序全绿。
8. 实现肌群恢复功能

### 2026_08_29

1. 完成 `main.py` 八阶段拆分：共享依赖进入 `runtime/`，纯规则进入 `domain/`，
   聊天、退出和上传编排进入 `workflows/`，HTTP 端点按资源进入 `routes/`。
2. `main.py` 从 5402 行收敛到 132 行，只负责 FastAPI 装配、首页与少量带退出条件
   的兼容导出；生产包不存在反向 `import main`。
3. 所有 HTTP 适配器统一为 `APIRouter`。针对 FastAPI 0.141 的惰性 included-router
   节点，在无 prefix/options 的前提下原位展开，继续保持阶段 0 冻结的扁平路由顺序。
4. 测试通过 `module_map.py` 跟随符号定义位置和消费者位置，`DEPS_CONSUMERS`
   单独守住打桩可达性，不再依赖容易假绿的 `main.X` 可替换依赖别名。
5. 阶段收尾验收：893 条测试通过，848 条原始基线 outcome 逐条未变；73 条路由、
   69 个 OpenAPI operation 与注册顺序不变；冷启动中位数约 988 ms。

## 目前遇到问题
1. 佳明数据丰富，不同种类的运动会有不同类型的数据保存，要实现一种弹性的多运动类型解析
2. 上下文工程，如果训练计划中有备忘录，且备忘录中对搜索的链接给了具体反馈，需要记住反馈，如果是好的反馈，可以考虑都用这个反馈，用户可能也会自己给出需要的链接，需要记住用户给的链接
3. 根据照片估算卡路里时识别不准，需要手动调整
4. 如何衡量哪些信息给强力LLM，哪些直接给轻量级LLM处理
5. 和Agent的文字聊天是不可控的，但是还是需要用正则表达式做初步处理，会出现很多漏洞，要不断补充正则表达式
6. 短路回答与回执丢失问题。输入“我昨天的训练计划是什么？另外我今天大腿非常酸，该怎么办？”
但只返回了昨天的训练计划，没有处理后面的问题。正确断言 A：不仅要输出本周数据，还要给出针对酸痛的文字建议，并附带明确的系统回执卡片。



