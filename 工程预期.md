# FitHealthAgent 工程预期（2026-08-23）

基线：`需求清单-20260823.md` 的 72 条修复项（DATA-17~39 / BUG-19~46 / AGENT-11~24 / ARCH-07~13）+ 18 条 `FEAT`，按该文件 §十「修改计划」的七批 + 四组新功能（F1~F4）交错执行。

## 一句话结论

**全职每天 8 小时，预计 ~36.5~ 23 人日，折合 ~7.5~ 4.5 周**（中位约 6 周，30 人日）。其中真正不可压缩的是第四批与第七批——它们要重放 25 份真实数据包并逐日回归，快不了。

> **2026-08-24 更新**：`FEAT-17`（身体电量 / 睡眠分数 / 恢复时间的字段逆向）已取消——身体电量不需要，Garmin 恢复时间由用户手输且已实现，睡眠分数则已用 2 份 CSV 真值 + 17 份日包实测定位（见 `需求清单-20260823.md` 的 `DATA-36`），并入第七批实现。原本"唯一一条建议设止损的条目"因此消失，工期与风险都下调了。

你现在可用的是 Claude 的 Opus 5、Opus 4.8，以及 ChatGPT 的 sol、terra。它们对本项目的价值不只是“多四个干活的”——**这轮审查暴露的头号问题是欺骗性的绿（自己给自己出题），而跨厂商复核是目前能拿到的最强独立性**。整份分配围绕这一点组织。

---

## 一、口径与前提

- **人日 = 8 小时全职**，按 5 天工作周折算日历时间。
- 工期含该阶段自己的回归与验收，**不含**需求变更、不含 Python 3.11 环境与 `vendor/` 依赖的一次性搭建（见「零号前置」）。
- 估算基于「代码修改量 + 需要重放/核对的真实数据量 + 出错后的排查成本」三项，不是纯代码行数。清单里已给的估计我大部分沿用，**第二、四、五、六批我上调了**（理由见表内），第七批清单没给估计，由我补。
- 表里的区间是 P50~P85，不是 P10。我不把最乐观值当承诺值。

### 零号前置（0.5 人日，做任何一批之前）

Python 3.11+ 环境（`plan_workflow.py` 用了 `StrEnum`，3.10 直接起不来）、`hello-agents==1.0.0` 的来源确认（不在公共 PyPI）、`vendor/` 目前是空的所以镜像构建必然失败（`ARCH-10`）。这三件不解决，第一批的验收都跑不起来。**注意：在 `DATA-22` 修好之前不要跑 `pytest tests/`，它会删掉真实的 `data/pending_workout.json`。**

---

## 二、四个模型的定位

两家厂商 × 两个层级（以下名称按你实际可用的模型称呼，不假设 ChatGPT 模型的内部版本号）：

| | Claude | ChatGPT |
|---|---|---|
| **旗舰** | `Claude Opus 5`（下称 `opus5`） | `ChatGPT sol`（下称 `sol`） |
| **次旗舰** | `Claude Opus 4.8`（下称 `opus4.8`） | `ChatGPT terra`（下称 `terra`） |

实际调度时按下面的优先级使用：`opus5` 负责高风险实现与最终语义裁决，`sol` 负责跨厂商复核、反例和双盲结论；`opus4.8` 负责明确修法的批量改造与前端，`terra` 负责其验收、回归重放和文档收尾。若某个模型暂时不可用，先保留该批次，不要把双盲结论或安全裁决降级为同一模型自审；最多将机械实现暂时交给同层模型，待原定复核模型恢复后再验收。

- **旗舰层（`opus5` / `sol`）**：承担「改错了不会报错、只会静默产生错数据或漏掉安全拦截」的条目，以及一切接口语义取舍与不可逆的数据迁移决策。本项目这类条目占比很高。
- **次旗舰层（`opus4.8` / `terra`）**：承担修法已经写得很具体的成体量改造、同构批量修改、前端实现、文案与死代码清扫。这些活交给旗舰是浪费，而且它们出错通常会立刻显形（页面报错、测试红），不属于原则 1 的范畴。

**同层内两家怎么选？** 默认按「谁不是这段代码的作者」来分——原实现是 `opus5` 写的，复核就交 `sol`，反之亦然。层级决定能不能干，厂商决定谁来复核。

> 关于这份清单本身：`需求清单-20260823.md` 是我（`opus5`）做的审查，所以**它的结论也应该被 `sol` 抽查一遍**，尤其是那些"已验证"标记。我给自己打的分不算数。建议在零号前置阶段花半天让 `sol` 挑三条最贵的（`AGENT-11`、`DATA-18`、`DATA-22`）独立复现一遍，确认再开工——这半天已含在零号前置里。

## 三、五条分配原则（比具体分配更重要）

1. **凡是「改错了不会报错」的条目，一律旗舰层主写**（`opus5` 或 `sol`）。判据不是难度，是失败的可见性。
2. **实现与验收测试必须跨厂商。** `opus5` 写实现 → `sol` 独立写验收；`opus4.8` 写 → `terra` 审。这轮审查抓到的假绿——字符串级断言 `"atomic_write_json" in source or "os.fsync" in source`、源码文本顺序断言、真实数据窗口失效后退化成 `assertEqual({}.keys(), {}.keys())`——全是「自己给自己出题」的产物。同厂同代模型的失败模式高度相关，换个次旗舰来审也只是换个人抄同一份答案；换厂商才真正换了先验。**写验收的一方不看实现代码，只依据清单里的「验收」条目出题。**
3. **凡是「从真实数据反推字段语义」的条目，`opus5` 与 `sol` 各自独立推一遍，两边结论一致才落代码。** 目前适用于 `DATA-18`（HRV 错位一位）与 `DATA-19`（动作名解码）。这是真正的双盲交叉验证——单模型自己推自己信，本质上还是猜。**不一致就是信号，不要调和，要么继续找证据要么放弃。** 另有一条硬性下限：**任何新字段都必须有 ≥2 天的外部真值同时命中才算数**。`DATA-36` 定位睡眠分数时，只用一天做逐值匹配会误收两个字段（`msg346.unknown_7`、`msg384.unknown_25` 在 08-10 恰好也等于 54），加上第二天就自动滤掉了——这条规矩不是理论洁癖，是实测出来的。
4. **前端与文案交次旗舰，但「错误码语义」由旗舰定接口**（`BUG-28` / `BUG-31` / `BUG-35`）。契约定错了后面全白改。
5. **一批之内不换手。** 清单里反复强调「同一条链路上的多个缺陷必须同批修」，同理，同一批里的实现不要在两个模型之间来回倒——交接成本会吃掉省下的钱。复核是另一回事，复核必须换手。

---

## 四、总表

| # | 阶段 | 条目数 | 清单原估 | **本文估计（人日）** | 累计 | 主力 | 复核 / 分工 |
|---|---|---|---|---|---|---|---|
| 0 | 零号前置（环境 + `ARCH-10` + 抽查清单） | — | — | **0.5** | 0.5 | terra | **sol**（复现三条结论） |
| 1 | 第一批：安全档位 | 4 | 0.5~1 | **0.5~1** | 1~1.5 | **opus5** | **sol**（独立出反例集） |
| 2 | 第二批：拦截真的生效 | 6 | 1~2 | **1.5~2.5** ↑ | 2.5~4 | **opus5** | **sol**（`BUG-26` 三表比对） |
| 3 | **F1：新功能 4 条**（+`BUG-22`） | 5 | — | **1~2** | 3.5~6 | opus4.8 | opus5（酸痛判定链） |
| 4 | 第三批：不可逆与丢数据 | 7 | 1~2 | **1.5~2.5** ↑ | 5~8.5 | **opus5** | **sol**（`DATA-21` 死锁）+ terra |
| 5 | 第四批：导入侧数据正确性 | 7 | 2~3 | **3.5~5** ↑↑ | 8.5~13.5 | **opus5 + sol 双盲** | terra（重放）+ opus4.8（迁移脚本） |
| 6 | 第五批：端点与前端 | 11 | 1~2 | **1.5~2.5** ↑ | 10~16 | opus4.8 | opus5（契约 + 留口清单） |
| 7 | **F2：新功能 4 条** | 4 | — | **1~2** | 11~18 | opus4.8 | sol（`FEAT-03` 去重键） |
| 8 | 第六批：记忆状态机与可维护性 | 17 | 2~3 | **3.5~5** ↑↑ | 14.5~23 | **opus5** | **sol**（`ARCH-09`）+ opus4.8 |
| 9 | **F3：新功能 6 条** | 6 | — | **2~3** | 16.5~26 | **opus5** | opus4.8 / terra（渲染层） |
| 10 | 第七批：数据安全底座与剩余项 | 18 | *缺* | **3~4.5**（我补） | 19.5~30.5 | opus5 + sol 分工 | opus4.8 / terra |
| 11 | **F4：新功能 3 条** | 3 | — | **1.5~2.5** | 21~33 | opus4.8 + opus5 | — |
| | 全局集成与返工缓冲（+10%） | | | **2.1~3.3** | **23~36.5** | | |

**折合日历时间（全职 8h、5 天/周）：乐观 4.5 周，中位 6 周，保守 7.5 周。**

条目核对：4+6+7+7+11+17+18 = 70 条修复项，加上不在任何一批里的 `BUG-22`（落在 F1）与 `AGENT-24`（决策项，落在 F2）= 72 条，与清单一致；FEAT 4+4+6+3 = 17 条（`FEAT-17` 已取消），不重不漏。§五 对全部 89 条都给了归属。

### 我上调了哪几批，为什么

- **第二批 1~2 → 1.5~2.5**：`AGENT-11` 不是改一个函数，而是把「执行侧用不截断的 `get_enforceable_facts()`、注入侧才受预算约束」这条分流铺到所有调用点（校验、频道过滤、`safety_constraints`）。漏一处就等于没改，而漏了不会报错。
- **第四批 2~3 → 3.5~5**：重放 25 份包本身不慢，慢的是逐日回归核对；**再加上原则 3 的双盲**——`opus5` 与 `sol` 各推一遍字段映射再对齐，比单跑一遍多约 0.5 人日。这 0.5 天买的是「不把错的字段语义永久写进库」，值。
- **第五批 1~2 → 1.5~2.5**：11 条，且 `BUG-46` 那条自身就是一张长清单（死代码 + 死端点 + 14 个小项）。
- **第六批 2~3 → 3.5~5**：17 条是全场最多，其中 `ARCH-09`（测试治理）一条就可能吃掉 1.5 人日——要通读整个测试基线，把假绿、定时炸弹、对真实数据的依赖逐条处理，还要重写前面几批已经改过一遍的测试。清单自己也说了「ARCH-09 建议放这批最后做，否则要改两遍」。
- **第七批（清单未给）→ 3~4.5**：18 条，且 `DATA-34`（multisport 丢 session 摘要）、`DATA-35`（`merge_sets` 吸收休息段）、`BUG-45`（多日 `history`）都不是小改。原本还含 `DATA-36` 的字段逆向，现在字段已确证、降级成照着写，所以比 8-23 版少了半天。

---

## 五、逐阶段的模型分配与理由

### 零号前置（0.5 人日）

`terra` 处理环境与 `ARCH-10`（`vendor/` 为空、依赖只锁到版本号），机械活。同时 `sol` 独立复现清单里最贵的三条结论——`AGENT-11`（Top-5 截断导致校验静默失效）、`DATA-18`（HRV 整体错位一位）、`DATA-22`（跑测试删真实文件）。这三条分别决定了第二、四、三批的方向，**如果其中任何一条我判断错了，后面十几人日都在错误的前提上跑**。

### 第一批：安全档位（0.5~1 人日）——`opus5` 写，`sol` 出题

`BUG-19` → `BUG-20` → `BUG-21` → `AGENT-15`

四条串在同一条判定链上，任何一条改错的后果是「急症漏报」，且不会有任何报错，属于原则 1。核心动作是把小句切分抽成**一个**共用函数（`main.py:1326-1329` 的 `_CLAUSE_SEPARATORS` 已有雏形），供 `BUG-19` 的否定判定、`BUG-25` 的否决、`AGENT-15` 的疑问否决三处共用——这是接口取舍，`opus5` 定。

**`sol` 在这一批的角色最重要，也最能体现原则 2**：让它**不看实现**，只依据验收标准（「背景句 + 症状句」必须 emergency、胸部在恢复快照里时 `胸口痛` 仍须 emergency、`手臂有点酸痛` 必须 sore、`腰伤的人可以照常练吗？` 不得 409）独立生成一套中文反例集。现有 `tests/test_health_safety_gate.py` 一直是绿的，恰恰因为它的样本全是"症状词紧邻"的写法——出题的人和写实现的人是同一个，就会一起漏掉同一类输入。换厂商出题是打破这个的最直接办法。

### 第二批：拦截真的生效（1.5~2.5 人日）——`opus5` 写，`sol` 比对

`AGENT-11` → `BUG-23` → `BUG-24` → `BUG-25` → `BUG-26` → `AGENT-23`

`AGENT-11` 的截断/不截断分流是全局语义改动，必须排第一（它决定校验器能不能拿到约束），`AGENT-23` 是它的自然副产品。

`sol` 接 `BUG-26`：三处映射与窗口不一致，让它独立读 `muscle_recovery.py`、`soreness_store.py` 与 `肌群恢复实现计划.md` 里的映射表，**先产出一份差异表再讨论怎么改**。问题 3（`role/zh` 不稳定）必须排在问题 1（次要肌群窗口）之前。

### F1：新功能 4 条（1~2 人日）——`opus4.8` 写，`opus5` 把关判定链

`FEAT-01` → `FEAT-02` → `FEAT-04` → `FEAT-15`（含 `BUG-22` 修法 1）

- `opus4.8`：`FEAT-01` 的未识别动作清单、`FEAT-02` 的删除入口。后端能力前四批已备齐，这里是界面出口，修法明确，次旗舰足够。
- `opus5`：`FEAT-04` 的 `cleanup_expired` 从物理删除改成标记 `expired`（改的是数据保留语义），以及 `FEAT-15` + `BUG-22` 共用的 `soreness_prompt_regions` 机制——`BUG-22` 是「每条消息都跑酸痛解析、把普通提问写成已恢复并把恢复窗口归零」，静默写坏数据，按原则 1 归旗舰。

### 第三批：不可逆与丢数据（1.5~2.5 人日）——`opus5` 写，`sol` 专审死锁

`DATA-22` → `DATA-20` → `DATA-21` → `DATA-23` → `BUG-27` → `DATA-29` + `BUG-34`

`DATA-22` 必须第一个做——在它修好之前每跑一次测试就删一次你的待确认训练。

**`DATA-21` 单独拉出来给 `sol` 复核**：`write_recovery_point()` 的插入位置涉及 `JsonFileLock` **不可重入**，这是全表最容易写出死锁的一条，而死锁的表现是备份卡住而不是报错，排查成本极高。旗舰对旗舰、跨厂商地过一遍锁的持有路径，这半小时非常划算。必须按清单给的两种方案之一处理，**不要直接在锁内调**。

- `terra`：`BUG-27`（3 秒轮询顶掉未确认导入）是纯前端。
- `DATA-29` + `BUG-34` 的 `applied_to_form` 必须同一个模型同一次改完，否则会出现「合计与明细分叉」——按原则 5 不要拆。

### 第四批：导入侧数据正确性（3.5~5 人日）——**`opus5` 与 `sol` 双盲**

`DATA-17` → `DATA-18` → `DATA-19` → `DATA-24` + `DATA-25` + `DATA-26` → `DATA-28`

这批的本质是「拿真实数据反推字段语义」：HRV 七个字段整体错位一位、力量 FIT 21% 动作名张冠李戴、同时间戳日级指标被 `INSERT OR IGNORE` 吃掉 26%（`resting_metabolic_rate` 与 `resting_heart_rate` 同时有值的行数实测为 0）。**这正是原则 3 的适用场景，也是本项目最该花两个旗舰的地方**——推错一位，错的语义会永久写进 `health.db`，而且看起来完全正常。

具体做法：`opus5` 与 `sol` 各自拿 `data/health-imports/` 的原始包与 `data/health.db` 独立推 `DATA-18` 的字段顺序假设和 `DATA-19` 的 `_EXERCISE_SUBTYPE` 分域规则，**互相不看对方的结论**，然后对齐。一致才落代码；不一致说明证据不足，继续找证据或缩小改动范围。

- `terra`：执行 25 份包的重放并产出逐日回归报表。量大但机械。
- `opus4.8`：写迁移脚本。
- **不可逆的两个决定仍由 `opus5` 拍板**（`sol` 复核）：`DATA-18` 走 `_ensure_column` 加新列、旧列保留，**不要原地改语义**；`DATA-17` 的归并放在解析侧，`DEDUPED_INTENSITY_SQL` 的 `MAX` **不要改成 `SUM`**（跨文件重叠导入需要它）。
- `DATA-19` **刻意不迁移历史**——反直觉，要在代码注释里写清楚为什么，否则下一轮会有人「顺手补上」。
- 范本：`health_importer.py:75-96` 的 `_STRESS_TIME_FIELDS` 是全仓库处理「上游字段名不可信」的正确写法，`DATA-18` 照它改，连那段解释原因的注释一起照抄结构。

### 第五批：端点与前端（1.5~2.5 人日）——`opus4.8` 写，`opus5` 定契约

`BUG-28` → `BUG-31` → `BUG-29` + `BUG-30` → `BUG-32` + `BUG-33` + `BUG-36` → `BUG-35` + `BUG-41` + `BUG-42` → `BUG-46`

11 条里 9 条是前端 JS，次旗舰的主场，`opus4.8` 主写、`terra` 分担 `BUG-46` 的死代码清扫（`buildTrainingRecordsPanel` 86 行子树、九处 `MEMORY_CONFLICT` 死分支、`CHECKIN_CATEGORY` 死分支、重复的 `_region_muscle_ids`）。

`opus5` 先定两件事，然后就不必再介入：

- **错误码契约**（原则 4）：`BUG-28`（`updateWorkout` 丢 `code`，冲突自愈全是死代码，保存会永久卡死）、`BUG-31`（`/chat` 不检查 `res.ok`）、`BUG-35`（三处冲突码缺口）。
- **`BUG-46` 的「留三个口子」清单**（这一步最容易一刀切删过头）：`GET /health/range`、`/health/daily/{day}`、`/health/sleep/{day}` 留给 F2 的 `FEAT-09`；`/health/imports/{import_id}` 留给 F4 的 `FEAT-16`；`MuscleLoad.role` 与 `muscle_loads` 留给 F3 的 `FEAT-08` + `ARCH-13`。其余照删。

### F2：新功能 4 条（1~2 人日）——`opus4.8` 写，`sol` 复核一处

`FEAT-09` → `FEAT-03` → `FEAT-18` → `FEAT-05`

趁第五批刚动过前端一次性做完，四条改的基本是同一批文件。`FEAT-09` 是纯接线排第一，`FEAT-18` 三个小出口，`FEAT-05` 的 `source: "manual_meal"` 白名单 + `_validate_meal_estimates` 扩展。

**`sol` 复核 `FEAT-03` 的去重键改造**：把 `content_hash` 全局去重改成 `(content_hash, date)` 或 memo 合并，会影响 `plan_store` 里已有的数据，属于「改错了静默丢用户填的内容」（`BUG-36` 就是这么来的）。

这一步同时把 `AGENT-24`（关闭外部模型时的失效清单）的两个决策落地：离线时能不能把 Markdown 按计划纳管（`FEAT-18a`）、能不能手动记一餐（`FEAT-05`）。

### 第六批：记忆状态机与可维护性（3.5~5 人日）——`opus5` 写，**`ARCH-09` 必须给 `sol`**

`AGENT-12` → `AGENT-13` → `AGENT-14` → `AGENT-16`~`AGENT-21` + `BUG-39` → `ARCH-07` + `ARCH-08` + `ARCH-12` + `ARCH-13` → `ARCH-09` + `ARCH-10` + `ARCH-11`

- **`opus5`**：`AGENT-12/13/14` 是同一个状态机的三个缺口（版本基准、空事实、软删除墓碑），一起改比分三次改省事，而且状态机是 F3 全部六条新功能的地基。`ARCH-07`（「文本→身体区域」四份平行实现合一）与 `ARCH-08`（三个 JSON store 缺 RLock、`plan_store` 读方法完全不持锁）也归它。
- **`opus4.8`**：`AGENT-16`~`AGENT-21` 是六条同构的规则/词表修改（极性冲突、同义词归一、敏感词黑名单把"病史/病历"误列、caution 就整体跳过抽取、频道排除用精确等值、急性伤病候选把整条消息当事实值），修法都写得很具体，适合批量推进。`BUG-39`（周计划宽松解析）也在这里。
- **`sol` 接 `ARCH-09` 测试治理，放这批最后做。这是全表最该跨厂商的一条。** 要识破的假绿，正是 `opus5`/`opus4.8` 这一系写出来的；让同一系去审自己的测试，等于让出题人改自己的卷子。已知样本给 `sol` 当起点即可（字符串级断言、源码文本顺序断言、真实数据窗口失效后退化成 `assertEqual({}.keys(), {}.keys())`），剩下的让它自己找。
- **`terra`**：`ARCH-13` 的欢迎页文案（「本阶段先展示状态，不改变训练计划」在阶段 3/4 之后已经不成立）、`ARCH-11`、`ARCH-12` 的文档同步。

### F3：新功能 6 条（2~3 人日）——`opus5` 主

`FEAT-10` → `FEAT-11` → `FEAT-14` → `FEAT-12` → `FEAT-13` → `FEAT-08`

六条全部长在第六批的状态机上，跨模块因果最密的一组。可以下放的两块：

- `opus4.8`：`FEAT-11` 的记忆使用日志（`validate_generated_training_plan` 已能算出违规明细，按 fact 归档即可，成本很低）。
- `terra`：`FEAT-08` 的渲染层——与 `ARCH-13` 同一个 `_format_muscle_recovery_lines`，两条一起改只动一次。

### 第七批：数据安全底座与剩余项（3~4.5 人日）——两个旗舰分工

`DATA-27` → `DATA-30`~`DATA-39` → `AGENT-22` → `BUG-37` + `BUG-38` + `BUG-40` + `BUG-43` + `BUG-44` + `BUG-45`

- **`opus5`**（并发、原子性、安全裁决）：`DATA-30`（并发上传的失败分支删掉在用的原始文件）、`DATA-31`（隐私开关 fail-open + 全仓库唯一无锁 store）、`DATA-32`（恢复后只读降级不复位，坏条目还会被回写）、`DATA-33`（备份与删除两个方向都漏隔离文件目录）、`DATA-38`（三处旁路手写原子写）、`BUG-45`（多日 `history`，影响窗口计算）、`AGENT-22`（缓存键设计 + 校验器 `resolver` 参数）。
- **`sol`**（解析器，与 `opus5` 的部分互不重叠，天然形成双人复核）：`DATA-34`（multisport 丢整段 session 摘要）、`DATA-35`（`merge_sets` 吸收跨窗口休息段，时间轴留空洞）、`DATA-36`（「停止撒谎」+ 睡眠分数入库）、`DATA-28` 遗留核对。
  - **`DATA-36` 已经不是逆向工作了**，字段位置在 8-24 用 2 份 CSV 真值 + 17 份日包全部确证：主源 `SLEEP_DATA.msg521.unknown_1`，交叉校验 `METRICS.msg330.unknown_2`，日期绑定用睡眠结束时刻的本地日期（17/17 命中）。照清单里的 `DATA-36` 直接写即可，**不要再重新推一遍**。唯一要留神的是它与 `DATA-28` 读同一条 `msg521` 的睡眠起止，必须复用第四批定下来的口径。
- **`opus4.8`**：`DATA-27`、`DATA-37`、`DATA-39`、`BUG-37`、`BUG-38`、`BUG-40`、`BUG-43`、`BUG-44`。`BUG-37` 顺带把回执拼接抽成 `_with_ack(reply)` 让所有 return 分支统一走——否则下一个新分支还会漏。
- **`terra`**：数据核对与回归报表。

### F4：新功能 3 条（1.5~2.5 人日）——收尾

`FEAT-16` → `FEAT-07` → `FEAT-06`

- `opus4.8`：`FEAT-16`（`DATA-27` 修法 2 的界面壳，照 `workout_store.list_quarantined()/dismiss/delete` 三件套的范本做）、`FEAT-07`（周汇总补容量与恢复视角，数据在 `MuscleRecoverySnapshot` 里现成；顺手把第七批刚入库的睡眠分数也纳进来）。
- `opus5`：`FEAT-06`（训练量突增与连续高强度日）——恢复裁决之外最大的安全校验缺口，原则 1。
- 原第四条 `FEAT-17` 已取消，这一组不再有双盲环节。

---

## 六、两个最可能让预期落空的地方

1. **第四批重放后可能推翻现有假设**。`DATA-18` 的「整体错位一位」是从现有数据推出来的结论，全量重放 25 份包时很可能冒出第二种错位模式（比如某个固件版本的包字段顺序不同）。真出现的话第四批要 +1~2 人日。双盲能更早发现这件事，但发现了还是得花时间。
2. **第六批的测试治理会外溢**。`ARCH-09` 之前的五批会大量改测试，`sol` 治理时会发现更多假绿，而修假绿常常意味着**发现一个之前没被抓到的真 bug**。这是好事，但会挤占工期，我给的 3.5~5 已经含了一部分，仍可能不够。跨厂商审查会让这个外溢**变大而不是变小**——这正是做它的理由，但排期上要有心理准备。

> 原第一条风险（`FEAT-17`/`DATA-36` 的字段逆向可能做不成）已在 2026-08-24 消除：睡眠分数字段用 5 天 CSV 真值 + 17 份日包确证，身体电量与恢复时间不做。**这也是唯一一条曾建议设止损的条目，现在全表没有需要止损的工作了。**

另外两个次级风险：`DATA-21` 的 `JsonFileLock` 不可重入很容易写出死锁（表现是备份卡住而不是报错）；`AGENT-22` 的模型兜底目前每轮聊天同步调用、每个未识别动作最多阻塞 10 秒且串行，改缓存键时如果 mtime 失效条件设计错了，会变成「缓存了错的肌群映射」——比不缓存更糟。

## 七、里程碑：可以停下来的四个点

不是所有人都会一口气做完 36 人日。按价值密度，这四个点各自都是完整可交付的状态：

- **M1 = 零号前置 + 第一批 + 第二批（约 2.5~4 人日）**。做完这两批，「急症拦截在自然中文下生效」和「后端确定性校验真的在跑」这两件事成立。这是全表投入产出比最高的一段，**强烈建议无论如何先做完**。
- **M2 = + F1 + 第三批（约 5~8.5 人日）**。做完不再有「跑测试删掉你的训练」「重导一次丢掉原始包」这类会真的损失已输入数据的路径，且前两批的成果有了用户侧出口。
- **M3 = + 第四批 + 第五批 + F2（约 11~18 人日）**。导入的数据从此是对的，界面上「后端已经算对但用户看不到」的一类问题清零。**到这里项目已经是一个可以放心长期用下去的状态**，剩下的更接近打磨。
- **M4 = 全部（23~36.5 人日）**。

## 八、成本与调度的几点建议

- **旗舰用量集中在第一、二、三、四、六批与 F3**，合计约 13~20 人日；其余约 9~14 人日在次旗舰层。如果按 token 成本算，真正贵的是第四批的双盲（同一批原始数据要过两个旗舰）和第六批的 `ARCH-09`（要通读整个测试基线）。**这两处都不建议省**——第四批省下来的钱会以「库里永久存着错的字段语义」的形式还回去，第六批省下来的会以「继续相信假绿」的形式还回去。
- **可以省的地方**：F1、F2、第五批、F4 的大部分，交次旗舰即可，这四段合计约 5.5~9 人日。
- **如果某一天只有一家厂商可用**：优先保证原则 2（跨厂商验收）在第一批与第六批 `ARCH-09` 这两处落实，其余可以退化成同厂旗舰↔次旗舰复核。这两处是「假绿」风险最集中的地方——一个是安全拦截，一个是测试基线本身。
- **不建议为了并行而并行**。你是单人调度，四个模型同时开工的交接成本会超过收益。按上表顺序串行推进，只在第四批的双盲那一处真正并行。
