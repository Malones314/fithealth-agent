## 阶段 0：开工前的三件事（0.5 小时）

**0.1 先确认 `BUG-26` 问题 3 的状态。** `ARCH-07` 的修法原文写着"与 BUG-26 问题 3 一起做（那条也是在拆 `muscle_map` 的表）"。`BUG-26` 属第二批，如果已经改过映射表，`ARCH-07` 合并时不能把它推翻。开工前 grep 一遍 `muscle_map.py` 的 `MUSCLE_RULES` 有没有 `BUG-26` 的修改痕迹。

**0.2 把 `REGION_LEXICON` 表先写出来通读一遍。** 它是后面 `ARCH-15`、`AGENT-21`、`constraint_regions`、酸痛解析四处的共用底座，表错了后面全错。

**0.3 确认能跑 pytest。** 我这边沙箱 bash 已彻底失效，全程只做了静态阅读，**一行测试都没跑过**。下面每个阶段的验收都假设你能在 3.11 环境执行——如果不能，先解决这件事，否则整批改完没有任何客观依据。

---

## 阶段 1：三条静默失效（1.5 小时，优先级最高）

这三条的共同点是"不报错、后果不可见"，而且都是十几行的改动。

### 1.1 AGENT-18：删三个敏感词 + level1 限定 user

`information_router.py:48-50` 删掉 `病史`、`病历`、`社保`；`_level1_sensitive_check`（`:55-64`）的输入从"拼好的全角色文本"改成只拼 `role == "user"` 的消息。

**为什么排第一**：一个健康 Agent，模型自己问一句"你有相关病史吗"就把整次对话的**全部**长期事实否掉，理由只写"包含敏感个人信息"。影响面是"一条都不保存"，触发概率极高。

**验收**：构造一次对话，bot 消息含"病史"、user 消息含周计划与伤病限制 → 断言两条事实都保存；user 消息含真实手机号 → 断言仍然拦下（正则组还在）。同改 `tests/test_information_router_memory_safety.py` 的 level1 断言。

### 1.2 AGENT-14：被拒绝的内容不得以摘要形式回到上下文

`main.py:866-878` 的 `format_cross_session_memories` 逐条注入 `item["summary"]`，完全不看 `rejected_fact_ids`。加一个判断：条目存在任何 rejected 事实时**只注入已确认事实行，不注入 summary**（摘要是不可裁剪的自由文本，无法只删掉那一句）。

同时补齐另外两处：
- `_acute_injury_memory_candidate`（`main.py:1711`）的去重集合从 `status == "active"` 扩到 `{"active", "rejected"}`，与 `_immediate_memory_candidate`（`:1639`，已改对）口径一致
- `cleanup_expired`（`info_store.py:823`）给 rejected 事实单独一条 90 天保留期，不要跟着原 TTL 走——`recovery_status` 只有 72 小时，三天后墓碑消失又能重新建卡

**验收**：一条记忆 3 个事实，拒绝 1 个 → `format_cross_session_memories` 输出里不含 summary 原文、含另 2 条事实行；同一事实再次出现 → 不生成新卡片；模拟 4 天后 `cleanup_expired` → rejected 事实仍在。

### 1.3 AGENT-12：`confirm_entry` 补 `base_version`

`info_store.py:1092` 之后加 `fact["base_version"] = current_version + 1`，与 `set_fact_confirmation:1000` 对齐。

**验收**：确认整条 → 取消确认其中一条 → 再确认 → 不抛 `MemoryConflictError`。这条现在是重试永远失败的死循环。

---

## 阶段 2：回执与控制流（1 天）——你报的症状在这里解决

**顺序必须是 ARCH-14 先、ARCH-15 后。** 理由：14 建立单一出口和 AST 不变量测试，15 再动那三个分支时测试就能守着它。反过来做的话 15 改完没有守卫，而且 14 还要再碰一遍同样的返回点。

### 2.1 ARCH-14：`/chat` 的返回体收敛到单一出口

`main.py:1730-2286` 的 13 个返回点里只有 5 个拼了 `soreness_ack`。**不要给另外 8 个各加一句**——那还是手抄，下一个新分支照样漏。

三步：

1. 建请求级构造器 `_chat_response(reply, *, artifact=None, source=None, status_code=200, **extra)`，内部**无条件**拼 `soreness_ack`，并统一带上 `soreness_saved` / `asked_regions` 这类状态字段
2. `chat` 函数体内所有 `return JSONResponse({...})` 改走它
3. **加 AST 不变量测试**：解析 `main.py`，定位 `chat` 的函数体，断言其中不存在裸的 `JSONResponse(` 调用

第 3 条同时是给 `ARCH-09` 的正面样板——它断的是 AST 形状，重构改名不会误报，漏掉 helper 一定报。这正是要用来替换那 50 多处 `assertIn(<源码片段>, body)` 的写法。

**急症那条（`:1801`）显式豁免**：急症回复不该被酸痛回执稀释，但**必须**带 `soreness_saved: true`，让前端提示"已记录腿部酸痛"。豁免要写在代码注释里说明理由，不要让它看起来像漏了。

**顺手做 AGENT-19 的残留**：`:1808` 的条件已改成 `(health_risk is None or health_risk.level == "caution")`，但 urgent 档仍不调 `_immediate_memory_candidate`。在 `_acute_injury_memory_candidate` 之后补一次调用（两者已各自去重）。同时把硬编码的 `"caution"` 换成 `health_safety.CAUTION` 常量。这段就在 `:1791-1808`，与 2.1 要动的返回构造在同一个函数里，一次进去改完。

**验收**：对 13 个返回点逐个构造用例，断言每一个的 `reply` 都含酸痛回执（急症那条断言 `soreness_saved` 为真）；`我最近膝盖有点肿，以后别给我安排深蹲了` → 断言既生成 `recovery_status` 候选，也生成 `avoid_exercise=深蹲` 候选。

### 2.2 ARCH-15：短路分支区分"展示数据"与"阻断操作"

判据：

| 分支 | 类别 | 处理 |
|---|---|---|
| `local_training_records`（1888） | 展示数据 | **不再 return**，artifact 收进 `pending_view_artifact`、提示语进 `pending_reply_prefix`，继续往下走 |
| `local_nutrition_records`（1911） | 展示数据 | 同上 |
| `local_profile`（2017） | 展示数据 | 同上 |
| `emergency`（1801） | 阻断危险 | 保留 return |
| `onboarding`（2035） | 必须先补信息 | 保留 return，但回复里说明"你还问了另一件事，补完档案后我再回答" |
| 计划校验 409（1986） | 阻断危险 | 保留 return，同上说明 |

`pending_view_artifact` / `pending_reply_prefix` 这两个名字 `tests/test_view_intent_stress_tz_and_trend_dedup.py:558-559` 已经在断言，**但这两个标识符在 `main.py` 和 `templates/index.html` 里全仓搜不到**——那条测试现在是假绿（守着一个不存在的东西却过了）。就用这两个名字实现，顺便把那条断言改成真的行为断言。

**验收**（就用你那句输入）：`我昨天的训练计划是什么？另外我今天大腿非常酸，该怎么办？` → 响应同时包含：训练记录 artifact、"已记录腿部酸痛（sore，72 小时）"的回执、Agent 对"大腿酸怎么办"的实质回答。三者缺一即失败。

---

## 阶段 3：记忆语义收尾（0.5 天）

### 3.1 AGENT-17 重做

现在加的是 `"long_term_sedentary": ("sedentary", "long term sitting", "sedentary worker")`（`info_store.py:79`）——全英文，中文 `久坐` 族一条都不归一，而且是全表唯一的英文 snake_case canonical（其余 9 条都是中文），真命中会往中文上下文里写一个英文 id。

三步全做：
1. canonical 改回中文：`"长期久坐": ("久坐", "久坐人群", "久坐族", "长时间坐着", "办公室久坐", "sedentary", "long term sitting")`
2. `canonicalize_body_posture_value`（`:117-132`）改成返回全部命中的 `list[str]`，`normalize_memory_facts` 在多命中时展开成多条事实（`fact_id` 由内容派生，天然唯一，零迁移），单命中行为不变
3. `_DURABLE_MEMORY_CUE`（`main.py:134-138`）补 `久坐`

**验收**：`久坐`/`长期久坐`/`久坐人群` 三种写法归到同一个 canonical；`盆骨前倾且核心无力` 展开成两条独立事实；`我平时久坐办公` 触发即时候选。同改 `tests/test_memory_validity.py:131`。

### 3.2 AGENT-20 补完

`_normalise_channel`（`youtube_tool.py:30-31`）已经剥掉非 alnum，但三处没做：
1. `:153` 仍是 `not in self.avoid_channels` 等值匹配 → 改双向子串匹配
2. **`str.isalnum()` 对中文返回 True**，所以"那个AthleanX频道"归一后还带中文，永远匹配不上英文频道名 → 归一函数要额外剥掉 CJK 修饰词，或改为"抽取连续 ASCII 字母数字段"
3. `_normalise_channel` 提到 `youtube_search.py`（无 store 依赖，两处都能引），让 `validate_generated_training_plan` 用同一个函数，消掉两套口径
4. `main.py:905` 的候选抽取正则字符类补 `一-龥`

**验收**：`Athlean X` / `那个AthleanX频道` / `ATHLEAN-X™` 三种输入都能排除频道 `ATHLEAN-X™`；中文频道名能被抽取成候选；过滤器与校验器对同一组输入给出相同判断。

### 3.3 BUG-39 补两个出口

宽松解析主体已做（`invalid_days` 收集 + `if not days: return None`），但：
1. **混合写法仍整条丢**：`{"mon":"胸","周二":"背"}` → `legacy` 判假 → `raw_days = raw.get("days")` 为 None → `return None`（`info_store.py:321-328`）。需求现象点名的例子原样存在。修法：非 legacy 分支里若 `raw` 本身的键有部分落在 `WEEKDAY_KEYS`，按 legacy 宽松处理并把非法键计入 `invalid_days`
2. **`invalid_days` 没有任何出口**：`:369-374` 构造的 `schedule` 不带它，也没有日志。修法原文加粗警告过——"**必须同时**在待确认卡片上显示解析后的完整结果供用户核对，否则宽松解析会让部分周计划不完整地生效"。把 `invalid_days` 放进返回值，待确认卡片渲染"已识别 5 天，跳过：周二、周日"

**验收**：`{"mon":"胸","周二":"背"}` → 返回含 mon 的 schedule 且 `invalid_days == ["周二"]`；待确认卡片显示跳过的键；`days` 全无效时仍返回 None。

---

## 阶段 4：统一口径重构（1 天）

### 4.1 ARCH-07：`REGION_LEXICON`（含腰的决定）

腰跨区域这件事让扁平表不够用。放在 `muscle_map.py`（无 store 依赖，谁都能引）：

```
REGION_LEXICON: dict[str, tuple[tuple[str, str], ...]]
    部位词 -> ((region, role), ...)
    role 与 MUSCLE_RULES 的 role 同义：
      primary   = 这个词主要指该区域
      secondary = 该区域会被连带影响
```

腰的条目：`"腰": (("核心", "primary"), ("背部", "secondary"))`，`下背`/`腰部`/`后腰` 同。

**关键设计：两个消费者取不同的角色子集，而不是维护两张表。**

| 消费者 | 取哪些 role | 理由 |
|---|---|---|
| 酸痛解析（`parse_soreness_reply`） | **只取 primary** | 一句"腰酸"不该连带压制背部训练——那正是 `AGENT-21` 在修的过宽屏蔽 |
| 限制/伤病扫描（`constraint_regions`） | **primary + secondary** | 伤病宁可多防；"腰伤"该同时避开核心与背部负荷 |

提供 `regions_for_text(text, *, include_secondary: bool) -> set[str]`，两处各传自己的参数。`REGION_ALIASES` 保留为从 lexicon 派生的只读视图（向后兼容，`AGENT-21` 已经在用它）。`TRAINING_SUBJECT_RULES` 由 `MUSCLE_RULES` 按 region 聚合生成。

**⚠️ 这会改变现有行为**：今天 `REGION_ALIASES["核心"] = ("核心","腹部","腹")` 没有腰，所以"腰还酸"解析不出任何东西。改完之后它会写一条核心的酸痛记录。这是想要的修复，但属于可感知的行为变化，要有专门用例。

**不变量测试**（这是 `ARCH-07` 修法明确要求的）：
- `MUSCLE_RULES` 里出现的每个 region 都在 lexicon 里有词条
- lexicon 里的每个 region 都属于 `REGIONS`
- `muscle_recovery` 与 `main` 都引用同一个 `REGION_LEXICON`（用 AST 断言 import，不用源码文本）

**验收**：`腰还酸` → 核心的 sore 记录、**不产生**背部记录；`腰伤，别安排大重量` → `constraint_regions` 返回 `{核心, 背部}`；四处引用同一常量。

### 4.2 ARCH-12：Garmin 恢复小时收敛到一处

`muscle_recovery.normalise_garmin_hours(value) -> float | None`，`context_budget.py`、`main.py`、`muscle_recovery.py` 三处校验全改调它，各自把 `None` 转成自己的错误响应（现在 `_normalise_garmin` 是**抛异常**而不是返回错误，将来有调用方跳过前两道校验就会变 500）。

同步文档：`肌群恢复实现计划.md` §B.2 与 §7.1 写的是 `hours_remaining = max(0, baseline) + garmin`（对已恢复肌群也加），而代码刻意只对 `baseline_remaining > 0` 的肌群加成。**代码的行为更合理**，改文档、别改代码，并写清理由——否则下一个人照文档改就引入回归。

### 4.3 ARCH-08：三个 JSON store 补 RLock

`plan_store.py`、`soreness_store.py`、`info_store.py` 的所有**公开**方法（读和写都算）套 `RLock` + `JsonFileLock`，照 `storage.py:17/438` 的分层：`_read`/`_write` 保持无锁。

**必须先做的检查**：`RLock` 可重入，**`JsonFileLock` 不可重入**。所以如果某个公开方法内部调用了另一个公开方法，加锁后会死锁。开工前把这三个文件里"公开方法调公开方法"的地方全部列出来，改成调内部无锁版本。这是 `DATA-21` 踩过的同一个坑。

**验收**：并发调用同一 store 的读写方法不产生拼接垃圾；单线程里嵌套调用不死锁（加一条带 timeout 的测试，死锁会超时而不是挂住整个测试进程）。

### 4.4 ARCH-13 补文案

`main.py:4372` 那句"本阶段先展示状态，不改变训练计划"要删掉——恢复状态在阶段 3/4 之后已经参与压制周计划、生成 409 和拒绝计划。改成说明它**如何参与裁决**以及**如何纠正**（对话里说明动作、或到数据管理改酸痛记录）。

`raw_sets` 部分已完成（`main.py:4290-4291` 的"原始 X 组，有效容量 Y"），不用动。

与阶段 2 一起改会更省：`ARCH-15` 之后酸痛回执会出现在更多场景，"你说的酸痛被记了 / 它会这样影响计划 / 这样纠正"是同一套语义，一次写完比分两次一致。

### 4.5 ARCH-11 数据目录默认绝对路径

`settings.py:37-40` 的 `Path(DEFAULT_DATA_DIR)` 改成基于仓库根的绝对路径（`Path(__file__).resolve().parents[1] / "data"`）。

风险评估：Docker 显式设了 `FITHEALTH_DATA_DIR=/app/data`，不受影响；`conftest.py` 也显式设了，不受影响。受影响的只有"从别的 cwd 跑脚本"——而那正是要修的 bug。

---

## 阶段 5：ARCH-10 环境（0.5 小时，可任意时候插入）

`vendor/` 只有 `.gitkeep`，镜像构建必然失败。要么把 `hello-agents==1.0.0` 的 wheel 放进去，要么改 Dockerfile 不依赖 `vendor/`。依赖锁只到版本号那一半，建议同时补 hash（`requirements.lock` 加 `--hash`）。

这是hello-agents的项目地址：https://github.com/jjyaoao/HelloAgents

`starlette==1.6.0` 与 `fastapi==0.141.1` 的兼容性我离线核实不了，这条要你在能联网的环境确认。

---

## 阶段 6：ARCH-09 测试治理（1~1.5 天，必须放最后）

前面五个阶段都会大量改测试，先治理要改两遍——这是清单原本的排序建议，仍然成立。

治理对象按类分：

**(a) 源码文本断言，约 50 处。** `assertIn(<源码片段>, body)` 只证明某个字符串还写在文件里：重构改名就红，逻辑改错却绿。分三类处理——能改成行为断言的改（首选）；只能验结构的改成 AST 断言（样板就是阶段 2.1 那条）；纯粹守着某字符串存在的删掉。分布：`test_extended_health_fields` 11 处、`test_checkin_fit_and_editor_fixes` 6 处、`test_journal_backup_and_restore_safety` 5 处、`test_reset_transactionality` 4 处，其余散落。

**(b) 已知假绿两处，必须查明原因再改。**
- `test_durable_writes_and_profile_locking.py:286` 的 `"atomic_write_json" in source or "os.fsync" in source`——`or` 短路，一边成立就绿，等于只验了一半
- `test_view_intent_stress_tz_and_trend_dedup.py:558-559` 断言 `pending_view_artifact` / `pending_reply_prefix`，而这两个标识符全仓不存在却仍然绿——**先查清它为什么绿**，这比改它更重要，因为同样的机制可能让别的断言也在空转

**(c) 对真实数据的依赖。** `tests/test_sleep_session_fields.py` 有 `skipif` 且明确不许退化成空断言，可作样板。其余依赖 `data/` 的要么加同样的 skipif，要么改用固定 fixture。**禁止**出现"真实数据窗口失效后退化成 `assertEqual({}.keys(), {}.keys())`"那种永久假绿。

**(d) 定时炸弹。** 写死日期窗口的断言逐条找出来，改成相对当前时间或冻结时钟。

**(e) `conftest.py` 只在 pytest 生效。** 这个仓库全是 `unittest.TestCase`，`python -m unittest discover tests` 完全绕过 `conftest.py` → `FITHEALTH_DATA_DIR` 未设 → `DATA-22` 原样复现（reset 用例删真实 `data/pending_workout.json`）。把环境变量设置挪到 `tests/__init__.py`（unittest 的包发现会执行它），或两处都放。同时按 `DATA-22` 修法 3 在 `ResetEndpointTest._originals`（`test_reset_transactionality.py:264-277`）里补上 `workout_store`——现在纯靠环境变量兜底，没有第二道防线。

**产出物**：一份治理清单（每条测试标"已改行为断言 / 已改 AST / 已删 / 保留并说明理由"）+ 一条阻止回退的检查（新测试不得使用源码文本断言，可用 AST 扫描实现）。

---

## 工期与验收

| 阶段 | 内容 | 人日 |
|---|---|---|
| 0 | 前置确认 | 0.1 |
| 1 | AGENT-18 / AGENT-14 / AGENT-12 | 0.2 |
| 2 | ARCH-14 + AGENT-19 / ARCH-15 | 1.0 |
| 3 | AGENT-17 / AGENT-20 / BUG-39 | 0.5 |
| 4 | ARCH-07 / 12 / 08 / 13 / 11 | 1.0 |
| 5 | ARCH-10 | 0.1 |
| 6 | ARCH-09 | 1.0~1.5 |
| | **合计** | **3.9~4.4** |

**批次级验收**（全部改完后一次性跑）：

1. 你那句复合输入同时得到 artifact + 回执 + 实质回答
2. `/chat` 的 AST 测试通过（无裸 `JSONResponse`）
3. `腰还酸` 只写核心、`腰伤` 拦核心+背部
4. bot 说"病史"不再否掉整次记忆
5. 拒绝过的事实不再以摘要形式出现在注入上下文里
6. 确认整条后取消再确认不抛 409
7. `python -m unittest discover tests` 与 `pytest tests/` 两种跑法下，`data/pending_workout.json` 都未被触碰
8. 治理清单里每条测试都有明确归属，没有"保留但说不出理由"的

**分工建议**（按你的跨厂商原则）：阶段 1、2、4.1 涉及安全语义与控制流，归 `opus5`；阶段 3、4.2~4.5 修法已经很具体，`opus4.8` 足够；**阶段 6 必须给 `sol`**——要识破的假绿正是 opus 这一系写出来的，让同一系审自己的卷子等于没审。阶段 5 交 `terra`。