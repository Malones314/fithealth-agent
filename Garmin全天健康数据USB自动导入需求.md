# Garmin 全天健康数据 USB 自动导入需求（审查稿）

> 状态：待审查  
> 日期：2026-08-14  
> 适用范围：FitHealthAgent 个人本地 Windows 版本  
> 不包含：训练活动数据自动导入。训练 `.fit` 继续由用户从 Garmin Connect 网站导出并手动上传。

## 1. 结论与可行性

方案总体可行，并且不依赖 Garmin Connect 的非官方接口，能够规避 Garmin 登录和私有接口变化造成的同步失效。

但正式开发前必须完成一个设备验证门槛：不同 Garmin 型号、固件和 USB 模式暴露的目录及 FIT 消息不同。部分设备使用普通盘符，部分设备使用 MTP；部分指标可能只在 Garmin Connect 云端计算，无法从手表文件 1:1 还原。因此第一阶段必须先制作只读盘点工具，用真实手表确认数据覆盖率，再决定哪些指标进入正式需求。

推荐策略：

- 每次洗澡或充电时连接手表，后台自动发现设备。
- 只读扫描手表的健康数据候选目录，将新文件复制到本地暂存区后解析。
- 自动导入已经验证的全天心率、压力、睡眠和静息心率数据。
- 前端显示连接状态、导入结果、数据覆盖范围和异常。
- 每日连接优于每周连接；在确认手表文件保留周期前，不承诺每周连接不会丢数据。
- GarminDB 不作为核心依赖。只有当某项必要指标被证明无法从设备读取时，再作为可选补充方案评估。

## 2. 产品目标

1. 用户不需要输入 Garmin 账号或密码。
2. 用户不需要手动寻找手表目录和选择健康文件。
3. 程序连接手表后自动识别是否存在尚未导入的健康数据。
4. 导入过程不会修改或删除手表中的任何文件。
5. 重复连接、重复扫描和文件更新不会产生重复数据。
6. 导入结果在前端可见、可追踪、可重试、可删除。
7. Agent 使用结构化健康数据生成恢复建议，但不负责文件发现、判重和落库。

## 3. 非目标

- 不自动导入 `GARMIN/Activity` 中的训练活动。
- 不实现 Garmin Connect 登录、网页抓取或非官方 API 同步。
- 不保证本地计算的睡眠分期、压力或静息心率与 Garmin Connect 完全一致。
- 不向手表写入文件，不同步训练计划、路线或设置。
- 第一版不支持 macOS/Linux；接口设计保留后续扩展空间。
- 第一版不处理实时蓝牙心率流。

## 4. 候选数据目录

实际目录必须以真实设备盘点结果为准，程序不得因为目录名称不存在而报致命错误。

| 优先级 | 候选目录 | 可能数据 | 处理策略 |
|---|---|---|---|
| P0 | `GARMIN/Monitor/` | 全天心率、步数、压力、强度分钟等监测数据 | 只读扫描并识别 FIT 消息类型 |
| P0 | `GARMIN/Metrics/` | HRV、生理指标或每日指标 | 只读扫描，按型号验证 |
| P1 | `GARMIN/Sleep/` | 睡眠区间或睡眠阶段 | 目录存在时扫描，按型号验证 |
| P2 | 其他含 `.fit` 的健康目录 | 固件或型号特有数据 | 仅在盘点阶段记录，不默认导入 |
| 排除 | `GARMIN/Activity/` | 训练活动 | 本需求明确不自动导入 |
| 排除 | `GARMIN/TempFIT/` | 未完成或临时活动 | 禁止自动导入 |
| 排除 | `GARMIN/NewFiles/` | 写入手表的导入入口 | 禁止读取为健康来源，更禁止写入 |
| 排除 | `Courses/Workouts/Sports` | 路线、训练模板、运动设置 | 不扫描 |

## 5. 指标覆盖验收表

盘点阶段需要把同一天的本地解析结果与 Garmin Connect 页面进行人工对照。

| 指标 | 最低可用数据 | 第一版目标 | 不能获取时的处理 |
|---|---|---|---|
| 全天心率 | 带时间戳的心率采样点 | 保存时间序列、日均/最低/最高、覆盖率 | 标记设备不支持，不从训练 FIT 推断全天数据 |
| 压力 | 带时间戳的压力值或区间 | 保存时间序列、日均/最高、有效覆盖时长 | 前端显示“设备文件未提供” |
| 睡眠 | 入睡、醒来、总时长 | 保存睡眠区间；有阶段时再保存深/浅/REM/清醒 | 不自行伪造 Garmin 睡眠分期 |
| 静息心率 | 设备提供的每日值，或有明确可复现规则 | 优先保存设备值；计算值必须标记 `derived` | 与 Garmin Connect 不一致时展示来源和算法 |
| HRV | 设备文件存在对应消息 | 可选保存夜间/每日值 | 第一版允许不支持 |

## 6. 数据与判重设计

全天心率可能每天产生数百到数千个数据点，不建议继续写入单个 `daily_records.json`。建议新建本地 SQLite 数据库 `data/wellness.db`。

### 6.1 导入清单 `device_imports`

| 字段 | 用途 |
|---|---|
| `id` | 导入任务 UUID |
| `device_id` | 设备稳定标识；取不到序列号时使用经过哈希的设备特征 |
| `source_path` | 手表内相对路径，不保存不必要的系统绝对路径 |
| `file_size` / `modified_at` | 快速判断文件是否变化 |
| `sha256` | 文件内容强判重 |
| `parser_version` | 解析器版本，支持未来重新解析 |
| `status` | `copied/parsed/imported/skipped/error` |
| `error_code` | 稳定错误码，不存完整敏感内容 |
| `imported_at` | 导入时间 |

### 6.2 时间序列 `wellness_samples`

建议唯一键：`device_id + metric_type + timestamp`。

字段至少包括：`timestamp`、`local_date`、`metric_type`、`value`、`unit`、`source_file_hash`、`source=device`、`quality`。

同一路径的监测文件可能会持续追加内容，文件哈希会变化。重新解析更新后的文件时，对已存在时间戳执行 UPSERT，不能重复插入。

### 6.3 每日汇总 `wellness_daily`

字段至少包括：日期、静息心率、最低/最高/平均心率、心率覆盖率、平均/最高压力、压力有效分钟、睡眠开始/结束/总时长、各睡眠阶段、HRV、来源、是否为本地推导、更新时间。

每日汇总由确定性 Python 代码计算，Agent 只读取结果并解释。

## 7. 设备监控状态机

```text
disconnected
  -> detected
  -> inventory
  -> copying
  -> parsing
  -> importing
  -> imported | no_new_data | partial | error
  -> disconnected
```

要求：

- 普通盘符设备优先使用卷信息检测；MTP 设备使用独立的 Windows Portable Devices 适配器。
- 后台每 10-15 秒检测一次即可，不需要高频轮询。
- 文件大小和修改时间连续两次稳定后才能复制，避免读取仍在写入的文件。
- 所有解析都针对本地副本，不直接解析设备文件。
- 设备中途拔出时任务安全失败，已成功提交的数据保留，未完成事务回滚。
- 单进程只允许一个设备扫描任务，防止 Uvicorn 重载或重复 Worker 重复导入。

## 8. 前端需求

### 8.1 设备状态

在现有页面顶部或上传区域附近显示紧凑状态：

- `未连接手表`
- `已检测到 Garmin，正在检查数据`
- `正在导入：3/8`
- `导入完成`
- `没有新数据`
- `导入部分失败`

### 8.2 导入通知

成功后显示例如：

> 已导入 2026-08-13 至 2026-08-14 的健康数据：心率 1,426 条、压力 682 条、睡眠 1 段、静息心率 2 天。

通知必须包含：日期范围、指标类型、数据点数量、跳过数量和失败数量。不能只显示“导入成功”。

### 8.3 数据管理

新增“设备健康数据”区域：

- 最近一次扫描时间和设备状态。
- 导入历史、来源文件、解析状态和错误原因。
- “立即重新扫描”按钮。
- “重新解析”按钮，用于解析器升级。
- 按日期删除导入数据。
- 是否保留原始 FIT 副本的设置。

第一版前端可每 5 秒请求一次设备状态接口；稳定后再考虑 SSE，不要求 WebSocket。

## 9. 后端接口草案

| 方法 | 路径 | 用途 |
|---|---|---|
| `GET` | `/device/status` | 当前连接状态、扫描阶段和进度 |
| `POST` | `/device/scan` | 用户手动触发一次扫描 |
| `GET` | `/wellness/imports` | 导入历史和错误信息 |
| `GET` | `/wellness/daily?start=&end=` | 每日健康汇总 |
| `GET` | `/wellness/samples?date=&metric=` | 某日某指标的时间序列 |
| `POST` | `/wellness/imports/{id}/retry` | 重试失败导入 |
| `POST` | `/wellness/imports/{id}/reparse` | 使用当前解析器重新解析本地副本 |
| `DELETE` | `/wellness/daily/{date}` | 删除指定日期设备数据 |

接口仍需遵守现有服务端请求预算和输入校验规则。

## 10. 实现步骤表

| 阶段 | 优先级 | 实现内容 | 交付物 | 验收标准 | 主要风险 |
|---|---:|---|---|---|---|
| 0. 设备实测准备 | P0 | 确认手表型号、Windows 识别方式、连接后的根目录 | 设备信息记录 | 能确认盘符或 MTP，不修改设备 | 型号差异 |
| 1. 只读盘点工具 | P0 | 枚举候选目录、文件大小/时间/hash；解析并仅输出 FIT message 类型和时间范围 | `garmin_device_inventory` 命令和盘点 JSON | 连接后 15 秒内生成清单；无任何写设备行为 | MTP 读取复杂 |
| 2. 数据覆盖实验 | P0 | 连续 3-7 天在充电时盘点；与 Garmin Connect 页面人工比对 | 指标覆盖报告 | 明确心率、压力、睡眠、静息心率分别能否获取及保留天数 | 云端派生指标缺失 |
| 3. Go/No-Go 审查 | P0 | 根据覆盖报告冻结第一版指标 | 已批准指标清单 | 每个指标都有真实文件和样本证据 | 未验证就进入开发 |
| 4. 设备检测适配器 | P0 | 实现盘符检测；如必要再实现 MTP/WPD 适配器 | `DeviceDetector` 接口及 Windows 实现 | 插拔可检测；非 Garmin 设备不误识别 | WPD 接口稳定性 |
| 5. 安全复制与判重 | P0 | 稳定性检查、复制到 inbox、SHA-256、导入清单 | `DeviceFileCollector` | 重复连接不重复复制；中途拔出不损坏数据 | 文件持续更新 |
| 6. 健康 FIT 分类器 | P0 | 按 FIT message 类型分类，不只依赖文件夹名称 | `WellnessFitClassifier` 和脱敏 fixtures | 未知文件安全跳过并记录原因 | 固件消息变化 |
| 7. 指标解析器 | P0 | 实现已批准的心率/压力/睡眠/静息心率解析 | 版本化解析模块 | 样本结果与覆盖报告一致；时区正确 | 指标语义误判 |
| 8. SQLite 存储 | P0 | 建表、迁移、UPSERT、事务、每日汇总 | `wellness.db` Store | 更新文件重扫无重复点；故障回滚 | 大量时序数据 |
| 9. 后台监控服务 | P1 | FastAPI lifespan 启停、轮询、任务锁、状态机 | `DeviceMonitorService` | 后台自动扫描；不会启动重复 Worker | Reload/多进程重复任务 |
| 10. API 与前端状态 | P1 | 状态、进度、导入历史、重试和删除接口；前端通知 | 设备状态和健康数据界面 | 用户能看到日期范围、数量和失败原因 | 前端轮询噪声 |
| 11. Agent 上下文接入 | P1 | 按用户问题查询每日汇总，必要时查询有限时间序列 | 只读健康数据工具 | Agent 不接收原始整日数据；回答标注日期和来源 | 上下文膨胀 |
| 12. 回归与故障测试 | P0 | 重复文件、追加文件、损坏 FIT、拔出、跨午夜、时区、无目录 | 自动化测试和实机测试报告 | 所有故障均有稳定状态，不丢已提交数据 | 实机样本不足 |
| 13. 原始文件保留策略 | P1 | 配置保留/删除本地副本及重新解析能力 | 设置项和清理任务 | 默认策略明确，删除不会影响已汇总数据 | 隐私和磁盘占用 |

## 11. 第一阶段盘点工具的输出示例

```json
{
  "device": {
    "transport": "drive_or_mtp",
    "device_id_hash": "...",
    "model": "unknown_until_detected"
  },
  "files": [
    {
      "relative_path": "GARMIN/Monitor/example.fit",
      "size": 12345,
      "modified_at": "ISO-8601",
      "sha256": "...",
      "fit_message_types": ["file_id", "monitoring_info", "monitoring"],
      "first_timestamp": "ISO-8601",
      "last_timestamp": "ISO-8601",
      "parse_status": "ok"
    }
  ]
}
```

盘点 JSON 默认不输出具体心率值，只输出结构、时间范围和数量，方便先确认可行性。

## 12. 验收场景

1. 未连接手表时服务正常运行，前端显示未连接。
2. 连接手表后 15 秒内进入扫描状态。
3. 没有新文件时显示“没有新数据”，不产生重复记录。
4. 文件路径相同但内容追加后，只导入新增时间点。
5. 扫描过程中拔出设备，任务标记失败或部分完成，数据库保持一致。
6. 损坏或未知 FIT 文件不会阻止其他文件导入。
7. 同一天多次连接不会重复计算或重复通知相同数据。
8. 跨午夜和 UTC/北京时间转换正确。
9. 前端能看到导入日期范围、各指标数量、跳过和失败情况。
10. Agent 查询“昨晚睡眠和今天恢复如何”时，只获取必要的每日汇总，并标注数据来源为手表本地导入。

## 13. 待用户审查的决策

- [ ] D1：第一版仅支持 Windows。
- [ ] D2：同意先开发只读盘点工具，盘点完成前不承诺具体指标。
- [ ] D3：默认每 10-15 秒检测设备；仅连接状态变化时扫描。
- [ ] D4：发现新数据后自动导入，不要求每次弹窗确认。
- [ ] D5：原始 FIT 本地副本默认保留 30 天，以支持解析器升级后重新解析。
- [ ] D6：接受本地推导的静息心率/睡眠结果可能与 Garmin Connect 不完全一致，并在界面标注来源。
- [ ] D7：第一版使用 SQLite 保存高频健康数据，不写入现有单个 JSON 记录文件。
- [ ] D8：每日连接作为推荐方式；完成保留周期实验后再判断每周连接是否可靠。

## 14. 推荐审批结果

建议批准 D1、D2、D3、D4、D7、D8。

D5 建议改为可配置：默认保留 30 天，用户可选择“不保留、30 天、永久”。

D6 必须等盘点和对照实验后逐指标审批：原始心率通常可以接受本地数据；睡眠阶段、压力和静息心率必须先验证设备文件语义及与 Garmin Connect 的差异。
