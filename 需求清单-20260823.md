# FitHealthAgent 代码审查与需求清单（2026-08-23）

> 上一份清单是 `需求清单-20260819.md`，其中列出的 DATA-01~16、BUG-01~18、AGENT-01~10、ARCH-01~06 已基本收尾。本次是**全量重审**：既覆盖 08-20 之后新增的模块（肌群恢复、酸痛反馈、餐食估算、wellness/METRICS 导入、周汇总、新前端面板），也复查了此前"已修复"的条目是否真的接线、有没有在修的过程中引入新缺陷。
>
> **本文不改代码**，只给需求清单与修改计划。

## 编号体系

沿用旧清单的族，从各族当前最大号往后接，便于你直接说"修 DATA-19"：

| 族 | 含义 | 本次新增区间 |
|---|---|---|
| `DATA-xx` | 数据正确性、丢失、损坏 | DATA-17 ~ DATA-39 |
| `BUG-xx` | 功能错误（含前端） | BUG-19 ~ BUG-46 |
| `AGENT-xx` | Agent 能力与上下文工程 | AGENT-11 ~ AGENT-24 |
| `ARCH-xx` | 架构、测试、环境 | ARCH-07 ~ ARCH-13 |
| `FEAT-xx` | 建议新增的功能（新族） | FEAT-01 ~ FEAT-18（FEAT-17 已取消，实为 17 条） |

每条都标了**状态**：`已验证`＝读了代码并在沙箱里实测过（真实 `data/` 只读、真实 FIT/ZIP、按文件路径单独加载模块跑函数）；`疑似`＝代码路径确定但需要特定数据或运行环境才能观测。

## 一句话结论

这一轮最要紧的不是"少了什么功能"，而是**三条安全与正确性链路各自都在静默失效**：

1. **急症拦截在最自然的中文说法下不生效**——`胸口有点疼`、`疼得很厉害`、`我最近没睡好，今天胸口很痛` 全部漏报（BUG-19）；即使命中了，也会被酸痛反馈的合成串覆盖成低一档（BUG-20）。这是关闭外部模型时唯一生效的防线。
2. **训练类硬约束会被记忆 Top-5 预算挤掉，而后端计划校验读的是同一份被截断的列表**（AGENT-11）——界面上显示"已确认：禁止深蹲、RPE 上限 7"，模型没收到，校验也没启用，全程无报错。
3. **健康数据在导入侧就在丢**：同时间戳的设备日级指标被 `INSERT OR IGNORE` 吃掉约 1/4（静息心率 411 行里**没有一行**与静息代谢共存，DATA-17）；HRV 七个字段整体错位一位，"昨夜平均"其实是 5 分钟峰值（DATA-18）；力量训练里 21% 的动作名张冠李戴（DATA-19）。

另有两处会**真的丢用户已输入的数据**：3 秒轮询把未确认的导入连同编辑内容顶掉且救不回来（BUG-27）；跑一次 `pytest tests/` 会删掉真实的 `data/pending_workout.json`（DATA-22）。以及一条不可逆路径：导入备份不落恢复点、整目录替换（DATA-21）。

---

# 零、收尾审查（2026-08-27，逐条核对当前代码）

第一~七批与 F1~F4 提交完毕后的全量复核结果。判定依据是**当前工作树的源码**（分支 `0827-F4`），不是提交信息。

**总计 89 条：已完成 86 条 ／ 部分完成 0 条 ／ 未完成 0 条 ／ 用户决定不做 2 条 ／ 已取消 1 条。**

> 2026-08-27 收尾：`DATA-18` 已实现；原 10 条"部分完成"已全部补齐（见下）；`DATA-19`
> 与 `FEAT-01` 按用户决定标记为不做；`FEAT-17` 早前已取消。全量测试 848 passed。

## 2026-08-27 补齐的 10 条

| 编号 | 补了什么 |
|---|---|
| `BUG-23` | `blocked` 的首个条件由 `not load.needs_reduction` 改为只看 `hours_remaining > 0`，`explicitly_requested` 判断这才真正生效；冗余的 `soreness_level != "painful"` 删除（painful 交给 `dynamic_safety` 的区域限制，不在两处重写）。原缺陷"有任何酸痛反馈即整体关闭硬校验"至此才真正修掉 |
| `BUG-25` | `explicitly_requested_recovery_regions` 改为按小句切分（复用 `health_safety.CLAUSE_SEPARATORS`，全仓库仍只有这一套定义），**先整句否决、再匹配肯定形态**，且肯定形态必须带请求语气。文档原文案例 `帮我安排今天的训练，腹部不要练` 现在返回 `[]`；`腰部不适影响训练` 也不再被当成"明确要求练腰" |
| `BUG-26` | **问题 3**：新增 `MUSCLE_META`（`muscle_id → (中文名, 区域)`，每 id 一条唯一真值），`MuscleRule` 只留 `muscle_id/keywords/exclude/role`（`zh`/`region` 改属性从 META 取），删掉后写覆盖的 `_RULES_BY_ID`，`muscles_for_sport` 强制 `role="primary"`，模型兜底改走 `MuscleHit.for_muscle`。**问题 1**：新增 `SECONDARY_WINDOW_FACTOR = 0.5`，当天只有次要命中的肌群窗口折半；`recovering` 条目带上 `role`，区域压制只采纳主项负荷。**问题 2** 补上覆盖率不变量测试 |
| `DATA-23` | `_collect_members`/`_read_archive` 改为按成员流式落盘（`_stream_into` 边拷边算 sha256/size，`_BackupMembers` 惰性读回），导出与恢复都不再把整份归档留在内存 |
| `DATA-24` | 设备指标加本地时刻闸门。**只加在静息心率与其基线上**——`resting_metabolic_rate`/`utc_offset_minutes` 是"设备对当天的陈述"，00:0x 给出完全正常，一刀切会凭空丢数据。用 `substr(timestamp_local, 12, 5)` 取墙钟，不用 sqlite 的 `time()`（那个会把带偏移量的 ISO 串换算成 UTC，本地 09:00 变 01:00 被误挡） |
| `DATA-25` | 强度分钟改 `is not None`，真实为 0 的当天不再写成 NULL、`intensity` 不再从 `available_sections` 消失；活动类型白名单改由 `_COUNTED_ACTIVITY_TYPES_SQL` 从常量插值生成，"同一口径"变成结构性的 |
| `DATA-34` | 抽出 `_in_window(item, start, end)`，`start is None` → 不过滤的契约写进 docstring 并加了回归 |
| `BUG-31` | 前端新增 `renderPlanValidation` 与 `applyWorkflowState`：逐条渲染 `plan_validation.violations`，并据 `workflow_state`（`needs_clarification`/`constraint_conflict`/`validation_failed`）禁用"保存计划"按钮 |
| `BUG-46` | 三个小项：档案补全提示去掉写反的 `intro.error` 条件并列出 `missing_fields`；journal 警告改为"有 message 就显示"，档位按 `available` 决定；`/analyze_food` 补 `except Exception` 回 502 + `error` 键（原先落到 FastAPI 默认 500 `{"detail":...}`，前端读 `data.error` 得 undefined） |
| `ARCH-08` | sqlite 快照移出 JSON 锁窗口；`JsonFileLock` 的 Windows 分支换成 `LK_NBLCK` + 显式退避重试，超时抛带中文说明的 `TimeoutError`（`OSError` 子类，既有 `except OSError` 链不受影响），并保证 `__enter__` 中途失败不泄漏句柄 |

新增回归：`tests/test_remaining_partials.py`（28 条）、`tests/test_arch08_data23_streaming_and_locking.py`、`tests/test_fit_parser_sport_routing.py` 的窗口契约用例、`tests/test_hrv_field_remap.py`（12 条）。顺带把 `tests/test_calorie_trend_and_bmr_removal.py` 里那条 grep SQL 文本的断言改成结构断言（ARCH-09 方向）。

## 🚫 用户决定不做（2 条）

两条同源于一个决定：**手表识别出的动作名本来就不准，动作名一律靠用户自己审核并手动修正，不依赖设备识别。**

| 编号 | 决定 |
|---|---|
| `DATA-19` | **不做精确动作名解码。** 当前实现已达成安全目标（`unknown_2` 分支已删、查表被 `sv=None` 短路、永远安全回落到类别中文名，不再张冠李戴）；剩余的分域枚举表与 `sv==255` 过滤不再做 |
| `FEAT-01` | **不做未识别动作清单与人工指认。** 既然设备识别出的动作本来就是错的，列"未识别动作"没有意义——列出来的不是真实缺口。映射不上的动作名继续在 `build_recovery_snapshot` 里静默 `continue`，这是接受的行为 |

## 未完成（0 条）

原本的两条（`DATA-18`、`FEAT-01`）已分别在 2026-08-27 实现／标记为不做。

## 部分完成（0 条）

下表是 2026-08-27 收尾前的差距记录，保留备查；每一条的补法见上面「补齐的 10 条」。


| 编号 | 已做 | 还缺 |
|---|---|---|
| `BUG-23` | `explicitly_requested` 参数已加并已从两处传入 | `main.py:455-456` 的 `blocked` 仍以 `not load.needs_reduction` 起头，已排除全部 needs_reduction 负荷，所以追加的 `not (load.needs_reduction and load.region in explicitly_requested)` 是**死代码**；文档指出的冗余条件 `soreness_level != "painful"` 也仍在 |
| `BUG-25` | 否定否决集合已加，`今天不想练腿`、`昨天练了腿…` 已修正 | 只在否定词**位于部位名之前**时生效。文档原文案例 `帮我安排今天的训练，腹部不要练` 仍返回 `['核心']`。未按修法复用小句切分／`is_negated_line` |
| `BUG-26` | 问题 2 全做完（`BASE_RECOVERY_HOURS` 补齐 10 项，值与 §B.2 分档一致）；问题 3 的 `_append_event` 已支持 secondary→primary 升级 | 问题 1 **未做**：`muscle_recovery.py:566` 的窗口仍只按 `muscle_id` 查表，次要肌群拿到与主项相同的窗口；区域压制与 `reduce`/`avoid` 仍采纳次要负荷。问题 3 未拆 `MUSCLE_META`，`_RULES_BY_ID` 仍后写覆盖，`muscles_for_sport` 未强制 `role="primary"` |
| `DATA-24` | 建行门槛 `sample_count >= 10` 已加，`get_daily_overview` 已改取"最新一个满足门槛的日期" | 修法 (3) 未做：设备指标查询无 04:00 时刻阈值，单条 00:0x 读数仍可能被当作当天值 |
| `DATA-25` | `steps`/`distance_m` 已加活动类型过滤 | 强度分钟仍是真值判断（`health_store.py:1006`、`:1010`），**真实为 0 的当天仍写成 NULL**，第四批验收条款"那 7 天显示 0 分钟而不是无数据"未达成；类型字面量也仍硬编码，未改用 `COUNTED_ACTIVITY_TYPES` 插值 |
| `DATA-23` | 导出侧未压缩／压缩后双闸门已加，两个常量已提到 1 GiB／3 GiB | 修法 (3) 流式落盘未做，导出峰值内存仍约为数据量的 2 倍 |
| `DATA-34` | `_parse_multisport` 已抽成独立函数、逐字段聚合、`start_bound is None` 时不过滤 | 未抽出修法点名的 `_in_window(item, start, end)` helper。纯重构差异，功能无缺口 |
| `BUG-31` | 非 2xx 且带 `reply` 的回合已记入 `sessionConversationHistory` | `plan_validation` / `workflow_state` 在 `templates/index.html` 里仍**零消费**（已 grep 确认），结构化违规提示与保存按钮禁用未实现 |
| `BUG-46` | 主要死代码已清：`buildTrainingRecordsPanel` 子树与 `.record-viewer` CSS、九处 `MEMORY_CONFLICT` 死分支（收敛为 `memoryConflictMessage`）、重复的 `_region_muscle_ids`、`CHECKIN_CATEGORY` 分支；`MuscleLoad.role` 已真正被读取；趋势图非有限值已前置过滤；`missing_fields` 已消费；死端点按计划有意保留待 FEAT 接线 | 三个小项未做：① `index.html:4463` 档案补全提示条件仍写反（`profile && !profile.complete && intro.error`，正常路径永不提示）；② `index.html:4465` journal 警告仍只在 `!storage.available` 时读；③ `main.py:279` `/analyze_food` 仍只捕获 `FoodAnalysisError` |
| `ARCH-08` | 三个 store 均已加 `RLock`，`plan_store` 读方法已纳入 | `json_file_lock.py:24-27` 的 Windows 分支仍是裸 `msvcrt.locking(LK_LOCK)`，无显式重试循环与中文错误；`backup_service.py:236` 的 sqlite 快照仍在 JSON 锁窗口内 |

## 其余说明

- `AGENT-24` 是信息类条目（关闭外部模型时的失效能力清单），本身无需修改。
- `FEAT-17` 已于 2026-08-24 取消，无遗留。
- `AGENT-11` 已按审查意见采用 `get_enforceable_memories()`（保持"记忆条目列表"契约，避免 ChatGPT sol 指出的接口漏洞），但键集合是内联而非共享模块级常量，且保存路径的校验仍传 `get_context_memories(n=5)`——目前不出错只因硬约束总被前置到 n 之外。属可维护性隐患，非当前缺陷。
- `DATA-22` 已按审查意见**无条件**改写 `FITHEALTH_DATA_DIR`（而非 `setdefault`）并断言不等于仓库 `data/`。
- `DATA-20` 采用了修法 1（`keep_raw_file=True`）而非修法 2（事务化 `reimport()`），数据丢失路径已消除。

---

# 一、P0：安全档位与拦截失效

## BUG-19 健康风险正则要求症状词字面紧邻，急症在自然中文下全漏报

- 状态：✅ 已修复（2026-08-24；隔离回归已验证）
- 位置：`fithealth_agent/health_safety.py:42-66`（规则表）、`:74-84` + `:103-108`（否定窗口）、`:111-133`
- 实测漏报：`胸口很痛`、`胸口有点疼`、`胸部隐隐作痛`、`我胸口压得难受`、`我的腰疼得特别厉害`、`心跳得很快`、`膝盖非常肿`、`刚才差点晕过去` → 全部 `None`。只有零间隔写法（`胸口闷`、`我今天胸痛`）才命中。
- 另一半问题：`_BEFORE_WINDOW = 14` 的否定窗口**不切小句**，`我最近没睡好，今天胸口很痛`、`以前练腿都没事，今天胸口发闷`、`上次是膝盖，这次胸痛得厉害`、`没有力气了，感觉快要晕倒` 全部被前半句的 `没/以前/上次` 连坐否决。
- 后果：`main.py:1738` 的 emergency 短路不触发，模型照常出训练建议；`main.py:2151` 的 artifact 拦截也不触发。模块 docstring 写的"宁可多报不可漏报"与实现相反。
- 修法：
  1. 站位词与症状词之间插入**排除标点**的受限缺口：`胸(?:口|部|前区|骨后)?[^，。；\n]{0,4}?(?:痛|疼|闷|发闷|压榨|紧缩|憋|发紧)`；`(?:疼|痛)得[^，。；\n]{0,4}?(?:厉害|受不了|不行|无法忍受|钻心)`；`心[跳率][^，。；\n]{0,4}?(?:很快|过快|太快|异常|不规律|乱)`。
  2. 否定判定改为**先按 `，。；！？\n、` 切出命中点所在小句、只在小句内回看**（`main.py:1326-1329` 的 `_CLAUSE_SEPARATORS` 已有同样做法，直接复用，不要再写第三套）。
  3. 单字触发词 `没`/`无` 收紧为 `没有|没什么|没事|无`，否则"没睡好""没吃饭"会一直误否决。
  4. 补词形：`胸口发紧`、`差点晕过去`、`站不稳`、`肿得厉害`。
- 必须同改：`tests/test_health_safety_gate.py:41` 的样本表——现有 18 条用的全是紧邻写法，所以这个洞一直是绿的。新增用例必须含"背景句 + 症状句"的形态。
- 风险：会增加误报，但误报代价只是多一段就医提示；漏报代价不对称。

## BUG-20 酸痛反馈覆盖 `health_risk`，把 emergency 降级成 urgent

- 状态：✅ 已修复（2026-08-24；端点级回归已验证）
- 位置：`main.py:1728-1731`
- 现象：`health_risk = screen_health_risk(message)` 之后**无条件**执行 `health_risk = screen_health_risk("、".join(painful_regions) + "剧烈疼痛")`。实测 `screen_health_risk("胸口痛")` = emergency，而合成串 `"胸部剧烈疼痛"` 中间隔了"剧烈"两字，emergency 正则不匹配，只命中 urgent 的"剧烈疼痛"。触发前提只是胸部最近 7 天练过（快照里有 `chest` 才会把"胸部"放进 `asked_regions`）。
- 后果：用户说"胸口痛"却拿到普通对话 + 减量提示，而不是"立即停止运动并就医"。
- 修法：不要覆盖，取更严重者。把档位比较（`emergency > urgent > caution`）与 `labels` 合并做成 `health_safety.merge_findings(a, b)` 导出，`main.py` 只调它，不在路由层重写档位逻辑。更干净的做法是让 `screen_health_risk` 接受结构化输入（区域 + 级别），彻底取消"用中文串在模块间传语义"。
- 必须同改：`tests/test_health_safety_gate.py` 增加端点级断言——胸部已进入恢复快照时，`胸口痛` 仍须返回 emergency。

## AGENT-11 训练类硬约束被 Top-5 挤掉，后端计划校验随之静默失效

- 状态：已验证（沙箱实测）
- 位置：`fithealth_agent/info_store.py:863-879`（`is_hard_constraint` + `hard + ordinary[:n]`）；消费点 `main.py:1898`、`:2030`、`:2098`、`:2160-2166`
- 现象：`is_hard_constraint` 只承认 `health.injury_or_constraint`、`health.recovery_status`、`plan.weekly_schedule`。`training.avoid_exercise` / `max_rpe` / `max_sets_per_exercise` / `max_total_sets` / `required_plan_elements` / `youtube.avoid_channel` 全被当普通记忆，受 `n=5` 限制。实测：确认「禁止深蹲」「RPE 上限 7」「排除某频道」后再确认 6 条 importance=5 的普通偏好，`get_context_memories(n=5)` 返回的 5 条全是普通偏好，**三条限制一条不剩**。
- 后果最严重的部分不是"模型没看到"，而是 `validate_generated_training_plan` 拿的是**同一份被截断的 memories**（`main.py:2160`），于是 forbidden/RPE/组数/必需元素列表同时变空——上一轮专门加的确定性校验就此失效，不报错、不留痕。`youtube_channels_to_avoid`（`main.py:2098`）同样失效。
- 修法（两处分开，缺一不可）：
  1. **执行侧不该受注入预算影响**：给 `InfoStore` 加 `get_enforceable_facts()`（内部 `resolve_confirmed_memory_facts(self._read_all())`，不做 Top-K），`main.py:1907`（`safety_constraints`）、`:1915`、`:2098`、`:2160` 全改用它。
  2. **注入侧**把 `is_hard_constraint` 扩成「凡 `validate_generated_training_plan` 会用到的 `(namespace, key)`」，并把这份集合提成**模块级常量供两边共用**，避免再次分叉。这些行并入 `hard_fact_lines` 后可绕过 Top-K（`format_cross_session_memories:847` 的 4000 字符预算是给普通事实的）。
- 关联：AGENT-23（档案视图显示"已生效"而模型没收到）是同一根因的表现。

**ChatGPT sol审查：** 核心缺陷成立，已用隔离的 `InfoStore` 复现：确认 `training.avoid_exercise`、`training.max_rpe`、`youtube.avoid_channel` 后，再加入 6 条同为 `importance=5` 且更新的普通事实，`get_context_memories(n=5)` 确实只返回 5 条普通事实；`main.py` 的生成后校验、保存时校验和频道过滤也确实消费这份截断结果。但当前修法存在接口漏洞：这里描述的 `get_enforceable_facts()` 返回 `resolve_confirmed_memory_facts(...)` 的结果，即“事实列表”，而 `validate_generated_training_plan`、`youtube_channels_to_avoid`、`resolve_plan_context` 当前接收的是“记忆条目列表”，并会在内部再次调用 `resolve_confirmed_memory_facts(memories)`。如果只按文中所说把调用参数直接替换为事实列表，解析结果会变空，约束仍然失效。应二选一：提供不截断的 `get_enforceable_memories()` 保持现有消费者契约；或让所有执行侧消费者明确改为接收已解析事实，并删除内部二次解析。共享常量也应覆盖所有执行侧会消费的键（包括 `health.*` 与 `youtube.avoid_channel`），不能只按某一个校验函数当前使用的键来定义。

## BUG-21 「有点酸痛」被判成 painful，把普通 DOMS 升级成急性损伤路径

- 状态：✅ 已修复（2026-08-24；酸痛分级回归已验证）
- 位置：`fithealth_agent/muscle_recovery.py:46-50`、`:108-115`
- 根因：`_PAINFUL_PATTERN` 含裸 `疼|痛`，且判定顺序**先于** `_SORE_PATTERN`，而 `痛` 是 `酸痛` 的子串。于是最自然的说法「手臂有点酸痛」「腿酸痛」「胳膊酸疼」全落 painful。
- 连锁后果：落盘 72 小时 painful → BUG-20 的合成串 → `avoid` 区域 → `dynamic_safety` 追加"停止该区域训练" → 计划校验按区域拦截该部位全部动作 → 用户明确要求练该部位时 409。用户唯一解法是去数据管理手删记录。文档 §B.3 明确规定「有点酸/还有点紧」= `sore`（只减量）。
- 修法：`_PAINFUL_PATTERN` 的裸 `疼|痛` 改成 `(?<!酸)(?:疼|痛)`，保留 `刺痛|剧痛|使不上劲|无力`；含"酸"的小句优先判 sore，但"又酸又疼"不得被降级。
- 必须同改：`tests/test_soreness_stage3.py:31` 只测了「手臂有点酸」，恰好绕过。补 `酸痛/酸疼/有点酸痛 → sore`、`刺痛/疼得厉害 → painful` 两组。

---

# 二、P0：正在产生错误数据

## DATA-17 同时间戳的设备日级指标被 `INSERT OR IGNORE` 静默吃掉，静息心率可能整天消失

- 状态：已验证（真实包实测）
- 位置：`fithealth_agent/health_store.py:410-439`（两张表的 `UNIQUE(source_file_id, timestamp_utc)`）、`:708-745`（写入）、`fithealth_agent/health_importer.py:278-291` + `:293-321`（产出）
- 现象：同一个 WELLNESS.fit 里 `monitoring_info`（静息代谢率 + UTC 偏移）与私有消息 `unknown_211`（静息心率 + 基线）**经常带同一个 timestamp**，各自 append 成一条 `device_metrics`，落库时后一条被 `OR IGNORE` 丢掉。实测 `data/health-imports/8fbb4a72c5af1a56-2026-08-21.zip`：9 个 WELLNESS 文件全部命中碰撞，34 条设备指标丢 9 条（26%），**被丢的固定是静息心率**（`monitoring_info` 在消息流里更靠前）。
- 库里的铁证：`SELECT COUNT(*) FROM device_daily_metrics WHERE resting_metabolic_rate IS NOT NULL AND resting_heart_rate IS NOT NULL` = **0**（411 行里一行都没有）。同一份包的 `12729122043_WELLNESS.fit` 在 13:56 既有 RMR 也有 RHR=55，库里只剩 `rmr=2230, rhr=NULL`。
- 强度分钟同理：同一 timestamp 上不同 `activity_type` 的 monitoring 消息各带一份**增量**，实测 437 条丢 8 条，损失 moderate 60 s——2026-08-21 库里记 `moderate_activity_min=17.0`，真实值 18.0。
- 目前日级值还能活下来，只因为一天有多个 WELLNESS 文件、总有一个不碰撞。**一天只有一个 WELLNESS 文件时（跨零点那天、USB 按天单文件导入的场景）当天静息心率整天消失且不留痕迹。**
- 修法：在 `_parse_fit_source` 里**按 `timestamp_utc` 归并后再产出**，而不是在落库时靠 UNIQUE 兜。`source["device_metrics"]` 与 `source["intensity_observations"]` 从 list 改成 `dict[timestamp_utc, entry]`：设备指标按列 merge（同列已有值保留先到者并记 warning）；强度分钟把 `moderate_activity_s`/`vigorous_activity_s` **相加**（它们是区间增量，`health_store.py:69-80` 已论证过），`intensity_level` 取 max。
- **不要**改成 `INSERT OR REPLACE`——那只是把丢失方向调个头（RHR 行覆盖 RMR 行）。UNIQUE 键保留（它仍挡住同一文件内的真重复）。
- 必须同改：`DEDUPED_INTENSITY_SQL`（`health_store.py:72-80`）继续用 `MAX(...) GROUP BY` 是对的，**不要顺手改成 SUM**——跨文件重叠导入仍需 MAX 去重。修完需重放全部 25 份包（先看 DATA-20，现有回灌脚本会删原件）。

## DATA-18 HRV 七个字段整体错位一位，"昨夜平均"其实是 5 分钟峰值

- 状态：✅ **已修复（2026-08-27）**——按 FIT SDK 语义重映射，映射表与全部证据集中在 `health_importer._HRV_STATUS_FIELDS` 上方的注释里（照 `_STRESS_TIME_FIELDS` 的先例）。要点与原判定的两处订正：
  1. **标题"七个字段整体错位一位"不准确**：字段 0 `weekly_average` 没有偏，实际只有字段 1~5 需要重命名。
  2. **`baseline_balanced_high` 不是哨兵值**：全量实测在 28299/31278/32768/34257/35498/37236/38229/40960/43194 之间变化。既然拿不出对照证据，就不猜语义、不套 ms 闸门、也不当 HRV 指标暴露，按原值存进 `unmapped_balanced_high_raw` 等将来有真值再定——避免"又一次静默丢掉尚未识别但真实变化的数据"。
  3. 毫秒闸门 `5~200` 只作用于六个真·毫秒量，越界值**丢弃并写进 `warnings`**，与"缺字段"区分开。
  4. `hrv_status_summaries` 与 `daily_health_summary` 都走 `_ensure_column` 加 SDK 语义新列（`*_ms`），fitfile 命名的旧列原样保留，不原地改语义。汇总侧直接抄同名列，**不再手写第二遍映射**（上一版正是在这里和导入侧各错一处）。
  5. 前端 HRV 卡片改用 `last_night_average_ms` 作主数值、显示**平衡区**而不是"基线 35-37"，并另列七日平均与 5 分钟峰值。
  - 全量验证：22 份真实 HRV_STATUS.fit（20 个日期）**22/22** 同时满足「七日平均落在平衡区内」与「low_upper < balanced_lower < balanced_upper 严格单调」，与设备自报的 `status=balanced` 自洽；昨夜平均与同日 `hrv_value` 采样均值 20/20 落在 ±2 ms 内。
  - 回归：`tests/test_hrv_field_remap.py`（12 条，含"原始值 → ms"的钉子与两条独立一致性证据）。历史数据回灌：`scripts/backfill_hrv_status.py`（默认预演，`--commit` 前落恢复点；因为 `baseline_balanced_upper` 旧库根本没存，必须重读原始包）。
  - 原始判定如下
- 位置：`fithealth_agent/health_importer.py:352-368`、消费侧 `fithealth_agent/health_store.py:1288-1298`
- 现象：`12723504216_HRV_STATUS.fit` 原始值 `weekly_average=5888, last_night=5632, last_night_average=8192, baseline_low=4480, baseline_high=4736, baseline_balanced_low=6272, baseline_balanced_high=40960`。代码照抄 fitfile 的字段名并统一 `/128`。对照 FIT SDK 的字段序（`weekly_average` / `last_night_average` / `last_night_5_min_high` / `baseline_low_upper` / `baseline_balanced_lower` / `baseline_balanced_upper`），fitfile 的命名**整体偏了一位**：
  - 库里的 `last_night`（44）才是真正的昨夜平均——被独立数据钉住：同日由 `hrv_value` 采样算出的 `hrv_avg` 是 **45.5**；
  - 库里的 `last_night_average`（64）其实是**昨夜 5 分钟峰值**，被当成"平均"给了前端和 Agent；
  - `baseline_low`(35)/`baseline_high`(37) 其实是"低区上限"和"平衡区下限"，平衡区真实上限在被丢弃的 `baseline_balanced_low`(=49) 里。
- 后果：`get_daily_health` 把这七个字段整包交给 `query_daily_health` 工具，Agent 会得出"HRV 46 远高于基线上限 37"的结论，而设备自己给的 `status` 是 `balanced`——46 落在真实平衡区 [37, 49] 内。**16 天全部 HRV 状态都是这个口径**，而 HRV 正是恢复建议的主要输入。`baseline_balanced_high=40960/128=320 ms` 明显是哨兵值，目前没有任何范围闸门。
- 根因：把上游库的字段名当语义契约照抄。项目在 `_STRESS_TIME_FIELDS`（`health_importer.py:75-96`）里已经踩过"fitfile 字段名不可信"这一课并写了长篇论证，同一课没应用到 HRV。
- 修法：按 **SDK 语义**重新映射（`last_night_average ← values["last_night"]`、`last_night_5min_high ← values["last_night_average"]`、`baseline_low_upper ← values["baseline_low"]`、`baseline_balanced_lower ← values["baseline_high"]`、`baseline_balanced_upper ← values["baseline_balanced_low"]`），并给所有 HRV 值加 `5 <= ms <= 200` 闸门。
- 必须同改：`hrv_status_summaries` 与 `daily_health_summary` 的列名——**走 `_ensure_column` 加新列、旧列保留一段时间**，不要原地改语义（否则同一列名在新旧数据里含义不同，比现在更糟）；`get_daily_health` 的 `status_fields`；前端 HRV 卡片文案；照 `_STRESS_TIME_FIELDS` 的先例写一段"为什么是这个映射"的注释 + 一条钉住"原始值 → ms"的测试。改完重放全部包。

**ChatGPT sol审查：** 主要结论成立：字段 1~5 按清单列出的 SDK 字段序解释时，当前 `fitfile` 名称确实逐位错配；同日 `last_night=44 ms` 与独立采样均值 `45.5 ms` 接近，而当前暴露的 `last_night_average=64 ms` 明显更符合 5 分钟峰值。数据库中的 16 个日期也全部呈现“设备状态为 balanced，但旧列 `baseline_high=37` 会把当夜/周均值误解成越界”的矛盾。不过标题“七个字段整体错位一位”不准确：`weekly_average`（字段 0）本身没有错位，文中修法实际只重新命名字段 1~5；原始列表中的第 7 个浮点字段 `baseline_balanced_high`（字段 8）没有获得 SDK 语义映射。把它称为“明显的哨兵值”也缺少证据：全量真实包中该原始值会在 `28299`、`31278`、`34257`、`37236`、`40960`、`43194` 之间变化，并非常量哨兵。应先按 SDK 的字段 8 定义单独确认其语义和单位，再决定保存或丢弃；在此之前，不宜用统一的 `5 <= ms <= 200` 闸门把它静默过滤。范围校验应按字段语义分别制定，并记录被拒绝值，否则可能把尚未识别但真实变化的数据再次静默丢掉。

## DATA-19 力量训练动作名解码错位，21% 的组被张冠李戴

- 状态：🚫 **用户决定不做（2026-08-27）**——智能穿戴设备给出的训练名称本就不准，用户主要靠自己手动输入动作名称，不需要准确的动作名解码。当前实现已达成安全目标：`unknown_2` 分支已删，`fit_parser.py:291-292` 把 `sv`/`u2v` 硬编码为 `None` 使查表成为死代码、永远安全回落到 `_CATEGORY_ZH[main_en]`，不再张冠李戴。**剩余的分域枚举表、`sv==255` 过滤与名称解码测试不再做。** 原始判定如下
- 位置：`fithealth_agent/fit_parser.py:216-234`（`_EXERCISE_SUBTYPE` 扁平表）、`:291-295`（`unknown_2` 分支）、`:296-304`（查表）
- 问题一：FIT 的 `category_subtype` 是**按 category 分域**的（每个 category 各有一套 `*_exercise_name` 枚举、都从 0 开始），代码却用一张全局 `int -> 中文名` 表查。实测 `628992811_ACTIVITY.fit`：4 组的原始字段是 `category=(9, 65534, 7)` / `category_subtype=(2, ...)`——category 9 是 `flye`（飞鸟），flye 的 subtype 2 是哑铃飞鸟；代码查全局表拿到 `_EXERCISE_SUBTYPE[2] = "下斜卧推"`，于是 4 组飞鸟被标成下斜卧推，而 `category_raw` 老实写着 `flye`——**同一条记录里中文名和英文名互相矛盾**。19 个 active 组里 4 组（21%）被误标。
- 问题二：`unknown_2` 被注释为"最精准的 subtype"并排在 `category_subtype` **之前**，但实测它的取值成对互补、非重复项之和恒为 254/255（`(204,51)`、`(178,178,76)`、`(242,242,12)`…），这是**归一化到 255 的类别置信度分布**，不是枚举 id。目前侥幸未出事只因 `main_pos` 总落在大数一侧；而小数一侧的 `51=弓步蹲`、`63=引体向上`、`89=深蹲`、`102=三头下压` 都是表里的合法键，`category=(65534, 7, 0)` 这种形状一旦配上不同的 `unknown_2`，一组弯举就会被命名成"弓步蹲"。
- 后果：错名直接进 `daily_records.json`，交给 ReAct 做训练分析，也是肌群恢复推断的输入（BUG-26）。用户现有记录里的动作名全是手改过的，说明他一直在替解析器擦屁股。
- 修法：
  1. `_EXERCISE_SUBTYPE` 改成 `dict[str, dict[int, str]]`，外层键用 `main_en`（`bench_press`/`squat`/`flye`…），内层按 SDK 对应枚举填；`_decode_exercise_name` 改 `.get(main_en, {}).get(int(sv))`，查不到就回落 `_CATEGORY_ZH[main_en]`——**回落比猜错更有价值**，"飞鸟"总比"下斜卧推"对。
  2. `sv == 255` 排进无效值过滤（现在 255 会命中"通用动作"，把明确类别换成一句废话）。
  3. **删掉 `unknown_2` 分支**。它在三份文件里没有一次命中过正确名字，只提供风险；想留证据就原样塞进 `ActivitySegment.extra["category_confidence"]`（`to_dict` 已会带出 `extra`）。
- **不要做数据迁移**：历史记录的名字用户已手改过，重算会冲掉他的修正。三份真实 FIT 可做无回归比对；`tests/test_fit_parser_sport_routing.py` 不覆盖名称解码，需补用例。

---

# 三、P0：会丢数据 / 不可逆

## DATA-20 回灌与重导脚本"先删后重导"，一次解析失败即永久丢掉原始包

- 状态：已验证
- 位置：`scripts/backfill_health_fields.py:198-206`、`tool-output/reimport_real_health.py:17-39`；配合 `fithealth_agent/health_store.py:1569-1614`（`delete_import` → `_delete_raw_file`）、`fithealth_agent/health_importer.py:644-663`
- 现象：循环体先 `store.delete_import(id)`（提交事务后 **unlink `data/health-imports/` 里的原始 zip/fit**），再 `service.import_file(...)`。而 `import_file` 的解析发生在 `raw_path.write_bytes(content)` **之前**：解析抛异常时新文件根本没写出来，`except Exception` 只 `print` 一行 `✘` 就 `continue`，内存里的 `content` 随下一轮迭代丢弃。结果是 import 行、级联样本、唯一可再解析的原始文件三者同时消失，脚本继续跑完并打印"失败 1 份"。**这条脚本存在的理由恰恰是"解析器刚扩展过"，解析失败是最可能的分支。**
- 现场证据：`health_imports` 里有 3 条 `raw_path` 指向已不存在的文件（`0bc92c8ea48b49ef-628224559.zip`、`6debb86f49d8180d-2026-08-15.zip`、`b6dae22b1de75135-628252995.zip`）。
- `tool-output/reimport_real_health.py` 更危险：没有 `--apply`，一跑就改真实库，且备份逻辑是 `if not BACKUP_PATH.exists()`——而 `tool-output/health-before-content-reparse.db` 已存在（450 KB / 08-14，那时库还很小，现在 25 MB），再跑一次会**完全跳过备份**，用一份 8 天前的快照冒充退路。路径还硬编码到桌面。
- 根因：`delete_import` 的语义是"用户主动删除导入"（删原件是对的），却被复用成"重导入前清场"的原语，两者对原始文件的期望正好相反。
- 修法（推荐第 2 条）：
  1. 最小改动：`delete_import(..., keep_raw_file: bool = False)`，脚本传 `True`。同一 sha256 的 raw 文件名 `{digest[:16]}-{name}` 完全一致，`import_file` 会写回同一路径、内容逐字节相同，不会产生重复文件。
  2. 正确设计：给 `HealthImportService` 加 `reimport(import_id, filename, content)`——**先解析拿到结果**，再在同一个 sqlite 事务里删旧行 + `save_import`，失败整体回滚，原始文件全程不动。
  3. 顺带把脚本的 `except` 从"打印后继续"改成"打印后 `return 1` 中止"——继续下一份只会扩大损失面。
  4. 删除 `tool-output/reimport_real_health.py`，或改成默认预演 + `--apply` + 备份走 `LocalBackupService.write_recovery_point()`。同目录的 `verify_health_import.py` 是正面对照（全程在 `TemporaryDirectory` 里），可留。
- 另需同改：`health_importer.import_file` 把 `raw_path.write_bytes(content)` 提到解析之前，或在失败路径里保证 raw 文件仍存在。

## DATA-21 导入备份不落恢复点，且整目录替换——恢复动作本身不可逆

- 状态：已验证
- 位置：`main.py:3660-3679`；`fithealth_agent/backup_service.py:386-403`、`:439-454`
- 现象：`/data/reset` 严格遵守"写不出快照就一个字节都不删"（`main.py:4363-4377`），但 `/data/backup/import` 在 `restore(content)` 前**没有任何快照**。restore 会：`os.replace` 覆盖 4 个必备 JSON；v2 备份还会 `unlink` 备份里没有的可选 JSON；`_move_aside` + `os.replace` **整目录替换** `health-imports/` 与 `hr_streams/`（`transaction_dir` 在 `finally` 里 `shutil.rmtree`，挪走的旧目录随之永久消失）；覆盖 `health.db`。
- 后果：「导入一份两周前的备份看看」= 永久删除这两周新导入的全部原始包、心率流与库内容。回滚只在 restore **自身失败**时生效，成功的 restore 没有退路。测试 `tests/test_journal_backup_and_restore_safety.py:398-407`（`test_restore_replaces_the_directory_wholesale`）甚至把这个破坏性当作期望钉住了。
- 根因：DATA-14 定下的"不可逆批量删除必须先落恢复点"只应用到了 `/data/reset`，而 restore 的破坏性与它等价。
- 修法：在 `LocalBackupService.restore` 里，`_read_archive` 校验通过之后、进入 `_apply` 之前调用 `write_recovery_point()`；写不出快照就抛错、拒绝恢复。需要把 `_RECOVERY_POINT_NAME` / `_next_recovery_point_path` 的前缀参数化（例如 `pre-restore-`），并让 `list_recovery_points` 同时识别两种前缀。
- **加锁顺序坑（必须注意）**：`write_recovery_point` 内部会 `export_bytes` → `_locked_json_targets`，而 `JsonFileLock` 不可重入。所以要么在 `stack.enter_context(self._locked_json_targets())` **之前**调用，要么把 `export_bytes` 拆出一个不再加锁的 `_export_locked()` 在锁内复用。
- 同改：前端确认文案补上"已自动生成恢复点 X"（`templates/index.html:2309-2310` 现在只有一句"此操作不可撤销"，防护强度低于风险更小的 `/data/reset`）。

## DATA-22 跑一次 `pytest tests/` 会删掉真实的 `data/pending_workout.json`

- 状态：已验证（静态推导，刻意没实跑）
- 位置：`tests/test_generated_plan_safety_rules.py:7`（模块级 `import main`，未设 `FITHEALTH_DATA_DIR`）→ `fithealth_agent/workout_store.py:38`（`_PERSIST_PATH = data_path("pending_workout.json")`，**模块级求值**）→ `main.py:4317-4320`、`:4337`（`clear_pending_workout_step` → `clear_current()`）→ `workout_store.py:318`（`unlink(missing_ok=True)`）
- 现象：pytest 在**收集阶段**就 import 全部测试模块，按字母序 `test_generated_plan_safety_rules.py` 早于 `test_reset_transactionality.py`，它顶层 `import main` 且不设环境变量，于是 `settings.data_dir()` 返回相对路径 `data`，`_PERSIST_PATH` 永久绑定到仓库根下**真实的** `data/pending_workout.json`。`ResetEndpointTest.setUpClass`（`tests/test_reset_transactionality.py:265-287`）很小心地替换了 8 个 store 实例，但 `workout_store` 是**模块**而不是实例、`_PERSIST_PATH` 是模块级常量，替换不到。随后每条调用 `/data/reset` 的用例（至少 6 条）都会 unlink 一次真实文件。
- 现场吻合：`data/pending_workout.json.lock` 存在（19:36 前后被 touch 过）而 `data/pending_workout.json` 不存在。
- 丢的是待确认训练，看似临时数据，但它可能携带 1802 条 1Hz 心率这类别处不存在的内容（正是 DATA-05 那份被误隔离文件的价值所在）。
- 修法（前两条建议都做）：
  1. 加 `tests/conftest.py`，在任何 `import main` 之前 `os.environ.setdefault("FITHEALTH_DATA_DIR", <进程级临时目录>)`。
  2. `workout_store.py:38` 的模块级常量改成函数 `def _persist_path() -> Path: return data_path("pending_workout.json")`，所有引用点（`clear_current`、`_quarantine_files`、`_quarantine_path`、`_next_corrupt_path`、`reload_from_disk`、`:380` 的写入）改为调它。这同时修掉"容器里 import 后再设 `FITHEALTH_DATA_DIR` 则待确认训练写错位置"的问题——这是全仓库唯一违反 `settings.py:23-24` 明文约定（"每次调用都重新读环境变量"）的地方。
  3. `ResetEndpointTest` 里 `mock.patch` 掉 `main.workout_store`。

**ChatGPT sol审查：** 核心风险成立。当前默认收集顺序下，`tests/test_generated_plan_safety_rules.py` 会在未设置 `FITHEALTH_DATA_DIR` 时顶层导入 `main`，使 `workout_store._PERSIST_PATH` 绑定仓库的真实 `data/pending_workout.json`；`ResetEndpointTest` 虽替换了 8 个 store 实例，却没有替换 `main.workout_store` 模块，成功、部分失败及恢复点相关的多条 reset 用例都会执行真实的 `clear_current()`。但修法 1 使用 `os.environ.setdefault(...)` 仍是漏洞：若运行测试的 shell 已把 `FITHEALTH_DATA_DIR` 指向生产/真实目录，`setdefault` 不会覆盖它，测试仍会删真实数据。测试启动必须无条件把该变量改到会话级临时目录，并保证目录对象存活到整个 pytest 进程结束；同时在导入应用前断言解析后的数据目录不等于仓库 `data/`。修法 2 也不应只把常量机械改成“每次读取环境变量”的函数，因为测试或运行时若中途修改环境变量，同一份待确认状态的读写可能跨目录；更稳妥的是让 workout store 像其他 store 一样由构造参数固定根目录并可依赖注入。修法 3 只能隔离 reset 用例，不能替代收集阶段的全局保护。

## DATA-23 备份能导出但导不回来：解压上限只在恢复侧生效

- 状态：✅ **已修复（2026-08-27）**——三条修法全部落地。导出侧未压缩／压缩后双闸门 + 常量提到 1 GiB／3 GiB；`_collect_members`／`_read_archive` 改为按成员流式落盘（`_stream_into` 边拷边算 sha256/size，`_BackupMembers` 惰性读回），导出与恢复都不再把整份归档留在内存
- 位置：`fithealth_agent/backup_service.py:82-85`、`:227-241`；导出侧 `:196-220` 无任何体积检查
- 现象：`export_bytes` 把所有成员读进内存拼 zip，不做体积判断，永远"成功"。`_read_archive` 有两道闸门：压缩后 > 100 MiB 或 `sum(info.file_size) > 300 MiB` 直接 `ValueError`。当前实测数据目录未压缩合计 26.5 MB（`health.db` 25 MB），压缩后 4.0 MB。`health.db` 主要是分钟级心率样本，按约 1.5 MB/天的增速，未压缩总量在半年到一年内越过 300 MiB——此后导出的备份**在需要它的那一刻**才会被拒绝。`/data/reset` 的恢复点走同一条 `validate/restore` 路径，同样失效。
- 修法：
  1. `export_bytes` 拼完 `members` 后立即校验未压缩合计与压缩后大小，超限抛错，并在 `/data/backup/export` 与 `write_recovery_point` 的调用方给出明确提示（"数据已超过备份上限，请先归档历史 health-imports"）。
  2. 两个常量提到与实际增长匹配的量级（例如 1 GiB / 3 GiB）。
  3. `_read_archive` / `_collect_members` 改成按成员流式落盘——现在导出峰值内存约为数据量的 2 倍。

## BUG-27 3 秒轮询把未确认的导入连同编辑内容顶掉，之后既救不回也丢不掉

- 状态：已验证
- 位置：`templates/index.html:3726`（`setViewerMode('training')`）、`:2871`（`loadViewerDay()` 未 await）、`:2852→2861`、`:3718`、`:3792`、`:3823`、`:3864`
- 现象：`refreshWorkoutState` 在确认"有 pending workout"之后、**在指纹短路（`:3728`）之前**无条件 `setViewerMode('training')`；后者结尾是未 await 的 `loadViewerDay()`，若当天有已保存记录就走 `loadViewerSelection → loadSavedTrainingRecord(item.id, false)`，写入 `activeSavedTrainingRecordId` 与 `$confirm.dataset.savedRecordId`。因为 fetch 比同步的 `renderWorkout(data.workout)`（`:3735`）晚落地，**最终留在编辑区里的是已保存记录**。
- 之后每一次 tick 都被 `:3718` 的 `if (savedRecordEditingTarget()) return;` 挡掉，pending workout 再也不会被渲染；而 `recoverPendingWorkout`（`:3792`）与 `discardPendingWorkout`（`:3823`）同样以 `savedRecordEditingTarget()` 提前返回——那次导入既不能继续编辑、不能撤销、也不能清空，只能刷新页面。**用户带着未提交编辑时，编辑内容直接消失。** 触发条件只是"当天已有保存记录 + 新导入一份 FIT"。
- 修法：
  1. `:3726` 移到 `:3728` 的指纹短路**之后**，且只在 `viewerMode !== 'training'` 时才切；更好的做法是给 `setViewerMode` 加 `{reload: false}` 参数，轮询路径只切 tab 不重新拉数据。
  2. `loadViewerDay` 加自增 `viewerLoadToken`，回调里 token 不匹配就丢弃结果。
  3. `loadViewerSelection` 在 `hasWorkout && !savedRecordEditingTarget()` 时不得自动 `loadSavedTrainingRecord`（改为要求用户显式点选）。
  4. `refreshWorkoutState` 加在飞标志，避免 `setInterval` 叠请求。

## DATA-29 打卡字段合并语义：已保存的值无法清空，餐盘估算"全删"无效

- 状态：已验证
- 位置：`fithealth_agent/storage.py:144-190`（`merged.update(record)`）+ `fithealth_agent/daily_checkin.py:112-136`（`if value in (None, ""): continue`）+ `templates/index.html:1321`（`if (pendingMealEstimates.length) payload.meal_estimates = ...`）
- 现象：表单留空的字段在校验阶段就不进 `record`，`upsert_dated_record` 又只做字段合并，两者叠加使"清空某字段"在系统里**根本不可表达**。体重误填 65 可以改成 66，但永远删不掉；错填的 `cheat_meal=true` 同理。
- 餐盘估算更直接：用户把当天所有餐次都"移除"再保存时，请求里没有 `meal_estimates` 键，磁盘上那份数组原样保留，UI 提示"已更新"，重开弹窗全部回来。移除其中一部分（数组非空）才正常。
- 修法：
  1. 前端 `submitCheckin` **无条件**写 `payload.meal_estimates = pendingMealEstimates`（含空数组）；`_validate_meal_estimates` 放开"空列表报错"，只保留上限 20。
  2. 引入显式清空通道，不改默认合并：`validate_daily_checkin` 额外返回 `cleared: list[str]`（收 payload 里**显式为 `None`** 的已知字段，用 `field in payload and payload[field] is None` 与"没传这个键"区分）；`upsert_dated_record(..., clear_fields=())` 在 `merged.update(record)` 之后 `pop`。前端清空的输入框要发 `null` 而不是省略键。
  3. `meal_estimates` 建议直接改为整体替换而非合并。
- 关联：`applied_to_form` 的丢失（BUG-34 一节）会让删除/编辑餐次不回写记录级热量，两者要一起修才不会出现"合计与明细分叉"。

---

# 四、P1：数据口径与静默丢失

## DATA-24 跨零点的"幽灵日"成了默认健康概览，还带着前一天的静息心率

- 状态：✅ **已修复（2026-08-27）**——(1)(2) 早前已做；(3) 现按 `substr(timestamp_local, 12, 5) >= '04:00'` 加了本地墙钟闸门。**只加在静息心率与其基线上**：`resting_metabolic_rate`／`utc_offset_minutes` 属于「设备对当天的陈述」，00:0x 给出完全正常，一刀切会凭空丢数据——范围校验按字段语义分别制定。刻意不用 sqlite 的 `time()`，它会把带偏移量的 ISO 串换算成 UTC，本地 09:00 变 01:00 反而被误挡
- 位置：`fithealth_agent/health_store.py:1312-1338`（`get_daily_overview`）、`:913-923`（建行条件）、`:902-912` + `:94-102`（设备指标取当天最后一条）、`health_importer.py:260-271`（只对活动行做了午夜回拨）
- 现象：`get_daily_overview()`（前端默认视图）返回 **2026-08-23**：`heart_rate = {min:66, max:66, avg:66.0, samples:1}`、`device.resting_heart_rate = 61`。这一天库里只有一个 00:00 整的采样点——它是 08-22 那份包里 `16:00Z` 的样本，本地时间正好落在 08-23 00:00；那个 `resting_heart_rate=61` 是 08-22 的值。注释（`:94-96`）说"取当天最后一条正好避开前一天的值"，但某天**只有**残留行时必然取到前一天。
- 修法：(1) `_rebuild_daily_summary` 给建行加最小证据门槛（`sample_count >= 10` 即可挡掉单点残留，正常一天有 1000+ 点），只有残留行的日期走已有的 `DELETE FROM daily_health_summary` 分支；(2) `get_daily_overview` 的 `MAX(day)` 改成"最新一个满足门槛或有睡眠数据的日期"；(3) 设备指标要求时刻晚于某阈值（如 04:00），单条 00:0x 的读数按语义属于前一天。
- **不要**把午夜样本统一回拨 1 秒：`timestamp_utc` 是跨文件去重的唯一键（`DEDUPED_HEART_RATE_SQL`），动它会让重叠导入的去重失效。

## DATA-25 真值判断代替 `is not None`：强度分钟 0 写成 NULL、步数空写成 0

- 状态：✅ **已修复（2026-08-27）**——强度分钟改 `is not None`，真实为 0 的当天写 0 而不是 NULL，`intensity` 不再从 `available_sections` 消失（第四批验收条款「那 7 天显示 0 分钟而不是无数据」达成）；活动类型白名单改由 `_COUNTED_ACTIVITY_TYPES_SQL` 从 `COUNTED_ACTIVITY_TYPES` 插值生成，「同一口径」从注释约定变成结构约束
- 位置：`fithealth_agent/health_store.py:944-955`（强度）、`:872-873` + `:965`（步数）
- 现象：`if has_intensity and intensity["moderate_s"]` —— `0` 是 falsy，于是当天增量确实为 0 时写成 `None`，`intensity` 整节从 `available_sections` 消失。实测 16 天里 **7 天**（08-08、09、10、11、13、16、22）落到这个分支，而这些天各有 420-490 条观测。08-08 走了 8371 步、活动 110.7 分钟却显示"没有强度数据"。反方向：`steps` 取 `SUM(COALESCE(steps,0))`，有行时永不为 NULL，只有 `generic` 观测的日子会显示"0 步"而不是"没有步数数据"。
- 附带口径不对称：`active_calories`/`active_time_s` 用 `activity_type IN ('walking','running','cycling')` 过滤以避开 `generic` 重复计数，`steps`/`distance_m` 却对全部类型求和。当前 `generic` 无 steps 所以没出事，一旦某个固件给 `generic` 也填 steps，步数立刻翻倍。
- 修法：强度改 `is not None`；`steps`/`distance_m` 加同一套类型过滤，并把硬编码的字面量列表改成用已有的 `COUNTED_ACTIVITY_TYPES` 常量插值（与 `DEDUPED_*_SQL` 同思路），让"同一口径"是结构上的。

## DATA-26 未过完的当天，日汇总用整天静息代谢，与小时曲线是两套口径

- 状态：已验证
- 位置：`fithealth_agent/health_store.py:928-943`（日汇总）对 `:1352-1427`（`_cumulative_calories_by_hour`）
- 现象：小时曲线刻意把 RMR 按 `×(h+1)/24` 摊分并"只画到真正有数据的最后一小时"，理由写得很清楚；而 `_rebuild_daily_summary` 给 `resting_calories` 赋的是**整天 2230 kcal**。于是同一天里日汇总说"总消耗 2292"、小时曲线末点说"1022"，两者用同一个词。整天完整时相等（实测 08-22 两者都是 2292.0），**只有未过完的当天会错——而那正是用户最常看的一天**。
- 修法：`_rebuild_daily_summary` 按当天已覆盖时长摊分（用已算出的 `coverage_end` 或 `daily_activity_observations` 的最大 `timestamp_local` 取 `h`，写 `RMR×(h+1)/24`），日速率原值继续留在已有的 `resting_metabolic_rate` 列，两者语义与名字分开。
- 同改：`get_daily_health` 的文案；`tests/test_calorie_trend_and_bmr_removal.py` 那条"整日曲线末点等于日汇总"的断言在 h=23 时系数为 1 仍成立，不用放宽，但应**再加一条**"只覆盖到 10 点的日子，日汇总 resting 不得等于整天 RMR"。历史 16 天全是完整日，值不变，无需迁移。

## DATA-27 覆盖导入丢 `hr_stream` 指针，孤儿心率流与孤儿原始文件都没有出口

- 状态：已验证
- 位置：`fithealth_agent/workout_store.py:1165-1227`；`health_store.py:1569-1614` + `:1616-1625`
- 现象一：`record` 字典里没有 `hr_stream` 键，而覆盖分支 `update_record_if_revision(..., record=record)` 是**整条替换**。一条带 `hr_stream` 摘要的已保存训练被一份没有心率流的 FIT 覆盖后，摘要消失而 `data/hr_streams/<id>.json` 仍在磁盘上——没人引用、也没有入口能看到或删掉；反之新的有流时会用同一个 `record_id` 覆盖旧文件，旧的 1Hz 流无声换掉。这里还是**两次独立写盘**（先落不含摘要的记录 → 写流文件 → 再补摘要），中间崩掉就留孤儿。
- 现象二：`health-imports/` 同样没有对账能力。`clear()` 只删"库里列得出来的"，`delete_import` 只删自己那份。实测磁盘上有 3 个孤儿（`0aa9e133bbac2418-1_.csv`、`3c00d4116bbaab77-csv`、`552ef2eeb2b16463-2026-08-15_2_.zip`），库里也有 3 条指向已不存在的文件，两边都没有界面能看到。对比 `workout_store.list_quarantined()/delete_quarantined()` 已经补齐了"列出 + 标记 + 删除"三件套。
- 修法：(1) 覆盖分支先从 `duplicate.get("record")`（`workout_store.py:1134` 已在手上）读出旧 `hr_stream`，按"新的有流用新的、没有则保留旧的"合并进 `record`；真要换掉旧流时先调已存在但无人调用的 `HRStreamStore().delete(old_id)`。(2) 新增 `HealthStore.audit_raw_files() -> {"orphans": [...], "missing": [...]}` 与 `delete_orphan_raw_file(name)`（走 `resolve_raw_file` 做归属校验），挂到数据管理面板与隔离文件区块并列。**不要**在 `clear()` 里 `rmtree(raw_dir)`——会删掉用户手动放进去待导入的文件。

## DATA-28 睡眠起点忽略文件里明确的 `sleep start` 事件，躺床时长少算

- 状态：**已修复（2026-08-24）**，且顺带发现问题比原判严重得多，见下
- 位置：`fithealth_agent/health_importer.py`（`_sleep_bed_window` / `_sleep_session`）、`health_store.py:get_sleep`
- 原现象：终点读了 `event=sleep / event_type=stop`，起点却用第一条 `sleep_level` 的时间戳。实测 5 天里首条分期记录比 `sleep start` 晚 1 / 5 / 7 / 11 / 14 分钟，这段既不进 `time_in_bed_min` 也不进 `_night_heart_rate` 窗口。
- **更严重的问题（原清单没抓到）**：`duration_min` 当时按「深+浅+REM 累加」算，而 FIT 里那条 `sleep_level` 时间线一晚只有 15~33 条，是**设备端粗分期**，与 Garmin Connect 的分期不是一个口径。拿 5 份睡眠 CSV 真值核对，旧算法每晚都少报：

  | 日期 | 旧算法 | Connect 真值 | 少报 |
  |---|---|---|---|
  | 2026-08-10 | 315 | 457 | −142 |
  | 2026-08-19 | 283 | 427 | −144 |
  | 2026-08-20 | 322 | 401 | −79 |
  | 2026-08-21 | 381 | 477 | −96 |
  | 2026-08-22 | 288 | 301 | −13 |

  分期时长在 FIT 里**根本不存在**（原值 / ×60 / ×2 / ÷2 / 占睡眠比 / 占躺床比 都试过，五天找不到一致字段），所以不可能靠调整累加方式修好。
- 实际修法：躺床起止改由 `sleep start`/`stop` 事件给出；**睡眠时长 = 躺床 − 清醒分钟**（清醒分钟见 `DATA-36` 确证的 `METRICS.unknown_384.unknown_24`），5/5 天与 CSV 精确吻合，另有第 6 天旁证——`health_store` 旧注释里引用的 2026-08-14「6 时 48 分 = 408 分钟」也与新算法一致。粗分期改名为 `device_stage_*` 并带 `stage_source`，前端标注"手表粗分期（与 Connect 口径不同）"，不再冒充。回归见 `tests/test_sleep_session_fields.py`；历史数据用 `scripts/backfill_sleep_sessions.py` 回灌（16 天，已验证 0 不匹配）。


## DATA-30 并发上传同一份内容时，失败分支会删掉已有导入正在引用的原始文件

- 状态：已验证（代码路径确定，需两次并发上传才触发）
- 位置：`fithealth_agent/health_importer.py:649-679`
- 现象：`raw_path` 只由 `digest[:16]` + 清洗后文件名决定，所以同一份内容永远映射到同一路径。`find_import_by_hash` 与 `save_import` 之间没有互斥，第二次上传的 `INSERT` 撞 `sha256 UNIQUE` 抛 `IntegrityError`，而 `except Exception: raw_path.unlink(missing_ok=True)` 会把**前一次导入正在引用的那个文件**删掉——库行还在、原始文件没了，正是开发日志里定性为"静默的数据损坏"的形态。
- 修法：把 `find_import_by_hash` + 写文件 + `save_import` 整段放进已有的 `self.store.exclusive_access()`（`health_store.py:316-325`），顺带消掉竞态本身；单用户串行化无影响。最小改动版：`wrote_raw_file = not raw_path.exists()`（在 `write_bytes` 之前判断），只有真写了才在失败分支 unlink。

## DATA-31 隐私开关 fail-open：读失败静默翻回"开启"，且是全仓库唯一无锁的 JSON store

- 状态：已验证
- 位置：`fithealth_agent/external_model_settings.py:24-31`、`:33-37`
- 现象：`_read` 对 `OSError` 与 `JSONDecodeError` 一律返回 `dict(DEFAULT_SETTINGS)`，而默认是 `{"external_models_enabled": True}`——文件损坏/被占用/权限异常时，用户**显式关掉的开关被静默翻回开启**。这是"要不要把健康数据发给外部模型"的开关。同时它完全不用 `JsonFileLock`，而 `backup_service._locked_json_targets`（`:147-150`）会为 `external_model_settings.json` 加锁——加了没人认，恢复期间对它的并发写不受任何隔离。
- 修法：`_read` 区分"文件不存在 → 默认"与"存在但读/解析失败 → fail-closed（返回 `{"external_models_enabled": False}` 或抛异常）"，并在 `/health/storage-status` 暴露该降级状态；`get`/`set_external_models_enabled` 套 `RLock + JsonFileLock`（`_read`/`_write` 保持无锁，与 `UserProfileStore` 的分层一致）；`__init__:21-22` 的首次写入放进锁内并做双重检查。

## DATA-32 恢复备份后 `InfoStore` 的只读降级状态不复位，坏条目还会被回写

- 状态：已验证
- 位置：`fithealth_agent/info_store.py:532-533`、`:641-643`、`:683-688`；`main.py:3670-3679`
- 现象：`_degraded_reason` / `_unreadable_entries` 是**进程内内存状态**。用户按提示导入好备份把文件换掉后，`import_backup` 只调了 `workout_store.reload_from_disk()`，**没调 `info_store.revalidate()`**，于是恢复成功但记忆库仍一条读不出、写入继续被拒（`storage_status` 在已降级时不会重试）。更糟：`_unreadable_entries` 里还留着恢复前那份坏文件的条目，下一次写入会把它们**追加回新恢复的文件**（`:689` 的 `payload = list(items) + list(self._unreadable_entries)`）。
- 修法：`import_backup` 在 restore 成功后调 `info_store.revalidate()`（它已会清 `_unreadable_entries`），结果放进响应体让前端显示。更彻底：给 `LocalBackupService` 加 `on_restored: list[Callable[[], None]]` 钩子，在独占期结束前依次调用，`main.py:114-116` 装配时把 `info_store.revalidate` 传进去。`health_store` 已在 restore 内部 `reopen()`，可作对照。

## DATA-33 备份与"删除全部数据"都不覆盖隔离文件目录（两个方向都不对称）

- 状态：已验证
- 位置：`fithealth_agent/backup_service.py:75-80`（`BACKUP_DIRECTORIES` 只有 `health-imports`、`hr_streams`，全文无 `quarantin`）；`main.py:4329-4339`（`_reset_steps` 九项无隔离项）
- 现象：① 备份不含 `pending_workout.corrupt-*`，恢复后隔离列表消失——而这些文件按设计是"唯一还能救回那份训练"的载体。② "删除全部数据"清了 9 项却留下隔离文件，用户以为清空了，下次启动 `noticeQuarantinedWorkouts`（`index.html:4067`）又把旧训练捞出来提示他。
- 修法：`BACKUP_DIRECTORIES` 增加隔离目录；`_reset_steps` 增加 `("quarantined_removed", "隔离的待确认训练文件", workout_store.clear_quarantined)`（需新增该函数，复用 `_quarantine_files()`）。**两处必须同改**，否则语义继续不一致。

## DATA-34 multisport 分支丢掉整段 session 摘要，且某段缺 `start_time` 时丢光它的全部分段

- 状态：✅ **已修复（2026-08-27）**——`_parse_multisport` 独立函数与逐字段聚合早前已做；现补上 `_in_window(item, start, end)` helper，`start is None` → 不过滤的契约写进 docstring 并加了回归。这个契约原先一条测试都没有，正是同类问题（BUG-10）当初能出厂的原因
- 位置：`fithealth_agent/fit_parser.py:869-886`
- 现象：多 session 分支新造 `SessionSummary(sport="multisport", ...)`，只保留 start/elapsed/timer/distance/calories/avg_hr/max_hr——`avg_speed_mps`、`max_speed_mps`、`avg_cadence`、`total_ascent_m`、`total_descent_m`、`avg_power_w`、`training_effect`、`anaerobic_effect` **全部归零**；`sport` 不走 `_sport_to_zh`，界面直接显示英文；`avg_hr` 取第一个 session 的值而不是按时长加权。更严重的是 `local["lap"]/local["set"]` 的过滤条件含 `start_bound and ...`：某个子 session 没有 `start_time`（中断活动最常见的形状）时条件恒 False，**那个 session 的所有 lap 与 set 一条不剩地被丢**——与刚修完的 BUG-10 是同一类问题，只是这处没被覆盖到。
- 修法：(1) 汇总 session 改为在 `summaries` 上逐字段聚合（距离/热量/时长求和，速度/踏频/功率按时长加权，`training_effect` 取 max，`avg_hr` 按 `total_elapsed_s` 加权，`sport` 用 `"多运动（A+B）"`）；(2) 时间窗过滤抽成 `_in_window(item, start, end)`，`start_bound is None` 时**不过滤**（收下尚未被前面 session 认领的消息）并把归属失败记进 `ParsedActivity.note`；(3) 把这块从 886 行那条 300+ 字符的单行表达式拆成独立函数 `_parse_multisport(...)`，否则没法写测试。

## DATA-35 `merge_sets` 吸收跨越窗口边界的休息段，时间轴上留下无人覆盖的空洞

- 状态：已验证
- 位置：`fithealth_agent/workout_store.py:976-997`、`:1020-1041`
- 现象：`window_segments` 用"相交"条件判定 `absorbed_rest` 并删掉，而新建的 `merged` 只覆盖 `[new_start, new_end]`。开始于 `new_start` 之前或结束于 `new_end` 之后的休息段，其窗口外那部分时长从时间轴上消失（返回值的 `absorbed_rest_duration_s` 却把它算进去了）。
- 修法：把 `new_start/new_end` 扩成"选中动作 ∪ 被吸收休息"的外包区间并重算 `new_dur`（更符合 `:916-922` 的文档承诺）；或反向把 `absorbed_rest` 条件收紧成"完全落在窗口内"。**若选前者必须注意**：心率窗口随之变化，`unselected_active` 那道检查必须在**扩展后的窗口**上重跑，否则会放过一个原本该拒绝的合并。顺序敏感，建议把窗口计算抽成纯函数再测。

## DATA-36 METRICS 文件宣称提供 `metrics_snapshot`，实际一条数据都没入库；睡眠分数已定位

- 状态：已验证（"零数据"部分）／**睡眠分数字段已定位并用 2 份 CSV 真值 + 17 份日包实测确认（2026-08-24）**
- 位置：`fithealth_agent/health_importer.py:426-427`（无条件 append）、`:313-321`（METRICS 唯一被读的是 `local_time` 推出的 UTC 偏移）
- 现象：`if source["kind"] == "metrics": data_types.append("metrics_snapshot")` 只看文件类型、不看是否真解析出东西。库里 90 个 `kind='metrics'` 的 source 每个都在导入详情里宣告提供 `metrics_snapshot`，实际只贡献一行 `utc_offset_minutes=480`。用户在界面上看到"已导入 metrics_snapshot"，Agent 却查不到任何对应指标——这次是**运行时数据在撒谎**。
- 同时缺来源的数据：`sleep_summaries` 表**0 行**，所以 `score`/`quality`/`hrv_7d_status` 一个都拿不到（原本只来自睡眠 CSV，而那两份 CSV 在库里已无对应 import 行）。
- 立刻可做：把 `metrics_snapshot` 改成按实际产出判断（其余数据类型都是这样判的，只有 metrics 破了规矩），在此之前先删掉这一行，让界面诚实。

### 睡眠分数：证据已齐，**已于 2026-08-24 实现**

代码见 `health_importer` 的 `_SLEEP_*` 常量块与 `_sleep_session()`、`health_store` 的 `sleep_sessions` 表；回归见 `tests/test_sleep_session_fields.py`；历史回灌用 `scripts/backfill_sleep_sessions.py`。以下是当时的定位过程与证据，保留备查。

**真值来源**：`data/health-imports/0aa9e133bbac2418-1_.csv`（2026-08-10，分数 **54**）与 `de224d84dd35a846-0821_.csv`（2026-08-21，分数 **68**）。

**分数在每份日包里重复存了 4 处，且完全相等**：

| 文件 | 消息 | 字段 |
|---|---|---|
| `*_SLEEP_DATA.fit` | `msg521` | `unknown_1` |
| `*_SLEEP_DATA.fit` | `msg346` | `unknown_6` |
| `*_METRICS.fit` | `msg330` | `unknown_2` |
| `*_METRICS.fit` | `msg384` | `unknown_2` |

**实测范围**：`data/health-imports/` 里 17 份日包（2026-08-07 ~ 08-22）**四处全部一致，零冲突**，取值 40~82，两天与 CSV 真值精确相符。

**单日回归会误收，两天才够**：`msg346.unknown_7` 与 `msg384.unknown_25` 在 08-10 也等于 54，但 08-21 不等于 68——只拿一天做逐值匹配就会把它们当成分数字段。同理 08-21 那天 `file_id.type=44` 恰好等于 CSV 的「平均夜间 HRV 44 毫秒」，纯属巧合。**这是"必须先有对照证据"这条规矩最直接的例证，落地时也照此办：新增字段一律要求 ≥2 天真值同时命中。**

**建议主源用 `SLEEP_DATA` 的 `msg521`**，理由是同一条消息里还有睡眠起止，分数和它所属的睡眠时段天然绑在一起：

- `msg521.unknown_3` = 睡眠开始、`unknown_5` = 睡眠结束，与 `event`(21) 里 `event=74` 的 start/stop 时间戳**完全一致**（两处互为佐证）。
- **日期绑定 = 睡眠结束时刻的本地日期**，17/17 与导出包标注的日期一致。跨零点的夜晚也对：08-21 那晚 23:03 入睡、次日 08:07 醒，CSV 记的是 08-21。这条规则同时可用作 `DATA-24`「幽灵日」的判据。
- 入库前拿 `METRICS.msg330.unknown_2` 交叉校验，不一致就不写——两处来自**不同文件**，等于一道免费的一致性闸门。
- 仍要加范围闸门（0~100，超范围丢弃并记一条 warning），照 `_RESTING_HR_MESSAGE` 的先例。

**顺带确证了 `DATA-28`**：`msg521.unknown_3 → unknown_5` 的跨度就是躺床时长。08-21 为 544 分钟 = CSV 的睡眠 7 时 57 分 + 清醒 1 时 7 分；08-10 为 559 分钟 = 7 时 37 分 + 1 时 42 分。两天都精确到分钟，说明"以文件里明确的 sleep start 事件为起点"是对的。

**「质量」建议不存**：两个样本（68→一般、54→欠佳）与 Garmin 公开的分档（<60 欠佳 / 60–79 一般 / 80–89 良好 / 90+ 优秀）一致，但只有两点，不足以确证分界。存分数即可，需要显示时按分档现算并在界面注明是推算值。

**⚠️ 需要同时更正的旧结论**：`需求清单-20260819.md` 的 DATA-16 与 `开发日志.md:292` 都写着「睡眠分数与质量是 Garmin 云端算的、FIT 里没有」——**这是错的**，分数就在包里，17 天全部对得上。改这一条时请一并把那两处订正，否则下一轮又会照旧结论跳过。

**不做的部分**（用户 2026-08-24 决定）：身体电量不需要；Garmin 恢复时间由用户每次会话手输、已实现。原 `FEAT-17` 因此取消。


## DATA-37 脚本自制备份漏 WAL，且产物在 `data/` 永久堆积

- 状态：已验证
- 位置：`scripts/migrate_raw_path.py:129-133`（`shutil.copy2`）；对照正确做法 `backup_service.py:179-194`
- 现象：`health.db` 跑在 WAL 模式，已提交但未 checkpoint 的事务在 `-wal` 里，单独复制主库得到的是**缺少最近事务**的副本——`_snapshot_database` 里明确写了这个坑，脚本没绕开。脚本还直接 `sqlite3.connect` 改库，不经过 `HealthStore` 的访问锁、不持 `JsonFileLock`，也不检查服务是否在跑。
- 现场：`data/health.db.pre-rawpath-20260820T013523Z`（18 MB，一份完整旧健康库）、`data/daily_records.json.pre-hrbackfill-*`（60 KB）永久滞留在活数据目录里，既不进备份（正确），也没有保留策略，很容易被误认为当前数据。
- 修法：脚本一律改用 `LocalBackupService(...).write_recovery_point()`（落在 `data/recovery-points/`，受 `RECOVERY_POINT_KEEP=3` 约束、能在 UI 里看到和删除），失败即中止；`main()` 开头加"请先停止服务"提示与 `--force`。已有的两份手工副本确认无用后手删。

## DATA-38 三处旁路手写原子写，`atomic_json` 的加固无法传导

- 状态：已验证
- 位置：`fithealth_agent/info_store.py:694-703`、`hr_stream_store.py:115-123`、`workout_store.py:380` 附近；正本 `atomic_json.py:45-57`
- 现象：三处手写的 tmp→fsync→replace 与 `atomic_write_json` 等价，唯独**都没有 `_fsync_directory(path.parent)`**（POSIX 上目录项变更未落盘，后果可接受，但这正是 `atomic_json` 刻意补的一步）。守护这条不变量的 `tests/test_durable_writes_and_profile_locking.py:281-288` 只断言源码里出现 `"atomic_write_json" in source or "os.fsync" in source`——**字符串级检查，任何手写副本都能通过**，将来给 `atomic_write_json` 加固也不会传导到这三处。
- 修法：三处改调 `atomic_write_json`（`hr_stream_store.save` 必须传 `indent=None`，1802 条采样点加缩进会显著变大）。测试改成"mock `atomic_json.os.fsync`，断言每个 store 的写入都经过它"，与 `StoreWritePathsAreDurableTest._count_fsync` 同法。

## DATA-39 其他已确认的小项

- 状态：已验证
- **全中文文件名被剥掉扩展名**（`health_importer.py:661-663`）：`re.sub(...).strip("._")` 会把扩展名前的点一起吃掉，`"睡眠.csv" → "csv"`，磁盘上的 `3c00d4116bbaab77-csv` 就是它。修法：分开处理 stem 与 suffix（`stem = re.sub(r"[^A-Za-z0-9_-]+","_", Path(name).stem).strip("._") or "health"`，再拼回白名单里的 suffix）。**不要迁移已有文件名**——`resolve_raw_file` 只认文件名，改名会让 `raw_path` 全部失配（DATA-02 刚踩过）。
- **未来时间戳的记录静默丢弃**（`muscle_recovery.py:281-284`）：时区写错或手表时间偏移的记录整段消失在恢复推导之外，界面无提示。修法：`MuscleRecoverySnapshot` 加 `skipped_future` 计数并在 `/session/intro` 提示。
- **编辑酸痛记录会重置 72 小时时效**（`soreness_store.py:111-140`）：`update_report` 无条件把 `reported_at` 设为 now；改 `region` 还能绕过 `add_reports` 维护的"每区域最多一条"不变量，造出同区域两条生效记录，而 `_soreness_for`（`muscle_recovery.py:287-300`）只按列表顺序取最后一条。修法：保留原 `reported_at`（仅 `level` 变化时更新并重算 `expires_at`），写回前执行与 `add_reports` 相同的按区域去重，`_soreness_for` 显式按 `reported_at` 排序。

---

# 五、P1：功能错误（后端裁决）

## BUG-22 每条消息都跑酸痛解析，普通提问被写成"已恢复"并把恢复窗口归零

- 状态：已验证（实测 `parse_soreness_reply("腿部计划正常吗", ["手臂","腿部","胸部"]) == [("腿部","recovered")]`）
- 位置：`main.py:1701-1721`
- 现象：`asked_regions` 不是"刚才真的问过的部位"，而是**最近 7 天练过的全部区域**，所以任何一条聊天消息都会被当成对这些区域的酸痛回答来解析。配合 `_RECOVERED_PATTERN` 里的 `正常|还行|没问题|不酸了`，"腿部计划正常吗"会立即落盘一条 `recovered`，而 `recovered` 在 `muscle_recovery.py:415-417` 直接把该肌群 `hours_remaining` **归零**，回执还会说"已记录：腿部已恢复"。反向同理：描述别人或描述器械的句子也会落盘 painful。
- 修法（推荐第 1 条）：
  1. 只在确实提过问的那一轮之后解析：`/session/intro` 与上一轮回复若渲染了恢复清单，就在响应里带 `soreness_prompt_regions`，前端随下一条 `/chat` 回传，`main.py` 只用回传值作 `asked_regions`。
  2. 退而求其次的门控：消息含 `吗|？|\?|计划|怎么|为什么|安排` 时不写入——**与已有的 `_is_only_soreness_feedback`（`main.py:1559`）合并成一个函数**，别再加第三套关键词表。
- 这条与项目"不静默写入"的取向直接冲突，且 `recovered` 归零不可逆（旧记录被按区域替换掉）。

## BUG-23 恢复冲突校验被任意一条酸痛反馈整体关闭，漏掉了"用户明确要求"这个条件

- 状态：✅ **已修复（2026-08-27）**——`blocked` 的首个条件由 `not load.needs_reduction` 改为只看 `hours_remaining > 0`，追加的 `explicitly_requested` 判断这才真正生效：未恢复就拦，**除非**用户明确要求练这个区域。冗余的 `soreness_level != "painful"` 删除——painful 交给 `dynamic_safety` 的区域限制，不在两处重写。回归按原清单的警告构造了"带酸痛反馈的未恢复肌群"，否则怎么写都是绿的
- 位置：`main.py:419-425`
- 现象：`if load.hours_remaining > 0 and not load.needs_reduction and load.soreness_level != "painful"`——而 `needs_reduction` 就是 `soreness_level in {"sore","painful"}`（`muscle_recovery.py:411`），所以只要该肌群有任何一条生效反馈，它就被从 `blocked` 里剔除，"未恢复肌群被安排主项训练"这条硬校验对它彻底失效。文档 §E 写的例外是「同时属于 `reduce` **且** `explicitly_requested`」——代码把 `explicitly_requested` 丢了。`soreness_level != "painful"` 还是冗余条件，说明这段判断没被推敌过。
- 修法：`validate_generated_training_plan` 增加 `explicitly_requested: set[str]`（生成与保存两处都从 `resolved_plan_context["muscle_recovery"]["explicitly_requested"]` 传入），排除条件改为 `load.needs_reduction and load.region in explicitly_requested`；painful 继续交给 `dynamic_safety` 的区域限制，不单列条件。
- 注意：`recovery=None` 时行为不变，所以现有 15 条测试全绿也发现不了——补用例时必须传入非空 recovery。

## BUG-24 酸痛落在快照之外的肌群时被完全丢弃，painful 的全部拦截同时失效

- 状态：已验证
- 位置：`main.py:1009-1010`
- 现象：`reduce`/`avoid` 的唯一来源是 `snapshot.loads`，而快照只含"最近 7 天有可识别动作"的肌群。用户报"手臂疼"后若最近 7 天没有能映射到手臂的动作（只练了腿，或动作名被改成映射不上的写法），则 `avoid` 为空 → 不进 `dynamic_safety` → 计划校验没有区域限制可用 → `blocking_reasons` 的 `painful_conflicts` 永远为空 → 409 不会触发。**这条 72 小时的疼痛记录只在上报当轮起过作用，之后对所有裁决完全无效，而数据管理面板里它还显示"生效中"。**
- 修法：`_recovery_context_payload` 增加 `soreness_reports` 参数（`/chat` 与 `/session/intro` 已各自拿到 `list_reports(active_only=True)`），`reduce`/`avoid` 从**反馈的 region 字段**直接取并与快照推导结果求并集；`recovering` 保持只来自负荷。

## BUG-25 「不想练腿」被当成明确要求训练该部位

- 状态：✅ **已修复（2026-08-27）**——改为按小句切分（复用 `health_safety.CLAUSE_SEPARATORS`，全仓库仍只有这一套小句定义），**先整句否决、再匹配肯定形态**，且肯定形态必须带请求语气：动词在前（`想练腿`／`帮我安排腿`）或部位在前但小句另有请求线索（`腿部训练怎么安排`）。文档原文案例 `帮我安排今天的训练，腹部不要练` 现在返回 `[]`；`腰部不适影响训练` 也不再被当成"明确要求练腰"；`今天练腿，但手臂不要练` 正确地只返回腿部
- 位置：`main.py:967-978`
- 现象：正则的时间前缀是可选的，且完全不看否定词与时态。两个方向都错：(a) `main.py:1128` 用 `scheduled_region not in explicit_regions` 判断是否压制周计划，于是"今天不想练腿"反而**解除**腿部的恢复压制；(b) 若该区域是 `avoid`，`painful_conflicts` 命中 → 409 回"与已确认的安全限制冲突"，而用户说的正是不练。
- 修法：按小句切分（复用 `_CLAUSE_SEPARATORS`），先用 `不想|不要|别|不练|避免|跳过|昨天|上次|之前` 否决整个小句，再匹配肯定形态，并要求出现请求语气（`要练|想练|安排.*计划|帮我.*练`）而不是裸的"练"。沿用同一文件里 `is_negated_line`（`main.py:338`）已建立的机制，不要另写。

## BUG-26 肌群恢复的映射与窗口三处不一致，造成跨区域误拦

- 状态：✅ **已修复（2026-08-27）**——按修法给出的顺序做（先 3、再 1 和 2）：
  - **问题 3**：新增 `MUSCLE_META: dict[str, tuple[zh, region]]`，每个 id 一条唯一真值；`MuscleRule` 只留 `muscle_id/keywords/exclude/role`，`zh`／`region` 改为从 META 取的属性（既保证同一 id 名字稳定，又不动既有调用方）；删掉后写覆盖的 `_RULES_BY_ID`；`muscles_for_sport` 强制 `role="primary"`（`muscles_for_sport("跳绳")` 的小腿现在是主项/1.0，不再是次要/0.4）；模型兜底改走 `MuscleHit.for_muscle`，不再临时拼一条 `MuscleRule`。顺带修掉同一 id 两个中文名的问题（`calves` 曾有"小腿"/"小腿肌群"，`hamstrings` 曾有"股二头肌"/"腘绳肌"）。
  - **问题 1**：新增 `SECONDARY_WINDOW_FACTOR = 0.5`，当天只有次要命中的肌群（`_append_event` 升级过 role 后仍是 secondary）窗口折半；`_recovery_context_payload` 的 `recovering` 条目带上 `role`，区域压制只采纳主项负荷——"练一次背压掉手臂日"至此消除，次要负荷仅作提示文本。
  - **问题 2**：`BASE_RECOVERY_HOURS` 早前已补齐，现补上覆盖率不变量测试（`set(MUSCLE_META) == set(BASE_RECOVERY_HOURS)`），加肌群忘配窗口会直接变红而不是静默走 36 小时默认值。
- 位置：`fithealth_agent/muscle_recovery.py:20-35`（`BASE_RECOVERY_HOURS`）、`:406-414`（窗口只按 `muscle_id` 查表，不看 `role`）；`fithealth_agent/muscle_map.py:61-107`、`:113`（`_RULES_BY_ID` 后写覆盖）、`:159-169`；`main.py:1120-1145`（区域级压制）
- 三个互相叠加的问题：
  1. **次要肌群拿到与主项相同的窗口**。容量口径区分了主次（0.4 折扣），窗口没有。真实快照里 `小臂肌群 forearms effective_sets=1.2` 却拿到和主项一样的 36 小时，来源只是一个"哑铃罗马尼亚硬拉"的次要命中。于是"练一次背"让 `手臂` 区域进入 recovering → `_subject_recovery_region` 一匹配就 `recovery_blocked_schedule`，把周计划的手臂日压掉；"练一次胸"同理压掉肩部日。
  2. **`BASE_RECOVERY_HOURS` 缺 10 个 `muscle_id`**（`abductors, adductors, brachioradialis, forearms, lower_chest, obliques, serratus_anterior, trapezius, trapezius_upper, upper_chest`），全部按默认 36 小时处理。按文档 §B.2 分档，`upper_chest/lower_chest` 应 48、`brachioradialis/forearms/obliques` 应 24——既有低估也有高估，后者又放大了问题 1。
  3. **同一 `muscle_id` 的中文名与主次角色不稳定**。`_RULES_BY_ID` 是 dict 推导、同 id 最后一条规则胜出，而补充规则里 `calves`、`hamstrings` 的最后一条恰好是 `role="secondary"`，于是 `muscles_for_sport("跳绳")` 返回的小腿是次要、权重 0.4、名字"小腿肌群"（与文档 §2.3 的意图相反）；`_append_event` 只在首次建桶时保存 `hit`、之后不升级，所以同一天先处理到次要命中就把整条负荷标成 secondary 并用次要规则的中文名——真实快照里 08-21 的 `gluteus_maximus` 显示 `role=secondary`，尽管当天"臀桥/臀推"是主项。
- 修法：
  1. `build_recovery_snapshot` 对"当天只有 secondary 命中"的肌群按折扣算窗口（如 `baseline × 0.5`），或直接跳过 `effective_sets < 1.0` 的次要负荷；`_recovery_context_payload` 的 `recovering` 与 `main.py:1121-1134` 的区域压制**只采纳主项负荷**，次要负荷仅作提示文本。
  2. 补齐 10 个条目，并加源码不变量测试：`set(r.muscle_id for r in MUSCLE_RULES) == set(BASE_RECOVERY_HOURS)`。
  3. 拆成两层：`MUSCLE_META: dict[str, tuple[zh, region]]`（每 id 一条，唯一真值）+ `MUSCLE_RULES` 只留 `muscle_id/keywords/exclude/role`；`MuscleHit.from_rule` 的 `zh/region` 从 META 取；`SPORT_RULES` 展开时强制 `role="primary"`；`_append_event` 遇到 primary 命中时**升级**已有桶的 `role` 与 `hit`。
- 顺序：先做 3（否则 1 无法正确判断 role），再做 1 和 2。

## BUG-33 `create_training_plan` 分支不校验"回复是否真的是一份计划"

- 状态：已验证
- 位置：`main.py:2118-2136`，对比 `:2108`（`uploaded_plan` 分支有 `looks_like_complete_training_plan`）与 `:2138`（`scheduled_plan` 分支也有）
- 现象：意图路由判定"要生成计划"时，无论模型实际回了什么（反问、拒绝、澄清请求），都被包成 `type: "training_plan"` 的 artifact。`validate_generated_training_plan` 检查的是"与已确认记忆/恢复状态是否冲突"，并不校验"这是不是一份计划"，一段不含任何组数的短回复很可能全部通过 → 前端弹保存面板、`plan_draft_cache.remember` 把它当计划缓存 → 用户点保存就把一句反问存成了训练计划（`plan_store.add` 只要求正文非空，`plan_classifier` 不参与这条路径）。因 `content_hash` 去重，后续同内容还无法覆盖修正。
- 修法：`:2118` 的条件补 `and looks_like_complete_training_plan(answer)`，三个分支统一；不满足时不产出 artifact，`answer` 原样返回。

## BUG-37 本周汇总抢答需要判断的问题，且是唯一不拼酸痛回执的分支

- 状态：已验证
- 位置：`fithealth_agent/weekly_summary.py:10-33`、`main.py:1987-2004`
- 现象：`is_current_week_data_query` 只要求"周词 + 任一数据词"，而 `DATA_WORDS` 含 `情况/健康/恢复/营养/饮食/热量`。于是"这周饮食该怎么调整""本周恢复得怎么样，需要减量吗"被本地表格短路，用户拿到一张数据表而不是建议，且没有任何提示说明"我按查询处理了"。另外这个分支（`:2001-2004`）是全文件**唯一**不拼 `soreness_ack` 的返回路径（对比 `:2020`、`:2083`、`:2106`），所以"手臂有点酸，本周数据怎么样"会静默写入酸痛记录却不给回执——违反文档 §C.1"写入后立刻回执"。
- 修法：`is_current_week_data_query` 增加查询语气要求（`查看|看看|汇总|统计|数据|列一下` 之一），把 `饮食/营养/恢复/情况` 从单独触发词降级为需搭配语气词；`main.py` 把回执拼接抽成 `_with_ack(reply)` 帮助函数，所有 return 分支统一走它——否则下一个新分支还会漏。

## BUG-38 餐食估算的热量与宏量素是两套口径，低可信度确认可被绕过

- 状态：已验证
- 位置：`fithealth_agent/food_analysis.py:101`、`:110-112`；`fithealth_agent/daily_checkin.py:78-83`；`templates/index.html:1285`、`:1313`
- 现象一：`total_kcal = _kcal(result["total_kcal"]) or sum(items…)` 用模型给的总数（通常含它在 `assumptions` 里提到的"未计入的油脂/酱料"），而 `protein_g/carbs_g/fat_g` **只**由逐项累加。于是可能保存出"总热量 800 kcal，但三大宏量反推只有约 370 kcal"，差额没有任何字段解释，并在周汇总里累积。
- 现象二：`confidence` 与 `user_confirmed` 都是请求体里的自由字段，`_validate_meal_estimates` 只检查"`confidence == "low"` 时必须已确认"——把 `confidence` 改成 `"medium"` 就完全绕过关卡，`items` 与 `total_kcal` 也不会被重新核对。这道关卡的语义完全依赖前端诚实。
- 现象三：`_extract_json` 不支持 ```json 围栏（`muscle_map.py:172-184` 的同名函数支持），换视觉模型时会直接报"格式无效"。
- 修法：(1) 全部改用逐项之和，保留 `model_total_kcal` 作参考字段；偏差超过阈值（如 20%）时把差额显式表达成一个 `items` 条目"未计入的油脂/酱料（估算）"并补齐宏量，`range_low/high` 按新 total 重算。(2) `/analyze_food` 返回时附一个短时效的服务端签发标识（内存字典即可，键含 `confidence` 与 `total_kcal`），保存 `meal_estimates` 时要求携带并**以服务端记录的 `confidence` 为准**；再加 `abs(total_kcal - sum(items))` 的一致性上限。(3) `_extract_json` 支持围栏。

## BUG-39 周计划只要有一处不合法就整条静默丢弃

- 状态：已验证
- 位置：`fithealth_agent/info_store.py:286-361`（每个分支都 `return None`）、`:428-432`
- 现象：多一个未知顶层键、`days` 里出现一个非 `mon..sun` 的键（模型写了 `"周二"`）、`type` 拼错、`subject` 超 40 字——**整份周计划被丢掉**，`normalize_memory_facts` 随后 `continue`，这条事实凭空消失：不报错、不进待确认列表、用户以为已经说过了。混合写法 `{"mon":"胸","周二":"背"}` 会先判定为非 legacy，再因未知键返回 None。
- 修法：`parse_weekly_schedule` 增加宽松模式——跳过无法解析的单日、保留能解析的日，并把跳过的键通过返回值或日志暴露；只有 `days` 全部无效时才丢弃。与 `_read_all:664-681` 逐条隔离坏记录的做法一致："一处坏不该连坐"。**必须同时**在待确认卡片上显示解析后的完整结果供用户核对，否则宽松解析会让部分周计划不完整地生效。

## BUG-40 单条损坏的酸痛记录让 `/chat` 与 `/session/intro` 永久 500

- 状态：已验证
- 位置：`fithealth_agent/soreness_store.py:27-36`、`:77-90`、`:151-165`；调用点 `main.py:1701`、`:4133` 均无 try
- 现象：`list_reports` 对**全部**记录调 `_deserialize`，任何一条 region/level 非法或 `reported_at` 缺失就抛 `ValueError` → 每次聊天和每次进入系统都 500。更糟的是 `cleanup_expired:157-161` 对解析失败的行**保留**，所以坏行不会被清掉，**故障是永久的**，只能手工编辑 `data/muscle_soreness.json`。触发源包括人工编辑、恢复了旧结构的备份、以及未来 `REGIONS` 改名。
- 修法：`list_reports` 逐条 `try/except ValueError: continue`（可选地返回 `skipped` 计数供数据管理面板显示），保持 `_read_unlocked` 的整体格式校验不变；`cleanup_expired` 的坏行处理与读路径取向对齐。同仓库 `info_store` 有专门的"单条坏记录不连坐"测试（`tests/test_info_store_corruption_guard.py`），这里应对齐。

## BUG-43 酸痛回复的两处措辞缺口

- 状态：已验证
- **「都正常啦」既解析不出，又反过来要求澄清**（`muscle_recovery.py:132-135`、`:189-201`）：整句判定用了 `^...$` 锚定，多一个语气词就不匹配；接着走逐区域循环又没有部位名，返回空；此时 `soreness_reply_needs_clarification` 判定"有程度、问了多个区域、没点名区域"→ 回"请明确是哪一个区域"，而用户明明说了"都"。修法：改成 `re.search(r"(?:全部|全都|都)(?:正常|恢复|没感觉|没问题|还行|不酸|不疼)")`，并让两处**复用同一个已编译常量**（现在是 `:133` 与 `:195` 各写一遍同样的字面量）。
- **「手臂酸，怎么办」只回执不回答**（`main.py:1559-1564`、`:1756-1776`）：`_is_only_soreness_feedback` 的否决词表没有 `怎么办|要不要|需要吗|建议`，于是走 `local_soreness_feedback` 短路，用户只拿到"已记录：手臂酸痛…"。修法：反过来判定——只有当消息**去掉部位与程度词后几乎为空**（剩余非标点字符 < 4）时才算纯反馈；这样任何附加提问都会继续走正常流程（`soreness_ack` 已会拼在答复前）。

## BUG-44 周计划复合科目只取首个匹配区域，恢复压制会漏

- 状态：已验证
- 位置：`main.py:981-986`（`_subject_recovery_region` 返回首个命中，`REGION_ALIASES` 迭代顺序为 胸→背→腿→肩→手臂→核心）
- 现象：周计划科目常写成"背部+手臂""胸肩"（`subject_core_terms:315-335` 明确处理过复合科目），但恢复裁决只拿到第一个区域——"背部+手臂"在手臂未恢复时不会被压制。
- 修法：改成 `_subject_recovery_regions(subject) -> set[str]`，`recovery_blocked_schedule` 与 `suppressed` 改为"任一区域被拦即压制，reason 里列出具体区域"。调用点只有 `main.py:1120`。

## BUG-45 快照只保留每个肌群最近一天，跨天累计与文档承诺的多日列举都没实现

- 状态：已验证
- 位置：`fithealth_agent/muscle_recovery.py:403-405`（`latest = by_date[max(by_date)]`）
- 现象：`_append_event` 按 `(muscle_id, date)` 分桶、结构留了多日能力，取值时退化成单日。文档 §4 的目标文案是"肱二头肌：周三训练过（…6 组）、**周二训练过（…12 组）**"，实现只有一天；"连续两天练同一肌群"这类累积疲劳在窗口计算里完全体现不出来（连续三天各 4 组与单日 4 组的窗口完全相同）。
- 修法：`MuscleLoad` 增加 `history: tuple[tuple[datetime, float, tuple[str,...]], ...]`，`_format_muscle_recovery_lines` 与 `_recovery_checkin_items` 渲染多日；窗口计算可先保持只用最近一天（行为不变），"连续高强度日"留给 FEAT-06。

---

# 六、P1：功能错误（端点与前端）

## BUG-28 `updateWorkout` 丢掉 `code` 字段，冲突自愈全是死代码，保存会永久卡死

- 状态：已验证
- 位置：`templates/index.html:3639`（`return res.ok ? data : { error: data.error || '操作失败' }`）、消费点 `:3958`、`:3776`；后端 `main.py:2868-2878`，`workout_store.py:680/682/686`
- 现象：非 2xx 时只保留 `error`，`code` 被丢弃。后端把 `WORKOUT_ID_MISMATCH`/`STALE_WORKOUT_VERSION` 映射为 409、`INVALID_CONFIRMATION_TOKEN` 为 403，三者都带 `error` 键，必然走这条剥离路径。于是 `:3958` 的 `result.code === 'STALE_WORKOUT_VERSION' || 'WORKOUT_ID_MISMATCH'` 永远为假，`refreshWorkoutState(true)` 不执行，客户端手里仍是过期 token/version，**用户每次点"确认并保存训练"都得到同一句"训练内容已更新，请检查后重新确认"，只能刷新页面**。`INVALID_CONFIRMATION_TOKEN` 在前端出现 0 次。
- 对照：`dataRequest`（`:1563`）是正确的，它把 `code` 和 `status` 一起带回——所以走它的已保存记录路径（`STALE_RECORD_REVISION`）能自愈，走 `updateWorkout` 的 pending 路径不能。两个 fetch 包装器口径不一致。（`DUPLICATE_TRAINING` 例外可用：`workout_store.py:1151-1163` 的返回体没有 `error` 键，走 200，所以覆盖流程是活的。）
- 修法：`:3639` 改成与 `dataRequest` 同构 `{ error: ..., code: data.code, status: res.status }`；更好的是**删掉 `updateWorkout`，全部改用 `dataRequest`**。同改 `:3956-3964` 增加 `INVALID_CONFIRMATION_TOKEN`（重取 token 后提示重试）与 `NO_PENDING_WORKOUT`（清空面板）分支。

## BUG-29 上传新 FIT / 载入活动 / 载入隔离文件都会无条件覆盖未确认的训练

- 状态：已验证
- 位置：`main.py:2369`、`:4237`（`workout_store.set_current`）、`:2796-2807`（隔离重放）；前端 `templates/index.html:3070`、`:3198-3211`、`:2116-2124`。`hasWorkout` 的全部引用（`:941, 2843, 3407, 3738, 3792, 3823, 3869`）里**上传路径一次都没有**
- 现象：编辑区已有带未提交修改的待确认训练时，再拖入一个 FIT（或在活动选择器里点另一个活动、或在数据管理里点隔离文件的"载入编辑"），后端直接 `set_current` 覆盖、前端直接替换，**没有任何确认**。对比：撤销、恢复原件、清空、丢弃、永久删除隔离文件五个动作都有 `window.confirm`。
- 修法：`/upload_fit`、`/upload_health/activity`、`/workout_state/quarantined/restore` 增加 `overwrite_pending: bool = False`，`get_current() is not None and not overwrite_pending` 时返回 409 + `code: "PENDING_WORKOUT_EXISTS"`；前端在三个入口 `hasWorkout && !savedRecordEditingTarget()` 时先 `window.confirm`，确认后带 `overwrite_pending=true` 重发。

## BUG-30 「合并」没有任何确认，而对已保存记录的合并不可撤销

- 状态：已验证
- 位置：`templates/index.html:3756-3789`、`:3767`（走 `PATCH /data/training-records/{id}`）、`:3417`（`$undo.disabled = viewingSavedRecord || …`）、`:3792`；后端 `main.py:3470-3478`、注释 `:3237-3253`
- 现象：合并把 N 组塌成 1 组，是破坏性的。全文 19 处 `window.confirm` 覆盖了几乎所有其他写操作，**唯独合并没有**。对 pending workout 还能 `undo_last_edit`；对已保存记录，撤销按钮被硬置灰、`recoverPendingWorkout` 直接 return、后端没有副本（注释亦承认"已保存记录没有副本、合并不可回滚"）——**一次误点永久改写一条历史记录**。
- 修法：`$merge` 处理器开头按 `savedRecordEditingTarget()` 分两套确认文案，对已保存记录明确写"此操作不可撤销"。更稳的做法：`PATCH /data/training-records/{id}` 带 `merge_indices` 时先写一份合并前快照（可复用 `hr_streams` 旁挂目录的模式）。

## BUG-31 `/chat` 不检查 `res.ok`：409 那一轮的解释不进上下文，用户和 Agent 会绕圈

- 状态：✅ **已修复（2026-08-27）**——非 2xx 且带 `reply` 的回合已记入历史；现新增 `renderPlanValidation`（逐条渲染 `plan_validation.violations`）与 `applyWorkflowState`（`needs_clarification`／`constraint_conflict`／`validation_failed` 时禁用带 `data-plan-save` 的保存按钮；已保存过的按钮打 `planSaveDone` 标记，不会被重新启用）
- 位置：`templates/index.html:2960-2970`；后端 `main.py:1922-1931`（计划校验 409）、`:2061-2073`（安全冲突 409）、`:2089-2096`（需澄清 409）、`:1866-1879`（400）、`:1719` 与 `:2006-2026`（503）
- 现象：`res.ok` 被当成"是否记入历史"的开关（`:2967`），而不是"是否成功"。这些分支都带 `reply` 所以文本能显示，但**后端刚刚解释"与已确认的安全限制冲突""还没说明是休息还是换科目"的那一轮不进 `sessionConversationHistory`**，下一轮又是无上下文的。同时 `plan_validation` / `workflow_state` / `plan_context`（机器可读原因）在前端零消费。若哪天某分支只返回 `{"error": ...}`（如 `main.py:1495` 的结构化 error），用户会看到"（无回复）"而看不到原因。
- 修法：`:2960` 之后分流——`!res.ok` 且有 `data.reply` 时按"系统提示"渲染并**仍然记入历史**（这些是对话的一部分）；只有 `!data.reply` 才走 `data.error || '请求失败'`。同时消费 `data.plan_validation.violations` 渲染结构化提示，用 `workflow_state` 决定是否禁用保存按钮。

## BUG-32 档案更新卡片顶掉训练计划卡片，用户拿不到"保存计划"按钮

- 状态：已验证
- 位置：`main.py:2180`（`response_artifact = profile_update_artifact or artifact`）
- 现象：一条消息里既触发档案变更又生成了计划时，`artifact`（training_plan）被丢弃，前端只挂 `buildProfileUpdatePanel`（`index.html:2483`），计划保存面板不出现。正文此时已通过校验并写进 `plan_draft_cache`（`:2173-2179`），只能靠用户再说一句"保存刚才的计划"捞回——但用户不知道要这么做。首次补全档案后紧接要计划就会撞上。
- 修法：响应改为 `artifacts: [...]` 列表（保留 `artifact` 为首项以兼容），前端 `addMsg` 遍历挂载多个面板；或最小改动：`profile_update` 单独走一个键，`artifact` 只承载计划/记录类。同改 `index.html:2475-2485` 的三处 `artifact.type` 判断。

## BUG-34 数值输入的三处校验缺口，会静默写坏营养与训练数据

- 状态：已验证
- **`Number('') === 0` → 0 kg × 0 次**（`templates/index.html:3603-3611`）：`<input type="number">` 清空或非法时 `.value` 是空串，`Number('')` 为 0，且 `isFinite/isInteger/>=0` 三个检查全过。用户想"清掉重量重新填"却直接保存，后端 `_validate_saved_training_updates`（`main.py:3227-3233`）同样接受 0，那一组被永久写成 0 kg × 0 次，"重量应在 0-1000 kg 之间"的文案永远不会出现。修法：先 `const raw = input.value.trim(); if (raw === '') return editorError(...)`；后端建议区分"允许 0 重量的自重动作"与"空值"（若允许 0 kg，则要求 `repetitions >= 1`）。
- **`NaN` 序列化成 `null` → 删字段**（`:2761-2764`）：`payload[field] = value === '' ? null : Number(value)`，非数字输入得到 `NaN`，`JSON.stringify` 写成 `null`，后端 `main.py:3111-3112` 对 `None` 的处理是 `record.pop(field, None)`——**一次手误把当天的蛋白质记录直接抹掉**。同文件的 meal 分支用 `Number.isFinite` 正确拦住了（`:2757`），两个分支不一致。修法：`if (value !== '' && !Number.isFinite(n)) return nutritionStatus(...)`；后端把"显式 null = 删除"与"无该键 = 不变"写进注释并加测试。
- **`applied_to_form` 在从服务端载入时丢失 → 合计与明细分叉**（`:1275` 的 `normalizeMealEstimate` 不带该字段，唯一置位点是 `:1262`，消费点 `:1219`/`:1237`）：`loadCheckinInto` 载入已保存餐次后，用户编辑某个食物项或点"移除"时 `applyMealNutritionDelta` 被跳过——面板上的"合计"更新了（`:1238`），热量/蛋白/碳水/脂肪四个输入框没动，而 `submitCheckin` 提交的正是那四个框（`:1317-1319`）。差额会一直累积。修法：`loadCheckinInto` 的映射里补 `applied_to_form: true`（存盘的记录级热量本来就已含这些餐次），并加一次保存前的"合计与明细一致"校验。与 DATA-29 一起修。

## BUG-35 冲突码处理三处缺口：营养组永久 409、记忆冲突后不刷新、取消编辑用陈旧数组

- 状态：已验证
- **营养组不处理 `STALE_RECORD_REVISION`**（`templates/index.html:2773-2776`；后端 `main.py:3189-3193`）：`saveNutritionEditor` 只看 `result.error` 不看 `code`，`viewerItems[i].revision` 保持陈旧值，**每次点"保存营养组"都发同一个过期 revision，永久 409**，提示也不说该重新加载。训练记录那一侧的 `handleSavedRecordConflict`（`:3693-3699`）做对了，两条同构路径只做了一半。修法：把它泛化成 `handleStaleRevision(result, reloadFn)`，营养组传 `loadViewerDay`。
- **`MEMORY_CONFLICT` 唯一被正确识别的两处却跳过了重新拉取**（`:1819-1821`、`:1871-1873`）：显示"该事实已过期"后直接 `return`，不调 `loadDataOverview()`，列表继续渲染陈旧 `fact_id`，下一次点击命中同一个冲突。修法：两处 `return` 前补 `await loadDataOverview()`。
- **记忆事实"取消"编辑用闭包里的旧数组重绘**（`:1860` 的 `renderMemories(memories)`，`memories` 是 `:1770` 捕获的入参）：期间任何确认/拒绝/删除都已触发过 `loadDataOverview()`，取消编辑却把界面回退到变更前的状态且不请求服务端——用户会以为删除没生效，或以为已确认的事实又变回待确认。修法：`:1860` 改为 `await loadDataOverview()`。

## BUG-36 计划内容重复时，用户填的日期/科目/备忘录被静默丢弃

- 状态：已验证
- 位置：`main.py:3699-3710`、`fithealth_agent/plan_store.py:113-116`；前端 `templates/index.html:2506-2515`
- 现象：`plan_store.add` 按 `content_hash` 全局去重，命中就 `return {**duplicate, "duplicate": True}`——**新填的 date/subject/title/memo 一个都不写入**。前端显示"本地已有：<旧文件名>"并把四个输入框全部 `disabled`，用户刚写的训练反馈备忘录消失且没有任何提示。同一份计划想安排到另一个日期（很自然：同一套动作练两次）也做不到。
- 修法：命中重复时把非空的 `memo`（以及可选的 `date`/`title`）合并进已有条目并返回 `{"duplicate": True, "memo_updated": True}`；或把去重键改成 `(content_hash, date)`，允许同内容不同日期各存一条。前端 `:2508-2515` 据此改文案，明确告知备忘录去哪儿了。

## BUG-41 两处"把失败当成空态"的判断

- 状态：已验证
- **`loadCheckinInto` 把请求失败当成"这天还没打卡"**（`templates/index.html:1265-1272`，调用点 `:2269-2275`）：后端刻意让"这天没记录"返回 200 + `{"checkin": null}`（`main.py:3581-3599` 有注释），前端却把"请求失败"与"没记录"合并成同一分支。日期切换处理器先 `reset()` 再 await 加载，失败时用户面对一张空表单和一个**有数据的日期**，且"已载入…保存将更新这一条"的提示条不显示——他会以为这是新记录。修法：`if (data && data.error)` 单独一支并提示读取失败，考虑此时禁用保存按钮。
- **`isPersonalBackup` 把三态压成布尔**（`:3225-3235`、`:3244-3248`）：`return Boolean(response.ok && data.valid)` + `catch { return false }`，于是 `/data/backup/inspect` 抖动一次，个人备份 ZIP 就落到 `handleHealthFiles` 被当成 Garmin 数据上传，用户看到一串导入失败信息，而他本意是恢复备份。修法：返回 `'yes' | 'no' | 'unknown'`，`unknown` 时提示"无法判断这个 ZIP 的类型，请在数据管理里明确选择"并中止自动路由。

## BUG-42 按钮 disabled 泄漏与可重复提交

- 状态：已验证
- **撤销 / 恢复原件在网络失败后永久置灰**（`:3794-3795` 置位，唯一复位点 `:3417-3418` 在 `renderWorkout` 内，而 `refreshWorkoutState` 的 `else if (force)` 分支不碰这两个按钮、且 `catch (_) {}` 静默吞异常）：`点撤销 → /workout_state/update 报错 → /workout_state 也失败` 之后两个恢复入口永久灰掉，只能刷新。修法：`:3796-3805` 包进 `try/finally`，`finally` 里由 `workoutEditHistory` 计算并复位（抽成 `syncRecoveryButtons()`）。同类：`:1626`（外部模型开关）、`:3203`（活动选择器按钮只在 catch 里复位）。
- **「清空」按钮在自己的请求期间不置灰**（`:3825` 只置灰 `$discard`，而 `:3852` 的 `$clear` 共用同一个 `discardPendingWorkout`）：从工具栏进入时被禁用的是另一个按钮，可双击发两次 `action:'clear'`。后端幂等所以不损坏数据，但会与 `activityPickerFiles` 的剩余活动计算（`:3836-3846`）打架。修法：改为对 `event.currentTarget` 或模块级 `discardBusy` 标志置位。
- **每日记录表单可重复提交**（`:1311` 无在飞标志、提交按钮 `:757` 从不 disabled）：双击会连着看到"已保存 …"和"已更新 …"两条自相矛盾的通知（数据不会重复，upsert 在同一把锁内查重）。修法：提交期间禁用按钮。

## BUG-46 死代码、死端点与其他已确认小项

- 状态：✅ **已修复（2026-08-27）**——主要死代码早前已清；现补上剩余三个小项：① 档案补全提示删掉写反的 `intro.error` 条件（正常路径现在会提示），并把后端早就返回却无人消费的 `missing_fields` 列出来；② journal 警告改为"有 `message` 就显示"，档位按 `available` 决定（`available: true` 时也会返回 message，原先被丢掉）；③ `/analyze_food` 补 `except Exception` → 502 + `error` 键，不再落到 FastAPI 默认 500 `{"detail": ...}` 让前端读 `data.error` 得 undefined。死端点按计划有意保留待 FEAT 接线
- **死代码**：
  - `templates/index.html:2590-2675` 的 `buildTrainingRecordsPanel` 及其 86 行子树（含 `loadDetail`、`loadDay`）完全不可达，配套 CSS `:161-166` 的 `.record-viewer*` 五条规则只被它引用；内含 `:2662` 的 `replaceAll('-', '-')` 空操作。建议删除。
  - `main.py:2925-2928` 的 `CHECKIN_CATEGORY` 分支永不执行（唯一调用者 `_training_record_items` 已用 `is_training_record_item` 过滤，而 `CHECKIN_CATEGORY` 在 `NON_TRAINING_CATEGORIES` 里）。
  - `templates/index.html:3409`、`:3742` 的 `$confirm.classList.add/remove('show')` 是空操作（没有 `#btn-confirm.show` 规则，可见性由 `.workout-actions.show` 控制）。
  - `MuscleLoad.role`（`muscle_recovery.py:76`）全仓库无读取点，且值本身不可靠（BUG-26 问题 3）——**写了、错了、没人发现**；`muscle_loads` 属性（`:94-96`）零调用。要么按 BUG-26 修好后在 `/session/intro` 与 `_format_muscle_recovery_lines` 里暴露"主项/次要"，要么删掉。
  - `_region_muscle_ids`（`muscle_recovery.py:102-105`）与 `soreness_store.muscle_ids_for_region`（`:23-24`）是逐字相同的两份实现，将来给某区域加肌群时改一处就分叉。删掉一份。
  - `templates/index.html:1766, 1830, 1853, 2342, 2352, 2364, 2372, 2382, 2401` 九处 `MEMORY_CONFLICT` 三元判断挂在从不返回该 code 的端点上（例如删除训练计划失败时会显示一段关于"记忆事实"的文案）。条件永远为假所以不会真的显示，但这是复制粘贴扩散的死分支。删掉，真正需要的两处抽成 `memoryConflictMessage(result)`。
- **死端点**（是否保留请自行决定，但不该继续以"已实现"的姿态躺着）：`GET /health/range`（`main.py:4248`，全仓库零引用）；`GET /health/daily/{day}`（`:3887`）、`GET /health/sleep/{day}`（`:4264`）、`GET /health/imports/{import_id}`（`:4275`）仅被一次性脚本 `tool-output/verify_health_api.py` 引用；`DELETE /data/records/{record_id}`（`:3746`）只被测试当负例调用，UI 一律走批量删除。前四个建议接线到健康面板（见 FEAT-09）。
- **其他小项**：
  - `#viewer-content-title`（`index.html:702`）从不读写，切到营养组时标题仍固定显示"训练组列表"。
  - 趋势图遇到非有限值静默不画线（`:1367` 的 `isFinite` 过滤只用于算刻度，`:1382-1384` 画点不过滤），全非法时 Y 轴标签渲染成 `NaN`、摘要显示"NaN · N 个数据点"。修法：`:1367` 之前先过滤，为空走已有的"暂无数据"分支。
  - `GET /data/backup/export` 无异常处理（`main.py:3637-3645`），磁盘满/库被占用时返回 500，而前端是整页导航（`:2291-2293`），用户离开应用看到一个错误页。修法：加 `except (OSError, ValueError, sqlite3.Error)` 返回带 `error` 键的 500；前端改 `fetch` + `Blob`（`downloadMarkdown` 已有模式）。
  - `PUT /settings/external-models` 不返回 `local_features`（`main.py:4216` vs `GET` 的 `:4198-4203`），而前端把两者喂给同一个渲染函数（`:1609-1613`），于是"关闭后仍可使用"那行说明在用户第一次拨动开关后消失——恰恰是最需要它的人看不到。修法：把 `local_features` 抽成模块级常量供两个端点展开。
  - `/health/storage-status` 的 journal 警告被算出来又丢掉（`main.py:3562-3576` 在 `available: true` 时也返回 `message`，前端 `:4045-4046` 只在 `!available` 时读）。修法：`if (storage.message) addNotify(...)`。`memory_store`/`health_store`/`maintenance` 三个子对象目前全部无人消费。
  - `/upload_health` 的逐文件失败明细（`main.py:2738-2739` 的 `results`/`errors`）前端只显示聚合 `message`，多文件导入时用户看不到哪个文件为什么失败、也无法重试单个文件。
  - `main.py:4044`（前端 `:4044`）档案未完善的提示条件写反：`if (profile && !profile.complete && intro.error)`——只有 `/session/intro` **失败**时才提示补全档案，正常路径永不提示；`missing_fields`（`main.py:3910`）也无人消费。
  - 上传校验文案与实现不符（`index.html:3008-3009`）：守卫只放行 `.fit/.md/.txt`，文案却写"请选择 .fit、.zip、.csv 或 .md"，而 `.zip/.csv` 早在 `handleFiles`（`:3319-3325`）就分流走了。
  - 保存日期的确认文案经过"本地解析 + 强制北京时区"双重换算（`:1548` 的 `new Date(value)` 解析无偏移量的裸时间戳 = 按本地时区，`:1553` 再按 `Asia/Shanghai` 渲染，消费点 `:3913-3914`），非 UTC+8 机器上弹窗时间与实际落盘日期不一致。同类裸解析还在 `:1647`、`:2085`、`:2170`、`:4081`。修法：后端在 `to_public_dict()` 里直接给带偏移量的 ISO 或已格式化好的字段，前端只展示。（正面确认：全文没有 `toISOString()` 当本地日期用的经典 bug，`localDateISO` 与 `changeOverviewDay` 都写对了。）
  - 中英文混排的用户可见文案：`index.html:1314`、`:3927`、`:3937`、`:3948`、`:3951`（重复训练分支整段英文）；后端 `workout_store.py:1160`、`:1193` 的英文 `message`。
  - `$send` 在 `finally` 里被无条件启用（`:2984`、`:3303`），即使此时 `sessionEnded` 已置真或外部模型已关闭（后果被 `:2933` 的提前 return 兜住，只是按钮状态与实际能力不符）。
  - `/analyze_food` 只捕获 `FoodAnalysisError`（`main.py:229-233`），其他异常变成 FastAPI 默认 500 `{"detail": ...}`，前端读 `data.error` 得到 `undefined`。
  - `#message-resizer`（`:643`）有 `tabindex="0"` + `role="separator"` 但只绑了 pointer 事件，键盘用户无任何响应（`#layout-resizer` 有 `keydown`，`:1514`）。无障碍缺口。

---

# 七、P1：Agent 能力与记忆正确性

（AGENT-11 已列在第一节，它是 P0。）

## AGENT-12 编辑旧版本的单例事实后永远无法再确认，409 无限循环；确认操作也不幂等

- 状态：已验证（沙箱实测）
- 位置：`fithealth_agent/info_store.py:966-1004`（`edit_fact` 不刷新 `base_version`）、`:931-940`（版本比较）；端点 `main.py:3829-3840`
- 现象一：先确认周计划 A（version 1），再确认周计划 B（version 2）；用户想改回去，编辑 A 的值——`edit_fact` 把 `user_confirmed` 置 False 但 `base_version` 仍是 0；再确认 A 时 `current_version(2) > base_version(0)` → `MemoryConflictError` → 409"请重新确认"，而**重试走的是同一条比较，永远失败**。用户只能删掉 B 才能改 A。`plan.weekly_schedule` 与三个 `max_*` 共享同一个 slot（`FACT_VERSION_SINGLETON_KEYS:132-137`），所以这是最常用的编辑路径。
- 现象二：对同一 `fact_id` 连续两次 `set_fact_confirmation(confirmed=True)`，第二次抛冲突——第一次确认把 slot version 抬到 1 却没同步 `base_version`。双击、网络重试、两个标签页同时确认都会撞上，提示还是误导性的"有更新事实"。
- 修法：(1) `edit_fact` 写回前把 `base_version` 刷成当前 slot 版本，并删掉继承来的 `version`——编辑是用户**看到当前状态之后**做的显式决定，基准就该是当前版本；(2) `set_fact_confirmation` 在 `confirmed and fact["user_confirmed"]` 时幂等短路，确认成功后写 `fact["base_version"] = fact["version"]`，让"本条自己造成的版本推进"不算冲突（冲突检测只应针对**别的**事实推进过 slot 版本的情况）。
- 同改：`main.py:3853-3870` 的返回体要带回新的 `fact_id`（id 由内容派生，编辑后会变）。对应旧清单 `上下文工程修改.md` 第 18 条，目前完全未实现。

## AGENT-13 零事实的记忆条目可以被"确认"，未经证据核验的摘要永久进入上下文

- 状态：已验证（沙箱实测）
- 位置：`fithealth_agent/info_store.py:1027`（`all([])` 为真）、`:852`（`summary_only`）；`main.py:2258-2272`
- 现象：`_level3_llm_decide` 只要求"至少一条事实通过证据核验"（`information_router.py:266-273`），但那些事实随后可能在 `normalize_memory_facts` 里被全部丢弃（namespace/key 配对非法——`_FACT_KEYS` 是跨命名空间的扁平枚举，模型很容易产出 `("youtube","max_rpe")`；数值越界；weekly_schedule 解析失败见 BUG-39）。实测：facts 全被丢弃后仍写入一条 `facts: []` 的条目；`confirm_entry` 因 `all([])` 为真直接标成已确认；`get_context_memories` 的规则 2 随即把这条**只有摘要、从未逐条核验**的自由文本注入上下文。logout 路径下 `memory_retention_class([], "constraint")` → `long_term` → `expires_at=None`，**永久有效**。
- 修法：(1) `confirm_entry` 在 `facts` 为空时 `raise ValueError("该记忆没有可确认的事实")`，端点回 400；(2) `logout`（`main.py:2258`）在规范化后 facts 为空时不落盘，改为返回 `not_saved` + 诊断原因（对应 `上下文工程修改.md` 第 27 条的可观测性）；(3) `summary_only` 的兼容口子追加条件（`metadata.legacy` 标记或 `created_at` 早于某时间点），把它限定在存量数据上。

## AGENT-14 被拒绝的事实没有墓碑：摘要原文仍注入，事实本身还会被反复重建

- 状态：已验证（沙箱实测）
- 位置：`fithealth_agent/info_store.py:946-964`（`reject_fact` 直接 `pop`）、`:828-879`；`main.py:1594-1607`（即时路径去重）、`:2258-2272`（logout 无去重）
- 现象一：一条记忆有 3 个事实、用户拒绝其中 1 个后，`item["summary"]`（描述了全部 3 个事实的原文）仍随剩下的已确认事实一起注入（`main.py:1467` → `format_cross_session_memories:859-874`）——被拒绝的内容以自然语言形式回到上下文。`get_context_memories` docstring 声称的"安全边界"只覆盖了"全部事实都被拒绝"这一种情况。
- 现象二：拒绝 `avoid_exercise:硬拉` 后再次保存同一事实，会原样生成新的待确认卡片——`_immediate_memory_candidate` 的去重集合只含**仍然存在**的事实，被 pop 掉的没有记录，`/logout` 连这层去重都没有。**用户每次退出都要重新拒绝一遍。**
- 修法：`reject_fact` 改软删除（保留事实但置 `status="rejected"`，扩 `FACT_STATUS`，并在 `resolve_confirmed_memory_facts:505` 与 `get_context_memories:848` 明确排除），条目上记 `rejected_fact_ids`；两条去重集合都改为包含 rejected 的 `(namespace,key,value)`，命中就不生成候选；条目存在任何 rejected 事实时**不注入 `summary`**，只注入已确认事实行（摘要是不可裁剪的自由文本，无法只删掉那一句）。`cleanup_expired` 给 rejected 事实设一个较长保留期（如 90 天）再物理删除。

## AGENT-15 安全绕过正则对提问和否定句误报，且 409 不区分用户是否在要计划

- 状态：✅ 已修复（2026-08-24；问句、否定句与计划请求回归已验证）
- 位置：`main.py:1034-1043`（`_SAFETY_BYPASS_PATTERN`）、`:1151-1160`（`blocking_reasons`）、`:2061-2073`（无条件 409）
- 实测误报：`为什么不能忽略我的腰伤照常练？`、`腰伤的人可以照常练吗？`、`伤好了以后能照常训练吗`、`请解释一下为什么不建议硬练`、`我不想硬练，帮我安排轻一点的`。只要记忆里有任何一条已确认的 `health.*` 事实，这些消息就在 `:2061` 被 409 拦下，返回固定文案，Agent 完全不回答。上一轮修 BUG-01 时只处理了"不管"，没有处理提问与否定语境（`:1029-1033` 的注释担心的正是这个）。
- 修法（两处都改）：(1) 正则命中后前置一个否决：命中点前 8 字内有 `不想|不能|不该|不建议|为什么|能不能|可以吗|算不算`，或句尾为 `吗|么|？|\?` 则不算 bypass（参照 `health_safety._is_negated` 与 `requested_plan_date` 的小句切分）；(2) `:2061` 的 409 增加条件 `chat_intent is not None and chat_intent.create_training_plan`——安全限制该拦的是"生成计划"，不是"讨论限制"，与相邻的 `rest_today`、`clarification_required` 两个分支保持一致。
- 注意：`painful_conflicts` 那条 blocking reason 走同一个 409，同样应只在请求生成计划时阻断；否则用户说"腿疼"再问"腿疼要怎么恢复"会被挡在门外（与 BUG-25 叠加）。

## AGENT-16 同一个动作可以同时是"避免"和"偏好"，极性冲突不检测

- 状态：已验证（沙箱实测）
- 位置：`fithealth_agent/info_store.py:495-521`、`:717-722`（都按 `(namespace,key,value)` 精确对齐）
- 现象：同时确认 `training.avoid_exercise=深蹲` 与 `training.prefer_exercise=深蹲`，两条都会返回并注入——`format_cross_session_memories` 同时说"避免动作：深蹲"和"偏好动作：深蹲"，而 `validate_generated_training_plan:370-372` 只认 avoid：模型按偏好排了深蹲，后端把计划判为违规，用户无从得知是记忆自相矛盾。`max_rpe` 类单例有互斥处理，跨 key 的极性互斥没有。
- 修法：在 `FACT_DEFINITIONS` 旁定义互斥对 `{("training","avoid_exercise"): ("training","prefer_exercise")}`；`resolve_confirmed_memory_facts` 里按 `created_at` 让后出现的一方把对侧同 value 的事实 pop 掉；`set_fact_confirmation` 里同时检测——确认 avoid 时若已有同 value 的 confirmed prefer，应抛冲突让用户显式选择，而不是静默共存。

## AGENT-17 同义词归一缺"久坐"族，多病症合并值从来没人拆分

- 状态：已验证（沙箱实测 + 全仓 grep）
- 位置：`fithealth_agent/info_store.py:78-131`、`:444-445`；`information_router._SYSTEM_PROMPT:132`；`main.py:129-133`（`_DURABLE_MEMORY_CUE`）
- 现象：`盆骨前倾→骨盆前倾`、`塌腰→骨盆前倾` 正常；但 `久坐`/`长期久坐`/`久坐人群` 三者互不归一，各自成独立长期事实——`上下文工程修改.md` 第 19 条点名要求的正是这一组，而路由的 system prompt 又明确要求把 sedentary status 存成 `health.injury_or_constraint`，所以这三种写法必然出现。`canonicalize_body_posture_value` 的 docstring 说多义值"应由调用方拆成多条事实"，**grep 全仓没有任何调用方做拆分**，实测 `盆骨前倾且核心无力` 原样存成一条未归一的自由文本。归一还只作用于三个 key，同一个值在 `training.avoid_exercise` 上不归一（`['骨盆前倾','盆骨前倾']` 共存）。`_DURABLE_MEMORY_CUE` 里也没有 `久坐`，所以"我平时久坐办公"不含任何 cue、即时候选不触发，只能等退出。
- 修法：(1) `BODY_POSTURE_SYNONYMS` 增加 `"长期久坐": ("久坐","久坐人群","久坐族","长时间坐着","办公室久坐","Sedentary")`；(2) `canonicalize_body_posture_value` 改成返回全部命中的 `list[str]`，`normalize_memory_facts` 在多命中时展开成多条事实（`fact_id` 由内容派生，天然唯一，零迁移），单命中行为不变——`tests/test_memory_validity.py:131` 的断言需同改；(3) `_DURABLE_MEMORY_CUE` 补 `久坐`。
- 展开成多条会让一次对话产生更多待确认卡片，但每条可独立确认/拒绝，这正是逐条确认机制想要的粒度。

## AGENT-18 Level 1 敏感词把"病史/病历"列入黑名单，健康 Agent 的正常对话整段无法保存

- 状态：已验证
- 位置：`fithealth_agent/information_router.py:41-52`、`:55-64`、`:343-358`
- 现象：`joined` 拼进了**全部** user 与 bot 消息，任何一方提到"病史/病历/身份证/手机号/家庭住址"，level1 就返回 `save=False`，整次对话的所有长期事实（周计划、伤病限制、动作偏好）一条都不保存，理由只说"包含敏感个人信息"。而这是一个健康管理 Agent——"你有相关病史吗"几乎是它自己会说出的话，**模型自己的一句提问就能否掉整次记忆**。
- 修法：把 `病史`、`病历`、`社保` 从 `_SENSITIVE_KEYWORDS` 移除（它们不是标识符，正则组已覆盖真正的身份证/卡号/手机号）；level1 的判定范围限定为 `role == "user"` 的文本——助手输出里的关键词不代表用户吐露了隐私。更保守的做法是"命中时剔除该条消息后继续路由"，而不是整次放弃。同改 `tests/test_information_router_memory_safety.py` 的 level1 断言。

## AGENT-19 只要检测到任何健康风险（含最轻的 caution），即时长期记忆抽取就整体跳过

- 状态：已验证
- 位置：`main.py:1746-1754`（`if source == "chat" and health_risk is None`）
- 现象：`caution` 档含"头晕/发热/心悸/恶心/血压"，`urgent` 含"肿了/剧痛"。于是"我最近膝盖有点肿，以后别给我安排深蹲了"只会走 `_acute_injury_memory_candidate`（只存一条 `recovery_status`），那句明确的长期 `avoid_exercise` 不生成候选，必须等退出才可能被 `/logout` 捞回——而 `/logout` 依赖前端内存里的会话历史，条数与字符都有上限（`context_budget.py:14-17`）。这正是 `上下文工程修改.md` 第 13/25 条要解决的问题。
- 修法：条件收紧为 `health_risk is None or health_risk.level == health_safety.CAUTION`；对 `urgent` 档，在 `_acute_injury_memory_candidate` 之后仍调一次 `_immediate_memory_candidate`（两者已各自去重，不会重复建卡）。只有 `emergency` 应完全短路——那条路径在 `:1738` 已直接 return。

## AGENT-20 YouTube 频道排除用精确等值匹配，与计划校验是两套口径

- 状态：已验证
- 位置：`fithealth_agent/youtube_tool.py:54-58`、`:145-158`；候选来源 `main.py:898-912`
- 现象：`avoid_channels` 是 `channel.strip().casefold()` 的集合，过滤条件是**全字符串相等**。事实值来自模型抽取的用户原话（"Athlean X"、"那个AthleanX频道"），YouTube 返回的是频道全称（`ATHLEAN-X™`），几乎不会相等 → 排除静默失效；而 `validate_generated_training_plan:495-497` 用**子串**匹配去判违规——两套口径。另外 `main.py:907` 的正则要求频道名以 ASCII 字母数字开头，中文频道名永远抓不到。
- 修法：过滤改双向子串匹配（去掉空格与 `™®-_` 后双向 `in`），与校验器统一，归一函数提到 `youtube_search.py` 或 `info_store.py` 供两处共用；`main.py:907` 的字符类补 `一-龥`。子串匹配可能误伤同名前缀频道，但有 `avoidance_fallback`（`youtube_tool.py:156-158`）兜底，最坏只是回退到未过滤结果并提示。

## AGENT-21 急性伤病候选把整条用户消息当作事实值，产生过宽的部位屏蔽

- 状态：已验证
- 位置：`main.py:1641-1647`（`value = message[:120]`）；消费方 `:392-413`（`constraint_regions` 扫部位标记）
- 现象：一旦用户确认，这段原文进入 `active_safety_constraints`，`constraint_regions` 会在其中扫 `膝/腿/腰/背/肩/胸/肘/腕`——"我腿断了，上半身还能练吗，肩膀也有点紧"会把腿、肩、背一起屏蔽 7 天，且事实文本里还留着提问句，之后每次注入都带上。
- 修法：用 `finding.labels` + 命中部位构造简短规范值（如 `"疑似骨折或脱臼（腿部），需评估"`），原文放进 `evidence` 字段——`evidence` 不参与 `constraint_regions` 匹配，正是为审计准备的位置。需要一个从 `RiskFinding` 到部位的映射，可复用 `REGION_ALIASES`（并见 ARCH-07）。

## AGENT-22 未知动作的模型兜底每轮聊天同步调用，且校验器永远匹配不到兜底结果

- 状态：已验证
- 位置：`main.py:1524-1534`、`fithealth_agent/muscle_map.py:246-263`（`timeout=10`，无跨会话缓存）、`:266-282`；`main.py:410`、`:429`（校验器用纯本地 `muscles_for_exercise`）
- 现象一：每次 `/chat` 与 `/session/intro` 都重建快照，`exercise_cache` 只在单次快照内有效，所以**每一轮聊天都会为每个未识别动作名重新发一次请求，每个最多阻塞 10 秒且串行**——真实库里只要有两三个改过名的动作，聊天首字延迟就增加 20-30 秒。文档 §A.5 说"不建立跨会话缓存"，但没预料到快照会按请求重建。
- 现象二：计划校验用的是纯本地解析，所以由模型兜底才识别出的肌群在计划正文侧**永远匹配不上**——快照说"该肌群还需 20 小时"，校验器却看不到冲突，形成结构性假阴。
- 修法：(1) 加进程内短期缓存，键为「动作名 + `data/daily_records.json` mtime」（文档 §B 已认可 mtime 作为失效条件），或把兜底调用移出 `/chat` 同步路径（保存训练时解析一次并缓存）；(2) 校验器接受 `resolver` 参数，由调用方传入与快照相同的解析器（含缓存），默认值保持纯本地函数以维持可测性。

## AGENT-23 "查看个人信息"展示的已确认事实与实际注入给模型的不是同一份

- 状态：已验证
- 位置：`main.py:1960`（`confirmed_memory_profile(info_store.get_all())`，列出全部）对比 `:2030`（`get_context_memories(n=5)`）
- 现象：结合 AGENT-11，用户在界面上看到"已确认：避免深蹲、RPE 上限 7"，模型根本没收到，后端校验也没启用。**"显示已生效、实际未生效"是最难自查的一类缺陷。**
- 修法：修掉 AGENT-11（执行侧改用不截断的全量事实）之后两边口径自然一致；在此之前至少让 `confirmed_memory_profile` 标注哪些事实本轮未进入上下文。

## AGENT-24 关闭外部模型时失效的能力清单（供你决定哪些要补本地兜底）

- 状态：已验证（逐个调用点确认）
- **失效**：全部意图路由（`chat_intent_router.py:244` 直接返回全 False 的 `ChatIntent`）→ 无法创建训练计划、无法走"保存刚才的计划"卡片、无法更新档案、`view_nutrition_records` 只剩 `main.py:1840` 那 5 个关键词的兜底；一切跨会话记忆抽取（退出与即时两条路径都不保存任何事实）；上传 Markdown 的计划鉴定一律判 `is_plan=False`（`plan_classifier.py:97-102`，关键词层只能否决不能通过）。
- **仍生效**：`health_safety` 三档筛查、`validate_generated_training_plan`、`resolve_plan_context` 与周计划硬约束、"查看训练记录/个人信息/本周数据"的正则兜底（`main.py:1810`、`:1950`、`:1988`）、健康数据导入与 FIT 组编辑、备份。
- 建议：`view_nutrition_records` 的兜底关键词与 `is_training_record_query`（`main.py:533` 的完整实现）对齐；计划鉴定见 FEAT-18。

---

# 八、P2：架构、测试与环境

## ARCH-07 "文本 → 身体区域"有四份平行实现

- 状态：已验证
- 位置：`fithealth_agent/muscle_map.py:61-107`（`MUSCLE_RULES`）、`main.py:244-252`（`TRAINING_SUBJECT_RULES`）、`main.py:345-359`（`constraint_regions`）、`fithealth_agent/muscle_recovery.py:37-44`（`REGION_ALIASES`）
- 现象：四套关键词覆盖各不相同。举例：`constraint_regions` 把"腰"同时归到背部与核心；`REGION_ALIASES` 的核心别名是 `核心/腹部/腹`（没有腰）；`TRAINING_SUBJECT_RULES` 的核心是 `核心/腹/卷腹/平板支撑`。于是"腰部不适"这条限制会拦掉核心动作，但用户说"腰还酸"不会被解析成核心区域的酸痛。这正是仓库注释里反复提到的失效模式（"同一件事两处实现，必有一处悄悄失效"）。
- 修法：以 `muscle_map` 的 `region` 为唯一权威，把 `REGION_ALIASES` 与 `constraint_regions` 的部位标记合并成一张 `REGION_LEXICON: dict[str, tuple[str, ...]]` 放在 `muscle_map.py`（它没有 store 依赖，谁都能引）；`TRAINING_SUBJECT_RULES` 的动作关键词可由 `MUSCLE_RULES` 按 region 聚合生成。加一条不变量测试：四处引用同一常量。
- 与 BUG-26 问题 3 一起做（那条也是在拆 `muscle_map` 的表）。

## ARCH-08 三个 JSON store 缺进程内 RLock，`plan_store` 的读方法完全不持锁

- 状态：✅ **已修复（2026-08-27）**——修法 (1) 早前已做（三个 store 各加 `RLock`，`plan_store.list_plans`／`get` 已纳入）；现补上 (2)(3)：sqlite 快照移出 JSON 锁窗口（压缩本来就在锁外）；`JsonFileLock.__enter__` 的 Windows 分支换成 `LK_NBLCK` + 显式退避重试，超时抛带中文说明的 `TimeoutError`（它是 `OSError` 子类，既有 `except OSError` 链不受影响），并保证 `__enter__` 中途失败不泄漏文件句柄。POSIX 分支行为不变
- 位置：`fithealth_agent/json_file_lock.py:24-27`；`plan_store.py:92-96`（读，无锁）、`:111/137/154/164/173`；`soreness_store.py:44`；`info_store.py:538`。对照 `storage.py:16`、`:104` 的 `RLock` + `JsonFileLock` 双层
- 现象：`DailyRecordStore` 与 `UserProfileStore` 是双层锁，线程之间先在 RLock 上排队，永不在文件锁上互等。`TrainingPlanStore`/`SorenessStore`/`InfoStore` 只有文件锁。POSIX 上 `fcntl.flock` 在同进程不同 fd 之间也会阻塞（实测 4 写 3 读并发 60 次写入无丢失），但 **Windows 走 `msvcrt.locking(fd, LK_LOCK, 1)`，语义是"每秒重试 10 次后返回错误"**——等待上限约 10 秒，超时抛 `OSError` 而不是继续等。谁会持锁那么久：`export_bytes`（`backup_service.py:196-201`）一次拿下全部 7 把 JSON 锁再做 sqlite 快照 + 全量压缩，且**导出不受维护开关保护**（只有 restore 和 reset 走 `MAINTENANCE`）。当前实测 0.54 秒（快照 0.14 + 压缩 0.40，26.5 MB），离 10 秒还远，但随数据量线性增长；届时表现是导出期间一次保存计划/写酸痛记录直接 500 `Permission denied`。
- 另有 Windows 专属风险：`plan_store` 的 `list_plans`/`get` 不持锁，而 `os.replace` 覆盖一个正被打开读取的文件会抛 `PermissionError`——读窗口很短但非零。
- 修法：(1) 三个 store 各加 `self._write_lock = RLock()`，公开方法改 `with self._write_lock, JsonFileLock(self.path):`，私有 `_read_all/_write_all` 保持无锁（`storage.py:90-94` 的注释解释了为什么不能嵌套）；`plan_store.list_plans/get` 一并纳入。(2) 把 `_snapshot_database` 移出持锁窗口（压缩已经在锁外了）。(3) `JsonFileLock.__enter__` 的 Windows 分支显式循环重试并给出明确的中文错误，别让调用方看到裸 `Permission denied`。
- 注意：给 `plan_store` 的读加锁后，`main.py` 里若有"先 `get()` 再 `update()`"的调用序列仍是两段事务，该 read-modify-write 需要下沉到 store 内部（参照 `storage.upsert_dated_record`）。

## ARCH-09 测试基线：有欺骗性的绿、有定时炸弹、有对真实数据的依赖

- 状态：已验证
- 现状：`tests/` 共 60 个文件约 12000 行，**没有 `conftest.py`/`pytest.ini`**，`requirements.txt`/`requirements.lock`/`pyproject.toml` 里都没有 pytest——测试依赖"环境里恰好装了什么"。仓库根还留着 `.test-tmp/`、`.tmp-tests/`、`.test-tmp-logic-bug/` 三个目录。
- **欺骗性测试（删掉被测逻辑也照样绿）**：
  - `tests/test_json_store_locking.py:13-35`：文件名叫 locking，内容是纯串行的"写两次读一次"，把三个 store 的锁全删掉它照样过。
  - `tests/test_durable_writes_and_profile_locking.py:281-288`：`assertTrue("atomic_write_json" in source or "os.fsync" in source)`——字符串出现即通过，注释里提一句也算。DATA-38 那三处手写副本正是从这个缝里漏过去的。
  - `tests/test_extended_health_fields.py:547-557`：只断言源码文本顺序 `body.index("write_recovery_point()") < body.index("delete_import")`，完全没覆盖"delete 成功、reimport 失败"这条真实的数据丢失路径（DATA-20）。
  - `tests/test_session_intro_muscle_recovery.py:34-35`：在真实数据的 7 天窗口失效后退化成 `assertEqual({}.keys(), {}.keys())` + 空循环，变成**永久假绿**。
  - `tests/test_session_intro.py:92-97`：把 `resolve_confirmed_memory_facts` 换成自制 lambda，而有效期过滤（`info_store.py:505` 的 `fact_is_active`）唯一发生在真实实现里——fixture 里那个 `valid_until` 一行都没走到。
- **定时炸弹与真实数据依赖**：`tests/test_soreness_stage3.py:133-164` 依赖真实 `data/daily_records.json` + `datetime.now()` 的 7 天窗口，最后一条相关训练是 2026-08-21，**2026-08-28 起永久变红**；`tests/test_muscle_recovery.py:56-70` 直接读用户真实记录并断言 `effective_sets == 13.0`，用户在界面上改一条记录就红（且是 `StopIteration` error 而非 fail）。
- **测试污染**（DATA-22 的根因）：四个文件在模块顶层 `import main` 且不设 `FITHEALTH_DATA_DIR`（`test_generated_plan_safety_rules.py:7`、`test_immediate_memory_capture.py:10`、`test_session_intro_muscle_recovery.py:7`、`test_soreness_stage3.py:10`），按收集顺序它们最先执行，于是后面两个文件精心准备的临时目录对模块级绑定的路径无效。另外 `tests/test_workout_store.py:38-55`、`tests/test_quarantined_workout_recovery.py:115-132` 用假的 `fithealth_agent` 包顶替真包，清理时是 `sys.modules.pop` 而不是还原原对象，后续 import 会得到第二份模块实例（`isinstance` 与异常类型匹配静默失效）——`test_extended_health_fields.py:55-82` 有正确的 save/restore 写法可作模板。
- **关键路径无测试**：备份体积上限（DATA-23 因此没被发现）；"恢复把现有数据弄哪去了"（`RestoreContentTest` 全在验证恢复成功，`test_restore_replaces_the_directory_wholesale` 还把破坏性钉成期望）；`export_bytes` 的并发行为与"导出期间写入是否失败"；`health.db` 恢复后 `-wal/-shm` 被删与 `_validate_database` 的 `integrity_check` 拒绝分支；`ExternalModelSettingsStore` 的 fail-open 分支（DATA-31）；"降级 → 导入好备份 → 记忆是否可用"（DATA-32 因此能长期存在）；两个 `scripts/` 脚本的端到端行为（`migrate_raw_path.py` 连源码级断言都没有）；`JsonFileLock` 本身（跨进程行为、Windows 分支、`__enter__` 中途失败是否泄漏句柄）。
- 修法（最小治理动作）：加 `tests/conftest.py` 在收集阶段前统一注入临时 `FITHEALTH_DATA_DIR`；把 pytest 加入依赖清单；把上述三类"欺骗性断言"改成行为断言；把依赖真实 `data/` 的用例改成自带 fixture。

## ARCH-10 `vendor/` 是空的，镜像构建必然失败；依赖只锁到版本号

- 状态：已验证（vendor 为空）／疑似（`starlette==1.6.0` 与 `fastapi==0.141.1` 的兼容性离线无法核实）
- 位置：`vendor/`（只有 `.gitkeep`）、`requirements.lock:24`（`hello-agents==1.0.0`，不在公共 PyPI）、`Dockerfile:12-15` 与 `:36`（`pip install --find-links=/tmp/vendor -r requirements.lock`）、`tests/test_data_dir_and_docker.py:190-193`（只断言 `.gitkeep` 存在）
- 修法：把 wheel 放进 `vendor/`（`.gitignore` 排除它、README 说明来源），或在 compose 里默认注入 `PIP_EXTRA_INDEX_URL`；README 的首次使用步骤最前面写清"构建会失败除非先准备 wheel"。`requirements.lock` 建议用 `pip-compile --generate-hashes` 重新生成，测试依赖单列 `requirements-dev.lock`。

## ARCH-11 数据目录默认相对路径（与 DATA-22 同源）

- 状态：已验证
- 位置：`fithealth_agent/settings.py:37-40`
- 现象：有意保持相对（为了 `os.chdir` 隔离），代价是从任何非仓库根的 cwd 启动都会在别处凭空建一个 `data/`，而且这是测试污染链条的第一环。
- 修法：默认解析为 `Path(__file__).resolve().parents[1] / "data"`（绝对、稳定），"可切换"交给 `FITHEALTH_DATA_DIR`；测试改为设环境变量而非 `chdir`。这需要同改所有用 `os.chdir` 的测试，是一次有成本但收益明确的改动。
- 正面确认：数据卷本身没问题——`docker-compose.yml:39-42` 的 `./data:/app/data` 与 `Dockerfile:28` 的 `FITHEALTH_DATA_DIR=/app/data` 对齐，`.dockerignore` 排除了 `data/`，容器重建不会丢数据（DATA-10 确实修好了）。附带小项：`docker-compose.dev.yml:15` 的 `--reload-dir /app` + `WATCHFILES_FORCE_POLLING=true` 会递归轮询含 25 MB `health.db` 的整棵树（JSON 写入不会触发重启，uvicorn 默认只匹配 `*.py`，但轮询本身有开销），建议加 `--reload-exclude '/app/data/*'`。

## ARCH-12 Garmin 恢复小时有三套校验，且文档与实现的取舍不一致

- 状态：已验证
- 位置：`fithealth_agent/context_budget.py:206-221`、`main.py:3955-3973`、`fithealth_agent/muscle_recovery.py:303-310`、`:418-428`
- 现象：同一个 `0–96.0` 的契约写了三遍，其中只有 `main.py:3973` 做 `round(number, 1)`，`_normalise_garmin` 不四舍五入且**抛异常**而不是返回错误——将来若有调用方跳过前两道校验，这里会变成 500。另外文档 §B.2 写的是 `hours_remaining = max(0, baseline) + garmin`（对已恢复肌群也加），代码在 `:418-427` 刻意只对 `baseline_remaining > 0` 的肌群加成。**代码的行为更合理**（不让已恢复的肌群被凭空复活），但文档没同步，下一个人照文档改就会引入回归。
- 修法：校验收敛到一处（`muscle_recovery.normalise_garmin_hours(value) -> float | None`，两个入口各自转成自己的错误响应）；更新 `肌群恢复实现计划.md` §B.2 与 §7.1，明确"已恢复肌群不加成"并写清理由。

## ARCH-13 欢迎页文案与实际行为不符，且把加权容量当成"组数"展示

- 状态：已验证
- 位置：`main.py:4089`（"本阶段先展示状态，不改变训练计划"）、`:4008`（`sets_text = f"{load.effective_sets:g} 组"`）
- 现象：阶段 3/4 之后恢复状态已经参与压制周计划、生成 409 和拒绝计划，欢迎页仍说"不改变训练计划"，用户据此不会去纠正错误的映射结果。另外 `effective_sets` 是主项 1.0 + 次要 0.4 的加权值，界面直接写成"1.2 组"——真实数据里"小臂肌群：周五训练过（哑铃罗马尼亚硬拉，1.2 组）"实际是 3 组硬拉的次要负荷，与用户在训练记录里看到的组数不一致。
- 修法：文案改成说明恢复状态如何参与裁决（压制周计划、拒绝未恢复主项）以及**如何纠正**（对话里说明动作、或到数据管理改酸痛记录）；`_format_muscle_recovery_lines` 改成"有效容量 1.2（主项+次要折算）"或同时给出原始组数——需要 `MuscleLoad` 增加 `raw_sets` 字段，`_append_event` 里一并累加。

---

# 九、建议新增的功能（FEAT）

按"用户会自然期待但现在做不到"排序，前四条是明显缺口。

## FEAT-01 未识别动作清单与人工指认

**状态：🚫 用户决定不做（2026-08-27）**——手表识别出的动作本来就是错的，所以"未识别动作清单"没有意义：列出来的缺口不是真实缺口，真正的把关方式是用户自己审核并手动修正动作名。与 `DATA-19` 同一个决定的延伸（动作名一律靠人工审核，不依赖设备识别）。

映射不上的动作名仍会在 `build_recovery_snapshot` 里静默 `continue`，这是接受的行为。

原始建议如下（保留备查）：文档 §8 承诺"系统列出未识别动作，让用户决策"，但 `build_recovery_snapshot`（`muscle_recovery.py:384-386`）对映射不上的动作名直接 `continue`，名字既不进快照也不进 `/session/intro` 响应。加 `MuscleRecoverySnapshot.unmapped_exercises`，在欢迎页列出"这些动作我认不出，对应哪个部位？"并允许在对话里指定（一次性、只影响当次快照即可，不必落盘映射表）。这条同时是 DATA-19（动作名解码错位）与 BUG-24（反馈落在快照外）的用户侧出口。

## FEAT-02 打卡 / 营养记录 / 单条训练记录的删除能力

**当前完全删不掉打卡记录。** `CHECKIN_CATEGORY` 在 `NON_TRAINING_CATEGORIES` 里（`daily_checkin.py:163`），因此 `DELETE /data/records/{id}`（`main.py:3752`）与 `POST /data/records/delete-batch`（`:3769-3776`，用 `training_ids` 白名单过滤）都会对打卡记录返回 404，仓库里没有任何删除打卡记录的端点。配合 DATA-29（字段无法清空），一次误填就是永久的，唯一清除手段是"删除全部数据"。营养组同理只有 PATCH、没有 DELETE，且 meal 分支强制"至少保留一个食物项目"（`main.py:3136-3137`）、manual 分支强制"至少保留一项营养数据"（`:3123-3124`），整餐记错了无法删。另外 `DELETE /data/records/{record_id}` 已实现但 UI 零调用，对话侧与查看记录侧都没有删除按钮。

## FEAT-03 已保存训练计划的正文可编辑

`PATCH /plans/{plan_id}`（`main.py:3713-3727` → `plan_store.update`）只接受 `date/subject/title/memo`，`content` 与 `content_hash` 不可改；前端 `editPlanMetadata` 也只用 `window.prompt` 改这四项，`showPlanPreview` 是只读。想改一个动作只能删掉重新生成——而重新生成的正文若哈希相同又会被去重拒收（BUG-36）。

## FEAT-04 酸痛反馈的手动录入与历史留痕

`/data/soreness` 只有 PATCH 与 DELETE（`main.py:3790`、`:3808`），没有 POST：用户想直接勾"手臂 · 酸痛"只能在对话里说一句话，而解析器又有 BUG-21/22 的误判。另外 72 小时 TTL + `cleanup_expired` 物理删除（`soreness_store.py:151-165`）意味着过期即消失，无法回答"我这两周手臂酸过几次"，也无法验证恢复窗口的估算是否偏保守。建议：数据管理面板可新增一条；`cleanup_expired` 改为只标记 `expired` 或另存一条只追加的轻量日志。

## FEAT-05 餐次维度、手动餐次与离线兜底

`meal_estimates` 只有 `source/items/合计/confidence/assumptions/user_confirmed`（`daily_checkin.py:92-99`），没有餐次（早/中/晚/加餐）和拍照时间，一天多餐只能靠数组顺序区分，周汇总也无法按餐次分析。建议加 `meal_slot` 与 `photographed_at`（前端填、服务端白名单校验）。另外 `analyze_food` 在外部模型关闭时直接 503（`main.py:221-225`），此时连"手动填一条带食物明细的餐次"都做不到（`_validate_meal_estimates` 要求 `source == "food_photo_estimate"`）——建议允许 `source: "manual_meal"` 走同一套字段与校验。

## FEAT-06 训练量突增与连续高强度日的安全校验

文档 §9 明确列为"本方案不做"，但推导层已经具备基础（`_append_event` 的分桶按 `(muscle_id, date)` 保留了多日数据），只差一个周滑动均值基线。这是恢复裁决之外最大的安全校验缺口，建议与 BUG-45（多日 history）一起做。

## FEAT-07 周汇总补容量与恢复视角，手动营养按日期聚合

`build_current_week_reply` 只统计记录条数与分类（`weekly_summary.py:129-134`），没有本周总组数、按肌群/区域的容量分布，也不含当前恢复状态与酸痛反馈——而这些数据已在 `MuscleRecoverySnapshot` 里现成可用。另外 `manual_nutrition_days`（`:149-150`）按**记录条数**而非日期去重（变量名说的是 days），且"有照片估算就不统计当天手动营养"的互斥逻辑会漏掉用户手填的额外热量：应按日期聚合并把两种来源分别列出，而不是二选一。

## ~~FEAT-08 恢复窗口的可解释性与校准~~

界面只给最终小时数，不显示是怎么算出来的（基础窗口 × 容量调整 × 酸痛系数 + Garmin）。字段全部具备（`effective_sets`、`recovery_hours`、`soreness_level`、`garmin_recovery_hours`），在 `/session/intro` 的每条记录里附一行推导明细即可。没有这一行，用户无法判断"还需 20 小时"是否合理，也没有反馈闭环去校准那张固定表。

## FEAT-09 健康数据的区间与单日查询接线

`GET /health/range`、`GET /health/daily/{day}`、`GET /health/sleep/{day}` 三个端点实现完整却无人调用（BUG-46），健康面板只有 `/health/overview` + `/health/trend`。用户想看"某一天的睡眠分期"或"某段日期的心率明细"，能力在后端存在但界面没有。

## FEAT-10 `forget_memory` 确定性遗忘入口

`上下文工程修改.md` 第 8 条，未实现。用户说"忘掉我有骨盆前倾"目前只能靠模型在下次退出时产出 `status=cleared` 的事实、再等用户确认——绕了一整圈，而 `resolve_confirmed_memory_facts:517-518` 的 clear 分支要求 value 精确对齐，稍有措辞差异就清不掉。建议加一个按 `(namespace, key)` 或 `(namespace, key, value)` 批量清除的端点/工具，并落审计记录。

## FEAT-11 任务相关性检索、记忆使用日志、变更历史与回滚

- **相关性检索**（第 11 条，与 AGENT-11 同源）：当前只按 `(importance, created_at)` 排序。建议按意图分档——`create_training_plan` → 周计划 + 伤病 + 动作/强度限制优先；`search_youtube_video` → 频道偏好优先；`view_profile` → 全量。
- **记忆使用日志**（第 16 条）：哪次计划用了哪条事实、哪个动作因此被替换、哪次校验因此失败。`validate_generated_training_plan` 已能算出违规明细（`main.py:498`），按 fact 归档即可，成本很低。
- **变更历史与回滚**（第 17 条）：`version` 已在写，但旧值被单例替换后直接丢弃（`info_store.py:512-515`）。保留 `previous_value` 能让"改回上周的周计划"变成一次点击，也顺带绕开 AGENT-12 的编辑死锁。

## FEAT-12 会话级 / 日期区间约束

`ChatIntent.temporary_health_constraints` 已存在（`chat_intent_router.py:171-176`）但只影响单次请求。用户说"这周都别安排腿"没有任何承载体——既不是长期事实，也不是单轮约束。`normalize_memory_facts:481` 的 scope 枚举里已预留 `date_range`，但没有任何消费方。

## FEAT-13 计划校验失败后的自动修正回合

`main.py:2167-2169` 目前把违规清单贴给用户，让用户去催模型改。既然违规项是结构化的（超限组数、缺必需元素、含禁止动作、未恢复肌群），可以自动带着违规清单再跑一轮 Agent，最多重试一次。

## FEAT-14 在对话里主动确认记忆

`main.py:2201` 只在响应里挂一个 `memory_candidate` 字段。建议在回复正文里直接说"检测到长期偏好：X。要加入跨会话记忆吗？"，并支持下一轮用"记住、加入、好的、ok等正面回答添加/不用、不、否等负面回答不加"回答——对应第 25 条，目前只做了一半（候选生成了，确认动作仍在另一个界面里）。

## FEAT-15 恢复与酸痛的主动追问闭环

`soreness_store` 有 72 小时 TTL，但没有到期前的复查提示。建议在 `/session/intro` 对仍生效的 painful 区域主动问一句"腿部前天报告过疼痛，现在如何"，并支持一句话清除（目前用户只能去数据管理里删记录）。这条与 BUG-22 的修法 1（`soreness_prompt_regions`）是同一套机制，一起做最省事。

## FEAT-16 `health-imports` 与心率流的对账界面

见 DATA-27 的修法 2：`audit_raw_files()` + `delete_orphan_raw_file()`，与隔离文件区块并列。`missing` 那一侧对回灌脚本尤其有用——现在要跑一遍脚本才知道哪几份包已经丢了。

## ~~FEAT-17 身体电量 / 睡眠分数 / Garmin 恢复时间的字段逆向~~（2026-08-24 取消）

用户决定：身体电量不需要；Garmin 恢复时间由他在使用时手动填写，功能已实现。剩下的睡眠分数已在 `DATA-36` 里用 2 份 CSV 真值 + 17 份日包定位完毕，直接并入第七批实现，不再单列为新功能。**FEAT 总数由 18 条变 17 条。**


## FEAT-18 离线时上传计划的人工确认通道 / 隔离文件预览 / reset 半删重试

三个小出口：(a) 关闭外部模型时上传 Markdown 一律判非计划（`plan_classifier.py:97`），用户完全无法把已有计划纳入管理——给一个"无法鉴定，是否仍按计划处理"的确认；(b) 隔离文件只能载入/忽略/删除，不能查看内容，而"载入编辑"会覆盖当前 pending（BUG-29）——加一个只读预览；(c) `/data/reset` 的 `partial: true` 会逐项报告哪些没删成功（`main.py:4382-4399`，前端 `:2399-2406` 已正确区分），但没有"只重试失败项"的按钮，用户只能整个重来（第二次会再写一个恢复点）。

---

# 十、修改计划

分批的原则：**同一条链路上的多个缺陷必须同批修**，否则会出现"接通了拦截却立刻误拦"或"修了一半更糟"的情况（上一轮 BUG-01 + BUG-03 的教训）。每批结束都要有可跑的回归。

## 第一批：安全档位（半天到一天）

`BUG-19` → `BUG-20` → `BUG-21` → `AGENT-15`

必须一批做完，因为这四条串在同一条判定链上：正则漏报（19）→ 命中了也被覆盖降级（20）→ 酸痛被误判成疼痛送进这条链（21）→ 讨论限制的提问被 409 挡住（15）。单修任何一条，另外三条会让效果看不出来。

- 顺序敏感点：先把小句切分抽成**一个共用函数**（`main.py:1326-1329` 的 `_CLAUSE_SEPARATORS` 已有），BUG-19 的否定判定、BUG-25 的否决、AGENT-15 的疑问否决都用它——否则会变成第四套关键词表。
- 验收：新增一组"背景句 + 症状句"用例（`我最近没睡好，今天胸口很痛` 必须 emergency）；胸部在恢复快照里时 `胸口痛` 仍须 emergency；`手臂有点酸痛` 必须 sore；`腰伤的人可以照常练吗？` 不得 409。

## 第二批：拦截真的生效（一到两天）

`AGENT-11` → `BUG-23` → `BUG-24` → `BUG-25` → `BUG-26` → `AGENT-23`

这一批的共同点是"校验代码在跑，但输入是空的或错的"。AGENT-11 必须排第一（它决定校验器能不能拿到约束），BUG-26 的问题 3（role/zh 不稳定）必须排在问题 1（次要肌群窗口）之前。

- 关键取舍：**执行侧（校验、频道过滤、safety_constraints）改用不截断的 `get_enforceable_facts()`，注入侧才受预算约束**。两者混用是当前问题的根源。
- 验收：确认 3 条限制 + 6 条普通偏好后，`validate_generated_training_plan` 仍能拦下含禁止动作的计划；只练了背之后手臂日不被压制；`今天不想练腿` 不解除压制也不触发 409；报告"手臂疼"但最近没练手臂时，含手臂主项的计划仍被拒。

## 第三批：不可逆与会丢数据的路径（一到两天）

`DATA-22` → `DATA-20` → `DATA-21` → `DATA-23` → `BUG-27` → `DATA-29` + `BUG-34`

DATA-22 排第一——**在它修好之前，每次跑测试都在删你的待确认训练**。然后是三条脚本/备份路径（20/21/23），它们的共同点是"退路本身不可靠"。BUG-27 是唯一会丢用户已输入内容的前端缺陷。DATA-29 与 BUG-34 的 `applied_to_form` 必须同改，否则会出现"合计与明细分叉"。

- 顺序敏感点：DATA-21 的 `write_recovery_point()` 插入位置涉及 `JsonFileLock` 不可重入，务必按该条写的两种方案之一处理，不要直接在锁内调。
- 验收：`pytest tests/` 跑完后 `data/pending_workout.json` 未被触碰；构造一次解析失败的重导，原始包仍在；导入备份前生成了恢复点；当天已有保存记录时新导入一份 FIT，编辑区停在新导入的那份且撤销/清空都可用；清空全部餐次后重开弹窗确实为空且四项营养同步减少。

## 第四批：导入侧的数据正确性（两到三天，需重放全部包）

`DATA-17` → `DATA-18` → `DATA-19` → `DATA-24` + `DATA-25` + `DATA-26` → `DATA-28`

放在第四批是因为它们**需要先有可靠的重导入路径**（DATA-20 已在第三批修好）。DATA-17/18 改完必须重放全部 25 份包，DATA-19 则**刻意不迁移历史**。

- 顺序敏感点：DATA-18 走 `_ensure_column` 加新列、旧列保留，不要原地改语义。DATA-17 的归并放在解析侧，`DEDUPED_INTENSITY_SQL` 的 `MAX` 不要改成 `SUM`。
- 验收：`resting_metabolic_rate` 与 `resting_heart_rate` 同时有值的行数 > 0；2026-08-21 的 `moderate_activity_min` = 18.0；三份力量 FIT 的动作名与 `category_raw` 不再矛盾；默认健康概览不再落在只有 1 个样本的日期；那 7 天的强度显示"0 分钟"而不是"无数据"。

## 第五批：端点与前端的功能错误（一到两天）

`BUG-28` → `BUG-31` → `BUG-29` + `BUG-30` → `BUG-32` + `BUG-33` + `BUG-36` → `BUG-35` + `BUG-41` + `BUG-42` → `BUG-46`（删死代码）

BUG-28 与 BUG-31 排前面是因为它们让"后端已经正确返回的信息到不了用户"，修完之后其余问题的现场会清楚很多。

## 第六批：记忆状态机与可维护性（两到三天）

`AGENT-12` → `AGENT-13` → `AGENT-14` → `AGENT-16` + `AGENT-17` + `AGENT-18` + `AGENT-19` + `AGENT-20` + `AGENT-21` + `BUG-39`
之后 `ARCH-07` + `ARCH-08` + `ARCH-12` + `ARCH-13`，最后 `ARCH-09`（测试治理）+ `ARCH-10` + `ARCH-11`。

AGENT-12/13/14 是同一个状态机的三个缺口（版本基准、空事实、软删除），一起改比分三次改省事。ARCH-09 建议在这批的最后做——前面几批会大量改测试，先治理反而要改两遍。

## 第七批：其余数据安全底座与剩余项

`DATA-27` → `DATA-30` → `DATA-31` → `DATA-32` → `DATA-33` → `DATA-34` → `DATA-35` → `DATA-36`（"停止撒谎" + **落地睡眠分数**，字段已确证，见该条）→ `DATA-37` → `DATA-38` → `DATA-39` → `AGENT-22` → `BUG-37` + `BUG-38` + `BUG-40` + `BUG-43` + `BUG-44` + `BUG-45`

- 顺序敏感点：`DATA-36` 的睡眠分数入库要用 `SLEEP_DATA.msg521` 做主源、`METRICS.msg330` 做交叉校验。注意 `DATA-28`（睡眠起点）已在第四批修好，而两者读的是**同一条 `msg521` 的睡眠起止**——这里必须复用第四批定下来的那套口径，不要另写一遍解析，否则会出现两套睡眠时间。

## 新功能的建议节奏

**总节奏**：第一~四批（只修，不加功能）→ **F1：4 条** → 第五批 → **F2：4 条** → 第六批 → **F3：6 条** → 第七批 → **F4：3 条**。17 条 FEAT 全部分完，不重不漏；每组只用前面几批已经修好的东西，没有一条需要为它先额外改点别的。（原 `FEAT-17` 已于 2026-08-24 取消，FEAT 由 18 条变 17 条。）

**为什么前四批之间一条都不插**：那四批动的是判定链、写入路径和解析器，此时加界面等于在流沙上盖房子——`FEAT-01` 会列出一堆 DATA-19 造成的错名字，`FEAT-02` 的删除按钮会踩上 DATA-29 的合并语义，做完还得返工。

### F1（前四批之后、第五批之前）：4 条，一到两天

`FEAT-01` → `FEAT-02` → `FEAT-04` → `FEAT-15`（含 `BUG-22` 的修法 1）　**（`FEAT-01` 已于 2026-08-27 标记为不做，本组实际 3 条）**

共同点是**前四批把后端修对了，但用户看不见也改不动**——这四条是给前四批配用户侧出口，不做的话前四批的效果只存在于测试里。

- ~~`FEAT-01` 必须在 DATA-19（第四批）之后~~ **（2026-08-27：`FEAT-01` 与 `DATA-19` 均已按用户决定标记为不做——动作名靠人工审核，不依赖设备识别。本条排期作废。）**
- `FEAT-02` 依赖 DATA-29（第三批）的清空通道，否则"删一条打卡"和"把字段改回空"仍是两套语义。做完它，`DELETE /data/records/{record_id}` 就从 BUG-46 的死端点名单里出去了。
- `FEAT-04` 依赖第一批的 BUG-21：解析器还会把"有点酸痛"判成 painful 时，手动录入只是多开一个入口去喂同一个错分类。
- `FEAT-15` 与 `BUG-22` 的修法 1 是同一套 `soreness_prompt_regions` 机制——**注意 BUG-22 不在前七批的任何一批里，它就落在这儿**，别漏掉。

验收：~~欢迎页能列出未识别动作、在对话里指认一次即生效~~（`FEAT-01` 不做）；误填的打卡能删掉；数据管理里能手动加一条"手臂 · 酸痛"，且 72 小时过期后仍查得到历史；对仍生效的 painful 区域会主动追问，一句"好了"能清除。

### 第五批（端点与前端）：照原计划，但死代码清扫要留三个口子

`BUG-46` 那一步别一刀切：

- `GET /health/range`、`GET /health/daily/{day}`、`GET /health/sleep/{day}` **先留着**，F2 的 `FEAT-09` 就是把它们接线到健康面板（BUG-46 自己也是这么建议的）。
- `GET /health/imports/{import_id}` 也留着，但它的自然消费者不是健康面板而是 F4 的 `FEAT-16`（导入对账）；到 F4 若最终没用上再删。
- `MuscleLoad.role` 与 `muscle_loads` **不要删**，F3 的 `FEAT-08` + `ARCH-13` 要用它们渲染"主项/次要"——前提是 BUG-26 的问题 3（role 不稳定）已在第二批修好。

其余死代码（`buildTrainingRecordsPanel` 子树、九处 `MEMORY_CONFLICT` 死分支、`CHECKIN_CATEGORY` 死分支、重复的 `_region_muscle_ids`）照删不误。

### F2（第五批之后）：4 条，一到两天

`FEAT-09` → `FEAT-03` → `FEAT-18` → `FEAT-05`

第五批刚把 `/chat` 的错误码、冲突自愈、卡片顶替这些前端地基修好，趁手感还在一次性把小出口开完，四条改的基本是同一批文件。

- `FEAT-09` 排第一：纯接线、无新后端逻辑，做完正好收掉第五批留的口子。
- `FEAT-03` 必须在 `BUG-36`（第五批）之后——正文可编辑会大量产生"内容相同、只改元数据"的请求，去重逻辑不先改成合并语义或 `(content_hash, date)`，用户刚写的日期/科目/备忘录会被静默丢掉。
- `FEAT-18` 的三个小出口分别贴着第五批的三处现场：(b) 隔离文件预览与 BUG-29 同屏，(c) reset 半删重试与 `main.py:4382-4399` 的 partial 报告同屏，(a) 是 `AGENT-24` 那份"关闭外部模型时失效清单"里唯一完全没有兜底的入口。
- `FEAT-05` 放最后：它要同时加字段（`meal_slot`/`photographed_at`）和放宽 `source` 白名单，依赖第三批 BUG-34 的数值校验与 DATA-29 的清空语义都已到位；顺带把 AGENT-24 的第二个决策（离线时能不能手动记一餐）落地。

验收：健康面板能查单日睡眠分期与任意区间明细；改一个动作不必删掉重生成，且备忘录不再消失；关闭外部模型时仍能把一份 Markdown 按计划纳管、能手填一条带食物明细的餐次；隔离文件可只读预览且不覆盖当前 pending。

### 第六批（记忆状态机与可维护性）：照原计划

### F3（第六批之后）：6 条，两到三天

`FEAT-10` → `FEAT-11` → `FEAT-14` → `FEAT-12` → `FEAT-13`

前五条全部建立在第六批的 AGENT-12/13/14（版本基准、空事实、软删除墓碑）之上：没有墓碑，`FEAT-10` 的"忘掉"会被下一轮抽取原样重建；没有幂等确认，`FEAT-14` 在对话里回一句"记住"就会撞上 409 死锁。

- `FEAT-10` 先做，它是 `FEAT-11` 的变更历史与 `FEAT-12` 的区间约束的公共下层（两者都需要一个确定性的清除动作）。
- `FEAT-11` 的三块建议拆开按序做：相关性检索（与第二批 AGENT-11 同源，此时执行侧已经在用不截断的 `get_enforceable_facts()`）→ 使用日志 → 变更历史与回滚；最后那块顺带绕开 AGENT-12 的编辑死锁。
- `FEAT-13` 排在 `BUG-39`（第六批）之后：自动修正回合会带着违规清单重跑一轮，若周计划这条硬约束本身还在被"一处不合法就整条丢弃"，重试只会在一个不完整的约束集上打转。

验收：说"忘掉我有骨盆前倾"一次生效且留审计记录；生成计划时能看到用了哪几条事实、哪个动作因此被替换；"这周都别安排腿"在本周内持续生效、下周自动失效；校验失败时自动重试一轮并给出修正说明；恢复行写"还需 20 小时"时用户能看懂它是怎么算出来的。

### 第七批（其余数据安全底座与剩余项）：照原计划

### F4（第七批之后）：3 条，一到两天

`FEAT-16` → `FEAT-07` → `FEAT-06`

这三条都不是"接个界面"，而是依赖第七批才落地的能力，所以只能收尾做。（原第四条 `FEAT-17` 已取消：身体电量不需要、Garmin 恢复时间手输已实现、睡眠分数的字段已确证并并入第七批的 `DATA-36`。）

- `FEAT-16` 就是 `DATA-27` 修法 2 的界面壳（`audit_raw_files()` + `delete_orphan_raw_file()`），照 `workout_store.list_quarantined()/dismiss/delete` 三件套的范本做。
- `FEAT-07` 必须在 `BUG-37`（第七批）之后：周汇总现在会抢答"本周恢复得怎么样"，先收紧触发条件，再往里加容量与恢复视角，否则是在放大一个误触发的分支。"手动营养按日期聚合"那半条与 BUG-37 同文件，一起改。做这条时顺手把 `DATA-36` 落地的睡眠分数一并纳入周汇总——数据届时已经在库里。
- `FEAT-06` 明确依赖 `BUG-45`（第七批）的多日 `history`——没有跨天累计就没有周滑动均值基线，"训练量突增"无从判起。

验收：能看到哪些原始包丢了、哪些心率流成了孤儿并可清理；周汇总给出本周总组数与按区域的容量分布，照片估算与手动营养两种来源分别列出；连续三天练同一肌群会被识别为累积疲劳。

---

# 附：本次确认无问题的部分（避免后续被误改）

- **Docker 数据卷**：`./data:/app/data` 与 `FITHEALTH_DATA_DIR=/app/data` 对齐，`.dockerignore` 排除 `data/`，容器重建不丢数据。DATA-10 确实修好了。
- **`.env.example` 与 `.dockerignore`**：`FITHEALTH_DATA_DIR=` 留空并注明容器里由 compose 覆盖；`.env` / `.env.*` + `!.env.example` 的顺序正确。
- **`os.replace` 的原子性**：JSON 读到的要么是旧内容要么是新内容，不会读到半截。
- **备份的完整性校验**：篡改校验和、丢成员、多余成员、5 种路径穿越、v1/v2 兼容、回滚（文件与目录两条路径）、恢复点同秒命名与 prune 不误删、fsync 早于 rename、排空超时、中间件 503——`tests/test_journal_backup_and_restore_safety.py` 与 `test_reset_transactionality.py` 覆盖得相当扎实，本次只发现"恢复前不落快照"这一个空白（DATA-21）。
- **前端的日期本地化**：`localDateISO`（`index.html:3617-3623`）用 `getFullYear/getMonth/getDate` 拼接、`changeOverviewDay`（`:1140-1144`）用 `'T00:00:00'` 后缀解析，都是对的；全文没有 `toISOString()` 当本地日期用的经典 bug。
- **前端 DOM 完整性**：105 个 `getElementById` 目标与 HTML 的 id 集合无差集；35 个带 id 的 `<button>` 均有处理器；全文 `node --check` 通过；没有一处 `console.*`（等价问题表现为完全静默的 catch，已列在 BUG-27/41/42）。
- **`query_muscles_with_lite_model`、`is_training_record_item`、三个 `plan_workflow` 导入**：都已正确接线，不是死代码。
- **`_STRESS_TIME_FIELDS`（`health_importer.py:75-96`）**：这是全仓库处理"上游字段名不可信"的正确范本，DATA-18 应照它的写法改（含那段解释为什么的注释）。
- **`DEDUPED_HEART_RATE_SQL` / `DEDUPED_INTENSITY_SQL` 的 `MAX` 去重**：跨文件重叠导入需要它，修 DATA-17 时不要改成 SUM。
- **`workout_store.list_quarantined()/dismiss/delete` 三件套**：这是"启动时提示用户处理遗留数据"的正确范本（有持久化的已处理状态 + 删除出口），DATA-27 的 `health-imports` 对账应照它做。







